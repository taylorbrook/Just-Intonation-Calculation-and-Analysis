{
  "name": "tuning-systems",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "observable preview",
    "build": "observable build",
    "lint:types": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "ci": "npm run lint:types && npm run test && npm run lint && npm run format:check && npm run build"
  },
  "dependencies": {
    "@observablehq/framework": "1.13.4",
    "fraction.js": "5.3.4",
    "ji-lattice": "^0.3.2",
    "sonic-weave": "^0.14.1",
    "sw-synth": "^0.4.0",
    "xen-dev-utils": "^0.13.1"
  },
  "devDependencies": {
    "@types/node": "^20.19.39",
    "eslint": "^9.39.4",
    "prettier": "^3.8.3",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.59.1",
    "vitest": "^2.1.9"
  }
}
