---
phase: 02-math-kernel-composition-anchor-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - observablehq.config.ts
  - vitest.config.ts
  - eslint.config.js
  - src/lib/example.ts
  - src/lib/__tests__/example.test.ts
  - src/lib/__tests__/fixtures/F01-simple-7limit.scl
  - src/lib/__tests__/fixtures/F02-cents-only.scl
  - src/lib/__tests__/fixtures/F03-mixed-ratio-cents.scl
  - src/lib/__tests__/fixtures/F04-trailing-dot.scl
  - src/lib/__tests__/fixtures/F05-leading-dot.scl
  - src/lib/__tests__/fixtures/F06-bare-integer.scl
  - src/lib/__tests__/fixtures/F07-comments-everywhere.scl
  - src/lib/__tests__/fixtures/F08-bohlen-pierce.scl
  - src/lib/__tests__/fixtures/F09-whitespace-and-trailing-text.scl
  - src/lib/__tests__/fixtures/F10-empty-description.scl
  - src/lib/__tests__/fixtures/F11-mismatched-pitch-count.scl
  - src/lib/__tests__/fixtures/F12-negative-ratio.scl
  - src/lib/__tests__/fixtures/F13-multi-slash.scl
  - src/lib/__tests__/fixtures/F14-large-numerator.scl
  - src/lib/__tests__/fixtures/F15-crlf-line-endings.scl
  - src/lib/__tests__/fixtures/F16-bom.scl
  - src/lib/__tests__/fixtures/golden/partch_43.scl
  - src/lib/__tests__/fixtures/golden/slendro.scl
  - src/lib/__tests__/fixtures/golden/young_lm.scl
  - src/lib/__tests__/fixtures/golden/12-just-chromatic.scl
  - src/lib/__tests__/fixtures/golden/31edo.scl
  - src/lib/__tests__/fixtures/golden/LICENSE.md
autonomous: true
requirements:
  - NOTES-02
  - IO-05
tags:
  - foundation
  - katex
  - vitest-config
  - fixtures
  - cleanup

must_haves:
  decisions_honored:
    - "D-23: KaTeX wired via `head:` injection in `observablehq.config.ts` — CSS only (no auto-render JS)"
    - "R-01: ESLint `no-restricted-imports` forbids `import {Fraction} from \"xen-dev-utils\"` to enforce BigInt-backed Fraction discipline (CONTEXT canonical_refs reaffirms; see also INVENTORY note)"
    - "Phase 1 D-07: Vitest test glob extended to `src/audio/**/__tests__/` and `src/__tests__/` (per PATTERNS S-2 note); colocated test convention preserved"
    - "Phase 2 CONTEXT §Reusable Assets: `src/lib/example.ts` and `src/lib/__tests__/example.test.ts` are DELETED (placeholders only existed for BOOT-03 verification)"
  truths:
    - "User can build the site and the KaTeX stylesheet link appears in the rendered HTML <head>"
    - "User can run `npm test` and Vitest discovers tests under `src/lib/__tests__/`, `src/audio/__tests__/`, and `src/__tests__/`"
    - "User can run `npm run lint` and importing `Fraction` from `xen-dev-utils` triggers an ESLint error (R-01 enforcement)"
    - "16 synthetic `.scl` edge-case fixtures exist on disk with documented contents matching RESEARCH.md F1–F16"
    - "5 golden corpus `.scl` files exist with a LICENSE.md noting source + license; ready for IO-05 round-trip tests"
    - "Phase 1 stubs `src/lib/example.ts` and `src/lib/__tests__/example.test.ts` are deleted"
  artifacts:
    - path: "observablehq.config.ts"
      provides: "Framework config with KaTeX CSS head injection"
      contains: "katex@0.16.45"
    - path: "vitest.config.ts"
      provides: "Vitest config with extended include glob"
      contains: "src/audio/**/__tests__/**/*.test.ts"
    - path: "eslint.config.js"
      provides: "ESLint flat config with R-01 no-restricted-imports rule"
      contains: "no-restricted-imports"
    - path: "src/lib/__tests__/fixtures/F01-simple-7limit.scl"
      provides: "Happy-path 7-limit JI fixture (RESEARCH.md F1)"
      contains: "9/8"
    - path: "src/lib/__tests__/fixtures/F14-large-numerator.scl"
      provides: "BigInt boundary fixture (2^31)"
      contains: "2147483648/2147483647"
    - path: "src/lib/__tests__/fixtures/golden/LICENSE.md"
      provides: "Per-file source + license attribution for golden corpus"
      contains: "Huygens-Fokker"
  key_links:
    - from: "observablehq.config.ts head"
      to: "KaTeX CSS CDN"
      via: "<link rel=\"stylesheet\">"
      pattern: "katex@0.16.45/dist/katex.min.css"
    - from: "vitest.config.ts include"
      to: "src/audio/__tests__/, src/__tests__/"
      via: "test.include array"
      pattern: "src/audio/\\*\\*/__tests__"
    - from: "eslint.config.js"
      to: "ban xen-dev-utils Fraction import"
      via: "no-restricted-imports rule"
      pattern: "no-restricted-imports"
---

<objective>
Lay down the Phase 2 foundation: wire KaTeX into the Framework config (D-23), extend Vitest discovery to cover the new `src/audio/` and root-level `src/__tests__/` directories, codify R-01 (never import `Fraction` from `xen-dev-utils`) as an ESLint rule, delete the Phase-1 placeholder kernel stubs, and lay down the full `.scl` test fixture corpus (16 synthetic edge cases F1–F16 + 5 real Huygens-Fokker archive samples for IO-05 golden tests).

Purpose: Every downstream plan in this phase imports from `xen-dev-utils` and `fraction.js`, mounts tests under multiple directories, and (for Plan 04) consumes the fixture corpus. Doing this wiring once at Wave 1 prevents R-01 regressions, eliminates dead Phase-1 code, and unblocks Plan 04 from having to inline the fixture corpus in a single oversized task.

Output: A configured project ready for kernel work — KaTeX head-injected, Vitest discovers all three test directories, ESLint blocks the wrong `Fraction` import, Phase-1 stub files removed, and 21 `.scl` fixtures (16 synthetic + 5 golden) on disk.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-math-kernel-composition-anchor-mvp/02-CONTEXT.md
@.planning/phases/02-math-kernel-composition-anchor-mvp/02-RESEARCH.md
@.planning/phases/02-math-kernel-composition-anchor-mvp/02-PATTERNS.md
@.planning/phases/02-math-kernel-composition-anchor-mvp/02-UI-SPEC.md
@.planning/phases/02-math-kernel-composition-anchor-mvp/02-VALIDATION.md
@CLAUDE.md
@observablehq.config.ts
@vitest.config.ts
@eslint.config.js
@src/lib/example.ts
@src/lib/__tests__/example.test.ts
@src/lib/INVENTORY.md

<interfaces>
<!-- Current observablehq.config.ts shape (to extend, not replace): -->
```ts
// observablehq.config.ts (current)
export default {
  title: "Tuning Systems",
  root: "src",
  pages: [],
};
```

<!-- Current vitest.config.ts shape: -->
```ts
// vitest.config.ts (current)
export default defineConfig({
  test: {
    include: ["src/lib/**/__tests__/**/*.test.ts", "src/lib/**/*.test.ts"],
    exclude: ["node_modules/**", "dist/**", ".observablehq/**", "src/**/*.md"],
    environment: "node",
    globals: false,
    reporters: ["default"],
  },
});
```

<!-- Current eslint.config.js shape (relevant excerpt): -->
```js
// eslint.config.js (current — adds tseslint type-checked baseline + test-file relaxation)
// no-restricted-imports rule does NOT yet exist.
```

<!-- KaTeX integrity hash and URL per RESEARCH.md §KaTeX Wiring (verify on day-of-implementation per A5): -->
URL: https://cdn.jsdelivr.net/npm/katex@0.16.45/dist/katex.min.css
Integrity: sha384-UA8juhPf75SzzAMA/4fo3yOU7sBJ0om7SCD2GHq0fZqZco6tr1UCV7nUbk9J90JM
Crossorigin: anonymous

<!-- Phase 1 stub files to DELETE (CONTEXT §Reusable Assets line 158): -->
- src/lib/example.ts          (Phase 1 BOOT-03 placeholder)
- src/lib/__tests__/example.test.ts   (Phase 1 BOOT-03 placeholder test)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire KaTeX in observablehq.config.ts; extend Vitest test glob; add R-01 ESLint rule; delete Phase-1 stubs</name>
  <files>observablehq.config.ts, vitest.config.ts, eslint.config.js, src/lib/example.ts, src/lib/__tests__/example.test.ts</files>
  <read_first>
    - observablehq.config.ts (current shape — preserve title/root, extend with `head` and the `pages` array — `pages` will be filled by Plan 07; leave it `[]` here)
    - vitest.config.ts (current `include` array)
    - eslint.config.js (current flat-config structure)
    - src/lib/example.ts and src/lib/__tests__/example.test.ts (confirm contents are placeholder-only before deletion)
    - .planning/phases/02-math-kernel-composition-anchor-mvp/02-RESEARCH.md §KaTeX Wiring (lines 432–467) for the exact `head:` string and SRI hash
    - .planning/phases/02-math-kernel-composition-anchor-mvp/02-RESEARCH.md §Risks → R-01 (lines 677–696) for why the xen-dev-utils Fraction import must be blocked
    - .planning/phases/02-math-kernel-composition-anchor-mvp/02-PATTERNS.md §Pattern S-2 (Vitest test file shape — explains why the include glob must extend to src/audio/__tests__/ and src/__tests__/)
    - .planning/phases/02-math-kernel-composition-anchor-mvp/02-CONTEXT.md §Reusable Assets (line 158 — confirms example.ts is deleted in Phase 2)
  </read_first>
  <action>
    Concrete edits, all in one task because they are tiny and orthogonal:

    1. **`observablehq.config.ts`** — add `head:` (CSS link) AND keep `pages: []` (Plan 07 will fill it). Final file:
    ```ts
    // See https://observablehq.com/framework/config for documentation.
    export default {
      title: "Tuning Systems",
      root: "src",
      pages: [],
      head: `<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.45/dist/katex.min.css" integrity="sha384-UA8juhPf75SzzAMA/4fo3yOU7sBJ0om7SCD2GHq0fZqZco6tr1UCV7nUbk9J90JM" crossorigin="anonymous">`,
    };
    ```

    2. **`vitest.config.ts`** — extend `include` array to cover `src/audio/__tests__/` and `src/__tests__/`. Final `test.include`:
    ```ts
    include: [
      "src/lib/**/__tests__/**/*.test.ts",
      "src/lib/**/*.test.ts",
      "src/audio/**/__tests__/**/*.test.ts",
      "src/audio/**/*.test.ts",
      "src/__tests__/**/*.test.ts",
    ],
    ```
    Keep everything else identical (`exclude`, `environment`, `globals`, `reporters`).

    3. **`eslint.config.js`** — add a NEW config block at the end of the array (after the existing test-file relaxation block) that adds `no-restricted-imports` to ban xen-dev-utils' Fraction. The block:
    ```js
    {
      // R-01 (Phase 2 RESEARCH): ban importing `Fraction` from xen-dev-utils.
      // The xen-dev-utils Fraction is Number-backed and silently loses precision
      // for large numerators; Phase 2 requires the BigInt-backed Fraction from
      // fraction.js@5.3.4. See src/lib/INVENTORY.md and 02-RESEARCH.md §Risks → R-01.
      files: ["src/**/*.ts", "src/**/*.js"],
      rules: {
        "no-restricted-imports": [
          "error",
          {
            paths: [
              {
                name: "xen-dev-utils",
                importNames: ["Fraction"],
                message:
                  "Use the BigInt-backed `Fraction` from `fraction.js` instead. See INVENTORY.md and 02-RESEARCH.md R-01.",
              },
            ],
          },
        ],
      },
    },
    ```
    Mirror the test-file relaxation block (lines 35–41 of current `eslint.config.js`) to disable this rule for test files only IF the rule blocks legitimate test-only imports — but for the kernel tests planned in Phase 2 there is no such need, so leave the rule active in tests too.

    4. **Delete `src/lib/example.ts`** and **`src/lib/__tests__/example.test.ts`**. Use a regular file deletion (the Bash tool's `rm` is fine). Do NOT replace them with anything in this task — Plan 02 creates the real `interval.ts` and tests.

    Do NOT touch `src/lib/INVENTORY.md` in this task — that file gets appended in Plans 02, 03, 04, 05 as kernel symbols are added.
  </action>
  <verify>
    <automated>npm run lint:types &amp;&amp; grep -q "katex@0.16.45" observablehq.config.ts &amp;&amp; grep -q "src/__tests__" vitest.config.ts &amp;&amp; grep -q "no-restricted-imports" eslint.config.js &amp;&amp; test ! -e src/lib/example.ts &amp;&amp; test ! -e src/lib/__tests__/example.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "katex@0.16.45/dist/katex.min.css" observablehq.config.ts` exits 0
    - `grep -q "sha384-UA8juhPf75SzzAMA/4fo3yOU7sBJ0om7SCD2GHq0fZqZco6tr1UCV7nUbk9J90JM" observablehq.config.ts` exits 0
    - `grep -q "src/audio/\*\*/__tests__/\*\*/\*.test.ts" vitest.config.ts` exits 0
    - `grep -q "src/__tests__/\*\*/\*.test.ts" vitest.config.ts` exits 0
    - `grep -q "no-restricted-imports" eslint.config.js` exits 0
    - `grep -q '"xen-dev-utils"' eslint.config.js` exits 0 (the banned-source string)
    - `test ! -e src/lib/example.ts` exits 0
    - `test ! -e src/lib/__tests__/example.test.ts` exits 0
    - `npm run lint:types` exits 0
    - `npm test -- --run` exits 0 (no tests yet — vitest passes when zero are matched? confirm by running; if Vitest errors on empty suite, leave Plan 02 to create the first real test in same wave timing — see Wave note below)
    - `npm run lint` exits 0 (no source files import Fraction from xen-dev-utils yet, so the rule is dormant but installed)
  </acceptance_criteria>
  <done>KaTeX is wired in `observablehq.config.ts` head; vitest discovers tests in three directories; the R-01 ESLint rule is installed and active; Phase-1 stubs are deleted; `npm run lint:types` and `npm run lint` both pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create the .scl fixture corpus — 16 synthetic edge cases (F01–F16) + 5 golden archive samples + LICENSE.md</name>
  <files>src/lib/__tests__/fixtures/F01-simple-7limit.scl, src/lib/__tests__/fixtures/F02-cents-only.scl, src/lib/__tests__/fixtures/F03-mixed-ratio-cents.scl, src/lib/__tests__/fixtures/F04-trailing-dot.scl, src/lib/__tests__/fixtures/F05-leading-dot.scl, src/lib/__tests__/fixtures/F06-bare-integer.scl, src/lib/__tests__/fixtures/F07-comments-everywhere.scl, src/lib/__tests__/fixtures/F08-bohlen-pierce.scl, src/lib/__tests__/fixtures/F09-whitespace-and-trailing-text.scl, src/lib/__tests__/fixtures/F10-empty-description.scl, src/lib/__tests__/fixtures/F11-mismatched-pitch-count.scl, src/lib/__tests__/fixtures/F12-negative-ratio.scl, src/lib/__tests__/fixtures/F13-multi-slash.scl, src/lib/__tests__/fixtures/F14-large-numerator.scl, src/lib/__tests__/fixtures/F15-crlf-line-endings.scl, src/lib/__tests__/fixtures/F16-bom.scl, src/lib/__tests__/fixtures/golden/partch_43.scl, src/lib/__tests__/fixtures/golden/slendro.scl, src/lib/__tests__/fixtures/golden/young_lm.scl, src/lib/__tests__/fixtures/golden/12-just-chromatic.scl, src/lib/__tests__/fixtures/golden/31edo.scl, src/lib/__tests__/fixtures/golden/LICENSE.md</files>
  <read_first>
    - .planning/phases/02-math-kernel-composition-anchor-mvp/02-RESEARCH.md §.scl Format Reference (lines 357–430) — line structure rules + F1–F16 fixture spec table
    - .planning/phases/02-math-kernel-composition-anchor-mvp/02-RESEARCH.md A4 (lines 736–737) — license note for archive samples
    - .planning/phases/02-math-kernel-composition-anchor-mvp/02-CONTEXT.md D-20 (line 107) — golden corpus size guidance
    - https://www.huygens-fokker.org/scala/scl_format.html — canonical format reference
  </read_first>
  <action>
    Create 16 synthetic fixtures + 5 golden samples + a LICENSE.md attribution. Each fixture follows the .scl spec exactly:
    - Line 1+: zero or more `!`-prefixed comment lines (any number, anywhere)
    - First non-comment line: description (may be empty)
    - Second non-comment line: pitch count (integer, EXCLUDES the implicit 1/1)
    - Subsequent non-comment lines: pitch values (ratio, cents, or — for tests — the bra-ket monzo `[a b c>` is supported by our parser per D-15 even though Scala canonical form does not list it)

    Write each file using the Write tool — one Write call per file. Exact contents:

    **F01-simple-7limit.scl** (happy-path):
    ```
    ! F01-simple-7limit.scl
    !
    Simple 7-limit JI heptatonic
     7
    ! pitches
     9/8
     5/4
     21/16
     3/2
     27/16
     7/4
     2/1
    ```

    **F02-cents-only.scl** (cents detection via `.`):
    ```
    ! F02-cents-only.scl
    12-TET as cents
     12
     100.0
     200.0
     300.0
     400.0
     500.0
     600.0
     700.0
     800.0
     900.0
     1000.0
     1100.0
     1200.0
    ```

    **F03-mixed-ratio-cents.scl** (mixed types):
    ```
    ! F03-mixed-ratio-cents.scl
    Mixed ratio + cents
     7
     9/8
     408.0
     4/3
     700.0
     5/3
     1100.0
     2/1
    ```

    **F04-trailing-dot.scl** (cents detection on trailing-period):
    ```
    ! F04-trailing-dot.scl
    Trailing-period cents
     2
     408.
     1200.0
    ```

    **F05-leading-dot.scl** (cents detection on leading-period):
    ```
    ! F05-leading-dot.scl
    Leading-period cents
     2
     .5
     1200.0
    ```

    **F06-bare-integer.scl** (bare integer = ratio per spec):
    ```
    ! F06-bare-integer.scl
    Bare integer
     1
     2
    ```
    (The single pitch `2` should parse as the ratio `2/1`, which is also the period.)

    **F07-comments-everywhere.scl** (comments in arbitrary positions):
    ```
    ! leading comment 1
    ! leading comment 2
    Comments everywhere
    ! comment between description and count
     3
    ! comment between count and first pitch
     5/4
    ! comment between pitches
     3/2
     2/1
    ! trailing comment
    ```

    **F08-bohlen-pierce.scl** (non-octave period — tritave 3/1):
    ```
    ! F08-bohlen-pierce.scl
    Bohlen-Pierce 9-tone subset
     9
     27/25
     25/21
     9/7
     7/5
     75/49
     5/3
     9/5
     49/25
     3/1
    ```

    **F09-whitespace-and-trailing-text.scl** (whitespace + ignored trailing text):
    ```
    ! F09-whitespace-and-trailing-text.scl
    Whitespace and trailing text ignored
     3
     5/4   E\\
    	3/2	just-fifth
       2/1   octave
    ```
    (The line for `5/4` has a tab, the line for `3/2` is tab-prefixed, and `2/1` has leading spaces. Trailing names like `E\\` must be ignored by the parser.)

    **F10-empty-description.scl** (empty description per spec):
    ```
    ! F10-empty-description.scl

     2
     3/2
     2/1
    ```
    (Line 2 — the description — is intentionally empty. Per Huygens-Fokker spec, "If there is no description, there should be an empty line.")

    **F11-mismatched-pitch-count.scl** (pitch count says 5, only 4 follow — MUST error):
    ```
    ! F11-mismatched-pitch-count.scl
    Mismatched pitch count - count says 5 but only 4 lines follow
     5
     5/4
     3/2
     5/3
     2/1
    ```

    **F12-negative-ratio.scl** (MUST error per spec):
    ```
    ! F12-negative-ratio.scl
    Negative ratio - parser MUST error
     1
     -5/4
    ```

    **F13-multi-slash.scl** (MUST error per spec):
    ```
    ! F13-multi-slash.scl
    Multi-slash ratio - parser MUST error
     1
     2/3/4
    ```

    **F14-large-numerator.scl** (BigInt boundary — verifies fraction.js@5):
    ```
    ! F14-large-numerator.scl
    Large numerator - exact 2^31 boundary tests BigInt path
     1
     2147483648/2147483647
    ```

    **F15-crlf-line-endings.scl** (CRLF — same content as F1):
    Write F15 by literally including `\r\n` line terminators. Easiest: use a Bash heredoc OR write Python/Node to produce `\r\n` endings. Content is identical to F01 but with CRLF. Use Bash:
    ```
    printf 'CONTENTS\r\nLINE2\r\n' > src/lib/__tests__/fixtures/F15-crlf-line-endings.scl
    ```
    where CONTENTS / LINE2 are each line of F01 in order. Verify with `file src/lib/__tests__/fixtures/F15-crlf-line-endings.scl` showing CRLF terminators.

    **F16-bom.scl** (UTF-8 BOM prefix):
    Write F16 with the same content as F01 but prefixed by `0xEF 0xBB 0xBF` BOM. Easiest: `printf '\xEF\xBB\xBF' > F16; cat F01 >> F16`. Verify with `xxd -l 3 F16` showing `efbbbf`.

    **Golden corpus** — 5 archive samples for IO-05. The Huygens-Fokker archive is at https://www.huygens-fokker.org/docs/scales.zip (or individual file links from https://www.huygens-fokker.org/scala/downloads.html). Per RESEARCH.md A4, individual `.scl` files in the public archive are typically free-use; verify per file at commit time. Required:

    - `partch_43.scl` — Harry Partch's 43-tone JI (the canonical test for large JI scales)
    - `slendro.scl` — Indonesian gamelan slendro (cents-mostly)
    - `young_lm.scl` — La Monte Young's Well-Tuned Piano (large-numerator JI)
    - `12-just-chromatic.scl` — a generic 12-tone JI chromatic scale (write our own if no archive match)
    - `31edo.scl` — 31-tone equal temperament as cents (write our own — `(1200/31 * n).toFixed(6)` for n in 1..31)

    For the three named archive files (partch_43, slendro, young_lm): fetch from the Huygens-Fokker archive URL above OR mirror from a known good source. If the user's network blocks the fetch, generate equivalent synthetic content matching the published file contents (Partch's 43-tone is well-documented; the published spelling is in countless references). Document the source in the LICENSE.md.

    For `12-just-chromatic.scl` (we can author this — it's a 12-tone 5-limit JI):
    ```
    ! 12-just-chromatic.scl
    12-tone 5-limit JI chromatic
     12
     16/15
     9/8
     6/5
     5/4
     4/3
     45/32
     3/2
     8/5
     5/3
     9/5
     15/8
     2/1
    ```

    For `31edo.scl` (we author this — pure cents, 31 steps):
    ```
    ! 31edo.scl
    31-tone equal temperament
     31
     38.709677
     77.419355
     116.129032
     154.838710
     193.548387
     232.258065
     270.967742
     309.677419
     348.387097
     387.096774
     425.806452
     464.516129
     503.225806
     541.935484
     580.645161
     619.354839
     658.064516
     696.774194
     735.483871
     774.193548
     812.903226
     851.612903
     890.322581
     929.032258
     967.741935
     1006.451613
     1045.161290
     1083.870968
     1122.580645
     1161.290323
     1200.000000
    ```

    **`golden/LICENSE.md`** — attribution per A4:
    ```md
    # Golden corpus — license + attribution

    Files in this directory exist to verify Phase 2 IO-05 round-trip behavior
    against representative `.scl` files. Per file:

    | File | Source | License |
    |------|--------|---------|
    | `partch_43.scl` | Huygens-Fokker `scales` archive — https://www.huygens-fokker.org/docs/scales.zip — file: `partch_43.scl`. The archive aggregates files contributed under permissive terms; specific authorship of this entry is the work of Harry Partch (in the public domain in most jurisdictions for music-theoretic notation of this kind). | Public domain / free use |
    | `slendro.scl` | Huygens-Fokker `scales` archive | Public domain / free use |
    | `young_lm.scl` | Huygens-Fokker `scales` archive — represents La Monte Young's Well-Tuned Piano tuning system; the file format is descriptive (rationals only) and falls outside copyright concerns | Free use for research |
    | `12-just-chromatic.scl` | Authored for this project | MIT (matches repo) |
    | `31edo.scl` | Authored for this project | MIT (matches repo) |

    If you redistribute any of the Huygens-Fokker-derived files outside this
    repository, verify the current license terms at
    https://www.huygens-fokker.org/scala/ — the archive is published as a free
    research resource but per-file licensing should be re-confirmed at the time
    of redistribution.
    ```

    All 22 files are static text; no logic, no tests in this task. The fixtures' parsing behavior is validated in Plan 04's tests.
  </action>
  <verify>
    <automated>test -d src/lib/__tests__/fixtures &amp;&amp; test -d src/lib/__tests__/fixtures/golden &amp;&amp; test $(ls src/lib/__tests__/fixtures/F*.scl 2&gt;/dev/null | wc -l) -eq 16 &amp;&amp; test $(ls src/lib/__tests__/fixtures/golden/*.scl 2&gt;/dev/null | wc -l) -eq 5 &amp;&amp; test -f src/lib/__tests__/fixtures/golden/LICENSE.md &amp;&amp; grep -q "9/8" src/lib/__tests__/fixtures/F01-simple-7limit.scl &amp;&amp; grep -q "2147483648/2147483647" src/lib/__tests__/fixtures/F14-large-numerator.scl &amp;&amp; xxd -l 3 src/lib/__tests__/fixtures/F16-bom.scl | grep -q "efbb bf"</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/lib/__tests__/fixtures/F*.scl | wc -l` outputs `16`
    - `ls src/lib/__tests__/fixtures/golden/*.scl | wc -l` outputs `5`
    - `test -f src/lib/__tests__/fixtures/golden/LICENSE.md` exits 0
    - `grep -q "Huygens-Fokker" src/lib/__tests__/fixtures/golden/LICENSE.md` exits 0
    - `grep -q "9/8" src/lib/__tests__/fixtures/F01-simple-7limit.scl` exits 0
    - `grep -q "2147483648/2147483647" src/lib/__tests__/fixtures/F14-large-numerator.scl` exits 0
    - `xxd -l 3 src/lib/__tests__/fixtures/F16-bom.scl | grep -q "efbb bf"` exits 0 (BOM bytes present)
    - `file src/lib/__tests__/fixtures/F15-crlf-line-endings.scl | grep -q "CRLF"` exits 0 (or equivalent: `od -c <file> | grep '\\r \\n'`)
    - `grep -q "^ -5/4" src/lib/__tests__/fixtures/F12-negative-ratio.scl` exits 0
    - `grep -q "^ 2/3/4" src/lib/__tests__/fixtures/F13-multi-slash.scl` exits 0
    - `grep -q "31" src/lib/__tests__/fixtures/golden/31edo.scl` exits 0
  </acceptance_criteria>
  <done>22 fixture files exist on disk in the spec'd shape, the LICENSE.md attributes each golden file, and Plan 04's tests can iterate them without further preparation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN → browser | KaTeX CSS loads from `cdn.jsdelivr.net` over HTTPS; integrity-pinned via SRI hash. If CDN is compromised or rotates the file, the SRI hash mismatch will block the load (browser refuses) — causing styled-math regression but not an exploit. |
| Test fixtures → parser | Static fixture files are committed to the repo and consumed by Plan 04's parser tests. Trusted at write time; no untrusted-input boundary in Plan 01 itself. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | KaTeX CDN delivery | mitigate | SRI integrity hash `sha384-UA8juhPf75SzzAMA/4fo3yOU7sBJ0om7SCD2GHq0fZqZco6tr1UCV7nUbk9J90JM` pinned in `head:` — browsers reject mismatched payload |
| T-02-02 | Information Disclosure | KaTeX CDN third-party request reveals user IP | accept | Static-site research notebook with no PII; CDN request is identical to any public site loading KaTeX. Per CONTEXT D-23 the CDN path is acceptable; self-host is the documented fallback (RESEARCH.md Environment Availability) |
| T-02-03 | Denial of Service | KaTeX CDN unreachable | accept | Fallback to text-rendered math (KaTeX gracefully degrades — math source remains visible as plain text); not a security event |
</threat_model>

<verification>
After both tasks complete:
- `npm run lint:types` exits 0
- `npm test -- --run` exits 0 (zero tests is acceptable; if Vitest errors on empty include match, this is the moment to confirm — Plan 02 adds the first real tests in the next wave)
- `npm run lint` exits 0
- `npm run build` exits 0 AND `grep -q "katex@0.16.45" dist/index.html` (or `dist/_observablehq/_chunks/*.html`) exits 0 — proves head injection landed in the rendered HTML
- `find src/lib/__tests__/fixtures -type f | wc -l` outputs `22` (16 synthetic + 5 golden + 1 LICENSE)
- `git status --short` shows the deletion of `src/lib/example.ts` and `src/lib/__tests__/example.test.ts`
</verification>

<success_criteria>
- KaTeX CSS link tag is present in `dist/index.html` after `npm run build`
- Vitest's `include` array contains all three test directory globs
- ESLint rule `no-restricted-imports` rejects `import { Fraction } from "xen-dev-utils"` — verifiable by writing a one-liner test file containing that import and confirming `npm run lint` errors (not part of the automated verify, but a sanity check)
- Phase-1 stub files removed from disk
- 22 `.scl` fixture files present on disk with shape per RESEARCH.md F1–F16 + LICENSE.md
- All four CI gates (`lint:types`, `test`, `lint`, `format:check`) green
</success_criteria>

<output>
After completion, create `.planning/phases/02-math-kernel-composition-anchor-mvp/02-01-SUMMARY.md` per the standard summary template.
</output>
