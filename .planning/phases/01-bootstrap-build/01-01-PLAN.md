---
phase: 01-bootstrap-build
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .nvmrc
  - .gitignore
  - README.md
  - observablehq.config.ts
  - src/index.md
  - src/lib/.gitkeep
  - src/components/.gitkeep
  - src/data/.gitkeep
autonomous: true
requirements:
  - BOOT-01
requirements_addressed:
  - BOOT-01
tags:
  - bootstrap
  - observable-framework
  - scaffold

must_haves:
  decisions_honored:
    - "D-06: Framework default layout — `root` stays at `src/`; `src/lib/`, `src/components/`, `src/data/` directories created with `.gitkeep`"
    - "D-13: package.json contains the full script set verbatim — `dev`, `build`, `lint:types`, `test`, `test:watch`, `lint`, `format`, `format:check`, `ci`"
    - "D-17: `@observablehq/framework` is exact-pinned to `1.13.4` (no caret); other dependencies use caret ranges"
    - "D-18: `.nvmrc` contents equal `20`; `package.json#engines.node` is `>=20`"
    - "D-19: `.gitignore` contains `node_modules/`, `dist/`, `.observablehq/cache/`, `*.log`, `.env*`, `.DS_Store`"
    - "D-20: `README.md` contains project name, one-line description, `npm install && npm run dev` quickstart, link to deployed site, link to `.planning/PROJECT.md` — and NO CONTRIBUTING.md / CHANGELOG / docs site"
  truths:
    - "User can run `npm run dev` and see an Observable Framework site at http://localhost:3000"
    - "User can run `npm run build` and produce a `dist/` directory containing static HTML"
    - "Project follows the D-06 layout: src/ root with lib/, components/, data/ subfolders"
    - "Node version is pinned to 20 LTS via .nvmrc and package.json#engines"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with D-13 scripts and pinned @observablehq/framework@1.13.4"
      contains: '"@observablehq/framework": "1.13.4"'
    - path: ".nvmrc"
      provides: "Node 20 LTS pin per D-18"
      contains: "20"
    - path: ".gitignore"
      provides: "Standard ignores per D-19"
      contains: "node_modules/"
    - path: "README.md"
      provides: "Quickstart per D-20"
      contains: "npm run dev"
    - path: "observablehq.config.ts"
      provides: "Framework config (TS supported)"
    - path: "src/index.md"
      provides: "Landing page placeholder (final content arrives in Plan 03)"
  key_links:
    - from: "package.json scripts.dev"
      to: "@observablehq/framework binary `observable preview`"
      via: "npm run dev"
      pattern: '"dev":\\s*"observable preview"'
    - from: "package.json scripts.build"
      to: "@observablehq/framework binary `observable build`"
      via: "npm run build"
      pattern: '"build":\\s*"observable build"'
---

<objective>
Bootstrap an Observable Framework project at the repository root with the D-06 directory layout, pinned Framework version (D-17), Node 20 LTS pin (D-18), the full set of D-13 npm scripts, and the bare-minimum supporting files (`.gitignore` per D-19, `README.md` per D-20). After this plan, `npm install && npm run dev` must serve a working Framework site at localhost — even if the landing page is still the default scaffold (Plan 03 replaces it with the Fraction-renders-cents demo).

Purpose: Lay down the project skeleton so subsequent plans can layer on TS/Vitest/lint config (Plan 02), the xen-dev stack and the hello page (Plan 03), and CI/Pages deploy (Plan 04). This plan must NOT install kernel libraries, configure TypeScript, or write any logic — those are downstream concerns.

Output: A runnable Framework project: `package.json` with the exact D-13 scripts, `package-lock.json`, `.nvmrc`, `.gitignore`, `README.md`, `observablehq.config.ts`, `src/index.md`, and empty `.gitkeep` placeholders in `src/lib/`, `src/components/`, `src/data/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bootstrap-build/01-CONTEXT.md
@CLAUDE.md
@.planning/research/STACK.md

<key_decisions>
<!-- Locked decisions from CONTEXT.md that this plan must implement verbatim. -->

D-06: Framework default layout. `root` stays at `src/`. Subfolders: `src/lib/`, `src/components/`, `src/data/`. Markdown pages and code coexist under `src/`.

D-13: package.json scripts (verbatim — copy these exactly):
  - `"dev": "observable preview"`
  - `"build": "observable build"`
  - `"lint:types": "tsc --noEmit"`
  - `"test": "vitest run"`
  - `"test:watch": "vitest"`
  - `"lint": "eslint ."`
  - `"format": "prettier --write ."`
  - `"format:check": "prettier --check ."`
  - `"ci": "npm run lint:types && npm run test && npm run lint && npm run format:check && npm run build"`

D-17: Pin `@observablehq/framework` to exactly `1.13.4` (no caret). Other packages use caret ranges. `package-lock.json` MUST be committed.

D-18: `.nvmrc` contents = `20`. `package.json#engines.node` = `">=20"`.

D-19: `.gitignore` lines (exact):
  - `node_modules/`
  - `dist/`
  - `.observablehq/cache/`
  - `*.log`
  - `.env*`
  - `.DS_Store`

D-20: README.md content: project name, one-line description, `npm install && npm run dev` quickstart, link to deployed site (placeholder URL — `https://<user>.github.io/<repo>/` until D-04 wiring lands in Plan 04), link to `.planning/PROJECT.md`. NO CONTRIBUTING.md / CHANGELOG.

D-05: Do NOT add an `observable deploy` script (deprecated). Build is `observable build`; deploy is via GitHub Actions in Plan 04.
</key_decisions>

<framework_conventions>
<!-- From CLAUDE.md "Observable Framework Conventions" section -->

- Use `npm` (not pnpm/yarn). Framework's default and most reliable.
- `observablehq.config.ts` (TypeScript config file) is supported and preferred over `.js`.
- Markdown pages live directly under `src/` (e.g., `src/index.md`); subdirectories like `src/pages/` may be added later when there are multiple pages.
- The Framework scaffold from `npx @observablehq/framework@latest create` will produce its own boilerplate; this plan must REPLACE the scaffolded `package.json` scripts with D-13's exact set, and prune any sample pages it generates (we ship a single `src/index.md` placeholder).
</framework_conventions>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Initialize Framework project at repo root</name>
  <files>
    package.json,
    package-lock.json,
    observablehq.config.ts,
    src/index.md,
    src/lib/.gitkeep,
    src/components/.gitkeep,
    src/data/.gitkeep
  </files>
  <read_first>
    - .planning/phases/01-bootstrap-build/01-CONTEXT.md (D-06 layout, D-13 scripts, D-17 pinning)
    - CLAUDE.md (Tech Stack section — Recommended Stack table, Observable Framework Conventions)
    - .planning/research/STACK.md (Installation section — exact npm commands)
    - The current repo root (`ls -la /Users/taylorbrook/Dev/Tuning\ Systems/`) to confirm what's already there before bootstrapping
  </read_first>
  <action>
    Bootstrap the Framework project. Note: the repo currently contains only `CLAUDE.md`, `.planning/`, and editor cruft — there is no existing `package.json`. Steps:

    1. Run `npx @observablehq/framework@1.13.4 create` from the repo root with these answers (use `--yes` style flags or pipe answers; if interactive, answer):
       - Project location: `.` (current directory)
       - Project title: `Tuning Systems`
       - Use TypeScript: yes
       - Include sample files: minimal (we will prune)
       - Install npm dependencies: yes
       - Initialize git: NO (the user manages git)

       If `npx` chokes on the non-empty directory, use a temp directory and copy in: `cd /tmp && npx @observablehq/framework@1.13.4 create tuning-temp` (answer prompts), then move the generated files into the repo root with `rsync -a /tmp/tuning-temp/ /Users/taylorbrook/Dev/Tuning\ Systems/ --exclude=.git`. Delete `/tmp/tuning-temp` afterwards.

    2. After scaffold lands, OVERWRITE `package.json` to replace the generated scripts block with the D-13 set verbatim and pin Framework to `1.13.4` (no caret). The final `package.json` must look like:

       ```json
       {
         "name": "tuning-systems",
         "version": "0.0.0",
         "private": true,
         "type": "module",
         "engines": {
           "node": ">=20"
         },
         "scripts": {
           "dev": "observable preview",
           "build": "observable build",
           "lint:types": "tsc --noEmit",
           "test": "vitest run",
           "test:watch": "vitest",
           "lint": "eslint .",
           "format": "prettier --write .",
           "format:check": "prettier --check .",
           "ci": "npm run lint:types && npm run test && npm run lint && npm run format:check && npm run build"
         },
         "dependencies": {
           "@observablehq/framework": "1.13.4"
         }
       }
       ```

       Note: `dependencies` only contains Framework here — `devDependencies` (typescript, vitest, eslint, prettier) and additional runtime deps (xen-dev-utils etc.) are added by Plans 02 and 03. Do NOT add them in this task.

    3. Run `npm install` to regenerate `package-lock.json` against the rewritten `package.json`. Commit BOTH `package.json` and `package-lock.json`.

    4. Verify `observablehq.config.ts` exists and is TypeScript (rename from `.js` to `.ts` if the scaffold wrote `.js`). Minimal contents:
       ```typescript
       export default {
         title: "Tuning Systems",
         root: "src",
         pages: []
       };
       ```

    5. Prune scaffold sample pages and assets:
       - Delete any `src/example-*.md`, `src/dashboard.md`, `src/data/*.csv*`, `src/data/*.json*`, `src/components/*.{ts,js}` that the scaffold generated (we want clean folders).
       - REPLACE `src/index.md` with a minimal placeholder:
         ```markdown
         # Tuning Systems

         Project bootstrapping in progress. The hello-world Fraction.js demo lands in the next plan.
         ```
         (This will be replaced again in Plan 03.)

    6. Create empty `.gitkeep` files at `src/lib/.gitkeep`, `src/components/.gitkeep`, `src/data/.gitkeep` so the empty directory structure is preserved by git.

    7. Run `npm run build` once to confirm the scaffold produces a `dist/` directory without errors. Then delete `dist/` (it'll be in `.gitignore` by Task 2).
  </action>
  <verify>
    <automated>test -f package.json && test -f package-lock.json && test -f observablehq.config.ts && test -f src/index.md && test -d src/lib && test -d src/components && test -d src/data && grep -q '"@observablehq/framework": "1.13.4"' package.json && grep -q '"dev": "observable preview"' package.json && grep -q '"ci": "npm run lint:types && npm run test && npm run lint && npm run format:check && npm run build"' package.json && npm run build > /tmp/build.log 2>&1 && test -d dist && rm -rf dist</automated>
  </verify>
  <acceptance_criteria>
    - File `package.json` exists at repo root.
    - `grep -c '"@observablehq/framework": "1.13.4"' package.json` outputs `1` (exact pin, no caret).
    - `package.json` contains all 9 D-13 scripts: `dev`, `build`, `lint:types`, `test`, `test:watch`, `lint`, `format`, `format:check`, `ci` — verifiable with: `node -e "const p=require('./package.json'); ['dev','build','lint:types','test','test:watch','lint','format','format:check','ci'].forEach(k=>{if(!p.scripts[k]) {console.error('missing',k); process.exit(1)}})"` exits 0.
    - `package.json` `engines.node` equals `">=20"`: `node -e "const p=require('./package.json'); if(p.engines.node!=='>=20') process.exit(1)"` exits 0.
    - `package.json` `type` equals `"module"`.
    - `package-lock.json` exists and is committed.
    - File `observablehq.config.ts` exists (NOT `.js`) and contains `root: "src"`.
    - File `src/index.md` exists.
    - Directories `src/lib/`, `src/components/`, `src/data/` exist (each contains `.gitkeep`).
    - `npm run build` exits 0 and produces a `dist/` directory containing at least one `.html` file. (Test cleans up `dist/` afterwards since it's gitignored.)
    - No leftover scaffold sample files: `find src -name 'example-*.md' -o -name 'dashboard.md' | wc -l` outputs `0`.
  </acceptance_criteria>
  <done>
    Framework project scaffolded with D-06 layout. `npm install` succeeds. `npm run build` produces `dist/`. `package.json` has D-13 scripts and pins Framework to 1.13.4. Subsequent plans can install devDeps, configure tooling, install xen-dev stack, and add CI without revisiting the scaffold step.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write .nvmrc, .gitignore, and README.md</name>
  <files>
    .nvmrc,
    .gitignore,
    README.md
  </files>
  <read_first>
    - .planning/phases/01-bootstrap-build/01-CONTEXT.md (D-18, D-19, D-20)
    - .planning/PROJECT.md (for the one-line project description in README)
    - The current `.gitignore` (if any) — the Framework scaffold may have written one; we need to ensure final contents match D-19 exactly
  </read_first>
  <action>
    Write three files at the repo root with exact contents specified by D-18, D-19, and D-20.

    **`.nvmrc`** (D-18) — single line, no trailing newline ambiguity (one newline at EOF is fine):
    ```
    20
    ```

    **`.gitignore`** (D-19) — REPLACE any scaffold-generated `.gitignore`. Final contents (one entry per line, in this order):
    ```
    node_modules/
    dist/
    .observablehq/cache/
    *.log
    .env*
    .DS_Store
    ```
    Do NOT add `src/.observablehq` — Framework already manages caches under `.observablehq/` per D-19's note.

    **`README.md`** (D-20) — bare-minimum quickstart. Contents:
    ```markdown
    # Tuning Systems

    A research notebook + JI calculator built as an Observable Framework site — Markdown prose on tuning-systems theory living side-by-side with reactive JavaScript cells that compute, visualize, and audition just-intonation intervals.

    ## Quickstart

    ```bash
    npm install
    npm run dev
    ```

    Open <http://localhost:3000> in your browser.

    ## Build

    ```bash
    npm run build
    ```

    Produces a static site in `dist/`.

    ## Live site

    Deployed to GitHub Pages: <https://taylorbrook.github.io/tuning-systems/> (URL wired up in Phase 1 Plan 04 — placeholder until then).

    ## Project background

    See [`.planning/PROJECT.md`](./.planning/PROJECT.md) for the project's core value, constraints, and out-of-scope list.
    ```

    No CONTRIBUTING.md, no CHANGELOG.md — both explicitly excluded by D-20.
  </action>
  <verify>
    <automated>test -f .nvmrc && test -f .gitignore && test -f README.md && [ "$(cat .nvmrc | tr -d '[:space:]')" = "20" ] && grep -qx 'node_modules/' .gitignore && grep -qx 'dist/' .gitignore && grep -qx '.observablehq/cache/' .gitignore && grep -qx '\*.log' .gitignore && grep -qx '.env\*' .gitignore && grep -qx '.DS_Store' .gitignore && grep -q 'npm run dev' README.md && grep -q '.planning/PROJECT.md' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `.nvmrc` exists; `cat .nvmrc | tr -d '[:space:]'` equals exactly `20`.
    - `.gitignore` exists and contains exactly these lines (each on its own line, no others required, but each MUST be present):
      - `node_modules/`
      - `dist/`
      - `.observablehq/cache/`
      - `*.log`
      - `.env*`
      - `.DS_Store`
      Verify: `for line in 'node_modules/' 'dist/' '.observablehq/cache/' '*.log' '.env*' '.DS_Store'; do grep -Fxq "$line" .gitignore || { echo "missing $line"; exit 1; }; done` exits 0.
    - `README.md` exists.
    - `README.md` contains `npm install` AND `npm run dev` AND `.planning/PROJECT.md` AND `Tuning Systems` AND `Observable Framework`.
    - `README.md` does NOT contain `CONTRIBUTING` or `CHANGELOG` (per D-20 exclusion): `grep -ciE '(contributing|changelog)' README.md` outputs `0`.
  </acceptance_criteria>
  <done>
    Three project metadata files in place. `.nvmrc` pins Node 20. `.gitignore` matches D-19 exactly. `README.md` has the D-20 quickstart and links per spec. The repo is now in a state where a fresh clone + `nvm use && npm install && npm run dev` would Just Work.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Untrusted upstream packages installed at scaffold time. |
| Repo committed files → developer machine | `.gitignore` controls what leaks into git history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `@observablehq/framework` package | mitigate | Pin exact version `1.13.4` per D-17 (no caret). `package-lock.json` committed for SHA pinning of transitive deps. Future minor bumps are an explicit bump, not a silent drift. |
| T-01-02 | Information Disclosure | Secrets in committed files | mitigate | `.gitignore` includes `.env*` per D-19, preventing accidental commit of environment files. README documents no secrets. Phase 1 has no third-party API keys (D-04 defers custom domain; GitHub Pages uses GITHUB_TOKEN handled by CI in Plan 04). |
| T-01-03 | Tampering | `node_modules/` content | accept | Local-only artifact, gitignored. Supply-chain risk handled at `package-lock.json` level (next install verifies hashes). Framework + scaffold are first-party Observable code; risk is LOW. |
| T-01-04 | Denial of Service | Large `dist/` build artifacts in git | mitigate | `.gitignore` excludes `dist/`. CI builds fresh per push (Plan 04). |
</threat_model>

<verification>
After both tasks complete, the repo must satisfy:

1. **Fresh-clone smoke test:** `rm -rf node_modules dist && npm install && npm run build` exits 0 and produces `dist/`.
2. **Dev server smoke test:** `npm run dev &` starts a server on port 3000 (or Framework's default), then `kill %1` cleanly.
3. **Layout check:** `test -d src/lib && test -d src/components && test -d src/data` all pass.
4. **Pin check:** `grep -c '"@observablehq/framework": "1.13.4"' package.json` outputs `1` (no caret, no range).
5. **No leftover scaffold:** `find src -maxdepth 2 -name 'example-*.md' -o -name 'dashboard.md' | wc -l` outputs `0`.
</verification>

<success_criteria>
- [ ] `package.json` exists with all 9 D-13 scripts and Framework pinned to exactly `1.13.4`.
- [ ] `package-lock.json` exists and is committed.
- [ ] `.nvmrc` contains `20`.
- [ ] `.gitignore` contains the 6 D-19 entries.
- [ ] `README.md` matches the D-20 minimal spec.
- [ ] `observablehq.config.ts` exists (TypeScript, not JavaScript), declares `root: "src"`.
- [ ] `src/index.md` exists with placeholder content.
- [ ] `src/lib/`, `src/components/`, `src/data/` exist as directories (with `.gitkeep`).
- [ ] `npm run build` exits 0.
- [ ] BOOT-01 satisfied: project is scaffolded, build is deployable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-build/01-01-SUMMARY.md` documenting:
- What was scaffolded vs. what was rewritten (which scaffold defaults were replaced and why)
- The exact `@observablehq/framework` version committed (`1.13.4`)
- Confirmation that `npm run build` worked at end of plan
- Any deviations from D-06 layout (there should be none)
- File paths created / modified (absolute paths from repo root)
</output>
