---
phase: 06-exact-rational-ji-harmonic-generators
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/cps.ts
  - src/lib/__tests__/cps.test.ts
  - src/lib/INVENTORY.md
autonomous: true
requirements: [GEN-01]
must_haves:
  truths:
    - "cps([1,3,5,7], 2) octave-reduced equals the canonical 1-3-5-7 Hexany set (exact, deduped by BigInt n/d)"
    - "Dekany (2-of-5 {1,3,5,7,9}) yields 10 distinct degrees + period"
    - "Eikosany (3-of-6 {1,3,5,7,9,11}) yields 20 distinct degrees + period"
    - "Coincident products dedupe to one entry by exact n/d (never cents tolerance)"
    - "Out-of-bounds k or oversized factor set throws RangeError (D-14 caps)"
  artifacts:
    - path: "src/lib/cps.ts"
      provides: "Hand-rolled BigInt-exact CPS kernel primitive"
      exports: ["cps"]
      contains: "export function cps"
    - path: "src/lib/__tests__/cps.test.ts"
      provides: "Verified-vector test for Hexany + presets + caps"
      contains: "cps("
    - path: "src/lib/INVENTORY.md"
      provides: "Kernel inventory row for cps"
      contains: "| `cps`"
  key_links:
    - from: "src/lib/cps.ts"
      to: "src/lib/interval.ts"
      via: "Interval.mul / Interval.octaveReduce"
      pattern: "octaveReduce"
    - from: "src/lib/cps.ts"
      to: "xen-dev-utils"
      via: "kCombinations"
      pattern: "kCombinations"
---

<objective>
Ship `cps(factors, k, period)` — the flagship JI combinatorial kernel primitive (GEN-01).
A hand-rolled, BigInt-exact Combination Product Set: choose-k over a factor set, multiply
each subset, octave-reduce, dedupe by exact `n/d`, sort by cents, append period.

Purpose: CPS (Hexany / Dekany / Eikosany) is the JI structure the user cares about most.
It must be exact — deduped by BigInt ratio, never cents tolerance — and transparently owned
in our kernel (D-12: hand-rolled over `xen-dev-utils.kCombinations` for BigInt ownership,
not wrapping SonicWeave's `cps`).

Output: `src/lib/cps.ts` + its test + an INVENTORY.md row. Pure kernel — no DOM, no audio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-exact-rational-ji-harmonic-generators/06-CONTEXT.md

<interfaces>
<!-- The kernel contracts this primitive builds on. Extracted from src/lib/interval.ts. -->
<!-- Use these directly — no codebase exploration needed. -->

From src/lib/interval.ts (the universal currency — BigInt-backed Fraction, NEVER xen-dev-utils' Fraction per R-01):
```typescript
export class Interval {
  readonly fraction: Fraction;            // fraction.js@5.3.4 BigInt-backed
  constructor(input: FractionInput);      // accepts "5/4" string, {n,d}, bigint, number
  get cents(): number;                    // DISPLAY PROJECTION ONLY (Pitfall #1) — fine for SORTING
  mul(other: Interval): Interval;         // exact BigInt multiply, returns new Interval
  div(other: Interval): Interval;
  octaveReduce(period?: Interval): Interval;  // period-aware; default 2/1; throws if period <= 1/1
  equals(other: Interval): boolean;       // exact BigInt Fraction equality
  toString(): string;                     // fraction.toFraction() → "5/4"
}
```

From xen-dev-utils (already a project dependency — see CLAUDE.md stack):
```typescript
// kCombinations(arr, k) returns every k-element combination of arr's ELEMENTS
// (works on objects, so it works on Interval[] directly). Import named.
export function kCombinations<T>(set: T[], k: number): T[][];
```
</interfaces>

<exactness_rules>
- Dedupe ONLY by the exact `${n}/${d}` string key in a Set — NEVER cents-within-epsilon (Pitfall #1/#6). `iv.fraction.toFraction()` gives the canonical reduced `n/d` string.
- Factors are `Interval`s built from integer ratios; products stay BigInt-exact through `Interval.mul`. NO float anywhere except `iv.cents` used solely as a SORT KEY.
- Period defaults to `new Interval("2/1")` (D-07: JI families fix equave at octave; kernel still takes a `period` param so the capability exists, UI does not surface it).
- Result ends with the period interval (D-14 contract: scales end in their period).
</exactness_rules>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write the CPS verified-vector test</name>
  <files>src/lib/__tests__/cps.test.ts</files>
  <read_first>
    - src/lib/__tests__/ (read any existing kernel test, e.g. the diamond or mos test, to mirror the vitest import style and `describe/it/expect` shape)
    - src/lib/interval.ts (the Interval API the test asserts against — `.fraction.toFraction()`, `.cents`)
    - .planning/quick/260608-dyv-scale-generation/260608-dyv-PLAN.md lines 327-351 (Task 1.1 test inventory — the canonical vectors)
  </read_first>
  <behavior>
    - cps([1,3,5,7], 2): the 1-3-5-7 Hexany. Octave-reduced + sorted by cents, the degree `n/d` strings equal ["1/1","7/6","5/4","35/24","5/3","7/4","2/1"] (6 distinct degrees + period 2/1 = 7 entries). This is the RESEARCH-verified vector.
    - Dekany: cps([1,3,5,7,9], 2) yields 10 distinct degrees + period (11 entries).
    - Eikosany: cps([1,3,5,7,9,11], 3) yields 20 distinct degrees + period (21 entries).
    - Exact dedupe: a factor set producing a coincident product (construct one, e.g. factors that yield two subsets with the same product) yields ONE entry — assert by total length, and assert NO duplicate n/d strings in the result.
    - Caps (D-14): cps with k < 1 or k > factors.length throws RangeError; factors.length above the cap (~12) throws RangeError.
  </behavior>
  <action>
    Create src/lib/__tests__/cps.test.ts using vitest (`import { describe, it, expect } from "vitest"`). Build factor `Interval`s from integer strings (`new Interval("1/1")`, `new Interval("3/1")`, etc. — the factor set members are the integers as ratios over 1). Assert results by mapping the returned `scale.intervals` to `iv.fraction.toFraction()` and comparing to the literal expected arrays above (the Hexany vector is the load-bearing assertion). For cardinality presets assert `scale.intervals.length`. For dedupe assert `new Set(strings).size === strings.length`. For caps wrap the call in `expect(() => cps(...)).toThrow(RangeError)`. Import `cps` from "../cps.js" (Framework `.js`-extension convention). Run it and confirm it FAILS (module does not exist yet) — this is the RED state.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/cps.test.ts 2>&1 | grep -qi "fail\|cannot find\|error" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - cps.test.ts exists and imports `cps` from "../cps.js".
    - The Hexany assertion compares against the exact array ["1/1","7/6","5/4","35/24","5/3","7/4","2/1"].
    - Running the test FAILS (RED) because src/lib/cps.ts does not yet exist.
  </acceptance_criteria>
  <done>Test file written with all verified vectors; suite is RED (cps module absent).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement cps() and add the INVENTORY row</name>
  <files>src/lib/cps.ts, src/lib/INVENTORY.md</files>
  <read_first>
    - src/lib/cps.test.ts (the spec just written — implement against it)
    - src/lib/interval.ts (Interval.mul, octaveReduce, fraction.toFraction signatures)
    - src/lib/diamond.ts (the established kernel-primitive shape: pure function, RangeError caps, octaveReduce default 2/1, BigInt discipline — mirror its header-comment style)
    - src/lib/INVENTORY.md (the row format: `| Symbol | Source | Notes |`; the discipline that every new kernel symbol gets a row with Source + Reason)
    - .planning/quick/260608-dyv-scale-generation/260608-dyv-PLAN.md lines 340-348 (the verified implementation pattern + the exact INVENTORY row text)
  </read_first>
  <action>
    Implement `export function cps(factors: Interval[], k: number, period: Interval = new Interval("2/1")): Scale` in src/lib/cps.ts. Algorithm (RESEARCH-verified): (1) defense-in-depth caps FIRST — throw RangeError if `factors.length` outside [1, 12] (MAX_FACTORS = 12) or `k` outside [1, factors.length]; (2) `kCombinations(factors, k)` from "xen-dev-utils"; (3) for each subset, multiply members via `subset.reduce((acc, iv) => acc.mul(iv), new Interval("1/1"))` — products stay BigInt-exact; (4) `octaveReduce(period)` each product; (5) Set-dedupe keyed on `iv.fraction.toFraction()`; (6) sort the deduped Intervals by `.cents` ascending; (7) append `period` as the final interval (D-14); (8) return `new Scale(sorted, period)`. Import `Interval` from "./interval.js", `Scale` from "./scale.js", `kCombinations` from "xen-dev-utils". Write a header doc-comment mirroring diamond.ts (cite Pitfall #1/#6, D-07, D-12, D-14). Add the INVENTORY.md row exactly: `| \`cps\` | Custom (this repo) | Flagship JI structure; hand-rolled over xen-dev-utils' \`kCombinations\` for transparent BigInt ownership (OQ-4/D-12); SonicWeave's \`cps\` is the one-line alternative. Dedupe by exact \`n/d\` (Pitfall #1/#6), never cents. Period param exists per D-07; UI fixes it at 2/1. |` under a new `## Phase 6 entries` section (create the section if absent).
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/cps.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/__tests__/cps.test.ts` passes ALL cases including the exact Hexany vector ["1/1","7/6","5/4","35/24","5/3","7/4","2/1"].
    - `npx tsc --noEmit` is clean (no type errors).
    - cps.ts contains NO cents-tolerance comparison; dedupe key is `iv.fraction.toFraction()`.
    - cps.ts throws RangeError for k=0, k>factors.length, and factors.length=13.
    - INVENTORY.md has a `| \`cps\`` row under a Phase 6 section.
  </acceptance_criteria>
  <done>cps() passes every verified vector + caps test; tsc clean; INVENTORY row present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| widget params → cps() | Untrusted integer factor set + k cross into combinatorial enumeration (this plan is the kernel; the widget is Plan 05, but the cap MUST live here as the last line of defense). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Denial of Service | `cps()` combinatorial enumeration | mitigate | Cap `factors.length ≤ 12` (MAX_FACTORS) and `1 ≤ k ≤ factors.length` with RangeError BEFORE calling `kCombinations`. C(12,6)=924 subsets is the worst case — bounded. Uncapped, a large factor set freezes the browser tab (high severity). |
| T-06-02 | Tampering | exact-rational integrity | mitigate | Dedupe strictly by `${n}/${d}` BigInt string; no float path can launder distinct ratios into one (Pitfall #1/#6). |
| T-06-SC | Tampering | npm installs | accept | No new dependency added — `xen-dev-utils` is already in the project (CLAUDE.md stack). No install task in this plan. |
</threat_model>

<verification>
- `npx vitest run src/lib/__tests__/cps.test.ts` — all green.
- `npx tsc --noEmit` — clean.
- `npx eslint src/lib/cps.ts` — clean (R-01: no xen-dev-utils Fraction import).
- INVENTORY.md updated with the `cps` row.
</verification>

<success_criteria>
- cps([1,3,5,7], 2) returns the canonical Hexany exactly (the GEN-01 success criterion).
- Dekany / Eikosany presets yield correct cardinality.
- Exact dedupe; D-14 caps enforced; period appended.
- Kernel-only — no DOM, no audio imports.
</success_criteria>

<output>
Create `.planning/phases/06-exact-rational-ji-harmonic-generators/06-01-SUMMARY.md` when done.
</output>
