---
phase: 06-exact-rational-ji-harmonic-generators
plan: 06
type: execute
wave: 3
depends_on: ["06-02", "06-05"]
files_modified:
  - src/components/generate-harmonic.ts
  - src/components/generate-harmonic.css
  - src/components/__tests__/generate-harmonic.test.ts
  - src/pages/generate.md
autonomous: true
requirements: [GEN-02, GEN-03]
must_haves:
  truths:
    - "Picking 'Harmonic & interval divisions' methods mounts the harmonic widget with a sub-method select (harmonic / subharmonic / ADO / isoharmonic)"
    - "harmonic-segment is the DEFAULT sub-method of this widget and remains the page's default landing method (D-08)"
    - "Each sub-method shows the right inputs (lo/hi for segments, divisions+equave for ADO, start/diff/count for isoharmonic) and a reduce-to-octave toggle for segments/isoharmonic (D-04)"
    - "Default render (harmonic segment 8..16) shows the exact h/8 table; changing inputs re-renders"
    - "A ⏵⏵ Play button auditions; Send-to serializes the current scale (exact JI, ratio-per-line)"
  artifacts:
    - path: "src/components/generate-harmonic.ts"
      provides: "Pattern-2 harmonic-family widget with 4 sub-methods + reduce toggle"
      contains: "export function generateHarmonic"
    - path: "src/components/__tests__/generate-harmonic.test.ts"
      provides: "happy-dom tests for sub-method swap, default render, reduce toggle, Play"
      contains: "generateHarmonic"
    - path: "src/pages/generate.md"
      provides: "Harmonic optgroup registration + mount + default-method preservation (D-08)"
      contains: "generateHarmonic"
  key_links:
    - from: "src/components/generate-harmonic.ts"
      to: "src/lib/harmonic.ts"
      via: "harmonicSegment/subharmonicSegment/adoScale/isoharmonic calls"
      pattern: "harmonicSegment|adoScale|isoharmonic"
    - from: "src/pages/generate.md"
      to: "src/components/generate-harmonic.ts"
      via: "factory mount under Harmonic optgroup"
      pattern: "generateHarmonic"
---

<objective>
Ship the harmonic-family method widget (GEN-02, GEN-03): a Pattern-2 factory `generateHarmonic(synth, opts)`
with a sub-method select swapping between harmonic segment, subharmonic segment, ADO, and isoharmonic —
each calling its `harmonic.ts` builder (Plan 02), with the D-04 reduce-to-octave toggle on the
segment/isoharmonic sub-methods.

Purpose: This is the widget the page opens on. Per D-08, harmonic-segment becomes the DEFAULT sub-method
of this widget AND stays the page's default landing method — preserving Phase-5 boot behavior byte-for-byte
while the full harmonic roster (subharmonic / ADO / isoharmonic) fills in around it. Literal overtone form
is the composer's default (D-04); the reduce toggle folds to the octave on demand.

Output: `generate-harmonic.ts` + `.css` + tests + the `generate.md` Harmonic-optgroup wiring that replaces
the Phase-5 reference harmonic-segment with this richer widget WITHOUT changing the default-on-load behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-exact-rational-ji-harmonic-generators/06-CONTEXT.md
@.planning/phases/05-generate-surface-live-integration-foundation/05-UI-SPEC.md

<interfaces>
<!-- The kernel builders + Pattern-2 template + page host. -->

From src/lib/harmonic.ts (Plan 02 — the kernel builders):
```typescript
export function harmonicSegment(lo: number, hi: number, reduce?: boolean): Scale;     // default reduce=false (literal)
export function subharmonicSegment(lo: number, hi: number, reduce?: boolean): Scale;  // default reduce=false
export function adoScale(divisions: number, equave: Interval): Scale;                  // no reduce flag
export function isoharmonic(start: number, diff: number, count: number, reduce?: boolean): Scale; // default reduce=false
// all throw RangeError outside D-14 caps.
```

mosBuilder Pattern-2 template (src/components/mos-builder.ts): closure-local state, status region, `replaceChildren` re-render, kernel RangeError → status (preserve prior render). The `makeRatioField` idiom (numerator/denominator number inputs) is reused for the ADO equave.

Output components (reuse): `scaleTable(scale, baseHz, opts)`, `playScale(scale, synth, {baseHz})`.

D-09 defaults: harmonic segment 8..16; ADO 6; isoharmonic a sensible small chord (e.g. start 4, diff 1, count 4 → 4:5:6:7). Subharmonic default 8..16.

generate.md picker: the "Harmonic & interval divisions" family currently holds the single functional reference option `{ id: "harmonic-segment", text: "Harmonic segment (n : n+1 : … : 2n)" }`, AND the page has a standalone `segmentSizeInput` + `buildHarmonicSegmentText(n)` reference implementation that is the current default-on-load preview.
</interfaces>

<design_and_integration_rules>
- Sub-method select: harmonic / subharmonic / ADO / isoharmonic. Default selection = **harmonic** (D-08). Swapping the sub-method swaps the relevant inputs into a params sub-region (replaceChildren).
- Inputs per sub-method: segments → lo + hi integer inputs + a "reduce to octave" checkbox (D-04, default OFF = literal); ADO → divisions integer input + equave ratio field (reuse the makeRatioField n/d idiom) + NO reduce toggle; isoharmonic → start + diff + count integer inputs + reduce checkbox (D-04, default OFF).
- On any change: call the matching builder, render via scaleTable + playScale. Kernel RangeError → status region, preserve prior render (mosBuilder idiom). Expose the current Scale for Send-to.
- **D-08 migration (CRITICAL — anti-regression):** this widget REPLACES the Phase-5 standalone reference harmonic-segment as the default-on-load content, but the page must open on a harmonic segment 8..16 (or the existing 4 → 4..8 default — confirm which default produces the SAME first-render the user sees today; if the Phase-5 default was segment-size 4 → 4:5:6:7:8, set the widget's default harmonic segment to match that exact first-render to preserve byte-for-byte boot). Decide and document: either keep the Phase-5 `segmentSizeInput`/`buildHarmonicSegmentText` path as-is and ADD the richer widget under a NEW option id, OR migrate the default landing to this widget with an identical first-render. Prefer the migration with an IDENTICAL first render so the picker has ONE harmonic entry (cleaner), but ONLY if the first-render bytes match; if uncertain, keep the existing reference method working and add the widget as an additional option. Document the choice in the SUMMARY.
- generate-harmonic.css: `.generate-harmonic__*` classes, locked tokens only (D-15). `@import` in src/styles.css.
- Send-to: when this widget is active, `currentScaleText` serializes the widget's current Scale ratio-per-line (exact JI — `scale.intervals.map(iv => iv.toString()).join("\n")`).
</design_and_integration_rules>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN — generateHarmonic widget (4 sub-methods + reduce toggle)</name>
  <files>src/components/generate-harmonic.ts, src/components/generate-harmonic.css, src/components/__tests__/generate-harmonic.test.ts</files>
  <read_first>
    - src/components/mos-builder.ts (Pattern-2 template + makeRatioField idiom for the ADO equave)
    - src/components/__tests__/mos-builder.test.ts (happy-dom test idioms)
    - src/components/generate-cps.ts (the sibling widget from Plan 05 — mirror its structure + how it exposes the current scale, for consistency)
    - src/lib/harmonic.ts (the four builder signatures — Plan 02)
    - src/components/scale-table.ts, src/components/play-scale.ts (output components)
    - .planning/quick/260608-dyv-scale-generation/260608-dyv-PLAN.md lines 404-420 (Task 1.4 spec)
  </read_first>
  <behavior>
    - generateHarmonic(synth) returns an HTMLElement with a known root class (`.generate-harmonic`) + h2.
    - A sub-method `<select>` (harmonic / subharmonic / ADO / isoharmonic); default = harmonic.
    - Harmonic sub-method shows lo + hi inputs + a reduce checkbox; default render (8..16) shows a 9-row table (the exact h/8 segment incl. period). Assert tbody row count.
    - Switching to ADO shows a divisions input + an equave ratio field (n/d) and NO reduce checkbox; ADO 6 over 2/1 renders the 7-row AFDO-6 table.
    - Switching to isoharmonic shows start/diff/count inputs + a reduce checkbox.
    - Toggling the reduce checkbox on a wide harmonic segment re-renders (assert the row set changes / cents fold).
    - A ⏵⏵ Play button is present.
  </behavior>
  <action>
    First write src/components/__tests__/generate-harmonic.test.ts (happy-dom, vitest) for the behaviors above; mirror mos-builder.test.ts + generate-cps.test.ts. Confirm RED. Then implement src/components/generate-harmonic.ts: `export function generateHarmonic(synth: SynthHandle, opts: { baseHz?: number; precision?: number } = {}): HTMLElement`. Closure-local state for the active sub-method + each sub-method's params (defaults per D-09). Build: root `<section class="generate-harmonic">`, h2 "Harmonic & interval divisions", a sub-method select, a params sub-region (replaceChildren on sub-method change with the right inputs — segments: lo/hi + reduce checkbox; ADO: divisions + makeRatioField equave; isoharmonic: start/diff/count + reduce checkbox), status region, tableHost, playHost. `rebuild()` dispatches on the active sub-method to the matching harmonic.ts builder, renders scaleTable + playScale, sets `_currentScale`, clears status on success; kernel RangeError → status.textContent, preserve prior render. Expose the current scale (same mechanism as generate-cps.ts for consistency). Create generate-harmonic.css with locked-token classes. Make GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/generate-harmonic.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/__tests__/generate-harmonic.test.ts` passes — sub-method swap, default harmonic 8..16 (9 rows), ADO 6 (7 rows), isoharmonic inputs, reduce toggle re-render, Play present.
    - `npx tsc --noEmit` clean.
    - Default sub-method is harmonic (D-08); reduce default is OFF/literal (D-04).
    - All DOM via createElement + textContent (no innerHTML).
    - generate-harmonic.css uses only locked tokens (D-15).
  </acceptance_criteria>
  <done>generateHarmonic renders all four sub-methods, reduce toggle works, defaults match D-09; tests green; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the harmonic widget into generate.md, preserving the D-08 default landing</name>
  <files>src/pages/generate.md</files>
  <read_first>
    - src/pages/generate.md (FULL page — METHOD_FAMILIES Harmonic family, segmentSizeInput, buildHarmonicSegmentText, the params/preview swap blocks, currentScaleText, Send-to)
    - src/components/generate-harmonic.ts (the widget + its default first-render + how it exposes the scale)
    - src/components/generate-cps.ts (the Plan-05 mount precedent in generate.md — mirror the mounting approach)
    - .planning/phases/06-exact-rational-ji-harmonic-generators/06-CONTEXT.md (D-08, D-10)
  </read_first>
  <action>
    In src/pages/generate.md: (1) import `generateHarmonic`. (2) Instantiate once: `const harmonicWidget = generateHarmonic(synth, { baseHz })`. (3) Register under the "Harmonic & interval divisions" optgroup. Decide the migration per the design rule: PREFERRED — make the harmonic widget the content of the existing `harmonic-segment` method id (so the picker keeps ONE harmonic entry and the page opens on it by mounting `harmonicWidget` whose default sub-method+params produce the SAME first-render bytes the user sees today), retiring the standalone `segmentSizeInput`/`buildHarmonicSegmentText` path ONLY if the first render is byte-identical; OTHERWISE keep the reference path and add the widget under a distinct id. Whichever path, the page MUST open on a harmonic segment exactly as in Phase 5 (D-08). (4) In the params-host swap, mount `harmonicWidget` for the harmonic method id. (5) Send-to: extend `currentScaleText` so the harmonic-widget branch serializes the widget's current Scale ratio-per-line. (6) **VERIFY D-08:** on first load (placeholder selected, then defaulting to harmonic per the page's existing behavior) the preview shows the same harmonic segment as Phase 5. Do not regress the segment-size default first render.
  </action>
  <verify>
    <automated>npm run build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds; `npx tsc --noEmit` clean.
    - The Harmonic optgroup mounts the full widget (harmonic/subharmonic/ADO/isoharmonic).
    - The page's default-on-load preview is a harmonic segment IDENTICAL to Phase 5's first render (D-08 byte-for-byte).
    - `currentScaleText` for the harmonic branch serializes the widget's current scale ratio-per-line.
    - Manual smoke: open page → same harmonic segment as before; switch sub-methods → ADO/subharmonic/isoharmonic render + audition; Send-to round-trips.
  </acceptance_criteria>
  <done>Harmonic widget mounted via the picker; default landing unchanged (D-08); all sub-methods audition + Send-to works; build + tsc green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keyboard → numeric inputs | Untrusted lo/hi/divisions/start/diff/count + equave n/d cross into the harmonic builders. |
| widget scale → URL hash | The harmonic scale text crosses into Send-to serialization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Denial of Service | harmonic builders via widget inputs | mitigate | Relies on harmonic.ts D-14 caps (MAX_HARMONIC 1024, MAX_DIVISIONS 1000, MAX_ISO_COUNT 1024); RangeError surfaces in the status region without freezing (the mosBuilder error-handling idiom). Numeric inputs carry `min`/`max`/`step` as a first clamp. |
| T-06-14 | Cross-Site Scripting | widget DOM rendering | mitigate | All inputs/labels/status via `createElement` + `textContent`; numeric inputs are `type="number"`; no `innerHTML` with dynamic content. |
| T-06-15 | Tampering | Send-to URL serialization | mitigate | Reuses the existing `encodeScaleToHash` / 8 KB cap path (Phase-5 audited, unchanged). |
| T-06-SC | Tampering | npm installs | accept | No new dependency. No install task. |
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/generate-harmonic.test.ts` — all green.
- `npx tsc --noEmit` — clean.
- `npm run build` — succeeds.
- `npx eslint src/components/generate-harmonic.ts` — clean.
- Manual: page opens on the same harmonic segment as Phase 5 (D-08); all sub-methods work + audition + Send-to.
</verification>

<success_criteria>
- User can generate harmonic + subharmonic segments, ADO, and isoharmonic chords — exact integer-derived (GEN-02, GEN-03).
- adoScale(6) and harmonicSegment(8,16) match their verified vectors (via the Plan-02 kernel).
- D-04 reduce-to-octave toggle works; default is literal overtone form.
- harmonic-segment is the widget's default sub-method AND the page's default landing method (D-08).
</success_criteria>

<output>
Create `.planning/phases/06-exact-rational-ji-harmonic-generators/06-06-SUMMARY.md` when done.
</output>
