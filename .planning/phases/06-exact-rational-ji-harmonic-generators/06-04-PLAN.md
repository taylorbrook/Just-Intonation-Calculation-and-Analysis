---
phase: 06-exact-rational-ji-harmonic-generators
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/scale-table.ts
  - src/components/scale-table.css
  - src/components/__tests__/scale-table.test.ts
autonomous: true
requirements: [SURF-06]
must_haves:
  truths:
    - "scaleTable accepts a `tempered` option; when true the Ratio column is dropped entirely (cents + ¢-from-12tet only) — D-01"
    - "When tempered, a visible 'tempered' badge renders above/beside the table — D-02"
    - "When NOT tempered, the table is byte-for-byte the existing 4-column Degree|Ratio|Cents|¢-from-12tet (anti-regression)"
    - "The badge + cents-only variant use only locked 05-UI-SPEC theme tokens — no new tokens (D-15)"
  artifacts:
    - path: "src/components/scale-table.ts"
      provides: "scaleTable with the tempered (cents-only, no-ratio, badged) variant"
      contains: "tempered"
    - path: "src/components/scale-table.css"
      provides: "Badge styling using existing theme tokens"
      contains: "badge"
    - path: "src/components/__tests__/scale-table.test.ts"
      provides: "Tests for both the default 4-col and tempered 3-col + badge variants"
      contains: "tempered"
  key_links:
    - from: "src/components/scale-table.ts"
      to: "src/lib/scale.ts"
      via: "Scale type consumption"
      pattern: "Scale"
---

<objective>
Extend `scaleTable` with the TEMPERED variant (SURF-06): an opt-in `tempered` flag that (D-01)
DROPS the ratio column entirely — rendering only Degree | Cents | ¢-from-12tet — and (D-02)
shows a visible "tempered" badge so the distinction from exact JI is unmistakable at a glance.

Purpose: "Tempered, not laundered JI" is this phase's identity. EDO/ED-n output must be visibly
tempered: cents-only, no float-derived ratio masquerading as exact JI, badged. This is a SHARED
component change consumed by the ED widget (Plan 07). The non-tempered path MUST remain byte-for-byte
identical (the user's anti-regression sensitivity — every existing scaleTable caller is JI and must
not change).

Output: `scale-table.ts` + `.css` + tests. The badge styling matches the locked 05-UI-SPEC tokens
(D-15 — do not invent new tokens).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-exact-rational-ji-harmonic-generators/06-CONTEXT.md
@.planning/phases/05-generate-surface-live-integration-foundation/05-UI-SPEC.md

<interfaces>
<!-- The component being extended + the tokens to reuse. From src/components/scale-table.ts. -->

CURRENT src/components/scale-table.ts (extend, do not rewrite the JI path):
```typescript
export interface ScaleTableOpts {
  copyButton?: boolean;
  precision?: number;   // default 1 (0.1¢)
}
export function scaleTable(scale: Scale, baseHz: number, opts?: ScaleTableOpts): HTMLElement;
// thead innerHTML is STATIC (no interpolation) — safe; tbody cells use textContent (T-02-23).
// Current 4 columns: Degree | Ratio | Cents | ¢ from 12-TET.
```

Locked 05-UI-SPEC theme tokens to reuse for the badge (D-15 — NO new tokens):
`--theme-foreground-alt` (muted label color), `--theme-foreground-faint` (faint borders),
`--theme-background-alt` (subtle fill), `--sans-serif` (badge text), `--theme-orange`
(05-UI-SPEC reserves orange among the palette tokens — use it ONLY if the badge needs an
accent; prefer the neutral `--theme-foreground-alt` + faint border for an understated chip
consistent with the "no color-coding" restraint in the existing table). NEVER hard-coded hex.
</interfaces>

<design_rules>
- D-01: when `tempered === true`, the table has exactly THREE columns: Degree | Cents | ¢ from 12-TET. NO ratio column at all (stricter than "≈" — the user chose no ratio column).
- D-02: a "tempered" badge element (text "tempered", `.scale-table__badge` class) renders inside the wrapper above the table. createElement + textContent (never innerHTML for the badge — it's literal text but follow the discipline).
- Anti-regression: the default path (no `tempered` opt, or `tempered: false`) renders EXACTLY the current 4-column table — same thead innerHTML string, same tbody cells, same copy-button behavior. Existing callers (mosBuilder, dashboard, analysis) pass no `tempered` flag and MUST be unaffected.
- D-15: badge styling uses only existing 05-UI-SPEC tokens; understated chip (muted text + faint border + small radius), consistent with the table's restraint (no red/green color-coding). Add to scale-table.css under a `.scale-table__badge` rule.
</design_rules>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — tests for the tempered variant + anti-regression for the JI path</name>
  <files>src/components/__tests__/scale-table.test.ts</files>
  <read_first>
    - src/components/__tests__/scale-table.test.ts (the existing test — read its happy-dom setup + assertion idioms; extend, do not replace)
    - src/components/scale-table.ts (current 4-column structure being extended)
    - src/lib/scale.ts (Scale + Interval to build fixtures)
    - .planning/phases/06-exact-rational-ji-harmonic-generators/06-CONTEXT.md (D-01, D-02, D-15)
  </read_first>
  <behavior>
    - Default (no tempered opt): scaleTable(jiScale, 440) renders a `<thead>` with FOUR `<th>` (Degree, Ratio, Cents, ¢ from 12-TET) and NO `.scale-table__badge` — byte-for-byte the current behavior.
    - Tempered (`{ tempered: true }`): renders a `<thead>` with exactly THREE `<th>` (Degree, Cents, ¢ from 12-TET) — assert there is NO column whose header text is "Ratio"; assert tbody rows have exactly 3 `<td>`.
    - Tempered: a `.scale-table__badge` element exists with `textContent` === "tempered".
    - Tempered + copyButton still renders the copy button (cents-based CSV is acceptable — the existing scalaToCsv path is untouched here).
  </behavior>
  <action>
    Extend src/components/__tests__/scale-table.test.ts (happy-dom, vitest). Build a JI Scale fixture (e.g. from a few `new Interval(...)`) and a tempered fixture (any Scale — the flag, not the data, drives the variant). Add a `describe("tempered variant")` block asserting: (1) default render has 4 `<th>` and no badge; (2) tempered render has 3 `<th>`, none reading "Ratio", tbody rows have 3 `<td>`; (3) the `.scale-table__badge` exists with text "tempered". Run — confirm RED (the `tempered` opt does not exist yet, so the tempered assertions fail).
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/scale-table.test.ts 2>&1 | grep -qi "fail\|expected\|error" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - The test file has a tempered-variant describe block with the 3-column + badge assertions.
    - The default-path (4-column, no-badge) assertion is present as the anti-regression guard.
    - Running the test FAILS (RED) on the tempered assertions.
  </acceptance_criteria>
  <done>Tests written for both variants; suite is RED on the tempered path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — add the tempered variant to scaleTable + badge CSS</name>
  <files>src/components/scale-table.ts, src/components/scale-table.css</files>
  <read_first>
    - src/components/__tests__/scale-table.test.ts (implement against it)
    - src/components/scale-table.ts (extend in place — preserve the JI 4-column path exactly)
    - src/components/scale-table.css (add the `.scale-table__badge` rule using existing tokens)
    - .planning/phases/05-generate-surface-live-integration-foundation/05-UI-SPEC.md (the locked tokens; the "no color-coding" restraint)
  </read_first>
  <action>
    Add `tempered?: boolean` to `ScaleTableOpts`. In `scaleTable`, when `opts.tempered === true`: (1) append a badge before the table — `const badge = document.createElement("span"); badge.className = "scale-table__badge"; badge.textContent = "tempered"; wrapper.appendChild(badge);` (append BEFORE the table element); (2) set the thead innerHTML to the THREE-column static string `"<tr><th>Degree</th><th>Cents</th><th>¢ from 12-TET</th></tr>"`; (3) in the tbody loop, emit only the three cells `[String(i+1), cents.toFixed(precision), (delta>0?"+":"")+delta.toFixed(precision)]` — DROP the ratio cell. When `tempered` is falsy, take the EXACT existing code path (4-column thead string, 4 cells) — restructure with a branch but keep the JI branch character-identical to today. Keep all tbody cells on `textContent` (T-02-23). Add to scale-table.css: `.scale-table__badge { display: inline-block; font-family: var(--sans-serif); font-size: 13px; font-weight: 600; letter-spacing: 0.04em; text-transform: uppercase; color: var(--theme-foreground-alt); border: 1px solid color-mix(in oklab, currentColor 12%, transparent); border-radius: 4px; padding: 2px 8px; margin-bottom: 8px; }` — existing tokens only (D-15). Ensure scale-table.css is already `@import`ed in src/styles.css (it is — no styles.css change needed).
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/scale-table.test.ts && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/__tests__/scale-table.test.ts` passes — both default (4-col, no badge) and tempered (3-col, badge) cases.
    - `npx tsc --noEmit` clean; `npm run build` succeeds.
    - The non-tempered branch renders the identical 4-column thead string + 4 cells (anti-regression).
    - The tempered branch has NO "Ratio" `<th>` and a `.scale-table__badge` reading "tempered".
    - scale-table.css `.scale-table__badge` uses only `var(--theme-*)` / `var(--sans-serif)` — no hard-coded hex (D-15).
  </acceptance_criteria>
  <done>Tempered variant renders cents-only + badge; JI path unchanged; tests green; build passes; tokens-only styling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scale data → DOM cells | Cents/deviation values rendered into table cells (this component is a render boundary; the data is project-generated, not user-typed, but the textContent discipline holds). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Cross-Site Scripting | scaleTable cell + badge rendering | mitigate | All dynamic cell values and the badge use `createElement` + `textContent` — never `innerHTML`. The thead string stays STATIC (no interpolation) in both the 4-col and 3-col branches, so it remains innerHTML-safe (the existing audited pattern, T-02-22/23). |
| T-06-09 | Tampering (laundered JI) | tempered presentation | mitigate | D-01: the ratio column is structurally absent for tempered scales — no float-derived fraction can be presented as exact JI (SURF-06). The badge (D-02) makes the distinction explicit. |
| T-06-SC | Tampering | npm installs | accept | No new dependency. No install task. |
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/scale-table.test.ts` — all green (both variants).
- `npx tsc --noEmit` — clean.
- `npm run build` — succeeds.
- `npx eslint src/components/scale-table.ts` — clean.
- Manual: the existing Dashboard / Analysis / MOS-builder tables (all JI, no tempered flag) render unchanged.
</verification>

<success_criteria>
- Tempered scales render cents-only (no ratio column, D-01) with a visible "tempered" badge (D-02) — SURF-06 presentation contract.
- The JI 4-column path is byte-for-byte unchanged (anti-regression).
- Badge styling uses only locked tokens (D-15).
</success_criteria>

<output>
Create `.planning/phases/06-exact-rational-ji-harmonic-generators/06-04-SUMMARY.md` when done.
</output>
