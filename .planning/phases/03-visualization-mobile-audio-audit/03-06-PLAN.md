---
phase: 03-visualization-mobile-audio-audit
plan: 06
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - src/components/scl-io.ts
  - src/components/scl-io.css
  - src/index.md
  - src/styles.css
  - src/lib/INVENTORY.md
  - .planning/phases/03-visualization-mobile-audio-audit/mobile-audit.md
autonomous: false
requirements:
  - VIZ-01
  - VIZ-02
  - VIZ-03
  - IO-03
  - AUDIO-06
tags:
  - phase-3
  - integration
  - dashboard
must_haves:
  truths:
    - "User can export and re-import a .kbm with referenceKey != middleNote"
    - "Three viz widgets render on the dashboard below the audio + scl-io strip"
    - "effective baseHz threads through every audition path (audio panel + lattice + diamond + keyboard)"
    - "Use baseHz instead toggle surfaces only after a .kbm is imported"
    - "Floating Stop button visible only when synth.activeVoices > 0; Esc anywhere on page calls synth.panic()"
    - "Mobile Safari smoke-test passes in RDM (iPhone preset)"
  artifacts:
    - path: "src/components/scl-io.ts"
      provides: "Combined .scl + .kbm import/export widget"
      contains: "parseKbm"
    - path: "src/index.md"
      provides: "Dashboard wired with viz widgets + Esc + Stop button + kbm-aware effective baseHz"
      contains: "lattice"
    - path: "src/styles.css"
      provides: "Phase 3 imports + floating Stop-button rules + iOS auto-zoom suppression"
      contains: "stop-all-audio"
    - path: "src/lib/INVENTORY.md"
      provides: "Phase 3 entries section"
      contains: "Phase 3"
    - path: ".planning/phases/03-visualization-mobile-audio-audit/mobile-audit.md"
      provides: "RDM verification methodology + iOS quirks"
      contains: "Responsive Design Mode"
  key_links:
    - from: "src/index.md"
      to: "src/components/lattice.ts"
      via: "import + display in reactive cell"
      pattern: "lattice\\("
    - from: "src/index.md"
      to: "src/lib/kbm.ts"
      via: "parseKbm via sclIo callback; effectiveBaseHz derived from referenceHz/middleNote/referenceKey"
      pattern: "kbm\\.referenceHz"
    - from: "src/components/scl-io.ts"
      to: "src/lib/kbm.ts"
      via: "parseKbm + writeKbm + defaultKbmFor"
      pattern: "parseKbm|writeKbm"
---

<objective>
Wire all Phase 3 work into the live dashboard. Six tasks: extend sclIo for .kbm I/O; integrate viz widgets + Esc shortcut + Stop button + kbm-aware effectiveBaseHz into src/index.md; update styles.css with new @imports + Stop-button rules + iOS-zoom suppression; append Phase 3 entries to INVENTORY.md; create mobile-audit.md documenting RDM methodology + iOS quirks; human-verify the smoke-test on Safari RDM.

Per CONTEXT D-01, all viz ships on src/index.md (no theory pages). Per D-11, sclIo is the combined widget. Per D-13, imported .kbm applies to playback by default with override toggle. Per D-18, RDM is the verification target with documented residual risks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/03-visualization-mobile-audio-audit/03-CONTEXT.md
@.planning/phases/03-visualization-mobile-audio-audit/03-RESEARCH.md
@.planning/phases/03-visualization-mobile-audio-audit/03-PATTERNS.md
@.planning/phases/03-visualization-mobile-audio-audit/03-UI-SPEC.md
@.planning/phases/03-visualization-mobile-audio-audit/03-VALIDATION.md
@src/components/scl-io.ts
@src/components/scl-io.css
@src/index.md
@src/styles.css
@src/lib/INVENTORY.md
@src/lib/kbm.ts
@src/components/lattice.ts
@src/components/tonality-diamond.ts
@src/components/keyboard.ts
@src/audio/synth.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend src/components/scl-io.ts (+ CSS) to handle .kbm import/export</name>
  <files>src/components/scl-io.ts, src/components/scl-io.css</files>
  <read_first>
    - src/components/scl-io.ts (entire file — current ~170 lines; understand existing FileReader path + Blob download path before extending)
    - src/components/scl-io.css (existing styles)
    - src/lib/kbm.ts (Plan 02 exports — parseKbm, writeKbm, defaultKbmFor, KbmMapping)
    - .planning/phases/03-visualization-mobile-audio-audit/03-CONTEXT.md D-09, D-11, D-12, D-13
    - .planning/phases/03-visualization-mobile-audio-audit/03-PATTERNS.md lines 499-581 (extension pattern + new SclIoOpts fields + export-button cloning + status messages)
    - .planning/phases/03-visualization-mobile-audio-audit/03-UI-SPEC.md lines 128-141, 204-208 (exact button labels + status messages + error strings + interaction contract)
  </read_first>
  <action>
**A. Extend `SclIoOpts` interface** (PATTERNS lines 569-578) with three new optional fields: `onImportKbm?: (kbm: KbmMapping) => void;`, `kbmForExport?: KbmMapping;`, `baseHz?: number;`. Preserve existing `onImport` and `defaultFilename` fields verbatim.

**B. Add imports** at top of scl-io.ts: `import { parseKbm, writeKbm, defaultKbmFor, type KbmMapping } from "../lib/kbm.js";`.

**C. Update file picker accept attribute** from `.scl,text/plain` to `.scl,.kbm,text/plain`.

**D. Update import handler** to auto-detect by extension. Wrap the existing parseScl path in `if (ext === "kbm") { parseKbm + onImportKbm } else { existing parseScl + onImport }`. Use exact UI-SPEC strings:
- KBM import success: `Imported ${file.name} (size ${kbm.size}, ref ${kbm.referenceKey}, middle ${kbm.middleNote}).`
- SCL import success: `Imported ${file.name} (${parsed.intervals.length} intervals).`
- KBM parse error: `Could not parse ${file.name}: ${msg}. Verify the size, first key, last key, middle note, reference key, reference frequency, and formal octave fields.`
- SCL parse error (existing — preserve): `Could not parse ${file.name}: ${msg}. Check that line 3 is the pitch count and pitch lines use ratios (5/4) or cents (408.0).`

Detect ext via `file.name.toLowerCase().endsWith(".kbm") ? "kbm" : "scl"`. Reset `fileInput.value = ""` in finally.

**E. Add a second export button** alongside the existing `Download .scl` button. Wrap both in a new `.scl-io__export-row` div for layout (per UI-SPEC line 162). New button:
- Class: `play-btn scl-io__export`
- type: `button`
- textContent: exactly `Download .kbm` (UI-SPEC line 130)
- handler: `const kbm = opts.kbmForExport ?? defaultKbmFor(scale, opts.baseHz ?? 440);` → `writeKbm(kbm)` → Blob download identical to the existing scl download (try/finally URL.createObjectURL/revokeObjectURL, append/click/remove anchor for Firefox compat). After successful download set `status.textContent = ${filenameInput.value}.kbm.`

**F. Drop the existing `__filename-ext` span if present** per UI-SPEC line 207 — each export button appends its own extension to the user-typed filename. Verify: `! grep -E "__filename-ext|filename-ext" src/components/scl-io.ts`.

**G. Add to scl-io.css**:
```css
.scl-io__export-row {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}
```

Run `npm run test -- --run` and `npm run lint:types` to verify no regression.
  </action>
  <verify>
    <automated>grep -c "parseKbm\\|writeKbm\\|defaultKbmFor" src/components/scl-io.ts | grep -qE '^[3-9]' &amp;&amp; grep -c "Download .kbm" src/components/scl-io.ts | grep -q '^1$' &amp;&amp; grep -c "onImportKbm" src/components/scl-io.ts | grep -qE '^[1-9]' &amp;&amp; grep -c "scl-io__export-row" src/components/scl-io.css | grep -q '^1$' &amp;&amp; ! grep -E "filename-ext" src/components/scl-io.ts &amp;&amp; npm run test -- --run 2>&amp;1 | grep "passed" | head -1 &amp;&amp; npm run lint:types</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "parseKbm" src/components/scl-io.ts` returns >= 1
    - `grep -c "writeKbm" src/components/scl-io.ts` returns >= 1
    - `grep -c "defaultKbmFor" src/components/scl-io.ts` returns >= 1
    - `grep -c "Download .kbm" src/components/scl-io.ts` returns exactly 1 (exact UI-SPEC label)
    - `grep -c "onImportKbm" src/components/scl-io.ts` returns >= 1
    - `! grep -E "filename-ext" src/components/scl-io.ts` (span dropped per UI-SPEC line 207)
    - `grep -c "scl-io__export-row" src/components/scl-io.css` returns 1
    - 165 pre-existing tests still pass; lint:types and lint exit 0
    - Status messages match UI-SPEC verbatim (verify by grepping for the literal strings)
  </acceptance_criteria>
  <done>
    sclIo handles both .scl and .kbm with auto-detection; the dashboard wiring (Task 2) can consume `opts.onImportKbm` to populate `importedKbm` Mutable state.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Integrate viz widgets + Esc + Stop button + kbm-aware effectiveBaseHz into src/index.md</name>
  <files>src/index.md</files>
  <read_first>
    - src/index.md (entire ~95-line file; cell ordering matters)
    - .planning/phases/03-visualization-mobile-audio-audit/03-PATTERNS.md lines 680-757 (verbatim cell-by-cell additions)
    - .planning/phases/03-visualization-mobile-audio-audit/03-CONTEXT.md D-01, D-02, D-13, D-15, D-16, D-24
    - .planning/phases/03-visualization-mobile-audio-audit/03-UI-SPEC.md lines 210-222 (Stop button + toggle; exact aria-labels)
    - .planning/phases/03-visualization-mobile-audio-audit/03-RESEARCH.md lines 645-653 (Pitfall 11 — Esc bound in synth cell), 891-894 (Open Question 2 — Mutable + 100ms polling)
    - .planning/phases/03-visualization-mobile-audio-audit/03-03-SUMMARY.md (notes from Plan 03 about visibilitychange now in synth.ts)
  </read_first>
  <action>
Modify `src/index.md` in place. Preserve every existing cell verbatim except where explicitly modified.

**A. Extend the imports cell** to add: `lattice`, `tonalityDiamond`, `keyboard` from `./components/...js`, and `type KbmMapping` from `./lib/kbm.js`.

**B. Modify the synth cell** (currently `const synth = createSynth(); invalidation.then(...)`) to add (Pitfall #11 — Esc bound here so it doesn't rebind on scale edits):
- `const onKey = (e) => { if (e.key === "Escape") synth.panic(); };`
- `document.addEventListener("keydown", onKey);`
- `const audioActive = Mutable(false);`
- `const activeVoicesInterval = setInterval(() => { audioActive.value = synth.activeVoices > 0; }, 100);`
- Combine all cleanups into a single `invalidation.then(() => { document.removeEventListener("keydown", onKey); clearInterval(activeVoicesInterval); synth.dispose(); });`

**C. Insert new cells in the correct dependency order, AFTER the baseHz cell and BEFORE the audioPanel cell** (so effectiveBaseHz is defined before the audioPanel/lattice/diamond/keyboard cells consume it):

1. `const importedKbm = Mutable(null);` — initial state.
2. `const useBaseHzOverride = importedKbm ? view(Inputs.toggle({ label: "Use baseHz instead of imported .kbm", value: false })) : false;` — surfaces only when kbm imported (D-13).
3. `const effectiveBaseHz = (importedKbm && !useBaseHzOverride) ? importedKbm.referenceHz * Math.pow(2, (importedKbm.middleNote - importedKbm.referenceKey) / 12) : baseHz;` — D-24 formula.

**D. Modify the existing audioPanel cell** to use `effectiveBaseHz` instead of `baseHz`: `if (scale) display(audioPanel(scale, synth, effectiveBaseHz));`.

**E. Modify the existing sclIo cell** to add the Phase 3 callbacks:
```ts
if (scale) {
  display(sclIo(scale, {
    baseHz,
    onImport: () => { /* v1: not wired back to textarea — Phase 4 ANAL-04 */ },
    onImportKbm: (kbm) => { importedKbm.value = kbm; },
  }));
}
```

**F. Append three new viz cells AFTER sclIo** (D-02 full-bleed vertical stack, document order = lattice → diamond → keyboard):
- `if (scale) display(lattice(scale, synth, { baseHz: effectiveBaseHz }));`
- `if (scale) display(tonalityDiamond(scale, synth, { baseHz: effectiveBaseHz }));`
- `if (scale) display(keyboard(scale, synth, effectiveBaseHz));`

**G. Append the floating Stop button cell** at the end (UI-SPEC lines 210-216):
```ts
{
  const btn = document.createElement("button");
  btn.className = "stop-all-audio";
  btn.type = "button";
  btn.textContent = "Stop all audio (Esc)";
  btn.setAttribute("aria-label", "Stop all audio. Keyboard shortcut: Escape.");
  btn.style.display = audioActive ? "inline-flex" : "none";
  btn.addEventListener("click", () => synth.panic());
  display(btn);
}
```

**H. Preserve** the trailing `Read: [the syntonic comma →](pages/syntonic-comma)` link.

**Final cell order** (after parseError display):
1. importedKbm Mutable (NEW)
2. useBaseHzOverride view (NEW; depends on importedKbm)
3. effectiveBaseHz (NEW; depends on importedKbm + useBaseHzOverride + baseHz)
4. audioPanel (MODIFIED — uses effectiveBaseHz)
5. sclIo (MODIFIED — onImportKbm callback writes to importedKbm)
6. lattice (NEW)
7. tonalityDiamond (NEW)
8. keyboard (NEW)
9. Stop button (NEW)

Verify final file with `npm run build` to confirm Framework picks up the new cells without dependency errors.

**Forbidden:** binding Esc in any scale-dependent cell (Pitfall #11); allocating a new AudioContext (Pitfall #2); polling activeVoices at >10 Hz; modifying seedText/scaleText/parseScala/parseError cells.
  </action>
  <verify>
    <automated>grep -c "lattice(scale, synth" src/index.md | grep -q '^1$' &amp;&amp; grep -c "tonalityDiamond(scale, synth" src/index.md | grep -q '^1$' &amp;&amp; grep -c "keyboard(scale, synth, effectiveBaseHz)" src/index.md | grep -q '^1$' &amp;&amp; grep -c "effectiveBaseHz" src/index.md | grep -qE '^[3-9]|^[1-9][0-9]' &amp;&amp; grep -c "importedKbm" src/index.md | grep -qE '^[2-9]' &amp;&amp; grep -c "Stop all audio (Esc)" src/index.md | grep -q '^1$' &amp;&amp; grep -c "Use baseHz instead of imported .kbm" src/index.md | grep -q '^1$' &amp;&amp; npm run build 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All three viz factories invoked exactly once in src/index.md
    - effectiveBaseHz appears >= 3 times (definition + audioPanel + viz cells)
    - importedKbm appears >= 2 times (Mutable definition + onImportKbm assignment)
    - Stop button label and aria-label match UI-SPEC verbatim
    - Toggle label matches UI-SPEC verbatim ("Use baseHz instead of imported .kbm")
    - `npm run build` exits 0 (Framework cell-dependency resolution succeeds)
    - `npm run ci` exits 0 (lint:types + test + lint + format:check + build)
    - 165 tests still pass
  </acceptance_criteria>
  <done>
    Dashboard fully wired: three viz widgets render, kbm-aware effectiveBaseHz threads through every audition, Stop button + Esc shortcut work, "Use baseHz instead" toggle surfaces only after kbm import.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update src/styles.css with new @imports + Stop-button rules + iOS auto-zoom suppression</name>
  <files>src/styles.css</files>
  <read_first>
    - src/styles.css (entire file; current ~50 lines)
    - .planning/phases/03-visualization-mobile-audio-audit/03-PATTERNS.md lines 760-810 (verbatim additions)
    - .planning/phases/03-visualization-mobile-audio-audit/03-UI-SPEC.md lines 167-172 (additions spec); line 91 (Stop button uses --theme-red, override of accent rule — destructive variant)
    - .planning/phases/03-visualization-mobile-audio-audit/03-CONTEXT.md D-16, D-17
  </read_first>
  <action>
**A. Append three @import lines** for the new component CSS files (alphabetical order, after the existing 5 @imports for play-buttons/scale-table/ratio-pill/audio-panel/scl-io):
- `@import "./components/keyboard.css";`
- `@import "./components/lattice.css";`
- `@import "./components/tonality-diamond.css";`

**B. Append global iOS auto-zoom suppression rule** (D-17): a comment header explaining purpose, then `input, select, textarea { font-size: 16px; }`.

**C. Append the floating Stop-all-audio button rules** (D-16 + UI-SPEC line 91 — destructive --theme-red variant):
- `.stop-all-audio` rule with: `position: fixed; top: 16px; right: 16px; z-index: 10; min-width: 44px; min-height: 44px; padding: 12px 16px; font-family: var(--sans-serif); font-size: 14px; font-weight: 600; color: var(--theme-red); background: var(--theme-background); border: 2px solid var(--theme-red); border-radius: 6px; cursor: pointer;`
- `.stop-all-audio:hover, .stop-all-audio:focus-visible` invert: `background: var(--theme-red); color: var(--theme-background); outline: 2px solid var(--theme-foreground-focus); outline-offset: 2px;`

**Forbidden:** hard-coded hex colors anywhere (theme tokens only); @media queries (D-17 — single-column at all widths); modifying any pre-Phase-3 rule.
  </action>
  <verify>
    <automated>grep -c "@import.*keyboard.css" src/styles.css | grep -q '^1$' &amp;&amp; grep -c "@import.*lattice.css" src/styles.css | grep -q '^1$' &amp;&amp; grep -c "@import.*tonality-diamond.css" src/styles.css | grep -q '^1$' &amp;&amp; grep -c "stop-all-audio" src/styles.css | grep -qE '^[2-9]' &amp;&amp; grep -c "font-size: 16px" src/styles.css | grep -qE '^[1-9]' &amp;&amp; grep -c "var(--theme-red)" src/styles.css | grep -qE '^[2-9]' &amp;&amp; ! grep -E "#[0-9a-fA-F]{3,6}" src/styles.css &amp;&amp; ! grep -E "@media" src/styles.css</automated>
  </verify>
  <acceptance_criteria>
    - 3 new @import lines in src/styles.css (one each for keyboard, lattice, tonality-diamond)
    - .stop-all-audio class with both base + hover/focus rules (>= 2 occurrences of the class name)
    - --theme-red appears >= 2 times (Stop button uses destructive color)
    - Global `font-size: 16px` rule on inputs present
    - No hex colors; no @media queries
    - Existing 5 @imports preserved
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    Phase 3 global CSS surface complete: three new component stylesheets imported; floating Stop button styled destructively; iOS auto-zoom suppressed via 16px input font-size.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Append Phase 3 entries to src/lib/INVENTORY.md</name>
  <files>src/lib/INVENTORY.md</files>
  <read_first>
    - src/lib/INVENTORY.md (current 64 lines; uses `| Symbol | Source | Notes |` table format)
    - src/lib/kbm.ts (Plan 02 — KbmMapping, parseKbm, writeKbm, kbmToFrequencies, defaultKbmFor)
    - src/lib/diamond.ts (Plan 02 — DiamondCell, enumerateDiamond)
    - src/components/lattice.ts (Plan 04 — lattice, LatticeOpts, deriveLatticeBasis)
    - src/components/tonality-diamond.ts (Plan 05 — tonalityDiamond, DiamondOpts, deriveDiamondOddLimit)
    - src/components/keyboard.ts (Plan 05 — keyboard, KeyboardOpts)
    - .planning/phases/03-visualization-mobile-audio-audit/03-CONTEXT.md lines 186-187
  </read_first>
  <action>
Append a new section "## Phase 3 entries" at END of INVENTORY.md, with five sub-sections covering all 12 new symbols. Use the same `| Symbol | Source | Notes |` format as Phase 2 sections. Each Notes column must cite the requirement (VIZ-01/02/03, IO-03) AND relevant decisions (D-08, D-19, D-20, D-23, etc.) so future phases can grep for cross-references.

**Sub-section 1: Phase 3 — kbm I/O entries** (5 rows: KbmMapping, parseKbm, writeKbm, kbmToFrequencies, defaultKbmFor). Notes call out: three-named-fields discipline per Pitfall #7; permissive parser per RESEARCH OQ1; 1MB cap T-3-04; round-trip-stable layout; pure derivation Map<midi,Hz>; D-12 default values.

**Sub-section 2: Phase 3 — diamond entries** (2 rows: DiamondCell, enumerateDiamond). Notes call out: octave-bound reduction (NOT scale.period); Interval.equals for in-scale (Pitfall #1/#6); bounds [1, 1023].

**Sub-section 3: Phase 3 — lattice component entries** (3 rows: lattice, LatticeOpts, deriveLatticeBasis). Notes call out: wraps ji-lattice@0.3.2 + d3@7.9.0; spanLattice once per render NOT inside zoom (Pitfall #6); octave-only-scale empty-state (Pitfall #11); top-2-by-frequency truncation for >3 primes.

**Sub-section 4: Phase 3 — tonality-diamond component entries** (3 rows: tonalityDiamond, DiamondOpts, deriveDiamondOddLimit). Notes: hand-laid square (i,j) grid; SVG <title> tooltip; auto-derived odd-limit rounded UP to nearest of {7, 9, 11, 13, 15, 21, 31}.

**Sub-section 5: Phase 3 — keyboard component entries** (2 rows: keyboard, KeyboardOpts). Notes: linear-by-degree mapping (D-03); pointerdown/pointerup/pointerleave/pointercancel sustain via release callback; Enter/Space activation; note-only audition per RESEARCH OQ3 (no opts.audition); 0.1¢ default precision (Pitfall #16).

Keep each Notes cell terse but informative — about 1-3 sentences. Pre-existing Phase 1 + Phase 2 sections must NOT be modified.
  </action>
  <verify>
    <automated>grep -c "## Phase 3" src/lib/INVENTORY.md | grep -qE '^[3-9]' &amp;&amp; grep -c "KbmMapping" src/lib/INVENTORY.md | grep -qE '^[1-9]' &amp;&amp; grep -c "parseKbm" src/lib/INVENTORY.md | grep -qE '^[1-9]' &amp;&amp; grep -c "enumerateDiamond" src/lib/INVENTORY.md | grep -qE '^[1-9]' &amp;&amp; grep -c "deriveLatticeBasis\\|deriveDiamondOddLimit" src/lib/INVENTORY.md | grep -qE '^[2-9]' &amp;&amp; grep -c "tonalityDiamond" src/lib/INVENTORY.md | grep -qE '^[1-9]' &amp;&amp; grep -c "^| .keyboard" src/lib/INVENTORY.md | grep -qE '^[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - >= 4 new "## Phase 3" sub-section headings present
    - All 12 new symbols documented (KbmMapping, parseKbm, writeKbm, kbmToFrequencies, defaultKbmFor, DiamondCell, enumerateDiamond, lattice, LatticeOpts, deriveLatticeBasis, tonalityDiamond, DiamondOpts, deriveDiamondOddLimit, keyboard, KeyboardOpts — actual count: 15)
    - Phase 1 and Phase 2 sections unchanged (verified by `git diff` showing additions-only past the original last line)
    - Each new row Notes column references the requirement ID (VIZ-01/02/03, IO-03) and at least one decision (D-XX)
  </acceptance_criteria>
  <done>
    Pitfall #5 discipline traceable for every Phase 3 symbol; future phases can grep INVENTORY.md to know what already exists.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Create mobile-audit.md documenting RDM methodology + iOS quirks</name>
  <files>.planning/phases/03-visualization-mobile-audio-audit/mobile-audit.md</files>
  <read_first>
    - .planning/phases/03-visualization-mobile-audio-audit/03-CONTEXT.md D-18 + specifics lines 235-241 (exact list of items mobile-audit.md must document)
    - .planning/phases/03-visualization-mobile-audio-audit/03-RESEARCH.md Pitfall 5 (touch-action), Pitfall 7 (audioSession + hardware mute), Pitfall 8 (sync resume), Pitfall 9 (visibilitychange)
    - .planning/phases/03-visualization-mobile-audio-audit/03-VALIDATION.md lines 64-69 (manual-only verifications)
    - .planning/phases/03-visualization-mobile-audio-audit/03-03-SUMMARY.md (what synth.ts now does)
  </read_first>
  <action>
Create `.planning/phases/03-visualization-mobile-audio-audit/mobile-audit.md` as pure prose documentation. Five required H2 sections:

**Section 1: Verification Methodology.** Step-by-step instructions to open the dashboard in Safari macOS Responsive Design Mode (Develop menu, ⌘⌥R, iPhone 14 preset, navigate to localhost). Then a checklist of >= 10 smoke-test items: audio panel renders + plays; lattice renders and clicks audition; pinch-zoom works on lattice/diamond; keyboard tap audible + visual feedback; Stop button + Esc work; .kbm import surfaces toggle; toggle ON/OFF affects audition; no horizontal overflow at >= 320px; input taps don't trigger iOS auto-zoom.

**Section 2: RDM-Only Limitations (Documented Trade-offs).** Two subsections:
1. **iOS Hardware Mute Switch.** Behavior: hardware silent switch silences AudioContext output even when page UI shows playing. Mitigation in code: `navigator.audioSession.type = 'playback'` in synth.ts ensure() (Safari iOS 16.4+/macOS 14.4+). User-visible mitigation: tooltip on audio buttons advising to check the silent switch. Older iOS: documented limitation; do not pull the legacy `<audio>`-tag-with-silent-WAV workaround.
2. **iOS Autoplay-Policy Nuances.** Behavior: AudioContext.resume() must be synchronous inside a user-gesture handler. RDM is more permissive than physical iOS. Mitigation: synth.ts ensure() calls void ctx.resume() synchronously, no await. Trust the code path; accepted residual risk per D-18.

**Section 3: Phase 3 Code-Level Mitigations.** Table mapping Pitfalls 5/7/8/9/11 to code sites and mitigations: lattice/diamond CSS touch-action: none; keyboard CSS touch-action: manipulation; synth.ts audioSession + sync resume + visibilitychange; src/index.md Esc binding in synth cell.

**Section 4: Audio-Layer Fix Inventory (D-15).** Four checkboxes confirming all D-15 bullets verified in code: synchronous ctx.resume(), webkitAudioContext fallback preserved, visibilitychange listener with cleanup, page-level Stop button + Esc.

**Section 5: Future Work (Deferred).** Bullet list: physical-iPhone verification (deferred per D-18); iOS < 16.4 fallback (deferred per RESEARCH A5); legacy `<audio>`-tag silent-WAV trick (not needed).

Footer: `**Verified by:** {user, after running smoke-test checklist}` and `**Verification date:** {date}` placeholders for the human checkpoint to fill in.

This file is documentation-only — no executable code. Verify only that file exists with the required headings + content markers.
  </action>
  <verify>
    <automated>ls .planning/phases/03-visualization-mobile-audio-audit/mobile-audit.md &amp;&amp; grep -c "Responsive Design Mode" .planning/phases/03-visualization-mobile-audio-audit/mobile-audit.md | grep -qE '^[1-9]' &amp;&amp; grep -c "audioSession" .planning/phases/03-visualization-mobile-audio-audit/mobile-audit.md | grep -qE '^[1-9]' &amp;&amp; grep -c "silent switch" .planning/phases/03-visualization-mobile-audio-audit/mobile-audit.md | grep -qE '^[1-9]' &amp;&amp; grep -c "touch-action" .planning/phases/03-visualization-mobile-audio-audit/mobile-audit.md | grep -qE '^[1-9]' &amp;&amp; grep -cE "^## " .planning/phases/03-visualization-mobile-audio-audit/mobile-audit.md | grep -qE '^[5-9]'</automated>
  </verify>
  <acceptance_criteria>
    - File exists at expected path
    - >= 5 H2 sections
    - Mentions: "Responsive Design Mode", "audioSession", "silent switch", "touch-action", "synchronous", "visibilitychange"
    - Section 1 verification checklist has >= 10 checkbox items
    - Section 4 lists all four D-15 fixes as checkboxes
    - Footer placeholders for the human checkpoint
  </acceptance_criteria>
  <done>
    Mobile-audit documentation in place; user has clear smoke-test checklist; RDM trade-offs (hardware mute switch, autoplay-policy) explicitly documented as accepted residual risk per D-18.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Human verification — run mobile-audit smoke-test on Safari RDM</name>
  <what-built>
    All Phase 3 code is in place; tests pass; build succeeds. The user must run the smoke-test checklist from Section 1 of `.planning/phases/03-visualization-mobile-audio-audit/mobile-audit.md` on Safari macOS Responsive Design Mode (iPhone preset).
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and note the localhost URL.
2. Open Safari on macOS.
3. Develop menu -> Enter Responsive Design Mode (or ⌘⌥R).
4. Choose any current iPhone preset.
5. Navigate to the dashboard URL.
6. Walk the >= 10-item checklist in Section 1 of `mobile-audit.md`. Tick each that passes.
7. For any failing item: file a gap into `.planning/phases/03-visualization-mobile-audio-audit/03-VERIFICATION.md` (let `/gsd-verify-work` handle gap closure).
8. Update `mobile-audit.md` Section 5 footer with the verification date and your name.
  </how-to-verify>
  <resume-signal>
    Type "approved" if the entire checklist passes; type "gaps: <comma-separated checklist items>" if any failed (will be fed into `/gsd-plan-phase --gaps`).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-uploaded .kbm -> parseKbm -> dashboard state | Malformed .kbm could attempt absurd middleNote/refKey values; bounds checked in Plan 02 parseKbm |
| Esc keydown -> synth.panic | Page-wide Esc capture; synth.panic is idempotent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-23 | Tampering | Malformed .kbm via file picker | mitigate | parseKbm fails closed with field-named errors (Plan 02); status region announces error in plain textContent |
| T-3-24 | DoS | Esc listener leak across hot-reload | mitigate | Esc handler in synth cell with invalidation.then cleanup (Pitfall #11) |
| T-3-25 | DoS | Stop button polling | mitigate | 100ms / 10 Hz polling — accepted as cheap (RESEARCH OQ2); cleared on invalidation |
| T-3-26 | Information Disclosure | importedKbm holds user data in memory | accept | Static-site, no transport, same-origin only |
| T-3-27 | Spoofing | UI-SPEC copywriting drift | mitigate | Verify steps grep for exact strings ("Stop all audio (Esc)", "Use baseHz instead of imported .kbm") |
</threat_model>

<verification>
After all 6 tasks:
1. `npm run ci` exits 0 (lint:types + test + lint + format:check + build)
2. Phase 3 test count: 165 passing, 0 failing
3. `npm run dev` starts cleanly; no console errors on page load
4. Visual smoke test: dashboard shows audio panel + scl-io + lattice + tonality diamond + keyboard in vertical stack
5. Importing a .kbm fixture surfaces the "Use baseHz instead" toggle
6. Esc anywhere on page calls synth.panic
7. Floating Stop button appears when audio active, hidden when not
8. INVENTORY.md ends with "## Phase 3" section containing >= 4 sub-sections and >= 12 new rows
9. mobile-audit.md exists with all required sections
10. Human checkpoint approved (smoke-test passed in RDM)
</verification>

<success_criteria>
- VIZ-01, VIZ-02, VIZ-03, IO-03, AUDIO-06 all integrated end-to-end
- sclIo handles both .scl and .kbm with format auto-detection
- effectiveBaseHz threads through every audition path
- Use-baseHz-instead toggle surfaces only after kbm import; default OFF
- Floating Stop button + Esc shortcut globally bound in synth cell
- styles.css imports new component CSS + Stop-button rules + global 16px input font-size
- INVENTORY.md updated with all Phase 3 symbol rows
- mobile-audit.md documents RDM methodology + iOS quirks
- Human-verified mobile audit passes (or gaps filed)
</success_criteria>

<output>
After completion, create `.planning/phases/03-visualization-mobile-audio-audit/03-06-SUMMARY.md` documenting:
- Final test count (>= 165 passing, 0 failing)
- `npm run ci` is green
- Mobile-audit checkpoint outcome (approved with date, or gaps filed)
- INVENTORY.md row growth (~12 rows)
- Anything Phase 4 needs to know about the importedKbm/useBaseHzOverride/effectiveBaseHz triad — Phase 4 ANAL-04 (URL hash persistence) will thread through this same path
</output>
