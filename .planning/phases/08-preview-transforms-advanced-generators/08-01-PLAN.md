---
phase: 08-preview-transforms-advanced-generators
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/meru.ts
  - src/lib/__tests__/meru.test.ts
  - src/lib/constant-structure.ts
  - src/lib/__tests__/constant-structure.test.ts
  - src/lib/INVENTORY.md
autonomous: true
requirements: [GEN-10]
must_haves:
  truths:
    - "meruScale(1,1,1,1,7) returns exact-rational successive-ratio convergents including 3/2, 5/3, 8/5, 13/8 (BigInt iv.equals, never cents tolerance)"
    - "metallicLimitCents(1,1) ≈ 833.09¢ (irrational, cents-of-record — never a scale degree)"
    - "meruScale rejects over-cap term count and runaway magnitude with RangeError BEFORE/DURING enumeration (defense-in-depth)"
    - "isConstantStructure returns CS-✓ for the csgs([3/2],3) 7-note Pythagorean diatonic and CS-✗ with an ambiguousAt detail for a known non-CS scale"
  artifacts:
    - path: "src/lib/meru.ts"
      provides: "meruScale recurrence + metallicLimitCents readout"
      exports: ["meruScale", "metallicLimitCents"]
    - path: "src/lib/constant-structure.ts"
      provides: "isConstantStructure(scale) → { cs, ambiguousAt? } on exact BigInt subtension uniqueness"
      exports: ["isConstantStructure"]
    - path: "src/lib/__tests__/meru.test.ts"
      provides: "GEN-10 Wilson recurrence anchors (convergents exact, limit cents, caps)"
    - path: "src/lib/__tests__/constant-structure.test.ts"
      provides: "GEN-10 CS-check anchors (CS-✓ Pythagorean, CS-✗ ambiguousAt)"
  key_links:
    - from: "src/lib/meru.ts"
      to: "src/lib/interval.ts + src/lib/scale.ts"
      via: "BigInt Interval convergents composed into a Scale"
      pattern: "new Interval\\(`\\$\\{.*\\}/\\$\\{.*\\}`\\)"
    - from: "src/lib/constant-structure.ts"
      to: "src/lib/interval.ts"
      via: "exact BigInt interval-class subtension counting (Interval.equals, never iv.cents comparison)"
      pattern: "\\.equals\\("
---

<objective>
Ship the two new pure-kernel primitives Phase 8's advanced generators stand on, test-first (TDD):

1. `src/lib/meru.ts` — `meruScale(a, b, x0, x1, terms)` builds the Wilson recurrence `x_n = a·x_{n-1} + b·x_{n-2}` and returns its successive-ratio convergents as a `Scale` of exact BigInt `Interval`s, plus `metallicLimitCents(a, b)` returning the irrational metallic-mean limit in cents (display/readout only — NEVER a scale degree). (GEN-10 Wilson / metallic — D-09, D-11, D-12, D-19.)
2. `src/lib/constant-structure.ts` — `isConstantStructure(scale)` returns `{ cs: boolean; ambiguousAt?: ... }` by exact BigInt interval-class subtension-uniqueness counting on a built kernel `Scale` (D-14 readout source; D-20 — small new helper, NOT the awkward SonicWeave builtin).

Purpose: These are the only genuinely new MATH this phase. Everything downstream (the `generate-meru.ts` and `generate-cs.ts` widgets in Plan 03) is wiring over these + the existing `scaleFromSonicWeave` adapter. Land them first so the widgets build against shipped, tested contracts (interface-first / kernel-before-UI).
Output: Two kernel modules + two Vitest test files (the Wave 0 anchors from 08-VALIDATION.md) + two INVENTORY.md rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-preview-transforms-advanced-generators/08-CONTEXT.md
@.planning/phases/08-preview-transforms-advanced-generators/08-RESEARCH.md

<interfaces>
<!-- Contracts the executor builds AGAINST. Extracted from the kernel — no codebase exploration needed. -->

From src/lib/interval.ts (BigInt source of truth — R-01: Fraction comes from fraction.js, NEVER xen-dev-utils):
```typescript
export class Interval {
  readonly fraction: Fraction;            // BigInt-backed (fraction.js@5.3.4)
  constructor(input: FractionInput);       // accepts `${n}/${d}` string, bigint, number, {n,d}, Fraction
  get cents(): number;                     // DISPLAY PROJECTION ONLY (Pitfall #1) — never a kernel input
  get centsFrom12tet(): number;
  mul(other: Interval): Interval;          // immutable
  div(other: Interval): Interval;          // immutable
  octaveReduce(period?: Interval): Interval; // period-aware; default 2/1; throws if period <= 1/1
  equals(other: Interval): boolean;        // BigInt Fraction equality (use this for dedupe/subtension — NEVER cents)
  toString(): string;                      // fraction.toFraction(), e.g. "3/2"
}
```

From src/lib/scale.ts:
```typescript
export class Scale {
  readonly intervals: readonly Interval[]; // intervals[0] conventionally 1/1; LAST is the period (D-14)
  readonly period: Interval;               // must be > 1/1 at construction (CR-01 — throws RangeError otherwise)
  constructor(intervals: readonly Interval[], period?: Interval); // default period = last interval
  rotate(degree: number): Scale;
  reduce(): Scale;                         // period-aware
  dedupe(): Scale;                         // exact BigInt equality
  transpose(by: Interval): Scale;
}
```

Caps precedent (Phase-6 D-14 / generators.ts): defense-in-depth limits are module-level consts that throw `RangeError` BEFORE (and, for runaway growth, DURING) enumeration. Examples already shipped: `MAX_DIVISIONS = 1000`, `MAX_HARMONIC = 1024`, `MAX_FAREY_ORDER = 1000`.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: meru.ts — Wilson recurrence convergents (exact) + metallic-limit cents (RED→GREEN→REFACTOR)</name>
  <files>src/lib/meru.ts, src/lib/__tests__/meru.test.ts</files>
  <read_first>
    - src/lib/interval.ts (the BigInt Interval contract — constructor accepts `${n}/${d}`; equals() is BigInt, cents is display-only)
    - src/lib/scale.ts (Scale constructor + period rule: period must be > 1/1, default = last interval; this constrains the D-19 period choice)
    - src/lib/generators.ts (the Phase-6 defense-in-depth cap idiom — MAX_DIVISIONS/MAX_HARMONIC consts that RangeError before enumeration; mirror it for term-count + magnitude)
    - src/lib/__tests__/generators.test.ts (the kernel-test idiom: describe/it, iv.equals assertions for exact ratios, expect(() => ...).toThrow(RangeError) for caps)
    - .planning/phases/08-preview-transforms-advanced-generators/08-RESEARCH.md "Pattern 4: Wilson recurrence kernel" + "Wilson / metallic numerical verification" (the verified anchors and the (a+√(a²+4b))/2 limit formula)
  </read_first>
  <behavior>
    - RED: write src/lib/__tests__/meru.test.ts FIRST, asserting the verified anchors, run it, confirm it fails (module/export absent).
    - Test 1: `meruScale(1n, 1n, 1n, 1n, 7)` (Fibonacci/golden, D-12 default) — the successive-ratio convergents include, in order, the exact BigInt ratios 2/1, 3/2, 5/3, 8/5, 13/8 (assert via `iv.equals(new Interval("3/2"))` etc., NEVER via iv.cents tolerance). Assert the scale is exact rational (every interval round-trips through n/d).
    - Test 2: `metallicLimitCents(1, 1)` is within 0.01 of 833.09 (golden φ ≈ 1.618034 → 833.09¢). Add `metallicLimitCents(2, 1)` ≈ 1525.86¢ (silver) and `metallicLimitCents(3, 1)` ≈ 2068.41¢ (bronze) — the >1-octave cases that D-19 acknowledges.
    - Test 3 (caps — defense-in-depth, Pitfall 5 / D-11): `meruScale(1n,1n,1n,1n, terms)` with terms > MAX_MERU_TERMS throws RangeError; terms < 1 throws RangeError; a seed/coefficient combination whose recurrence exceeds MAX_MERU_MAGNITUDE throws RangeError raised DURING the recurrence (not silently truncated).
    - Test 4: the metallic limit is a SEPARATE float readout (metallicLimitCents) and NEVER appears as an interval inside the meruScale Scale (assert no Scale interval's cents is within 1e-6 of the limit cents for golden — i.e. the irrational limit was not folded in as a degree; SURF-06 / D-09 Pitfall 4).
  </behavior>
  <action>
    Implement `meruScale(a: bigint, b: bigint, x0: bigint, x1: bigint, terms: number): Scale` computing the integer recurrence `x_n = a·x_{n-1} + b·x_{n-2}` (all BigInt — fraction.js precision is irrelevant here, the sequence terms are integers), then forming successive-ratio `Interval`s `x_i / x_{i-1}` via the R-01 `new Interval(\`${String(seq[i])}/${String(seq[i-1])}\`)` round-trip. Define module-level caps `MAX_MERU_TERMS` (pick a musically-sensible ceiling, e.g. ~48 — Fibonacci ratios past ~12 terms are musically indistinguishable; justify in a comment) and `MAX_MERU_MAGNITUDE` (a BigInt ceiling guarding runaway term growth). Validate `terms` (1..MAX_MERU_TERMS) FIRST → RangeError; check each new term against MAX_MERU_MAGNITUDE DURING the loop → RangeError. D-19 discretion: leave convergents as the LITERAL recurrence ratios (do NOT octave-reduce by default — the recurrence ratios ARE the Mt. Meru reading; the transform strip gives the user the reduced view for free). Choose the Scale period to satisfy the scale.ts CR-01 rule (period > 1/1) — document the choice. Implement `metallicLimitCents(a: number, b: number): number` returning `1200 * Math.log2((a + Math.sqrt(a*a + 4*b)) / 2)` (irrational metallic mean → cents; D-09 cents-of-record, never a degree). Do NOT add any `tempered` field to Interval/Scale (Phase-6 D-03 — the tempered flag lives at the component layer; the metallic limit is flagged tempered by the Plan-03 widget, not here). GREEN: implement until the test passes. REFACTOR if needed.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/__tests__/meru.test.ts` passes (all four behavior groups green).
    - Source assertion: `src/lib/meru.ts` exports `meruScale` and `metallicLimitCents`; `grep -v '^ *\*' src/lib/meru.ts | grep -c 'MAX_MERU_TERMS\|MAX_MERU_MAGNITUDE'` ≥ 2 (both caps present in code, not just comments).
    - Source assertion: no `tempered` property is added to Interval or Scale (the metallic limit is a standalone `metallicLimitCents` number).
    - Behavior assertion: convergents asserted by `iv.equals(...)` (BigInt), never by a cents-within-epsilon comparison (`grep -c 'cents.*<.*1e-\|toFixed.*===' src/lib/meru.ts` for the convergent equality path returns 0 — cents only used in metallicLimitCents).
    - `npx vitest run` full suite stays green (~192 existing tests, anti-regression).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/__tests__/meru.test.ts</automated>
  </verify>
  <done>meru.ts ships meruScale (exact BigInt convergents, caps enforced) + metallicLimitCents (irrational cents readout, never a degree); meru.test.ts asserts the verified Fibonacci anchors and caps; full suite green.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: constant-structure.ts — isConstantStructure(scale) exact subtension-uniqueness helper (RED→GREEN→REFACTOR)</name>
  <files>src/lib/constant-structure.ts, src/lib/__tests__/constant-structure.test.ts</files>
  <read_first>
    - src/lib/interval.ts (Interval.equals is BigInt equality; .div() for interval-class differences; cents is display-only)
    - src/lib/scale.ts (Scale.intervals ordering convention — intervals[0] = 1/1, last = period)
    - src/lib/sonicweave.ts (scaleFromSonicWeave contract — Plan 03's CS widget feeds the BUILT Scale from csgs([3/2],3) into this helper; understand the {scale, tempered, error?} shape so the test can construct the same scale)
    - .planning/phases/08-preview-transforms-advanced-generators/08-RESEARCH.md "Critical Correction — csgs ordinal semantics" (csgs([3/2],3) → 7-note Pythagorean diatonic 9/8,81/64,729/512,3/2,27/16,243/128,2/1) + Open Question 2 (the tools.js:188–232 subtension-uniqueness algorithm to port onto BigInt Interval; return shape { cs, ambiguousAt? })
    - src/lib/__tests__/generators.test.ts (kernel-test idiom)
  </read_first>
  <behavior>
    - RED: write src/lib/__tests__/constant-structure.test.ts FIRST, run it, confirm it fails.
    - Test 1 (CS-✓): build the Pythagorean CS diatonic Scale directly from exact ratios `["1/1","9/8","81/64","729/512","3/2","27/16","243/128","2/1"]` (the csgs([3/2],3) result + the auto-prepended 1/1) and assert `isConstantStructure(scale).cs === true`.
    - Test 2 (CS-✗ with detail): build a KNOWN non-CS scale (e.g. a scale containing two distinct interval classes that subtend an equal interval — a documented small non-CS example; the classic is a scale where a 2-step and a 3-step span the same width) and assert `cs === false` AND `ambiguousAt` is present and identifies the colliding interval classes.
    - Test 3 (exactness): the check uses Interval.equals (BigInt) for subtension equality, NEVER cents tolerance — assert by constructing a near-miss scale where two subtensions are within ~0.5¢ but NOT exactly equal, and confirm it reports cs === true (a cents-epsilon implementation would wrongly flag it non-CS).
  </behavior>
  <action>
    Implement `isConstantStructure(scale: Scale): { cs: boolean; ambiguousAt?: { intervalClassA: number; intervalClassB: number } }` by porting the `tools.js` subtension-uniqueness algorithm onto the kernel `Scale`: for each scale-degree count k (1..N-1), compute the SET of intervals that subtend k steps (each computed via exact `Interval.div` between degree pairs, octave-reduced by `scale.period` where the wrap occurs), and verify that no interval value appears in TWO different step-counts' sets (constant structure ⇔ every interval has a unique subtension). Use `Interval.equals` (BigInt) for all interval comparisons — NEVER `iv.cents` within epsilon (Pitfall #1 / #6; this is the whole reason for a kernel helper over a float approach). On the first collision, populate `ambiguousAt` with the two colliding step-counts (phrase per D-20 — A2: the readout will render "ambiguous at <classes>"). Keep it ~25–40 LOC. Pure data — no DOM, no audio (three-layer purity).
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/__tests__/constant-structure.test.ts` passes (all three behavior groups green).
    - Source assertion: `src/lib/constant-structure.ts` exports `isConstantStructure`; the return type includes `cs: boolean` and an optional `ambiguousAt`.
    - Behavior assertion: the near-miss (Test 3) returns `cs === true`, proving exact BigInt comparison (not cents-epsilon).
    - Source assertion: `grep -c 'iv.cents\|\.cents' src/lib/constant-structure.ts` == 0 (no cents used in the CS decision; cents is forbidden as a kernel input here).
    - `npx vitest run` full suite stays green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/__tests__/constant-structure.test.ts</automated>
  </verify>
  <done>constant-structure.ts ships isConstantStructure returning { cs, ambiguousAt? } via exact BigInt subtension uniqueness; test asserts CS-✓ Pythagorean, CS-✗ with detail, and exact-not-epsilon behavior; full suite green.</done>
</task>

<task type="auto">
  <name>Task 3: INVENTORY.md — add the two new Phase 8 kernel rows</name>
  <files>src/lib/INVENTORY.md</files>
  <read_first>
    - src/lib/INVENTORY.md (the existing row format + the "## Phase 6/7 entries" section style; add a new "## Phase 8 entries" section with the Quick 260608-dyv subsection per CONTEXT canonical_refs)
  </read_first>
  <action>
    Append a `## Phase 8 entries` section with two rows under a `### Phase 8 — Wilson recurrence + constant-structure (Plan 08-01 / GEN-10)` subheading. Row 1: `meruScale`, `metallicLimitCents` (`src/lib/meru.ts`) — Source: Custom (this repo). Reason: SonicWeave has no Mt. Meru builtin (RESEARCH A-4 #15); hand-rolled exact-BigInt recurrence; metallic limit is irrational cents-of-record (D-09), never a degree; defense-in-depth term/magnitude caps (D-11). Row 2: `isConstantStructure` (`src/lib/constant-structure.ts`) — Source: Custom (this repo). Reason: D-20 — the SonicWeave `hasConstantStructure` builtin pushes its boolean onto the SW scale (awkward read-back through the {scale,tempered} adapter contract) and gives no "ambiguous at X" detail; a small exact-BigInt subtension-uniqueness helper on the built Scale is cleaner and stays on the BigInt path. All text via Markdown table cells (no code). Match the existing row prose density.
  </action>
  <acceptance_criteria>
    - `grep -c 'meruScale\|metallicLimitCents\|isConstantStructure' src/lib/INVENTORY.md` ≥ 3 (all three new symbols listed).
    - `grep -c 'Phase 8 entries' src/lib/INVENTORY.md` == 1.
    - Each new row records both a Source and a Reason (the INVENTORY discipline: custom symbols explain why not upstream).
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'meruScale\|isConstantStructure' src/lib/INVENTORY.md</automated>
  </verify>
  <done>INVENTORY.md has a Phase 8 section documenting meruScale/metallicLimitCents and isConstantStructure with Source + Reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| widget params → meruScale | untrusted seeds (x0,x1), coefficients (a,b), term-count reach the recurrence (Plan 03 widget validates; this kernel enforces caps as defense-in-depth) |
| built Scale → isConstantStructure | a Scale produced from user-entered csgs generators (Plan 03) is analyzed here; pure read, no enumeration blow-up risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Denial of Service | `meruScale` recurrence | mitigate | `MAX_MERU_TERMS` cap validated BEFORE the loop + `MAX_MERU_MAGNITUDE` checked DURING the loop → RangeError (Phase-6 D-14 / D-11 defense-in-depth). Fibonacci-rate (~φ^n) growth cannot run away or produce a multi-thousand-digit ratio that stalls the tab. |
| T-08-02 | Tampering | `metallicLimitCents` cents-of-record | mitigate | The irrational limit is a standalone float readout (D-09); it is NEVER admitted as a Scale interval, so no float-derived ratio is laundered as exact JI (SURF-06). Verified by the Task-1 Test-4 assertion. |
| T-08-03 | Tampering | `isConstantStructure` decision | accept | Pure BigInt comparison on an already-built Scale; no untrusted code path. The only correctness risk (cents-epsilon conflation of distinct intervals) is mitigated by the exact-equality requirement (Test 3), not a security control. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: zero new dependencies; `sonic-weave@0.14.1` already vetted + patched in Phase 7). slopcheck N/A. No [ASSUMED]/[SUS] packages → no blocking-human checkpoint required. |
</threat_model>

<verification>
- `npx vitest run src/lib/__tests__/meru.test.ts src/lib/__tests__/constant-structure.test.ts` — both new test files green.
- `npx vitest run` — full suite green (anti-regression: ~192 existing tests stay green per PROJECT.md).
- `npm run lint:types` — clean (strict-TS `noUncheckedIndexedAccess`; the recurrence loop must guard `seq[i-1]!`/`seq[i-2]!` access correctly).
</verification>

<success_criteria>
- `meruScale(1,1,1,1,7)` produces exact convergents incl. 3/2, 5/3, 8/5, 13/8 (GEN-10 Wilson, success criterion 3).
- `metallicLimitCents(1,1)` ≈ 833.09¢, flagged tempered downstream, never a degree (success criterion 3 + SURF-06).
- `isConstantStructure` returns CS-✓ for the csgs([3/2],3) Pythagorean diatonic and CS-✗ + ambiguousAt for a non-CS scale (GEN-10 CS readout source, D-14).
- Two INVENTORY rows with Source + Reason.
- Full Vitest suite + lint:types green.
</success_criteria>

<output>
Create `.planning/phases/08-preview-transforms-advanced-generators/08-01-SUMMARY.md` when done.
</output>
