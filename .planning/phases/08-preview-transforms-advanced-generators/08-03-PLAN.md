---
phase: 08-preview-transforms-advanced-generators
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/generate-meru.ts
  - src/components/generate-meru.css
  - src/components/__tests__/generate-meru.test.ts
  - src/components/generate-cs.ts
  - src/components/generate-cs.css
  - src/components/__tests__/generate-cs.test.ts
  - src/lib/INVENTORY.md
autonomous: true
requirements: [GEN-10]
must_haves:
  truths:
    - "generate-meru default landing = Fibonacci/golden (a=1,b=1, seeds 1,1, ~7 convergents) showing exact-JI ratios 2/1, 3/2, 5/3, 8/5, 13/8 with the φ ≈ 833.09¢ metallic-limit readout flagged tempered beside the table (D-09/D-12) — limit is never a scale degree"
    - "generate-meru exposes presets (golden/silver/bronze + cited Wilson Meru seeds, D-10), editable seeds/coefficients/term-count (D-11), isTempered() === false (convergents are exact JI), getScale() returns the convergent Scale"
    - "generate-cs default landing = csgs([3/2], 3) → 7-note Pythagorean diatonic exact-JI CS-✓ (the CORRECTED ordinal — NOT csgs([3/2],7) which yields 41 notes and errors)"
    - "generate-cs second field is LABELLED 'ordinal' (not 'note count' / 'size') with a helper note; ordinal capped low (≤ ~6–8)"
    - "generate-cs shows a live CS-status readout '✓ constant structure' / '✗ not CS (ambiguous at …)' from isConstantStructure on the returned Scale (D-14)"
    - "generate-cs surfaces adapter errors in its status region and PRESERVES the prior preview (D-16); no raw SonicWeave syntax exposed (D-13 — chips + ordinal only)"
  artifacts:
    - path: "src/components/generate-meru.ts"
      provides: "Wilson recurrence / metallic generator widget (GEN-10)"
      exports: ["generateMeru"]
    - path: "src/components/generate-cs.ts"
      provides: "Constant-structure generator widget via csgs (GEN-10)"
      exports: ["generateCs"]
  key_links:
    - from: "src/components/generate-meru.ts"
      to: "src/lib/meru.ts (meruScale + metallicLimitCents)"
      via: "direct kernel call from the widget rebuild()"
      pattern: "meruScale\\(|metallicLimitCents\\("
    - from: "src/components/generate-cs.ts"
      to: "src/lib/sonicweave.ts (scaleFromSonicWeave) + src/lib/constant-structure.ts (isConstantStructure)"
      via: "composeSource() → csgs([gens], ordinal) → scaleFromSonicWeave → isConstantStructure on the result"
      pattern: "scaleFromSonicWeave\\(|isConstantStructure\\("
---

<objective>
Build the two new "Advanced" generator widgets (GEN-10), each a Pattern-2 `(synth, opts) => HTMLElement` factory that renders its own table + ⏵⏵ Play and exposes `getScale()` / `isTempered()` (the established Phase-6/7 widget contract):

1. `src/components/generate-meru.ts` — Wilson recurrence / metallic (Mt. Meru). Calls `meruScale(...)` (Plan 01) for the exact-rational convergent Scale and `metallicLimitCents(...)` for the tempered-flagged limit readout shown BESIDE the table (D-09 — never a degree). Presets (golden/silver/bronze + cited Wilson Meru seeds, D-10), editable seeds/coefficients/term-count with defense-in-depth caps (D-11), default landing = Fibonacci/golden (D-12). EXACT JI → `isTempered() === false`.
2. `src/components/generate-cs.ts` — constant-structure via `scaleFromSonicWeave("csgs([gens], ordinal)")` (the structural twin of `generate-fokker.ts` minus the mode toggle). Generator chips (ratios, D-13 chip idiom) + an **ordinal** field, a live CS-status readout from `isConstantStructure` on the returned Scale (D-14), errors → status region + preserve prior preview (D-16). **Default landing = `csgs([3/2], 3)` — the CORRECTED ordinal (RESEARCH Critical Correction: `csgs([3/2], 7)` yields 41 notes and fail-closes; ordinal 3 is the 7-note Pythagorean diatonic). The field is labelled "ordinal", NOT "note count".**

Purpose: Deliver the two remaining v1.1 generators. These depend on Plan 01's kernel (`meru.ts` + `constant-structure.ts`); they do NOT yet register into `generate.md` — that is Plan 04 (single-owner append to the page).
Output: Two widget modules + their CSS + two Vitest (happy-dom) test files (Wave 0 anchors) + two INVENTORY component rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-preview-transforms-advanced-generators/08-CONTEXT.md
@.planning/phases/08-preview-transforms-advanced-generators/08-RESEARCH.md
@.planning/phases/05-generate-surface-live-integration-foundation/05-UI-SPEC.md

<interfaces>
<!-- Contracts the executor builds AGAINST. -->

From src/lib/meru.ts (Plan 01 — shipped before this plan):
```typescript
export function meruScale(a: bigint, b: bigint, x0: bigint, x1: bigint, terms: number): Scale; // exact convergents; throws RangeError on cap violation
export function metallicLimitCents(a: number, b: number): number; // irrational limit in cents (readout only — never a degree)
```

From src/lib/constant-structure.ts (Plan 01):
```typescript
export function isConstantStructure(scale: Scale): { cs: boolean; ambiguousAt?: { intervalClassA: number; intervalClassB: number } };
```

From src/lib/sonicweave.ts (Phase 7 — shipped):
```typescript
export interface SonicWeaveResult { scale: Scale | null; tempered: boolean; error?: string; }
export function scaleFromSonicWeave(src: string): SonicWeaveResult; // NEVER throws; fail-closes; auto-prepends 1/1
```

The Phase-6/7 widget contract (every generator widget returns this shape):
```typescript
interface GenerateXElement extends HTMLElement {
  getScale(): Scale | null;
  isTempered(): boolean;
}
```

generate-fokker.ts reusable internals (READ the file — clone, don't re-derive):
- `makeChipInput(labelText, nameStem, getList, setList, validate, placeholder, cap, onListChanged?)` — generic chip list + Add/× + per-instance cap + onListChanged; chip labels via textContent (XSS-safe). Reuse VERBATIM for the CS generator chips (D-13).
- `parseRatio(raw)` — validates `n/d` with BigInt (no prime-limit ceiling), returns normalized `"n/d"` or null. Reuse for the CS chips.
- `makeIntField(labelText, nameAttr, value, onInput, {min,max})` — labelled `<input type=number>` with clamp + rebuild. Reuse for the CS ordinal field AND the Meru seed/coefficient/term-count fields.
- The `composeSource() → scaleFromSonicWeave(src) → rebuild()` flow with status region + "preserve prior preview on error" (D-16): on `result.scale === null`, write `result.error` to status and DO NOT clear tableHost/playHost.
- The live readout pattern (`readout` span with aria-live=polite) — the CS-status readout mirrors the Fokker "→ N notes" readout (D-14 mirrors D-12).

generate-cps.ts preset-select idiom (PRESETS record + a `<select name="…-preset">` whose change handler fills the fields and re-renders) — reuse for the Meru preset roster (D-10).

scaleTable + playScale (read-only contracts):
```typescript
scaleTable(scale: Scale, baseHz: number, opts?: { precision?: number; tempered?: boolean }): HTMLElement; // tempered:true → cents-only + badge
playScale(scale: Scale, synth: SynthHandle, opts?: { baseHz?: number }): HTMLElement; // ⏵⏵ Play (.play-btn--scale)
```

makeRatioField (mos-builder.ts) — n/d pair; available if a Meru seed is better as a ratio field, but seeds/coefficients are integers so makeIntField is the natural fit.

VERIFIED csgs/gs semantics (08-RESEARCH Critical Correction — runtime-probed against sonic-weave@0.14.1):
- `csgs([3/2], 3)` → 7 notes (9/8, 81/64, 729/512, 3/2, 27/16, 243/128, 2/1) — THE default.
- `csgs([3/2], 7)` → 41 notes, several throw "Denominator above safe limit" → adapter fail-closes. DO NOT use as default.
- `csgs(generators, ordinal, …)` — ordinal selects the ordinal-th CS scale by INCREASING SIZE (ordinal 0=trivial, 1→3, 2→5, 3→7, 4→12). The CS cardinalities of the fifth-chain are 2,3,5,7,12,…

VERIFIED Meru anchors (08-RESEARCH): Fibonacci convergents 2/1,3/2,5/3,8/5,13/8 exact; metallic limits golden(1,1)→833.09¢, silver(2,1)→1525.86¢, bronze(3,1)→2068.41¢.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: generate-meru.ts — Wilson recurrence / metallic widget (golden default, limit readout, presets, caps)</name>
  <files>src/components/generate-meru.ts, src/components/generate-meru.css, src/components/__tests__/generate-meru.test.ts</files>
  <read_first>
    - src/lib/meru.ts (Plan 01 — meruScale + metallicLimitCents contracts + the caps that throw RangeError; the widget must catch and surface them)
    - src/components/generate-cps.ts (the preset-select idiom: PRESETS record + <select> change handler that fills fields; the Pattern-2 closure-local-state + getScale()/isTempered() shape; the exact-JI scaleTable path with NO tempered flag)
    - src/components/generate-fokker.ts (makeIntField, the status region + "preserve prior preview on error" discipline, the live readout span pattern — the metallic-limit readout mirrors the Fokker "→ N notes" readout)
    - src/components/scale-table.ts (the exact-JI Ratio-column render path; the metallic-limit readout is a SEPARATE element BESIDE the table, flagged tempered — never a table row)
    - src/components/__tests__/generate-cps.test.ts (the happy-dom widget-test idiom: makeStubSynth, query rows/headers/badge/inputs, assert isTempered())
    - .planning/phases/05-generate-surface-live-integration-foundation/05-UI-SPEC.md (tokens — D-17 discipline: match, don't invent)
    - .planning/phases/08-preview-transforms-advanced-generators/08-RESEARCH.md "Pattern 4" + "Wilson / metallic numerical verification" + Assumption A3 (Wilson Meru seed presets MUST cite a source — Erv Wilson / anaphoria.com — Vallotti-precedent discipline from Phase 7)
  </read_first>
  <behavior>
    - Test 1 (default landing, D-12): generateMeru(stubSynth) renders the Fibonacci/golden convergents as EXACT JI — the table has a "Ratio" header (no tempered badge), and the rows include 3/2, 5/3, 8/5, 13/8 (textContent). isTempered() === false.
    - Test 2 (metallic-limit readout, D-09): a readout element BESIDE the table shows the golden limit ≈ 833.1¢ and is flagged/labelled tempered; assert the limit value does NOT appear as a table row (it is never a scale degree).
    - Test 3 (presets, D-10): a preset <select> offers at least golden/silver/bronze; selecting "silver" fills a=2,b=1 and updates the readout to ≈ 1525.9¢; each Wilson-Meru-seed preset has an associated citation (assert a citation string/attribute is present for the named-seed presets — A3).
    - Test 4 (editable params, D-11): seed (x0,x1), coefficient (a,b), and term-count inputs exist (makeIntField) and editing term-count re-renders the convergents.
    - Test 5 (caps surface gracefully, D-11): setting term-count above the cap (or seeds that overflow MAX_MERU_MAGNITUDE) surfaces the meruScale RangeError in the status region WITHOUT crashing and preserves the prior preview.
    - Test 6 (getScale + Play): getScale() returns the convergent Scale; a .play-btn--scale Play button is present.
  </behavior>
  <action>
    Implement `generateMeru(synth, opts: { baseHz?: number; precision?: number } = {}): GenerateMeruElement` (the GenerateMeruElement interface extends HTMLElement with getScale/isTempered, mirroring GenerateFokkerElement). Closure-local state: `a, b, x0, x1` (BigInt), `terms` (number), `preset` (string). Default state = golden: a=1n, b=1n, x0=1n, x1=1n, terms≈7 (D-12). A preset `<select>` (generate-cps idiom) with a PRESETS record: `golden {a:1,b:1,x0:1,x1:1}`, `silver {a:2,b:1,…}`, `bronze {a:3,b:1,…}`, plus a small roster of named Wilson Mt. Meru seed pairs — EACH preset entry MUST carry a citable source (Erv Wilson / anaphoria.com / en.xen.wiki) recorded in a comment + surfaced as a citation line/title in the UI (A3 — the Vallotti discipline; do NOT invent seed pairs without a source — if a citation cannot be sourced for a named Meru seed, ship only golden/silver/bronze rather than fabricate). Seed/coefficient/term-count via makeIntField (D-11) with min/max reflecting the meru.ts caps. rebuild(): call `meruScale(a,b,x0,x1,terms)` inside try/catch — on RangeError, write the message to the status region and PRESERVE the prior preview (the Fokker D-16 discipline); on success, render `scaleTable(scale, baseHz, { precision })` WITHOUT the tempered flag (exact JI, generate-cps path) into tableHost + `playScale(scale, synth, { baseHz })` into playHost, and update a metallic-limit readout element BESIDE the table showing `metallicLimitCents(Number(a), Number(b)).toFixed(1)+"¢"` flagged tempered (D-09 — readout only, NEVER appended to the scale). `isTempered() => false` (convergents are exact JI; the limit is a separate tempered readout). `getScale() => currentScale`. Write generate-meru.css matching 05-UI-SPEC tokens. All text via textContent.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/__tests__/generate-meru.test.ts` passes (all six behavior groups green).
    - Behavior assertion: default table is exact JI (Ratio header present, NO `.scale-table__badge`) and includes 3/2 + 13/8 rows; isTempered() === false.
    - Behavior assertion: the golden limit ≈ 833.1¢ appears in a readout flagged tempered and NOT as a table row (D-09 / SURF-06).
    - Source assertion: `grep -c 'meruScale\|metallicLimitCents' src/components/generate-meru.ts` ≥ 2 (kernel calls present).
    - Source assertion: every named Wilson-Meru-seed preset has a citation comment (`grep -ic 'anaphoria\|wilson\|xen.wiki' src/components/generate-meru.ts` ≥ 1) OR only golden/silver/bronze ship.
    - Source assertion: `grep -c 'innerHTML' src/components/generate-meru.ts` == 0.
    - generate-meru.css exists and is non-empty; `npx vitest run` full suite stays green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/__tests__/generate-meru.test.ts</automated>
  </verify>
  <done>generateMeru lands on golden Fibonacci convergents (exact JI), shows the tempered φ≈833.1¢ limit beside the table (never a degree), offers cited presets + editable params with caps that surface gracefully, and exposes getScale()/isTempered()===false; CSS matches 05-UI-SPEC; full suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: generate-cs.ts — constant-structure widget (csgs([3/2],3) default, ordinal field, CS-status readout)</name>
  <files>src/components/generate-cs.ts, src/components/generate-cs.css, src/components/__tests__/generate-cs.test.ts</files>
  <read_first>
    - src/components/generate-fokker.ts (THE STRUCTURAL TWIN — clone its shape minus the basis↔comma mode toggle: makeChipInput for generator ratio chips, makeIntField for the ordinal, composeSource()→scaleFromSonicWeave(src)→rebuild() with status region + preserve-prior-preview-on-error (D-16), the live readout span. generate-cs = generate-fokker with chips+ordinal and a CS-status readout instead of "→ N notes".)
    - src/lib/sonicweave.ts (scaleFromSonicWeave contract — {scale, tempered, error?}, never throws, auto-prepends 1/1; the CS widget's ONLY adapter call — no direct sonic-weave import, D-13)
    - src/lib/constant-structure.ts (Plan 01 — isConstantStructure(scale) → {cs, ambiguousAt?}; called on the RETURNED scale for the D-14 readout)
    - src/components/__tests__/generate-fokker.test.ts (the widget-test idiom: default-landing row count, chip select, isTempered(), error-preserves-preview)
    - .planning/phases/05-generate-surface-live-integration-foundation/05-UI-SPEC.md (tokens)
    - .planning/phases/08-preview-transforms-advanced-generators/08-RESEARCH.md "Critical Correction — csgs ordinal semantics" (THE NON-NEGOTIABLE default + field labelling + ordinal cap) AND "Pattern 3: SonicWeave-wrapped CS widget"
  </read_first>
  <behavior>
    - Test 1 (CORRECTED default landing): generateCs(stubSynth) defaults to generator chip ["3/2"] and ordinal 3, producing the 7-note Pythagorean diatonic. The table renders 8 rows (7 + the auto-prepended 1/1 unison, the Fokker D-13 precedent) as EXACT JI (Ratio header, no tempered badge). isTempered() === false. (Assert the composed source is `csgs([3/2], 3)`, NOT `csgs([3/2], 7)`.)
    - Test 2 (ordinal field labelling, D-13 + Correction): the second field is labelled "ordinal" (the label/aria-label contains "ordinal", and does NOT say "note count" or "size"). A helper note is present explaining "ordinal selects the Nth constant-structure scale by increasing size." The ordinal input has a low max (≤ ~8).
    - Test 3 (CS-status readout, D-14): a readout shows "✓ constant structure" for the default csgs result (isConstantStructure on the returned scale). Construct/feed a scale path that yields CS-✗ and assert the readout shows "✗ not CS (ambiguous at …)" with the ambiguousAt detail.
    - Test 4 (chips, D-13): generator ratio chips use the makeChipInput idiom (Add/×, parseRatio validation, textContent labels). No raw SonicWeave syntax is exposed in the form (no free-text textarea — D-13).
    - Test 5 (error handling, D-16): a generator/ordinal combination that makes the adapter fail-close (e.g. a high ordinal producing safe-integer overflow) surfaces the adapter error verbatim in the status region and PRESERVES the prior preview (tableHost not cleared).
    - Test 6 (getScale + Play): getScale() returns the CS Scale; a .play-btn--scale Play button is present.
  </behavior>
  <action>
    Implement `generateCs(synth, opts: { baseHz?: number; precision?: number } = {}): GenerateCsElement` as the structural twin of generateFokker minus the mode toggle. Closure-local state: `generatorChips: string[]` (default `["3/2"]`) and `ordinal: number` (default **3** — the CORRECTED value; csgs([3/2],3) is the 7-note Pythagorean diatonic). Reuse generate-fokker's `makeChipInput` (with `parseRatio` validation) for the generator chips (D-13) and `makeIntField` for the ordinal — the ordinal field MUST be labelled "ordinal" (label text + aria-label), NEVER "note count"/"size", with a short helper line: "Ordinal selects the Nth constant-structure scale by increasing size." Cap the ordinal input at a low max (e.g. 8) per the Correction (high ordinals overflow the adapter's safe-integer guard). `composeSource(): string | null` → returns `csgs([${generatorChips.join(", ")}], ${String(ordinal)})` (ordinal, NOT note count); null + status message if no chips. `rebuild()`: `scaleFromSonicWeave(src)` → on `result.scale === null` write `result.error` to status, PRESERVE prior preview (D-16); on success compute `{ cs, ambiguousAt } = isConstantStructure(result.scale)`, set the CS-status readout to `cs ? "✓ constant structure" : "✗ not CS (ambiguous at " + <phrasing of ambiguousAt> + ")"` (D-14), render `scaleTable(result.scale, baseHz, { precision, tempered: result.tempered })` + `playScale(...)`, store currentScale + the last tempered flag. `isTempered() => <last result.tempered>` (csgs from rational generators is exact JI → normally false; keep the conditional branch like rank-2 in case a float generator is typed). `getScale() => currentScale`. NO direct `sonic-weave` import — only `scaleFromSonicWeave` (D-13). Write generate-cs.css matching 05-UI-SPEC tokens. All text via textContent.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/__tests__/generate-cs.test.ts` passes (all six behavior groups green).
    - Behavior assertion (THE CORRECTION): the default composed source is `csgs([3/2], 3)` and the default table has 8 rows (7-note Pythagorean diatonic + 1/1); NOT 41 notes, NOT an error. `grep -c 'csgs(\[3/2\], 7)\|ordinal = 7\|ordinal: 7' src/components/generate-cs.ts` == 0.
    - Source assertion: the second field is labelled "ordinal" — `grep -ic 'ordinal' src/components/generate-cs.ts` ≥ 2 (label + helper) and `grep -ic 'note count' src/components/generate-cs.ts` == 0.
    - Behavior assertion: the CS-status readout shows "✓ constant structure" for the default (isConstantStructure called on the returned scale — `grep -c 'isConstantStructure' src/components/generate-cs.ts` ≥ 1).
    - Source assertion: no direct sonic-weave import — `grep -c "from \"sonic-weave\"\|from 'sonic-weave'" src/components/generate-cs.ts` == 0 (D-13; the adapter is the only boundary).
    - Source assertion: `grep -c 'innerHTML' src/components/generate-cs.ts` == 0; generate-cs.css exists; `npx vitest run` full suite stays green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/__tests__/generate-cs.test.ts</automated>
  </verify>
  <done>generateCs lands on the corrected csgs([3/2],3) 7-note Pythagorean diatonic (exact JI CS-✓) with an "ordinal"-labelled capped field + helper, a live CS-status readout from isConstantStructure, ratio chips (no raw SonicWeave), and error→status+preserve-preview; CSS matches 05-UI-SPEC; full suite green.</done>
</task>

<task type="auto">
  <name>Task 3: INVENTORY.md — add the two new Phase 8 component rows</name>
  <files>src/lib/INVENTORY.md</files>
  <read_first>
    - src/lib/INVENTORY.md (the "### Phase N — components entries" subsection style + the Phase 8 section added by Plan 01; append a components subsection under it)
  </read_first>
  <action>
    Under the existing `## Phase 8 entries` section (added by Plan 01), append a `### Phase 8 — Advanced generator widgets (Plan 08-03 / GEN-10)` subsection with two component rows (components are listed in INVENTORY for discoverability per the existing convention). Row 1: `generateMeru` (`src/components/generate-meru.ts`) — Custom (this repo). Notes: Pattern-2 factory; calls meruScale + metallicLimitCents (Plan 01); exact-JI convergent table (isTempered()===false), the metallic limit shown as a SEPARATE tempered-flagged readout (D-09, never a degree); cited presets (D-10, A3 Vallotti discipline); caps surfaced via status region (D-11/D-16). Row 2: `generateCs` (`src/components/generate-cs.ts`) — Custom (this repo). Notes: structural twin of generateFokker minus the mode toggle; generator ratio chips + an ORDINAL field (the CORRECTED csgs semantics — default csgs([3/2],3) is the 7-note Pythagorean diatonic, NOT csgs([3/2],7) which yields 41 notes; field labelled "ordinal", capped low); CS-status readout from isConstantStructure (D-14); error→status+preserve-preview (D-16); no raw SonicWeave exposed (D-13). Match existing row prose density.
  </action>
  <acceptance_criteria>
    - `grep -c 'generateMeru\|generateCs' src/lib/INVENTORY.md` ≥ 2.
    - The generateCs row records the corrected csgs ordinal semantics (`grep -ic 'ordinal' src/lib/INVENTORY.md` for the new row context ≥ 1).
    - Both rows record Source + Notes.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'generateMeru\|generateCs' src/lib/INVENTORY.md</automated>
  </verify>
  <done>INVENTORY.md documents generateMeru and generateCs with the csgs ordinal correction noted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Meru seed/coefficient/term-count inputs → meruScale | untrusted integers reach the recurrence; meruScale's caps (Plan 01) are the enforcement; the widget surfaces RangeError gracefully |
| CS generator chips + ordinal → scaleFromSonicWeave (csgs) | validated `n/d` chips + an integer ordinal compose the ONLY SonicWeave source; the adapter (Phase 7) is the trust boundary; no raw SonicWeave syntax exposed (D-13) |
| adapter error / CS-status text → DOM status region | adapter error messages + readout text rendered into the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | DoS | high csgs ordinal → 41+ note overflow | mitigate | Ordinal input capped low (≤ ~8) per the RESEARCH Correction; csgs's own `maxSize=100` throws "No constant structure found…" and the adapter fail-closes on safe-integer overflow → surfaced in the status region (D-16), prior preview preserved. The corrected default (ordinal 3) never trips this. |
| T-08-09 | DoS | Meru term-count / seed magnitude | mitigate | meruScale's MAX_MERU_TERMS + MAX_MERU_MAGNITUDE caps (Plan 01) throw RangeError; the widget catches it and writes to the status region (D-11/D-16), no crash. |
| T-08-10 | Tampering (code injection) | csgs source from chips | mitigate | The CS source is `csgs([...], n)` composed from `parseRatio`-validated `n/d` chips + an integer ordinal — no free-text reaches `evaluateSource` (D-13; the free-text SonicWeave path is a DIFFERENT widget, GEN-09/Phase 7). 8 KB input cap enforced in `scaleFromSonicWeave`. |
| T-08-11 | Tampering (XSS) | chip labels, presets, readout, status | mitigate | All dynamic text via createElement + textContent (makeChipInput textContent labels; readout/status textContent) — never innerHTML. Verified by `grep -c innerHTML == 0` on both widget files. |
| T-08-12 | Tampering (laundered temperament) | Meru limit + CS tempered flag | mitigate | Meru convergents are exact JI; the metallic limit is a standalone tempered-flagged readout (D-09), never a degree. CS isTempered() reflects the adapter's tempered flag so a float-generator CS scale serializes cents downstream (SURF-06). |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH audit: zero new deps; sonic-weave already vetted/patched Phase 7). slopcheck N/A; no [ASSUMED]/[SUS] packages → no blocking-human checkpoint. |
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/generate-meru.test.ts src/components/__tests__/generate-cs.test.ts` — both green.
- `npx vitest run` — full suite green (anti-regression).
- `npm run lint:types` — clean (strict-TS; BigInt seed handling + noUncheckedIndexedAccess).
- The csgs default MUST resolve to a 7-note scale (the Correction): the generate-cs test asserts 8 rows and source `csgs([3/2], 3)`.
</verification>

<success_criteria>
- generateMeru: golden Fibonacci convergents (exact JI), tempered φ≈833.1¢ limit beside the table (never a degree), cited presets, editable params + caps, isTempered()===false (GEN-10 Wilson, success criterion 3).
- generateCs: corrected csgs([3/2],3) 7-note Pythagorean CS-✓ default, "ordinal"-labelled capped field + helper, live CS-status readout, ratio chips (no raw SonicWeave), error→status+preserve-preview (GEN-10 CS, success criterion 3).
- Two INVENTORY component rows.
- Full Vitest suite + lint:types green.
</success_criteria>

<output>
Create `.planning/phases/08-preview-transforms-advanced-generators/08-03-SUMMARY.md` when done.
</output>
