---
phase: 08-preview-transforms-advanced-generators
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - src/pages/generate.md
  - src/styles.css
autonomous: false
requirements: [SURF-04, SURF-05, GEN-10]
must_haves:
  truths:
    - "Wilson/metallic and constant-structure register under the 'Advanced / algorithmic' optgroup in generate.md; selecting each mounts its widget (generateMeru / generateCs) into paramsHost"
    - "The circle-of-pitches viz and transform strip mount ONCE in the shared preview host, fed by the active widget's getScale()/isTempered() (D-03/D-06) — applying uniformly to every generator family (success criterion 4)"
    - "The transform strip's transformed scale (rotate/reduce/dedupe/transpose) is what Send-to serializes — tempered scales serialize cents-per-line, exact JI ratio-per-line (D-06 / SURF-06)"
    - "The shared circle + transformed table update live when the active widget's getScale() output changes (the first cross-widget shared-preview consumer — additive reactive wiring, Open Q1)"
    - "Empty-store boot of Dashboard/Analysis stays byte-identical (no change to those pages); the four new component CSS files are imported in styles.css"
  artifacts:
    - path: "src/pages/generate.md"
      provides: "Advanced-optgroup registration of generateMeru + generateCs, shared circle + transform-strip wiring, transformed-scale Send-to"
    - path: "src/styles.css"
      provides: "Four new @import lines (circle-of-pitches, scale-transform-strip, generate-meru, generate-cs)"
  key_links:
    - from: "src/pages/generate.md (shared preview host)"
      to: "circleOfPitches + scaleTransformStrip (Plan 02)"
      via: "single mount fed by active widget getScale()/isTempered()"
      pattern: "circleOfPitches\\(|scaleTransformStrip\\("
    - from: "src/pages/generate.md (Send-to)"
      to: "the transform strip's getTransformedScale()"
      via: "transformed scale → ratioPerLine/centsPerLine → writeSharedScale + encodeScaleToHash"
      pattern: "getTransformedScale\\("
    - from: "src/styles.css"
      to: "the four new component CSS files"
      via: "@import"
      pattern: "@import.*\\(circle-of-pitches|scale-transform-strip|generate-meru|generate-cs\\)"
---

<objective>
Wire Phase 8's pieces into the live page — the final, single-owner, append-only edit to `src/pages/generate.md` plus four `@import` lines in `src/styles.css`:

1. Register the two new generators (generateMeru, generateCs) under the existing **"Advanced / algorithmic"** `<optgroup>` (D-21) — add their METHOD_FAMILIES options, instantiate each widget ONCE, add the paramsHost + previewHost swap branches, and add their Send-to serializers (Meru = exact-JI ratio-per-line; CS = conditional on its live isTempered()).
2. Mount the circle-of-pitches viz + the transform strip ONCE in the shared preview host (D-03/D-06), fed by the ACTIVE widget's `getScale()`/`isTempered()`. The strip's transformed scale (not the raw generator output) becomes what the circle renders AND what Send-to serializes. This is the FIRST cross-widget shared-preview consumer — the novel reactive-wiring problem (RESEARCH Open Q1): widget-internal param edits don't tick Observable's reactive graph, so use the additive `onScaleChange`/`onChange` callback approach (the strip already exposes `onChange`; bind it to a `Mutable` that drives the shared circle + transformed table + Send-to).
3. Import the four new component CSS files in styles.css.

Purpose: This is the integration that makes SURF-04, SURF-05, and the GEN-10 widgets usable end-to-end, and enforces success criterion 4 (circle + strip apply uniformly to every family). It is sequenced LAST and owns generate.md exclusively (append-only — no parallel writes to this file).
Output: An updated generate.md + styles.css, plus a human-verify checkpoint covering the manual-only items (reactive update, click-audition, transformed-scale Send-to round-trip) from 08-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-preview-transforms-advanced-generators/08-CONTEXT.md
@.planning/phases/08-preview-transforms-advanced-generators/08-RESEARCH.md

<interfaces>
<!-- The page-wiring contracts. The executor extends generate.md following these EXISTING patterns. -->

generate.md current structure (READ the file in full first — these are the exact insertion points):
- METHOD_FAMILIES array (lines ~70–98): the "Advanced / algorithmic" family currently has ONE option `{ id: "fokker", text: "Fokker periodicity block", placeholder: false }`. APPEND two options to that family's `options` array: `{ id: "meru", text: "Wilson recurrence / metallic (Mt. Meru)", placeholder: false }` and `{ id: "cs", text: "Constant structure", placeholder: false }` (D-21).
- Widget instantiation cells (lines ~195–278): each widget is `const xWidget = generateX(synth, { baseHz });` instantiated ONCE so closure-local state survives picker swaps. ADD `const meruWidget = generateMeru(synth, { baseHz });` and `const csWidget = generateCs(synth, { baseHz });`.
- The reactive `currentScaleText` ternary (lines ~382–399) and `sendCurrentScaleTo` ternary (lines ~640–658): ADD `meru` and `cs` branches. Meru → `ratioPerLine(meruWidget.getScale())` (exact JI). CS → conditional `csWidget.isTempered() ? centsPerLine(...) : ratioPerLine(...)` (the rank-2/sonicweave conditional precedent). Reuse the existing `ratioPerLine`/`centsPerLine` helpers (lines ~344–356).
- The paramsHost swap block (lines ~421–485): ADD `else if (method === "meru") { paramsHost.replaceChildren(meruWidget); }` and `else if (method === "cs") { paramsHost.replaceChildren(csWidget); }`.
- The previewHost swap block (lines ~496–589): each widget branch shows a pointer caption (the widget renders its own table). ADD meru/cs caption branches. BUT this is where the shared circle + strip now also mount — see the wiring task.
- import block (lines ~5–21): ADD `import { generateMeru } from "../components/generate-meru.js";`, `import { generateCs } from "../components/generate-cs.js";`, `import { circleOfPitches } from "../components/circle-of-pitches.js";`, `import { scaleTransformStrip } from "../components/scale-transform-strip.js";`.

Reactive-read precedent (THE Open-Q1 solution basis): generate.md already reads widgets LIVE at call time because "the widget's internal state changes don't tick Observable's reactive graph" (lines ~282–296, ~636–639). The shared circle + strip must update on widget-internal edits. The additive approach (RESEARCH recommendation): the strip exposes `onChange(cb)`; the active widget can expose an optional `onScaleChange(cb)` OR the page re-reads `getScale()` and calls the strip's `setSource(scale, tempered)` inside a cell that re-runs on `method`/`baseHz` change, and the strip's own `onChange` drives a `Mutable` that re-renders the shared circle + transformed table. Use a `Mutable` (the existing `audioActive`/synth-cell precedent at lines ~35–38) as the reactive bridge.

The shared preview host is `previewHost` (display()'d element, lines ~186–188) — already the replaceChildren swap target.

scaleTransformStrip surface (Plan 02): `setSource(scale, tempered)`, `getTransformedScale()`, `getTempered()`, `onChange(cb)`.
circleOfPitches surface (Plan 02): `circleOfPitches(scale, synth, { baseHz, tempered })`.

styles.css @import block (lines ~20–43): ADD four lines after the last generate-* import:
`@import "./components/circle-of-pitches.css";`, `@import "./components/scale-transform-strip.css";`, `@import "./components/generate-meru.css";`, `@import "./components/generate-cs.css";`.

DO NOT touch Dashboard (index.md) or Analysis (analysis.md) — the empty-store boot-equivalence gate (SYNC-04, R1) must stay byte-identical. This plan is additive to generate.md only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register generateMeru + generateCs in the Advanced optgroup + CSS imports</name>
  <files>src/pages/generate.md, src/styles.css</files>
  <read_first>
    - src/pages/generate.md (the FULL file — METHOD_FAMILIES, the widget-instantiation cells, the currentScaleText + sendCurrentScaleTo ternaries, the paramsHost + previewHost swap blocks, the ratioPerLine/centsPerLine helpers, the import block; this is the append-only edit surface)
    - src/components/generate-meru.ts AND src/components/generate-cs.ts (Plan 03 — confirm the exported factory names + getScale()/isTempered() contract you wire to)
    - src/styles.css (lines ~20–43 — the component @import aggregation block; append after the last generate-* import)
    - src/components/generate-fokker.ts wiring in generate.md (the most recent Advanced-optgroup precedent: how fokkerWidget was added to METHOD_FAMILIES, instantiated once, given paramsHost/previewHost branches, and a fokkerScaleText serializer)
  </read_first>
  <action>
    Append-only edits to generate.md following the generate-fokker precedent EXACTLY: (1) add the two import lines for generateMeru + generateCs (and circleOfPitches + scaleTransformStrip, used in Task 2); (2) append `{ id: "meru", text: "Wilson recurrence / metallic (Mt. Meru)" , placeholder: false }` and `{ id: "cs", text: "Constant structure", placeholder: false }` to the "Advanced / algorithmic" family's options (D-21); (3) instantiate `const meruWidget = generateMeru(synth, { baseHz });` and `const csWidget = generateCs(synth, { baseHz });` ONCE (closure-local-state-survives-swaps precedent); (4) add `meruScaleText()` (exact JI → `ratioPerLine(meruWidget.getScale())`, fall back to seedText if null) and `csScaleText()` (conditional → `csWidget.isTempered() ? centsPerLine(scale) : ratioPerLine(scale)`) serializers reusing the existing helpers; (5) extend the `currentScaleText` ternary and the `sendCurrentScaleTo` ternary with `meru`/`cs` branches; (6) add `else if (method === "meru") paramsHost.replaceChildren(meruWidget);` and `else if (method === "cs") paramsHost.replaceChildren(csWidget);` to the paramsHost swap; (7) add meru/cs pointer-caption branches to the previewHost swap (the widget renders its own table — same as the other widget branches; Task 2 adds the shared circle/strip beneath). Append the four CSS `@import` lines to styles.css after the last generate-* import. All page text via textContent (the established generate.md discipline). Do NOT modify index.md or analysis.md.
  </action>
  <acceptance_criteria>
    - `grep -c 'generateMeru\|generateCs' src/pages/generate.md` ≥ 4 (import + instantiation + branches for each).
    - `grep -c 'id: "meru"\|id: "cs"' src/pages/generate.md` == 2 (both registered in METHOD_FAMILIES).
    - `grep -c 'circle-of-pitches.css\|scale-transform-strip.css\|generate-meru.css\|generate-cs.css' src/styles.css` == 4 (all four CSS imports present).
    - `npm run build` succeeds (generate.md compiles; the new imports resolve).
    - `git diff --stat src/pages/index.md src/pages/analysis.md` shows NO changes (additive to generate.md only — SYNC-04 byte-identical boot preserved).
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -c 'generate-meru.css\|generate-cs.css\|circle-of-pitches.css\|scale-transform-strip.css' src/styles.css</automated>
  </verify>
  <done>generateMeru + generateCs are registered under the Advanced optgroup (METHOD_FAMILIES + instantiation + paramsHost/previewHost branches + Send-to serializers); the four new component CSS files are imported; build green; Dashboard/Analysis untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the shared circle + transform strip once, fed by the active widget (reactive wiring)</name>
  <files>src/pages/generate.md</files>
  <read_first>
    - src/pages/generate.md (the previewHost cell ~186–188, the synth-cell Mutable precedent ~35–38, the reactive currentScaleText/currentScale cells ~382–413, the LIVE-read rationale comments ~282–296 + ~636–639 — the basis for the Open-Q1 solution)
    - src/components/scale-transform-strip.ts (Plan 02 — setSource(scale,tempered), getTransformedScale(), getTempered(), onChange(cb))
    - src/components/circle-of-pitches.ts (Plan 02 — circleOfPitches(scale, synth, { baseHz, tempered }) + empty-state behavior)
    - src/components/scale-table.ts (the transformed-scale table render path: tempered:true → cents-only + badge; exact JI → Ratio column — the transformed preview table reuses scaleTable verbatim)
    - .planning/phases/08-preview-transforms-advanced-generators/08-RESEARCH.md "Pitfall 6: Shared-preview reactivity" + "Open Questions Q1" (the additive onScaleChange/onChange + Mutable bridge approach) and 08-VALIDATION.md Manual-Only table (what is human-verify vs unit-test)
  </read_first>
  <action>
    Add the shared-preview wiring to generate.md (append-only). Introduce a function `activeWidgetScale()` that returns `{ scale, tempered }` from whichever widget `method` selects (read LIVE via getScale()/isTempered() — the existing live-read pattern; null scale → fall back to the parsed seed/currentScale). Mount the transform strip ONCE (instantiate `const transformStrip = scaleTransformStrip({ baseHz });` in its own cell so it survives re-renders) and the circle into the shared preview region (a dedicated `sharedPreviewHost` element appended under previewHost, or reuse previewHost — keep the per-widget caption AND add the circle + strip + transformed table beneath it, D-06: the widget's own table shows the RAW output; the shared area shows the TRANSFORMED scale that Send-to serializes). Wire the reactivity (Open Q1): in a cell that re-runs on `method` and `baseHz`, call `transformStrip.setSource(active.scale, active.tempered)`; bind `transformStrip.onChange((tScale, tTempered) => { … })` ONCE to re-render the shared circle (`circleOfPitches(tScale, synth, { baseHz, tempered: tTempered })`) + a transformed `scaleTable(tScale, baseHz, { tempered: tTempered })` into the shared host, and to update a `Mutable` (the `audioActive` precedent) that the Send-to cell reads so Send-to serializes the TRANSFORMED scale (getTransformedScale()) — tempered → centsPerLine, JI → ratioPerLine (D-06/SURF-06). For widget-internal param edits that don't tick the reactive graph, additionally re-call `transformStrip.setSource(activeWidgetScale())` whenever the strip fires onChange OR (if the widget exposes an onScaleChange hook) bind it; the manual-verify task confirms the live update. Ensure the circle empty-state shows for octave-only/empty scales (Plan 02 handles it; just pass the scale through). DO NOT mutate any widget's internal table from here (D-06 Pattern-2 encapsulation — the shared area is separate). All text via textContent.
  </action>
  <acceptance_criteria>
    - `grep -c 'scaleTransformStrip(' src/pages/generate.md` ≥ 1 (strip instantiated once).
    - `grep -c 'circleOfPitches(' src/pages/generate.md` ≥ 1 (circle mounted in the shared host).
    - `grep -c 'getTransformedScale(\|transformStrip.onChange\|setSource(' src/pages/generate.md` ≥ 2 (the transformed-scale + reactive wiring present).
    - Behavior assertion: the Send-to path serializes `transformStrip.getTransformedScale()` (the transformed scale), not the raw widget output — `grep -c 'getTransformedScale' src/pages/generate.md` ≥ 1 in the Send-to branch.
    - `npm run build` succeeds; `npm test` full suite green (anti-regression; no Dashboard/Analysis change).
    - `git diff --stat src/pages/index.md src/pages/analysis.md` shows NO changes.
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -c 'getTransformedScale\|circleOfPitches(\|scaleTransformStrip(' src/pages/generate.md</automated>
  </verify>
  <done>The circle + transform strip mount once in the shared preview host fed by the active widget's getScale()/isTempered(); the transformed scale drives the circle, the transformed table, and Send-to; reactivity uses the additive onChange/Mutable bridge; build + full suite green; Dashboard/Analysis untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the Manual-Only behaviors (reactive update, click-audition, transformed Send-to, csgs correction, metallic limit)</name>
  <what-built>
    Phase 8 is wired end-to-end: the Advanced optgroup now offers Wilson recurrence / metallic (Mt. Meru) and Constant structure; a circle-of-pitches viz and a rotate/reduce/dedupe/transpose strip mount once in the shared preview and apply to every generator family; the transformed scale is what Send-to serializes. These three behaviors cannot be asserted in jsdom (Web Audio playback, Observable reactive-graph updates on widget-internal edits, and the live store/URL round-trip) — they are the Manual-Only rows in 08-VALIDATION.md.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and open the Generate page, then verify:

    1. SHARED-PREVIEW REACTIVITY (08-VALIDATION Manual-Only row 1 / Open Q1 — the novel risk):
       - Pick a method (e.g. CPS). Confirm the shared circle-of-pitches and the transformed table appear below the widget.
       - Edit a widget param (e.g. change a CPS chip or the Meru term-count). Confirm the SHARED circle + transformed table update LIVE (not just the widget's own table). If they do NOT update on a widget-internal edit, the reactive wiring needs the onScaleChange hook — report it.
       - Switch to a method that produces an octave-only/empty scale (or the unison) and confirm the circle shows the friendly empty-state, not a blank/broken SVG.

    2. CLICK-AUDITION + HOVER (Manual-Only row 3):
       - Hover a circle marker → it highlights and a tooltip shows the pitch info (ratio for JI, cents for tempered) + signed cents-from-12-TET.
       - Click a marker → you hear the pitch via the page synth. Press Esc → audio stops.

    3. TRANSFORMED-SCALE SEND-TO ROUND-TRIP (Manual-Only row 2 / SURF-04):
       - Apply a mode rotation + a transpose in the strip. Confirm the transformed table + circle reflect the transform.
       - Click "Send to Dashboard". Confirm the Dashboard loads the TRANSFORMED scale (not the raw generator output), and the URL `#s=` hash reflects it.
       - Do the same with a TEMPERED scale (e.g. select Meru → no; use a tempered generator like rank-2 or ED, apply a transform): confirm the Send-to payload is cents-per-line (NOT ratios) — the tempered flag survived the transform (SURF-06).

    4. THE csgs CORRECTION (GEN-10):
       - Select "Constant structure". Confirm the default lands on a 7-note Pythagorean diatonic (NOT 41 notes, NOT an error), the second field is labelled "ordinal" with a helper note, and the CS-status reads "✓ constant structure".

    5. METALLIC LIMIT (GEN-10):
       - Select "Wilson recurrence / metallic". Confirm the default golden scale shows exact-JI ratio rows (3/2, 5/3, 8/5, 13/8) AND a separate "φ ≈ 833.1¢" limit readout flagged tempered BESIDE the table — the limit is never a table row.
  </how-to-verify>
  <action>
    BLOCKING human-verify checkpoint. After Tasks 1–2 land and `npm run build` + `npm test` are green, start `npm run dev` and PAUSE for the user to walk the five-step `<how-to-verify>` script above (the three Manual-Only rows from 08-VALIDATION.md — shared-preview reactivity / click-audition + hover / transformed-scale Send-to round-trip — plus the csgs-correction and metallic-limit spot checks). Do not proceed past this checkpoint until the user types "approved" or reports a failure. If the user reports the shared circle/transformed table do NOT update on a widget-internal param edit (the documented Open-Q1 risk), the active widget needs an additive `onScaleChange(cb)` hook the page binds — fix that and re-verify before resuming.
  </action>
  <verify>
    <human-check>User confirms all five steps (reactive live update, click-audition + hover, transformed-scale Send-to with tempered staying cents-per-line, csgs([3/2],3) 7-note default, golden φ≈833.1¢ limit beside the table) and types "approved".</human-check>
  </verify>
  <done>The three Manual-Only behaviors plus the csgs-correction and metallic-limit spot checks are confirmed working in `npm run dev`; the user has typed "approved".</done>
  <resume-signal>Type "approved" if all five verify, or describe what failed (especially the reactive update in step 1 — the documented risk).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| active widget getScale()/isTempered() → shared circle + strip | the page reads the active widget's output and renders/transforms it; read-only, no write-back into the widget (D-06 encapsulation) |
| transform strip getTransformedScale() → Send-to (store + URL) | the transformed scale crosses into writeSharedScale + encodeScaleToHash; the existing 8 KB cap + one-way data-flow guard apply |
| widget option text / captions → DOM | static + scale-derived strings rendered into the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 | Tampering (laundered temperament) | transformed-scale Send-to | mitigate | The Send-to branch keys on the strip's getTempered() (propagated from the active widget's isTempered() through the transform, D-18) so a tempered scale serializes centsPerLine, never ratioPerLine (SURF-06 / Pitfall 2). Verified in the human-verify step-3 tempered case. |
| T-08-14 | DoS | oversized transformed scale → Send-to | mitigate | encodeScaleToHash enforces the existing 8 KB plaintext cap → RangeError surfaced via the existing CAP_ERROR_COPY status (generate.md unchanged guard); writeSharedScale also caps at 8 KB. No new uncapped path. |
| T-08-15 | Tampering (regression) | Dashboard/Analysis boot equivalence | mitigate | This plan is APPEND-ONLY to generate.md + styles.css; index.md/analysis.md are not touched (acceptance criteria assert `git diff --stat` shows no change), so the SYNC-04 empty-store boot-equivalence gate (R1) stays byte-identical. |
| T-08-16 | Tampering (XSS) | new captions / option text / shared readouts | mitigate | All page text via createElement + textContent (the established generate.md discipline); the new optgroup option text is a locked literal string. No innerHTML with interpolation. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH audit: zero new deps). slopcheck N/A; no [ASSUMED]/[SUS] packages → no blocking-human checkpoint. |
</threat_model>

<verification>
- `npm run build` — succeeds (generate.md compiles; all new imports resolve).
- `npm test` (`npx vitest run`) — full suite green (anti-regression: ~192 existing tests + the Phase-8 component tests; this plan adds no new test file but must not regress).
- `npm run lint:types` — clean.
- `git diff --stat src/pages/index.md src/pages/analysis.md` — empty (no change; SYNC-04 boot equivalence preserved).
- Manual: the human-verify checkpoint covers the three Manual-Only rows from 08-VALIDATION.md (reactive update, click-audition + hover, transformed-scale Send-to round-trip) plus the csgs-correction and metallic-limit spot checks.
</verification>

<success_criteria>
- Wilson/metallic + constant-structure are selectable under the Advanced optgroup and mount their widgets (GEN-10).
- The circle viz + transform strip mount once and apply uniformly to every family's output (SURF-04 + SURF-05 + success criterion 4).
- The transformed scale is what Send-to serializes, with tempered scales staying cents-flagged (SURF-04 + SURF-06).
- The shared circle + transformed table update live on active-widget changes (Open Q1 — confirmed by human-verify).
- Dashboard/Analysis boot byte-identically (SYNC-04 preserved); four CSS imports added.
- Build + full Vitest suite + lint:types green.
</success_criteria>

<output>
Create `.planning/phases/08-preview-transforms-advanced-generators/08-04-SUMMARY.md` when done.
</output>
