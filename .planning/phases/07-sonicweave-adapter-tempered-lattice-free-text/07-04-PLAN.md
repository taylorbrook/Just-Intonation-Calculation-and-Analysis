---
phase: 07-sonicweave-adapter-tempered-lattice-free-text
plan: 04
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - src/pages/generate.md
  - src/styles.css
autonomous: false
requirements: [GEN-06, GEN-07, GEN-08, GEN-09]

must_haves:
  truths:
    - "The picker shows rank-2 + well-temperament under 'Regular / equal temperament', Fokker under 'Advanced / algorithmic', and free-text under a new fifth 'SonicWeave' optgroup"
    - "Selecting each new method mounts its widget into the params host and shows the pointer caption in the preview host (the Phase-6 mounted-widget pattern)"
    - "Send to Dashboard / Send to Analysis serializes each new scale: exact-JI (Fokker, pure rank-2, JI free-text) as ratio-per-line, tempered (well-temp, tempered rank-2/free-text) as cents-per-line via isTempered()"
    - "Each new widget's CSS is imported in src/styles.css and the four widgets render with house styling"
    - "Empty-store boot equivalence is unaffected: Dashboard/Analysis still boot byte-identically when nothing is sent (no regression)"
  artifacts:
    - path: "src/pages/generate.md"
      provides: "registration of all four Phase-7 widgets: optgroups, instantiate-once cells, params/preview swaps, Send-to branches"
      contains: "generateRank2"
    - path: "src/styles.css"
      provides: "@import lines for the four new widget CSS files"
      contains: "generate-rank2.css"
  key_links:
    - from: "src/pages/generate.md"
      to: "src/components/generate-rank2.ts"
      via: "import { generateRank2 } + instantiate-once cell + params/preview swap branch"
      pattern: "generateRank2"
    - from: "src/pages/generate.md"
      to: "src/components/generate-welltemp.ts"
      via: "import + instantiate + swap branch + Send-to cents-per-line"
      pattern: "generateWelltemp"
    - from: "src/pages/generate.md"
      to: "src/components/generate-fokker.ts"
      via: "import + instantiate + swap branch + Send-to ratio-per-line"
      pattern: "generateFokker"
    - from: "src/pages/generate.md"
      to: "src/components/generate-sonicweave.ts"
      via: "import + instantiate + swap branch + Send-to (conditional via isTempered)"
      pattern: "generateSonicweave"
    - from: "src/styles.css"
      to: "the four widget CSS files"
      via: "@import lines next to the existing generate-* block"
      pattern: "@import \"./components/generate-rank2.css\""
---

<objective>
Wire the four Phase-7 widgets into the Generate surface. Extend `src/pages/generate.md`'s native `<optgroup>` picker, instantiate each widget once (closure state survives picker swaps), add the params-host + preview-host swap branches, and extend the Send-to serialization (exact-JI → ratio-per-line; tempered → cents-per-line via `isTempered()`). Add four `@import` lines to `src/styles.css`. This is the additive integration step that makes GEN-06..09 reachable from the UI — and it must NOT regress the empty-store boot equivalence (SYNC-04).

Purpose: The widgets and adapter are built (Plans 01-03); this plan makes them user-reachable without touching any kernel or existing-method code path. The picker registration follows the exact Phase-6 `ed`/`cps` pattern. A human-verify checkpoint confirms the four widgets render, audition, and Send-to correctly in the live Observable Framework page (the one surface not coverable by happy-dom component tests).
Output: edits to `src/pages/generate.md` and `src/styles.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-sonicweave-adapter-tempered-lattice-free-text/07-RESEARCH.md
@.planning/phases/07-sonicweave-adapter-tempered-lattice-free-text/07-PATTERNS.md

<interfaces>
<!-- The four widget factories (Plans 02/03) + the generate.md registration anchors. -->

Widget factories (all Pattern-2 `(synth, opts) => HTMLElement`):
```typescript
import { generateRank2 } from "../components/generate-rank2.js";        // GEN-06, conditionally tempered
import { generateWelltemp } from "../components/generate-welltemp.js";  // GEN-07, always tempered
import { generateFokker } from "../components/generate-fokker.js";      // GEN-08, exact JI (never tempered)
import { generateSonicweave } from "../components/generate-sonicweave.js"; // GEN-09, conditionally tempered
// each element exposes getScale(): Scale | null; and isTempered(): boolean (rank2/fokker/sonicweave) — well-temp isTempered() always true
```

generate.md registration anchors (existing structure — extend, do not rewrite):
- `METHOD_FAMILIES` array (lines 66-86): the four optgroup definitions. Add rank-2 + well-temp options to "Regular / equal temperament"; add Fokker to "Advanced / algorithmic"; ADD a NEW fifth family object `{ label: "SonicWeave", options: [{ id: "sonicweave", text: "Free-text SonicWeave", placeholder: false }] }`.
- Instantiate-once cells (lines 190, 206, 215, 225): one `const xWidget = generateX(synth, { baseHz })` cell per widget (closure state survives mount/unmount across picker swaps — the Pattern-2 precedent).
- Params-host swap block (lines 312-352): add `else if (method === "rank2") { paramsHost.replaceChildren(rank2Widget); }` etc. for all four.
- Preview-host swap block (lines 363-420): add a pointer-caption branch per widget (each widget renders its own table+Play internally, like cps/ed).
- Send-to serialization: `currentScaleText` ternary (lines 281-291) + `sendCurrentScaleTo` ternary (lines 471-481) + the per-widget `xScaleText()` helpers (lines 238-280). EXACT-JI widgets (Fokker, and rank-2/free-text WHEN isTempered() is false) serialize ratio-per-line (the cpsScaleText idiom: drop leading 1/1); TEMPERED widgets (well-temp, and rank-2/free-text WHEN isTempered() is true) serialize cents-per-line (the edScaleText idiom: `iv.cents.toFixed(4)`, drop leading 0¢).

src/styles.css existing @import block (lines 35-39):
```css
@import "./components/generate.css";
@import "./components/generate-cps.css";
@import "./components/generate-harmonic.css";
@import "./components/generate-ji-set.css";
@import "./components/generate-ed.css";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the four widgets in generate.md + wire CSS imports</name>
  <files>src/pages/generate.md, src/styles.css</files>
  <read_first>
    - src/pages/generate.md (FULL — the existing registration structure: METHOD_FAMILIES lines 66-86; the native <select> builder lines 89-121; instantiate-once cells lines 182-226; the cpsScaleText/harmonicScaleText/jiSetScaleText/edScaleText helpers lines 238-280; currentScaleText ternary lines 281-291; params-host swap lines 306-353; preview-host swap lines 355-421; sendCurrentScaleTo ternary lines 471-492)
    - src/styles.css (the @import block lines 35-39)
    - src/components/generate-rank2.ts (Plan 02 — factory + element interface getScale/isTempered)
    - src/components/generate-welltemp.ts (Plan 02 — always tempered)
    - src/components/generate-fokker.ts (Plan 03 — exact JI, isTempered false)
    - src/components/generate-sonicweave.ts (Plan 03 — conditionally tempered)
    - 07-PATTERNS.md "Page registration (additive picker optgroups)" shared pattern (lines 281-283)
  </read_first>
  <action>
    Edit src/pages/generate.md (additive only — do NOT alter any existing method's cells, the empty-state branch, or the demo-seed path; SYNC-04 byte-equivalence must hold for the no-scale-sent case):

    1. METHOD_FAMILIES (lines 66-86): add `{ id: "rank2", text: "Rank-2 regular temperament", placeholder: false }` and `{ id: "welltemp", text: "Well-temperament", placeholder: false }` to the "Regular / equal temperament" family's options; add `{ id: "fokker", text: "Fokker periodicity block", placeholder: false }` to the "Advanced / algorithmic" family (replacing or alongside its placeholder — remove the "(coming soon)" placeholder for Advanced since it now has a real method); ADD a NEW fifth family object `{ label: "SonicWeave", options: [{ id: "sonicweave", text: "Free-text SonicWeave", placeholder: false }] }` after the Advanced family. The native <select> builder (lines 109-118) already iterates METHOD_FAMILIES, so the new optgroups render automatically.

    2. Add four instantiate-once cells (mirror lines 190/206/215/225), each importing and calling its factory once:
       `const rank2Widget = generateRank2(synth, { baseHz });`
       `const welltempWidget = generateWelltemp(synth, { baseHz });`
       `const fokkerWidget = generateFokker(synth, { baseHz });`
       `const sonicweaveWidget = generateSonicweave(synth, { baseHz });`
       Add the four `import { generateX } from "../components/generate-X.js";` statements to the page's import cell.

    3. Params-host swap block (lines 312-352): add four `else if (method === "rank2") { paramsHost.replaceChildren(rank2Widget); }` (and welltemp/fokker/sonicweave) branches, before the `method === ""` empty branch. Each widget owns its table+Play internally (the cps/ed mounted-widget pattern).

    4. Preview-host swap block (lines 363-420): add four pointer-caption branches (mirror the cps/ed caption branches lines 364-399) — each says the table + ⏵⏵ Play are shown above with the controls, and Send-to serializes the current scale (ratio-per-line for Fokker/JI; cents-per-line for tempered).

    5. Send-to serialization — add four `xScaleText()` helpers (mirror cpsScaleText lines 238-244 for exact-JI, edScaleText lines 273-280 for tempered):
       - `fokkerScaleText()` → exact JI, ratio-per-line (drop leading 1/1) — cpsScaleText idiom.
       - `welltempScaleText()` → tempered, cents-per-line (`iv.cents.toFixed(4)`, drop leading 0¢) — edScaleText idiom.
       - `rank2ScaleText()` and `sonicweaveScaleText()` → CONDITIONAL on the widget's `isTempered()`: if tempered, cents-per-line (edScaleText idiom); else ratio-per-line (cpsScaleText idiom). Read the widget LIVE at call time (the widget's internal edits don't tick Observable's reactive graph — the existing live-read rationale lines 230-237).
       Then extend BOTH the `currentScaleText` ternary (lines 281-291) AND the `sendCurrentScaleTo` ternary (lines 471-481) with `method === "rank2" ? rank2ScaleText() : method === "welltemp" ? welltempScaleText() : method === "fokker" ? fokkerScaleText() : method === "sonicweave" ? sonicweaveScaleText() : ...` branches.

    Edit src/styles.css: add four `@import` lines next to the existing block (lines 35-39):
       `@import "./components/generate-rank2.css";`
       `@import "./components/generate-welltemp.css";`
       `@import "./components/generate-fokker.css";`
       `@import "./components/generate-sonicweave.css";`
  </action>
  <verify>
    <automated>npm run build && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/generate.md` contains `generateRank2`, `generateWelltemp`, `generateFokker`, and `generateSonicweave` (all four imports + instantiate cells).
    - `src/pages/generate.md` METHOD_FAMILIES contains a `"SonicWeave"` family label and the four new option ids (`"rank2"`, `"welltemp"`, `"fokker"`, `"sonicweave"`).
    - `src/pages/generate.md` params-host swap contains `method === "rank2"`, `"welltemp"`, `"fokker"`, `"sonicweave"` branches; the Send-to ternaries reference `rank2ScaleText`/`welltempScaleText`/`fokkerScaleText`/`sonicweaveScaleText`.
    - `welltempScaleText` uses `.cents.toFixed(` (tempered cents-per-line); `fokkerScaleText` uses ratio-per-line (`.toString()`); `rank2ScaleText`/`sonicweaveScaleText` branch on `isTempered()`.
    - `src/styles.css` contains `@import "./components/generate-rank2.css"` and the other three.
    - `npm run build` exits 0 (Framework build succeeds — all imports resolve, optgroups render).
    - `npx tsc --noEmit` exits 0 and `npm run lint` exits 0.
    - No existing method cell (cps/ed/harmonic/ji-set/demo-seed/empty-state) is modified (git diff shows only additive hunks in those regions).
  </acceptance_criteria>
  <done>All four widgets are reachable from the picker, mount into the params host, render their own table+Play, and Send-to serializes each correctly (ratio-per-line vs cents-per-line). Build + types + lint green; existing methods untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Live-verify the four Phase-7 widgets on the Generate page</name>
  <action>HUMAN CHECKPOINT — no code change. After Task 1 ships, the developer manually verifies the four widgets on the live Generate page per the &lt;how-to-verify&gt; steps below, then types "approved" or reports issues.</action>
  <what-built>The four Phase-7 method widgets are registered in the Generate page: rank-2 regular temperament + well-temperament under "Regular / equal temperament", Fokker periodicity block under "Advanced / algorithmic", and free-text SonicWeave under a new "SonicWeave" optgroup. Each mounts its own controls + table + ⏵⏵ Play; Send-to serializes exact-JI scales as ratios and tempered scales as cents-per-line.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the Generate page (http://localhost:3000/pages/generate or the printed URL).
    2. Open the Method picker — confirm FIVE optgroups, with "Rank-2 regular temperament" + "Well-temperament" under Regular, "Fokker periodicity block" under Advanced, and "Free-text SonicWeave" under the new "SonicWeave" group.
    3. Select "Rank-2 regular temperament" → confirm it lands on quarter-comma meantone (7 rows, a "tempered" badge, cents-primary table, no Ratio column). Switch the preset to a pure one (e.g. Pythagorean) → confirm the Ratio column returns and the badge disappears. Click ⏵⏵ Play — confirm it auditions.
    4. Select "Well-temperament" → confirm Vallotti (12 rows, tempered badge). Try another preset (e.g. Werckmeister III) → 12 rows, still tempered. Play it.
    5. Select "Fokker periodicity block" → confirm the classic 12-tone block (12 rows, EXACT ratios, no tempered badge) and a live "→ 12 notes" readout. Toggle to comma mode → confirm the readout still shows 12 and the table is 12 exact ratios. Play it.
    6. Select "Free-text SonicWeave" → confirm the textarea is pre-filled with `cps([1,3,5,7], 2)` and the Hexany previews (6 rows). Type garbage and click Evaluate → confirm the raw error appears in the status region AND the prior preview is preserved. Confirm the docs link is present. Fix it back and re-evaluate.
    7. For one tempered method (e.g. well-temp) click "Send to Dashboard" → confirm the Dashboard loads the scale as cents (tempered). For one exact method (e.g. Fokker) click "Send to Analysis" → confirm it loads as ratios.
    8. Reload the Dashboard with NO scale sent (clear localStorage / fresh tab) → confirm it boots normally (no regression).
  </how-to-verify>
  <resume-signal>Type "approved" if all four widgets render, audition, and Send-to correctly, or describe any issue (wrong default, missing badge, broken readout, error not preserving preview, Send-to wrong serialization).</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` — Framework build green (imports resolve, five optgroups render)
- `npx tsc --noEmit` — types clean
- `npm run lint` — R-01 + ESLint green
- `npx vitest run` — FULL suite green (no regression in any prior phase's tests)
- Human-verify checkpoint approved (live page: all four widgets render/audition/Send-to; empty-store boot unaffected)
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| widget getScale()/isTempered() → Send-to serialization | The page reads each widget's live scale + tempered flag and serializes to store/hash text. |
| Send-to → shared store → Dashboard/Analysis textarea | The serialized scale text crosses into the consumer pages (existing Phase-5 one-way flow). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-15 | Tampering | Send-to serialization (laundering) | mitigate | `isTempered()` gates the cents-per-line vs ratio-per-line branch — tempered scales serialize as cents (never ratios), so no float-derived ratio is laundered as exact JI on the consumer page (SURF-06). |
| T-07-16 | Tampering | Send-to → store → consumer textarea | mitigate | Consumers set `textarea.value` (data, not markup) and re-dispatch `input` — the existing Phase-5 one-way flow; value is never `innerHTML`. |
| T-07-17 | Denial of Service | Send-to 8 KB hash cap | accept | The existing `encodeScaleToHash` RangeError → cap-error copy + hashless navigate (existing Phase-5 handler, unchanged). |
| T-07-18 | Spoofing/Regression | empty-store boot equivalence | mitigate | Registration is strictly additive — no existing method cell or boot path is modified; `npx vitest run` full suite (incl. the SYNC-04 boot-equivalence test) must stay green; human-verify step 8 confirms a no-scale-sent boot. |
</threat_model>

<success_criteria>
- Five optgroups; all four widgets reachable, mounted, auditionable.
- Send-to serializes exact-JI as ratios and tempered as cents-per-line via isTempered().
- Four CSS @imports added; widgets styled.
- Full suite + build + types + lint green; empty-store boot unaffected.
- Human-verify checkpoint approved.
</success_criteria>

<output>
Create `.planning/phases/07-sonicweave-adapter-tempered-lattice-free-text/07-04-SUMMARY.md` when done
</output>
