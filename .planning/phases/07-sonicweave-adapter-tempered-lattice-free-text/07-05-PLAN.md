---
phase: 07-sonicweave-adapter-tempered-lattice-free-text
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/generate-rank2.ts
  - src/components/__tests__/generate-rank2.test.ts
  - src/lib/sonicweave.ts
  - src/lib/__tests__/sonicweave.test.ts
autonomous: true
gap_closure: true
requirements: [GEN-06]
user_setup: []

must_haves:
  truths:
    - "Selecting the Magic preset with POTE/TE/CTE produces a scale built from the preset's documented generator (5/4 major-third chain), not a chain of fifths"
    - "Selecting the Hanson preset with POTE/TE/CTE produces a scale built from the preset's documented generator (6/5 minor-third chain), not a chain of fifths"
    - "The composed SonicWeave source for any POTE/TE/CTE render uses genN/genD as the rank2 generator, so the displayed Generator field and the produced scale agree"
    - "A free-text SonicWeave program containing a negative or zero interval (e.g. -3/2 or 0/1) returns a structured adapter error instead of silently laundering the sign or producing -Infinity cents"
  artifacts:
    - path: "src/components/generate-rank2.ts"
      provides: "composeSource() POTE/TE/CTE branch using the preset generator"
      contains: "rank2(${gen}"
    - path: "src/lib/sonicweave.ts"
      provides: "Sign/zero guard on the R-01 round-trip in scaleFromSonicWeave"
      contains: "f.s"
  key_links:
    - from: "src/components/generate-rank2.ts"
      to: "composeSource POTE/TE/CTE branch"
      via: "gen interpolated from genN/genD"
      pattern: "rank2\\(\\$\\{gen\\}"
    - from: "src/lib/sonicweave.ts"
      to: "structured error on non-positive interval"
      via: "f.s < 0n || f.n === 0n guard before new Interval"
      pattern: "f\\.s\\s*<\\s*0n"
---

<objective>
Close CR-01 (BLOCKER): the rank-2 widget's POTE/TE/CTE source composition hardcodes generator `3/2`, so the Magic (5/4) and Hanson (6/5) presets — and any custom generator — silently produce a tempered chain-of-fifths scale instead of the documented generator chain. The Generator field displays one ratio while the rendered scale is built from another. This restores GEN-06 to fully satisfied.

While in the adapter, also close the adjacent WR-01/WR-02 sign/zero-laundering defect: fraction.js v5 stores sign in `f.s`, so the R-01 round-trip drops the sign of negative rationals (`-3/2` → `3/2`) and accepts the zero interval (`0/1` → `-Infinity` cents). This guard also hardens rank-2 against WR-06 (a custom negative generator currently sign-launders to a positive scale).

Purpose: GEN-06 must produce the scale the user's displayed parameters describe. The displayed-vs-produced contradiction is the failed verification truth.
Output: Corrected `composeSource()` branch + adapter sign/zero guard, each with new tests that fail before the fix and pass after.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-sonicweave-adapter-tempered-lattice-free-text/07-VERIFICATION.md
@.planning/phases/07-sonicweave-adapter-tempered-lattice-free-text/07-REVIEW.md

<interfaces>
<!-- The adapter contract the rank-2 widget consumes. Extracted from src/lib/sonicweave.ts. -->
scaleFromSonicWeave(src: string): SonicWeaveResult
  // SonicWeaveResult = { scale: Scale | null; tempered: boolean; error?: string }
  // Never throws. All failure modes return { scale: null, tempered, error }.
  // R-01 round-trip currently: const f = iv.toFraction(); out.push(new Interval(`${String(f.n)}/${String(f.d)}`))
  // fraction.js v5: f.n and f.d are non-negative BigInts; the sign lives in f.s (a BigInt: -1n | 0n | 1n).

<!-- The rank-2 preset roster. Extracted from src/components/generate-rank2.ts. -->
PRESETS: Record<string, Rank2Preset>
  // magic:  { genN: 5, genD: 4, comma: "3125/3072",  up: 6, down: 3, defaultTuning: "POTE" }
  // hanson: { genN: 6, genD: 5, comma: "15625/15552", up: 6, down: 4, defaultTuning: "POTE" }
  // composeSource() closure-local state exposes genN, genD, up, down, preset, tuning.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix composeSource() POTE/TE/CTE branch to use the preset generator (CR-01)</name>
  <files>src/components/generate-rank2.ts, src/components/__tests__/generate-rank2.test.ts</files>
  <read_first>
    - src/components/generate-rank2.ts (the full file — composeSource() is at lines 288-302; the POTE/TE/CTE branch hardcodes `rank2(3/2, ...)` at line 301; closure-local genN/genD/up/down are declared at lines 133-138)
    - src/components/__tests__/generate-rank2.test.ts (test conventions: happy-dom env, makeStubSynth, dispatch change/input events, query by name attribute; the existing "selecting POTE re-renders tempered" test at lines 80-96 is the closest analog)
    - .planning/phases/07-sonicweave-adapter-tempered-lattice-free-text/07-REVIEW.md (CR-01 section lines 73-110 has the empirically-verified expected cents for Magic with 3/2 vs 5/4)
  </read_first>
  <behavior>
    - Test: selecting the Magic preset (genN=5, genD=4, defaultTuning POTE) composes a source whose generator token is `5/4`, NOT `3/2`. Assert against the composed source OR against an observable consequence of the generator (the rendered scale's cents differ from the 3/2 chain).
    - Test: selecting the Hanson preset (genN=6, genD=5) composes a source whose generator token is `6/5`, NOT `3/2`.
    - Test (regression guard): the existing POTE-with-Pythagorean-default behavior still renders tempered with a badge and no Ratio header (the current passing "selecting POTE re-renders tempered" test must remain green).
  </behavior>
  <action>
    In composeSource() (the POTE/TE/CTE branch, currently the final return at line 301), replace the hardcoded `rank2(3/2, ...)` with the preset's generator. Build a `gen` token exactly as the `pure` branch does: `const gen = `${String(genN)}/${String(genD)}`;` and return `rank2(${gen}, ${up}, ${down})\n${tuning}([${comma}])`. The `comma` and `tuning` parts of the branch are unchanged — only the generator literal changes from `3/2` to the genN/genD-derived token.

    Add tests to generate-rank2.test.ts that select the Magic preset and the Hanson preset (set `select[name="rank2-preset"].value` then dispatch a bubbling `change` event) and assert the produced scale is the preset's generator chain, not a fifth chain. Because composeSource() is closure-private, assert on an OBSERVABLE consequence: the REVIEW.md CR-01 section gives the empirically-verified Magic cents for the 5/4 chain (`59.8 320.2 380.1 439.9 700.3 760.1 819.9 1080.4 1140.2 1200.0`) versus the buggy 3/2 chain (`200.6 299.1 401.2 499.7 601.8 700.3 900.9 999.4 1101.5 1200.0`). Read the rendered cents from the scale table's Cents column (or from getScale() interval cents) and assert the second degree's cents matches the 5/4-chain value family (≈59.8 or ≈320.2 depending on sort), NOT ≈200.6. Pick whichever degree most cleanly distinguishes the two chains and document the chosen assertion in a comment. The test MUST fail against the current hardcoded-3/2 source and pass after the fix.

    Do NOT change the `pure` or `quarter-comma` branches, the up/down clamp logic, or the preset roster.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/generate-rank2.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "rank2(3/2" src/components/generate-rank2.ts` returns NO matches (the hardcoded 3/2 in the POTE/TE/CTE branch is gone).
    - Source assertion: the POTE/TE/CTE branch interpolates a `gen` token derived from genN/genD (grep for `rank2(\${gen}` succeeds, OR the branch interpolates `${String(genN)}/${String(genD)}`).
    - Behavior: the new Magic-preset test asserts the rendered scale matches the 5/4 generator chain (a degree near 59.8/320.2/380.1¢), distinguishing it from the 3/2 chain (200.6/299.1¢), and passes.
    - Behavior: the new Hanson-preset test asserts the 6/5 chain rather than the 3/2 chain, and passes.
    - Regression: all pre-existing tests in generate-rank2.test.ts remain green.
  </acceptance_criteria>
  <done>CR-01 closed: Magic and Hanson POTE/TE/CTE renders use the preset's documented generator; the displayed Generator field and the produced scale agree. Verification truth "POTE/TE/CTE render tempered" flips from FAILED for these presets to satisfied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard the adapter R-01 round-trip against negative and zero intervals (WR-01 / WR-02)</name>
  <files>src/lib/sonicweave.ts, src/lib/__tests__/sonicweave.test.ts</files>
  <read_first>
    - src/lib/sonicweave.ts (the full file — the R-01 round-trip is at lines 117-129; `const f = iv.toFraction(); out.push(new Interval(`${String(f.n)}/${String(f.d)}`))` at lines 121-122 drops the sign in f.s and admits f.n === 0n)
    - src/lib/__tests__/sonicweave.test.ts (test conventions: how it evaluates free-text source strings through scaleFromSonicWeave and asserts on result.scale / result.error / result.tempered; the existing cap and tempered-detection tests are the analogs)
    - .planning/phases/07-sonicweave-adapter-tempered-lattice-free-text/07-REVIEW.md (WR-01 lines 143-161 and WR-02 lines 163-174 give the empirical repro: `-3/2\n2/1` yields f.n=3,d=2 and laundered to positive; `0/1` yields -Infinity cents)
  </read_first>
  <behavior>
    - Test: scaleFromSonicWeave on a source containing a negative rational (e.g. `-3/2\n2/1`) returns `{ scale: null, error: <non-empty> }` instead of a positive-ratio scale.
    - Test: scaleFromSonicWeave on a source containing the zero interval (e.g. `0/1\n2/1`) returns `{ scale: null, error: <non-empty> }` instead of a scale with -Infinity cents.
    - Test (regression): existing positive-rational sources (the classic exact-JI cross-checks) still return a non-null scale with the same intervals and the same `tempered` value.
  </behavior>
  <action>
    In the `isFractional()` branch of scaleFromSonicWeave (lines 118-122), after `const f = iv.toFraction();`, add a guard BEFORE constructing the Interval: if `f.s < 0n || f.n === 0n`, return `{ scale: null, tempered, error: "Scale contains a non-positive interval." }`. fraction.js v5 stores the sign in `f.s` (a BigInt; `-1n` for negatives) and keeps `f.n`/`f.d` non-negative, so this is the correct sign test. Keep the existing `new Interval(`${String(f.n)}/${String(f.d)}`)` push for the passing case unchanged. Do NOT alter the tempered/irrational `else` branch, the cap check, the empty-scale guard, or the D-13 unison-prepend logic.

    Add tests to sonicweave.test.ts that evaluate a negative-rational source and a zero-interval source through scaleFromSonicWeave and assert `result.scale === null` and a non-empty `result.error`. Add (or confirm an existing) regression test that a positive-rational source still returns a non-null scale. Each new test MUST fail against the current sign-dropping implementation and pass after the guard.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/sonicweave.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "f.s" src/lib/sonicweave.ts` matches the new guard, and the guard returns a structured error (the `error:` key is set) before `new Interval(...)` is reached for non-positive intervals.
    - Behavior: scaleFromSonicWeave(`-3/2\n2/1`) returns `result.scale === null` with a non-empty `result.error` (no positive 3/2 laundering).
    - Behavior: scaleFromSonicWeave(`0/1\n2/1`) returns `result.scale === null` with a non-empty `result.error` (no -Infinity cents row).
    - Regression: a positive-rational source (existing exact-JI cross-check) still returns a non-null scale with unchanged intervals; full sonicweave.test.ts suite green.
  </acceptance_criteria>
  <done>WR-01/WR-02 closed: the adapter fails closed on negative and zero intervals instead of silently laundering the sign or emitting -Infinity cents. This also neutralizes WR-06 (a custom negative rank-2 generator now surfaces a real adapter error instead of a positive-ratio scale).</done>
</task>

</tasks>

<verification>
- `npx vitest run` — full suite green (no regression; previously 554/554).
- `grep -n "rank2(3/2" src/components/generate-rank2.ts` — zero matches.
- `grep -n "f.s" src/lib/sonicweave.ts` — the non-positive guard is present.
</verification>

<success_criteria>
- CR-01 closed: Magic/Hanson POTE/TE/CTE renders use the preset generator (genN/genD), verified by a test asserting the 5/4 and 6/5 generator chains rather than the 3/2 chain.
- WR-01/WR-02 closed: the adapter returns a structured error for negative and zero intervals, verified by tests.
- GEN-06 returns to fully satisfied; the verification truth "A pure-ratio rank-2 renders exact JI; POTE/TE/CTE render tempered" can flip to verified for all presets on re-verification.
- No regression: full vitest suite green.
</success_criteria>

<output>
Create `.planning/phases/07-sonicweave-adapter-tempered-lattice-free-text/07-05-SUMMARY.md` when done.
</output>
