---
phase: 07-sonicweave-adapter-tempered-lattice-free-text
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/generate-fokker.ts
  - src/components/__tests__/generate-fokker.test.ts
autonomous: true
gap_closure: true
requirements: [GEN-08]
user_setup: []

must_haves:
  truths:
    - "Adding a basis interval chip regenerates the per-axis Up/Down extent fields, so the new axis has its own editable extent controls (not frozen at 0/0)"
    - "Removing a basis interval chip regenerates the extent fields, so no stale extent input remains pointing at a removed axis"
    - "Editing the Up extent of a newly added basis axis changes the rendered scale cardinality (the axis is functionally usable)"
    - "Attempting to add a 7th basis chip is rejected with a visible status message at the MAX_BASIS (6) cap, not silently truncated"
    - "A comma ratio whose numerator or denominator exceeds 2^53 is preserved exactly (BigInt-validated), not rounded by parseInt"
  artifacts:
    - path: "src/components/generate-fokker.ts"
      provides: "Extent fields re-rendered on every basis-list change; BigInt comma validation; basis-aware chip cap"
      contains: "renderParams"
  key_links:
    - from: "src/components/generate-fokker.ts basis chip setList callback"
      to: "renderParams() / extent-field regeneration"
      via: "extent re-render invoked when the basis list changes"
      pattern: "renderParams\\(\\)|renderExtents\\(\\)"
    - from: "src/components/generate-fokker.ts parseRatio"
      to: "BigInt-validated comma normalization"
      via: "BigInt(...) instead of parseInt(...)"
      pattern: "BigInt\\("
---

<objective>
Close CR-02 (BLOCKER): the Fokker widget's basis-chip Add and Remove handlers call `renderChips()` + `rebuild()` but never `renderParams()`, so the per-axis Up/Down extent fields (created only inside `renderParams()`) are never regenerated when the basis list changes. Adding a basis axis leaves it with no extent controls (frozen at 0/0); removing a chip leaves stale extent fields writing to the wrong axis by index. The basis-chip-add workflow is functionally broken for GEN-08.

While in the same chip-handling code, also close the two adjacent WARNING defects on the exact lines being touched: WR-03 (the basis Add cap uses MAX_COMMAS=8 but the setList slices to MAX_BASIS=6, so the 7th chip silently vanishes) and WR-04 (`parseRatio` uses `parseInt`, rounding comma ratios past 2^53 — the project has no prime-limit ceiling). These are cheap, in-file, and directly adjacent to the blocker.

Purpose: GEN-08 must let users actually edit the extents of basis axes they add, and must not silently corrupt large comma ratios or swallow chips at the cap. The verification truth D-12 "the live readout updates with inputs" is partial precisely because added axes are inoperable.
Output: Extent fields regenerate on every basis-list change; BigInt comma validation; a basis-aware chip cap with a status message — each backed by tests that fail before and pass after.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-sonicweave-adapter-tempered-lattice-free-text/07-VERIFICATION.md
@.planning/phases/07-sonicweave-adapter-tempered-lattice-free-text/07-REVIEW.md

<interfaces>
<!-- The relevant closure-private structure of generate-fokker.ts. -->
makeChipInput(labelText, nameStem, getList, setList, validate, placeholder): HTMLElement
  // Builds the chip list + text input + "Add" button. Add handler currently:
  //   if (next.length >= MAX_COMMAS) return;   // MAX_COMMAS = 8, wrong for basis (MAX_BASIS = 6)
  //   ...setList(next); chipInput.value = ""; renderChips(); rebuild();   // NO renderParams()
  // Remove handler (inside renderChips): setList(next); renderChips(); rebuild();  // NO renderParams()
renderParams(): void
  // In basis mode: builds the basis chip input + one Up/Down makeIntField PER basis axis,
  //   then paramsRegion.replaceChildren(basisChips, ...extentCells).
  // In comma mode: builds the comma chip input only.
  // Currently called ONLY on mode change (line 469) and initial render (line 478).
parseRatio(raw): string | null
  // Currently uses parseInt(m[1], 10) / parseInt(m[2], 10) — loses precision past 2^53.
// Caps: MAX_BASIS = 6, MAX_EXTENT = 24, MAX_COMMAS = 8.
// basis setList: basisGenerators = next.map(...).slice(0, MAX_BASIS); ups/downs realigned to length.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Regenerate extent fields whenever the basis list changes (CR-02)</name>
  <files>src/components/generate-fokker.ts, src/components/__tests__/generate-fokker.test.ts</files>
  <read_first>
    - src/components/generate-fokker.ts (the full file — makeChipInput Add handler at lines 331-341 and Remove handler at lines 318-324 both call renderChips()+rebuild() but never renderParams(); renderParams() builds the extent fields at lines 348-409; the basis setList callback realigns ups/downs at lines 355-360)
    - src/components/__tests__/generate-fokker.test.ts (test conventions: happy-dom, makeStubSynth, query inputs by name attribute, dispatch change/input/click events; the mode-toggle and chip tests are the analogs; extent inputs are named `fokker-up-${i}` / `fokker-down-${i}`)
    - .planning/phases/07-sonicweave-adapter-tempered-lattice-free-text/07-REVIEW.md (CR-02 lines 112-140 — the DOM repro: default = 4 extent inputs for 2 axes; after adding basis 7 the chip appears but extent inputs stay at 4; the recommended fix splits extent rendering so the in-progress chip input value is not dropped)
  </read_first>
  <behavior>
    - Test: in basis mode, the default widget shows extent inputs for 2 axes (4 inputs: up/down for primes 3 and 5). After typing `7` into the basis chip input and clicking Add, the widget shows extent inputs for 3 axes (6 inputs), and the new axis (`fokker-up-2` / `fokker-down-2`) exists.
    - Test: after adding basis `7`, setting `fokker-up-2` to a positive value changes the rendered scale cardinality (the readout "→ N notes" and/or the table row count increases), proving the new axis is editable and contributes.
    - Test: removing a basis chip regenerates the extent fields so the input count matches the remaining axis count (no stale `fokker-up-1`/`fokker-down-1` left over for a removed second axis), and no crash occurs.
  </behavior>
  <action>
    Make the per-axis extent fields regenerate on every basis-list change. The clean structure (per the REVIEW.md CR-02 recommendation) is to have `renderParams()` (or a dedicated extent re-render) run from inside the basis chip's `setList`/Add/Remove path. Two acceptable approaches — pick one:

    (a) Add an optional `onListChanged?: () => void` callback parameter to `makeChipInput`, invoke it at the end of both the Add handler and the Remove handler (after `setList` + `renderChips`, before/around `rebuild`), and pass `renderParams` as that callback only for the basis chip instance (NOT the comma instance, whose params have no extent fields). Calling renderParams() rebuilds the whole basis params region including the chip input, which drops an in-progress chip input value — acceptable here because Add/Remove already clear/consume the input, but confirm the chip input is empty at that point.

    (b) Extract the extent-cell construction from renderParams() into a `renderExtents()` helper that mounts only the extent cells into their own host element, and call `renderExtents()` from the basis setList path. This avoids recreating the chip input. Prefer (b) if it does not balloon the diff; (a) is acceptable if the chip input is already cleared on Add and untouched on Remove.

    Whichever approach: after Add, the new axis MUST have its own `fokker-up-${i}` / `fokker-down-${i}` inputs wired to `ups[i]` / `downs[i]`; after Remove, the extent inputs MUST match the surviving axes with correct indices (no stale field writing to a removed index). Keep the existing ups/downs realignment in the basis setList (`ups = basisGenerators.map((_, i) => ups[i] ?? 0)`), the MAX_EXTENT clamp, and the comma-mode path unchanged.

    Add the three behavior tests above to generate-fokker.test.ts. Count extent inputs via `el.querySelectorAll('input[name^="fokker-up-"], input[name^="fokker-down-"]')` or by the `fokker-up-`/`fokker-down-` name prefix. Each test MUST fail against the current code (extent count stuck at 4 after Add) and pass after the fix.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/generate-fokker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the basis chip Add handler AND Remove handler each trigger an extent re-render — `grep -n "renderParams\|renderExtents" src/components/generate-fokker.ts` shows a call reachable from the basis chip setList/Add/Remove path (not only mode-change + initial render).
    - Behavior: after adding basis `7`, `el.querySelectorAll('input[name^="fokker-up-"]').length === 3` (one per axis), and `input[name="fokker-up-2"]` exists.
    - Behavior: setting `fokker-up-2` to a positive integer after adding basis 7 increases the "→ N notes" readout (and table row count) versus before — the new axis contributes.
    - Behavior: after removing a basis chip, the extent input count equals 2 × remaining axis count, with no input index pointing past the basis length; no exception thrown.
    - Regression: all pre-existing tests in generate-fokker.test.ts remain green.
  </acceptance_criteria>
  <done>CR-02 closed: adding or removing a basis chip regenerates the extent fields; new axes are editable and contribute to the scale; removed axes leave no stale fields. The D-12 readout truth flips from partial to satisfied for the basis-chip-add flow.</done>
</task>

<task type="auto">
  <name>Task 2: Basis-aware chip cap with status message (WR-03) and BigInt comma validation (WR-04)</name>
  <files>src/components/generate-fokker.ts, src/components/__tests__/generate-fokker.test.ts</files>
  <read_first>
    - src/components/generate-fokker.ts (the full file — makeChipInput Add cap at line 335 uses MAX_COMMAS; the basis setList slices to MAX_BASIS at line 357; parseRatio uses parseInt at lines 105-108; MAX_BASIS=6, MAX_COMMAS=8 at lines 76,78; the status region element is created at lines 213-218)
    - src/components/__tests__/generate-fokker.test.ts (test conventions for clicking Add and asserting chip count + status text)
    - .planning/phases/07-sonicweave-adapter-tempered-lattice-free-text/07-REVIEW.md (WR-03 lines 176-194 and WR-04 lines 196-213 give the recommended fixes — pass the cap as a parameter, write a status message at the cap; validate comma ratios with BigInt)
  </read_first>
  <action>
    WR-03 (basis cap): make `makeChipInput` cap-aware so basis chips cap at MAX_BASIS (6) and comma chips at MAX_COMMAS (8), and so hitting the cap writes a visible status message instead of returning silently. Add a `cap: number` parameter to `makeChipInput` (or pass it via the existing signature), replace the hardcoded `if (next.length >= MAX_COMMAS) return;` in the Add handler with `if (next.length >= cap) { status.textContent = `At most ${String(cap)} ${labelText.toLowerCase()}.`; return; }`, and pass `MAX_BASIS` for the basis instance and `MAX_COMMAS` for the comma instance. The basis setList `.slice(0, MAX_BASIS)` may remain as defense-in-depth but must no longer be the thing that silently eats a chip — the cap check now stops the 7th basis chip with a message before it is pushed.

    WR-04 (BigInt comma precision): in `parseRatio`, validate and normalize the captured `n`/`d` digit groups with `BigInt(...)` instead of `parseInt(...)`. After the regex match, compute `const n = BigInt(m[1]); const d = BigInt(m[2]); if (n < 1n || d < 1n) return null; return `${String(n)}/${String(d)}`;`. This preserves arbitrary-magnitude comma ratios exactly (the project has no prime-limit ceiling — CLAUDE.md disqualifies Number-backed ratio paths). Do not change the regex shape `^(\d+)\/(\d+)$` or the trimming.

    Add tests: (1) clicking Add a 7th basis chip leaves the chip count at 6 (MAX_BASIS) AND the status region carries a non-empty "At most 6 …" message; (2) a comma ratio with a >2^53 numerator (e.g. `9007199254740993/1`) round-trips through parseRatio unchanged (assert via the chip label text after adding it in comma mode, or a direct parseRatio call if exported — if parseRatio is not exported, assert through the comma-chip add path that the chip label preserves the exact digits, not a rounded value).
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/generate-fokker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "BigInt(" src/components/generate-fokker.ts` shows parseRatio normalizing with BigInt (no `parseInt` remaining inside parseRatio).
    - Source assertion: the makeChipInput Add handler caps at a passed `cap` parameter and sets a status message at the cap (grep for `status.textContent` inside the Add cap branch).
    - Behavior: adding a 7th basis chip keeps the basis chip count at 6 and surfaces a non-empty status message mentioning the limit; the input is not silently swallowed.
    - Behavior: a comma chip with a numerator above 2^53 preserves its exact digit string (no rounding), verified through the comma-chip label or a parseRatio assertion.
    - Regression: all pre-existing tests in generate-fokker.test.ts remain green; comma-mode default (81/80 + 128/125 → 12) still renders.
  </acceptance_criteria>
  <done>WR-03/WR-04 closed: the basis chip cap is correct (6) with user feedback at the limit, and comma ratios are BigInt-exact past 2^53.</done>
</task>

</tasks>

<verification>
- `npx vitest run` — full suite green (no regression).
- `grep -n "renderParams\|renderExtents" src/components/generate-fokker.ts` — extent re-render reachable from the basis chip add/remove path.
- `grep -n "BigInt(" src/components/generate-fokker.ts` — parseRatio uses BigInt.
</verification>

<success_criteria>
- CR-02 closed: basis-chip add/remove regenerates extent fields; new axes are editable and contribute; removed axes leave no stale fields — all verified by tests.
- WR-03/WR-04 closed: basis cap is 6 with a status message; comma ratios are BigInt-exact — verified by tests.
- GEN-08 returns to fully satisfied; the verification truth D-12 "live readout updates with inputs (extents product in basis mode)" can flip to verified on re-verification.
- No regression: full vitest suite green.
</success_criteria>

<output>
Create `.planning/phases/07-sonicweave-adapter-tempered-lattice-free-text/07-06-SUMMARY.md` when done.
</output>
