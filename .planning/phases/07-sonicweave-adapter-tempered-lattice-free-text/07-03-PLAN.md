---
phase: 07-sonicweave-adapter-tempered-lattice-free-text
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/generate-fokker.ts
  - src/components/generate-fokker.css
  - src/components/generate-sonicweave.ts
  - src/components/generate-sonicweave.css
  - src/components/__tests__/generate-fokker.test.ts
  - src/components/__tests__/generate-sonicweave.test.ts
autonomous: true
requirements: [GEN-08, GEN-09]

must_haves:
  truths:
    - "Fokker widget lands on the classic 5-limit 12-tone block (basis mode) rendering 12 exact-rational notes (Ratio column present, no tempered badge)"
    - "Fokker has a basis↔comma mode toggle (basis default); the live '→ N notes' readout updates with inputs (extents product in basis mode, |det| via fokkerCardinality in comma mode)"
    - "The classic 81/80 + 128/125 comma block renders exactly 12 exact-rational notes"
    - "Free-text widget lands on an evaluated cps([1,3,5,7],2) preview (Hexany) on first render"
    - "Free-text evaluates on button click only (never per keystroke); malformed input shows the raw compiler error in a status region and preserves the prior preview"
    - "Free-text exposes a docs link to the SonicWeave documentation (rel=noopener noreferrer)"
  artifacts:
    - path: "src/components/generate-fokker.ts"
      provides: "GEN-08 Fokker widget: basis-chip + comma-chip modes, live |det|/extents readout"
      exports: ["generateFokker", "GenerateFokkerElement"]
      min_lines: 130
    - path: "src/components/generate-sonicweave.ts"
      provides: "GEN-09 free-text widget: textarea + Evaluate button + docs link + status region"
      exports: ["generateSonicweave", "GenerateSonicweaveElement"]
      min_lines: 90
    - path: "src/components/generate-fokker.css"
      provides: "Fokker widget styles (var(--theme-*) tokens only)"
    - path: "src/components/generate-sonicweave.css"
      provides: "free-text widget styles incl. textarea rule (var(--theme-*) tokens only)"
  key_links:
    - from: "src/components/generate-fokker.ts"
      to: "src/lib/sonicweave.ts scaleFromSonicWeave"
      via: "compose parallelotope(...) source then call the adapter"
      pattern: "scaleFromSonicWeave"
    - from: "src/components/generate-fokker.ts"
      to: "src/lib/fokker.ts fokkerCardinality"
      via: "comma-mode live '→ N notes' |det| readout"
      pattern: "fokkerCardinality"
    - from: "src/components/generate-sonicweave.ts"
      to: "src/lib/sonicweave.ts scaleFromSonicWeave"
      via: "evaluate-on-click of the verbatim textarea program"
      pattern: "scaleFromSonicWeave"
---

<objective>
Build the two remaining Phase-7 widgets over the Plan-01 adapter: `generate-fokker.ts` (GEN-08) and `generate-sonicweave.ts` (GEN-09). Fokker is exact-rational (NOT tempered) with a basis↔comma mode toggle, a live "→ N notes" readout (extents product in basis mode, `fokkerCardinality` |det| in comma mode), landing on the classic 5-limit 12-tone block (D-11). Free-text is the escape hatch: a textarea + Evaluate button (click only, never per keystroke), raw compiler errors verbatim in a status region with the prior preview preserved, a docs link, landing on an evaluated `cps([1,3,5,7],2)` (D-13).

Purpose: GEN-08 delivers parked TEMP-08 (periodicity blocks) with the pedagogical comma→cardinality readout as a deliberate surface (D-12); GEN-09 delivers parked TEMP-07 (full DSL access). File-disjoint from Plan 02; page registration is Plan 04.
Output: two components + two CSS files + two component tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-sonicweave-adapter-tempered-lattice-free-text/07-RESEARCH.md
@.planning/phases/07-sonicweave-adapter-tempered-lattice-free-text/07-PATTERNS.md

<interfaces>
<!-- Contracts from Plan 01 + the Phase-6/further-reading analogs. Use directly. -->

From src/lib/sonicweave.ts (Plan 01 — the adapter both widgets call):
```typescript
export interface SonicWeaveResult { scale: Scale | null; tempered: boolean; error?: string }
export function scaleFromSonicWeave(src: string): SonicWeaveResult;
```

From src/lib/fokker.ts (Plan 01 — the comma-mode cardinality helper):
```typescript
export function fokkerCardinality(commaStrings: string[]): number; // |det| via integerDet; throws RangeError on non-square / over-cap
```

From src/components/scale-table.ts:
```typescript
export function scaleTable(scale: Scale, baseHz: number, opts?: ScaleTableOpts): HTMLElement;
// Fokker is EXACT JI → call WITHOUT opts.tempered (the 4-column Ratio path, generate-cps.ts:237 idiom)
```

From src/components/further-reading.ts (the external-docs-link idiom):
```typescript
export function furtherReading(items: FurtherReadingItem[]): HTMLElement;
// link.rel = "noopener noreferrer" for external URLs (further-reading.ts:39-41)
```

Element-interface contracts for Send-to:
```typescript
export interface GenerateFokkerElement extends HTMLElement {
  getScale(): Scale | null;
  isTempered(): boolean;   // ALWAYS false (Fokker blocks are exact rational)
}
export interface GenerateSonicweaveElement extends HTMLElement {
  getScale(): Scale | null;
  isTempered(): boolean;   // CONDITIONAL: from result.tempered (cps default = false; a tempered program = true)
}
```

Verified SonicWeave source strings (RESEARCH Pattern 4 + D-13, runtime-checked this session):
```text
basis mode (D-11 default): parallelotope([3, 5], [3, 1], [0, 1])  → 12 exact-rational notes
basis 16-note example:     parallelotope([3, 5], [2, 2], [1, 1])  → 16 exact-rational notes
free-text default (D-13):  cps([1,3,5,7], 2)                       → Hexany (6 notes, exact)
comma-mode cardinality:    fokkerCardinality(["81/80","128/125"]) === 12
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: generate-fokker.ts — GEN-08 Fokker block widget (basis + comma modes, live readout)</name>
  <files>src/components/generate-fokker.ts, src/components/generate-fokker.css, src/components/__tests__/generate-fokker.test.ts</files>
  <read_first>
    - src/components/generate-fokker.ts (confirm absent before Write)
    - src/components/generate-ed.ts (mode/sub-method swap: subSelect + renderParams + paramsRegion.replaceChildren lines 113-139 & 286-317 & 353-357; makeIntField lines 165-194; per-sub-method state slot lines 95 idiom)
    - src/components/generate-cps.ts (chip input: chipForm + renderChips + Add button lines 145-175 & 199-231 & 251-262; parsePositiveInt lines 65-74; the exact-JI scaleTable path line 237)
    - src/lib/fokker.ts (Plan 01 — fokkerCardinality for the comma-mode readout)
    - src/lib/sonicweave.ts (Plan 01 — the adapter)
    - src/components/__tests__/mos-builder.test.ts (test header + event-dispatch + status-region patterns)
    - src/components/__tests__/generate-cps.test.ts (chip-input event/assertion patterns)
    - src/components/generate-cps.css (style analog — rename BEM root to generate-fokker)
    - 07-PATTERNS.md "generate-fokker.ts" section (lines 165-180) + 07-RESEARCH.md Pattern 4 + Pitfall 6 + Open Question 2 (A3 — comma-mode enumeration choice)
  </read_first>
  <behavior>
    - Factory smoke: generateFokker(makeStubSynth()) returns an HTMLElement with class "generate-fokker".
    - Default landing (D-11): classic 5-limit 12-tone block, basis mode — parallelotope([3,5],[3,1],[0,1]) → 12 rows in an EXACT-JI table (the "Ratio" header IS present; NO "tempered" badge). isTempered() === false.
    - Mode toggle (D-09/D-10): a basis↔comma select; basis is default. Switching to comma mode swaps the params region to comma-ratio chips (default ["81/80","128/125"]).
    - Live "→ N notes" readout (D-12): a status-style element updates on every input change — basis mode shows the extents product; comma mode shows fokkerCardinality(commaStrings) (=== 12 for the default). The readout updates BEFORE the preview (a dedicated readout element, createElement + textContent).
    - Comma mode (A3): the 81/80 + 128/125 block renders exactly 12 exact-rational notes (assert length 12, all rows have a Ratio cell — exact JI).
    - Non-square comma set (e.g. a single comma) → the readout shows a clear "needs a square comma set" message (or "→ — notes") and does NOT crash; the prior preview is preserved (catch the fokkerCardinality RangeError).
    - Play button works (.play-btn--scale).
  </behavior>
  <action>
    Build generateFokker as a Pattern-2 factory copying the generate-ed.ts mode-swap shell. This widget is EXACT rational → call `scaleTable(scale, baseHz, { precision })` WITHOUT the tempered flag (generate-cps.ts:237 path); expose `isTempered() => false`. Closure-local state: mode ("basis" default), per-mode params preserved across the toggle (the generate-ed.ts:95 per-slot precedent): basis state = `{ basis:[3,5], ups:[3,1], downs:[0,1] }`; comma state = `{ commas:["81/80","128/125"] }`.

    Basis mode: a basis-interval chip input (generate-cps.ts chip idiom — chips are integer/ratio basis generators) + per-axis up/down integer fields (makeIntField). Comma mode: a comma-ratio chip input (D-19, chip idiom; each chip is an `n/d` string validated before insertion). Mode toggle preserves each mode's state.

    Live readout (D-12): a dedicated `createElement("span")` updated inside rebuild() BEFORE the preview — basis mode multiplies the extents (product of per-axis (up+down+1) or the appropriate count; match the verified parallelotope cardinality, e.g. [3,1]+[0,1] over a 2-basis → 12); comma mode calls `fokkerCardinality(commaStrings)` wrapped in try/catch (non-square → RangeError → readout shows a clear message, no crash). Text via textContent only.

    Source composition: basis mode → `parallelotope([${basis.join(", ")}], [${ups.join(", ")}], [${downs.join(", ")}])` (RESEARCH Pattern 4, verified [3,5],[3,1],[0,1] → 12). Comma mode (A3 — pick ONE enumeration path and verify the 81/80+128/125 block renders 12 exact notes): EITHER convert the comma kernel to a basis via xen-dev-utils `kernel`/`hnf` then `parallelotope`, OR derive the equivalent basis+extents and reuse the basis enumerator. Prefer the path that keeps comma mode's primary job the |det| readout + a comma→basis bridge (RESEARCH OQ-2 recommendation (a)). Call `scaleFromSonicWeave(src)`; render the exact-JI table + play; on error → status message + preserve prior render.

    Defense-in-depth (D-18): cap basis size, extents, and comma count BEFORE composing/enumerating (mirror cps.ts caps); fokkerCardinality already caps cardinality. Expose getScale() + isTempered() => false.

    CSS: copy generate-cps.css, rename BEM root to `generate-fokker`, var(--theme-*) tokens only; style the readout element.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/generate-fokker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/generate-fokker.ts` contains `export function generateFokker(`, `scaleFromSonicWeave`, and `fokkerCardinality`.
    - `generate-fokker.ts` calls `scaleTable(` WITHOUT a `tempered: true` argument and exposes `isTempered()` returning `false`.
    - Behavior (test): default render shows 12 rows WITH a "Ratio" header and NO `.scale-table__badge`.
    - Behavior (test): the live "→ N notes" readout text contains "12" for the default block; switching to comma mode with ["81/80","128/125"] also shows 12 and renders 12 exact-rational rows.
    - Behavior (test): a non-square comma set leaves the widget non-crashing (status/readout message present, prior `tbody tr` rows survive).
    - `src/components/generate-fokker.css` uses only `var(--` tokens.
    - `npx vitest run src/components/__tests__/generate-fokker.test.ts` exits 0.
  </acceptance_criteria>
  <done>Fokker widget renders the classic 12-tone block by default (exact rational), toggles basis↔comma with a live |det|/extents readout, renders the comma block as 12 exact notes, and never crashes on a non-square comma set. Test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: generate-sonicweave.ts — GEN-09 free-text escape hatch (evaluate-on-click, raw errors, docs link)</name>
  <files>src/components/generate-sonicweave.ts, src/components/generate-sonicweave.css, src/components/__tests__/generate-sonicweave.test.ts</files>
  <read_first>
    - src/components/generate-sonicweave.ts (confirm absent before Write)
    - src/components/generate-cps.ts (status + preserve-prior-render rebuild try/catch idiom lines 233-248)
    - src/components/further-reading.ts (the external docs link: furtherReading + rel="noopener noreferrer" lines 25-61)
    - src/components/generate-ed.ts (factory shell + getScale/isTempered exposure lines 359-362)
    - src/lib/sonicweave.ts (Plan 01 — the adapter; the 8 KB cap is enforced INSIDE the adapter via MAX_SCALE_TEXT_BYTES, so the widget surfaces result.error)
    - src/components/scale-table.ts (conditional tempered flag — pass { tempered: result.tempered })
    - src/components/__tests__/mos-builder.test.ts + generate-cps.test.ts (test header + event dispatch + status assertions)
    - src/components/generate-cps.css (style analog; add a textarea rule mirroring the input rule)
    - 07-PATTERNS.md "generate-sonicweave.ts" section (lines 183-201) + 07-RESEARCH.md GEN-09 + Pitfall 5 + D-13/D-14/D-15/D-16/D-20
  </read_first>
  <behavior>
    - Factory smoke: generateSonicweave(makeStubSynth()) returns an HTMLElement with class "generate-sonicweave".
    - Default landing (D-13): the textarea is pre-filled with `cps([1,3,5,7], 2)` AND one rebuild runs on construction so the Hexany (6 rows, exact JI) previews on first paint. isTempered() === false initially.
    - Evaluate-on-click ONLY (D-16): editing the textarea does NOT re-evaluate; clicking the "Evaluate" button re-evaluates. (Test: dispatch an `input` on the textarea with new text → preview unchanged; click Evaluate → preview updates.)
    - Malformed program (D-15): clicking Evaluate with invalid SonicWeave writes the raw adapter error message verbatim to a `[role="status"]` region via textContent AND preserves the prior `tbody tr` rows (does not clear the table).
    - A multi-line program (D-16) evaluates (e.g. a pitch list across lines) — accepts full programs, not just one expression.
    - A docs link (D-14/D-20) to the SonicWeave documentation is present as an `<a>` with rel="noopener noreferrer".
    - Tempered program → isTempered() reflects result.tempered (e.g. an EDO step program shows the tempered badge).
    - Play button works (.play-btn--scale).
  </behavior>
  <action>
    Build generateSonicweave as a Pattern-2 factory. A `<textarea>` (name attribute for happy-dom; optionally set maxLength as a first clamp; the 8 KB byte cap is enforced in the adapter) + an "Evaluate" `<button type="button">`. Wire `rebuild()` to the button click ONLY — do NOT add an input listener that re-evaluates (D-16, Anti-Pattern: never per keystroke).

    rebuild(): read `textarea.value`, call `scaleFromSonicWeave(value)`. On `result.error` (parse/eval error, empty scale, or over-cap) write `result.error` verbatim to the status region via textContent (D-15, raw compiler error, XSS-safe) and PRESERVE the prior table/play hosts (generate-cps.ts:233-248 idiom — do NOT clear them). On success render `scaleTable(scale, baseHz, { tempered: result.tempered })` + `playScale(...)`, set closure currentScale + the closure tempered boolean.

    Docs link (D-14/D-20): use the furtherReading helper (or its `<a rel="noopener noreferrer">` idiom) pointing at the SonicWeave documentation — pick the most stable URL at implementation time (RESEARCH Sources secondary: the dsl.md / advanced-dsl.md pair, e.g. https://github.com/xenharmonic-devs/sonic-weave/blob/main/documentation/dsl.md or the hosted API site). Place it near the textarea.

    Default landing (D-13): set `textarea.value = "cps([1,3,5,7], 2)"` and call rebuild() once at the end of construction so the Hexany previews on first paint. Expose `root.getScale = () => currentScale` and `root.isTempered = () => temperedFlag` (conditional, from result.tempered).

    CSS: copy generate-cps.css, rename BEM root to `generate-sonicweave`, var(--theme-*) tokens only; ADD a `textarea` rule (width:100%, monospace font, mirror the input[type=number] rule).
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/generate-sonicweave.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/generate-sonicweave.ts` contains `export function generateSonicweave(`, `scaleFromSonicWeave`, and a `<textarea>` (createElement("textarea")).
    - The Evaluate behavior is click-driven: the component adds a click listener on the Evaluate button calling rebuild, and does NOT call rebuild from a textarea `input` listener (grep: no `addEventListener("input"` on the textarea that triggers rebuild).
    - Behavior (test): default render shows 6 Hexany rows (exact JI); dispatching `input` with new text leaves the preview unchanged; clicking Evaluate updates it.
    - Behavior (test): a malformed program click leaves `[role="status"]` non-empty (raw error) AND the prior `tbody tr` rows present.
    - A docs `<a>` with `rel="noopener noreferrer"` is present.
    - `src/components/generate-sonicweave.css` contains a `textarea` rule and uses only `var(--` tokens.
    - `npx vitest run src/components/__tests__/generate-sonicweave.test.ts` exits 0.
  </acceptance_criteria>
  <done>Free-text widget previews the Hexany by default, evaluates only on button click, surfaces raw compiler errors while preserving the prior preview, and links to the SonicWeave docs. Test green.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/components/__tests__/generate-fokker.test.ts src/components/__tests__/generate-sonicweave.test.ts` — all green
- `npx tsc --noEmit` — types clean
- `npm run lint` — R-01 + ESLint green
- Note: CSS @import wiring + page registration are Plan 04 (this plan does not touch generate.md or styles.css to stay file-disjoint from Plan 02).
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| free-text textarea → adapter | THE new untrusted input vector (GEN-09): arbitrary user-typed SonicWeave evaluated by `evaluateSource`. |
| Fokker form inputs → adapter | Typed basis/extents/commas become a `parallelotope(...)` string. |
| adapter result → status/table render | Error text + ratios/cents rendered to the DOM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Denial of Service | free-text input size + cadence | mitigate | 8 KB cap enforced in the adapter (Plan 01); evaluate-on-click only (never per keystroke, D-16); SonicWeave is a scale DSL with no I/O sinks. |
| T-07-11 | Tampering / Info disclosure | raw compiler error rendering | mitigate | Error message written via `textContent` ONLY into `role="status" aria-live="polite"`; never `innerHTML` (D-15 raw-error-verbatim is text-safe). |
| T-07-12 | Tampering / Info disclosure | scale-table cell rendering | mitigate | All ratio/cents cells via `createElement` + `textContent` (scale-table.ts discipline); tempered programs render cents-primary + badge. |
| T-07-13 | Denial of Service | Fokker extents / comma count | mitigate | Defense-in-depth caps on basis size / extents / comma count validated BEFORE enumeration; `fokkerCardinality` caps cardinality + rejects non-square (RangeError caught → readout message). |
| T-07-14 | Tampering | external docs link | mitigate | Docs `<a>` uses `rel="noopener noreferrer"` (further-reading.ts idiom) — no `window.opener` leakage. |
</threat_model>

<success_criteria>
- generate-fokker (GEN-08): classic 12-tone block default, basis↔comma toggle, live |det|/extents readout (=12), comma block renders 12 exact notes, non-square safe. Test green.
- generate-sonicweave (GEN-09): Hexany default, evaluate-on-click, raw errors + preserved preview, docs link. Test green.
- `tsc --noEmit` + `npm run lint` green.
</success_criteria>

<output>
Create `.planning/phases/07-sonicweave-adapter-tempered-lattice-free-text/07-03-SUMMARY.md` when done
</output>
