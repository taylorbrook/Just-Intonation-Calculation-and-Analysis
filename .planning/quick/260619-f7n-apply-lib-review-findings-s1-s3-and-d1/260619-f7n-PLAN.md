---
quick_id: 260619-f7n
title: Apply src/lib review findings S1–S3 + D1
status: in-progress
date: 2026-06-19
source_review: .planning/quick/260618-wgg-.../260618-wgg-REVIEW.md
baseline_tests: 753/753 green
---

# Quick Task 260619-f7n — Apply review findings S1–S3 + D1

Behavior-preserving refactor of the `src/lib` math kernel. The existing
`src/lib/__tests__` suite (753 tests) is the regression gate; it must stay green.
One atomic commit per finding, `npm test` after each.

## Findings to apply

### S3 — shared immutable `UNISON` / `OCTAVE` singletons
Export `UNISON`/`OCTAVE` from `interval.ts`; replace local re-declarations in
`mos.ts`, `generators.ts`, `harmonic.ts`, `constant-structure.ts`, `sonicweave.ts`
and the per-call inline allocations in `interval.octaveReduce` + `cps` default param.
Safe because `Interval` is immutable. *(applied first — foundation)*

### S1 — unify dedupe→sort→append-period
Add `Interval.key` (canonical `n/d` string). Add exported
`finalizeScale(intervals, period, { sort })` in `scale.ts`: drop entries equal to
`period`, dedupe by `key`, optional sort by cents, append exactly one `period`.
Route `Scale.reduce`, `jiSubsetOfEdo`, `cps`, `harmonic.foldToOctave`,
`generators.foldExactSet`, `edo.bestJiInEdo`, `mos.buildMos` through it
(`{ sort:false }` for the two EDO-step builders to preserve insertion order).
Modernize `Scale.dedupe`'s O(n²) `.some(equals)` to a Set<key> pass.

### S2 — move `utf8ByteLength` + `normalizeLines` to `text.ts`
Verbatim duplicates in `scala.ts`/`kbm.ts` (and an inlined `normalizeLines` in
`scala-archive.ts`). Lift both into `text.ts` with a shared `MAX_FILE_INPUT_BYTES`.

### D1 — delete dead re-exports
Remove `monzoAdd` / `monzoSub` / `monzoScale` / `wilsonHeight` from `monzo.ts`
(0 repo-wide usages) and update the `INVENTORY.md` rows.

## Verify
- `npm test` green after every commit (target: 753/753, possibly +N from new helper tests if added — none planned).
- Final `npm run ci` (tsc + vitest + eslint + prettier + build) clean.
