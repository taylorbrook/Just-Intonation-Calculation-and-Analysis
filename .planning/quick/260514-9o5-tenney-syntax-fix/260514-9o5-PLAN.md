---
type: plan
quick_id: 260514-9o5
task: Fix SyntaxError on tenney-height.md — "Assignment to external variable 'tenneyWeighted'"
date: 2026-05-14
mode: quick
files_modified:
  - src/pages/tenney-height.md
autonomous: true
---

<objective>
Suppress the runtime error on `/pages/tenney-height` caused by a documentation
code block that Observable Framework was executing as a reactive cell.
</objective>

<diagnosis>
At line 273, the "Tenney-weighted error in the EDO ranker" section uses a
` ```js ` fenced block to *show* the two-line excerpt from `src/lib/edo.ts`:

```
const weight = Math.max(1, tenneyHeight(iv.monzo));
tenneyWeighted += absCentsError / weight;
```

Framework executes any `js`/`ts` fenced block as a reactive cell. The block is
not a self-contained cell — `weight`, `tenneyHeight`, `iv`, `absCentsError`,
and `tenneyWeighted` are all undefined in page scope — and the `+=` against
an undeclared identifier triggers Framework's runtime guard:

> SyntaxError: Assignment to external variable 'tenneyWeighted' (2:0)

Sibling page `src/pages/odd-limits.md` (lines 321, 329) demonstrates the
project's convention for display-only snippets: ` ```ts run=false `.
</diagnosis>

<tasks>
## Task 1: Tag the documentation block as non-executing

Change the fence on `src/pages/tenney-height.md:273` from ` ```js ` to
` ```ts run=false ` so Framework renders the snippet as syntax-highlighted
source instead of evaluating it. The closing fence and block contents are
unchanged.

The other `js` block on the page (line 327, `furtherReading([...])`) is a
real, self-contained reactive cell — leave it as-is.
</tasks>
</content>
