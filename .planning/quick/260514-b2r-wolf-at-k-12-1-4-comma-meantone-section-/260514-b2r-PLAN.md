---
phase: quick-260514-b2r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/spiral-of-fifths.ts
  - src/components/spiral-of-fifths.css
  - src/components/__tests__/spiral-of-fifths.test.ts
autonomous: true
requirements:
  - QT-260514-b2r-01  # tempered-branch heard-interval label
tags:
  - viz
  - spiral-of-fifths
  - meantone

discretion_resolved:
  question: "Reuse `.spiral-of-fifths__ratio` or add `.spiral-of-fifths__heard`?"
  decision: "Add new `.spiral-of-fifths__heard` class with identical typographic rules to `.spiral-of-fifths__ratio`."
  rationale: |
    Existing `.spiral-of-fifths__ratio` rules are purely typographic
    (font-family: var(--monospace); font-size: 12px; fill: var(--theme-foreground); pointer-events: none)
    and would render a cents string fine. But the CONTEXT default favors semantic
    separation (ratio ≠ cents), and the file already distinguishes `__ratio` (12px)
    from `__cents` (10px) by class. Adding `__heard` keeps the cascade clean and
    lets the two label sets diverge later (e.g. tighter glyph spacing for the
    decimal+`¢` shape) without touching the ratio rule.

must_haves:
  truths:
    - "Tempered-branch nodes render a heard-interval label above the dot at y=−10."
    - "Heard-interval label content is the octave-reduced cumulative cents from 1/1, formatted as `X.X¢` (1 decimal, U+00A2 cent glyph, unsigned)."
    - "Pure branch is unchanged: still emits the ratio label at y=−10 and never the heard-interval label."
    - "Existing signed cents-from-12-TET label at y=14 is unchanged in both branches."
    - "Tempered-branch click/audition behavior is unchanged."
  artifacts:
    - path: "src/components/spiral-of-fifths.ts"
      provides: "Tempered-branch heard-interval `<text class='spiral-of-fifths__heard'>` rendering"
      contains: "spiral-of-fifths__heard"
    - path: "src/components/spiral-of-fifths.css"
      provides: ".spiral-of-fifths__heard typographic rule"
      contains: "spiral-of-fifths__heard"
    - path: "src/components/__tests__/spiral-of-fifths.test.ts"
      provides: "Tempered-branch heard-label coverage + pure-branch absence assertion"
      contains: "spiral-of-fifths__heard"
  key_links:
    - from: "src/components/spiral-of-fifths.ts (DOM factory)"
      to: "y=-10 text slot"
      via: "ratio (pure) XOR heard-interval (tempered) — never both"
      pattern: "spiral-of-fifths__(ratio|heard)"
    - from: "src/components/spiral-of-fifths.css"
      to: ".spiral-of-fifths__heard"
      via: "typographic rules identical to .spiral-of-fifths__ratio"
      pattern: "spiral-of-fifths__heard"
---

<objective>
Add a per-node "heard interval" label to the tempered branch of the
spiral-of-fifths SVG component, placed at the y=−10 slot currently used
by the pure-branch ratio label. Label content is the octave-reduced
cumulative cents from 1/1, formatted to 1 decimal place with the `¢`
glyph (e.g. `0.0¢`, `696.6¢`, `193.2¢`).

Purpose: every tempered-branch caller — currently `src/pages/meantone.md`
"Wolf at k=12 (1/4-comma meantone)" section — gains a visible readout of
the audible pitch each node sounds, matching the existing audition
reduction `((cumulativeCents % 1200) + 1200) % 1200`. Without it the
node carries only the signed cents-from-12-TET label, leaving the actual
heard pitch unlabeled.

Output:
- `src/components/spiral-of-fifths.ts` — render the new tempered-branch
  `<text class="spiral-of-fifths__heard">` at y=−10 (mirrors the pure-branch
  ratio render at lines 224–232; strict-TS discipline preserved).
- `src/components/spiral-of-fifths.css` — new `.spiral-of-fifths__heard`
  rule with typographic properties identical to `.spiral-of-fifths__ratio`.
- `src/components/__tests__/spiral-of-fifths.test.ts` — extended with
  tempered-branch heard-label coverage and a pure-branch absence guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.planning/quick/260514-b2r-wolf-at-k-12-1-4-comma-meantone-section-/260514-b2r-CONTEXT.md
@src/components/spiral-of-fifths.ts
@src/components/spiral-of-fifths.css
@src/components/__tests__/spiral-of-fifths.test.ts

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

Existing pure-branch ratio render (src/components/spiral-of-fifths.ts lines 223–232) — TEMPLATE for the new tempered-branch render:

```
if (s.ratio) {
  const ratioText = document.createElementNS(SVG_NS, "text");
  ratioText.setAttribute("class", "spiral-of-fifths__ratio");
  ratioText.setAttribute("x", "0");
  ratioText.setAttribute("y", "-10");
  ratioText.setAttribute("text-anchor", "middle");
  ratioText.textContent = s.ratio.fraction.toFraction();
  g.appendChild(ratioText);
}
```

The new tempered render lives in the `else` branch of this same `if (s.ratio)` test (tempered branch sets `s.ratio = null` per spiralGeometry lines 122–130). The same `s.cumulativeCents` already on `SpiralStep` carries everything needed — re-do the octave reduction here (do NOT add it to SpiralStep; the audition reduction at the call-site stays the contract).

Octave-reduction formula (must match call-site at meantone.md:305 and the spiralGeometry tempered branch at line 117):
  `((cumulativeCents % 1200) + 1200) % 1200`

Existing `.spiral-of-fifths__ratio` rule (src/components/spiral-of-fifths.css lines 28–33):

```
svg.viz.spiral-of-fifths .spiral-of-fifths__ratio {
  font-family: var(--monospace);
  font-size: 12px;
  fill: var(--theme-foreground);
  pointer-events: none;
}
```

The new `.spiral-of-fifths__heard` rule duplicates these four declarations verbatim (per discretion_resolved above — semantic separation, identical visual style cascade).

Strict-TS conventions in this file (mandatory):
- `String(...)` coercion on every numeric `setAttribute` call.
- `textContent` only — never `innerHTML` (T-3-18 mitigation).
- `if (!s) continue` guard on the `steps[i]` access already present at line 206 — no changes needed there.
- U+002B `+` / U+2212 `−` minus sign on the signed cents label — but the new heard label is UNSIGNED (`[0, 1200)`), so no sign prefix.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tempered-branch heard-interval render + CSS + tests</name>
  <files>src/components/spiral-of-fifths.ts, src/components/spiral-of-fifths.css, src/components/__tests__/spiral-of-fifths.test.ts</files>
  <behavior>
    Tests to add to `src/components/__tests__/spiral-of-fifths.test.ts` (inside the existing `describe("spiralOfFifths factory (DOM smoke)", …)` block, after the existing tempered-branch ratio-suppression test at lines 100–104):

    - **tempered branch (1/4-comma meantone ≈ 696.578¢) emits a heard label at k=0, k=1, k=2**:
      Build the widget with `temperedFifthCents: 696.578`. Select `.spiral-of-fifths__heard` text nodes in document order. Assert the first three `textContent` values equal `"0.0¢"`, `"696.6¢"`, `"193.2¢"` respectively.
    - **tempered branch emits exactly n+1 heard labels for n=12**: count of `.spiral-of-fifths__heard` text nodes is 13.
    - **tempered branch heard label sits at y=−10**: read `getAttribute("y")` on the first `.spiral-of-fifths__heard` element, assert `"-10"`.
    - **pure branch emits ZERO heard labels at k=1 (and still emits the ratio label `3/2`)**: build the widget with no `temperedFifthCents` opt, assert `.spiral-of-fifths__heard` count is 0 and `.spiral-of-fifths__ratio` count is 13 (n+1 for n=12 — branch-separation guard, mirrors the existing tempered ratio-suppression test).
    - **tempered branch suppresses ratio labels but keeps cents labels** (extend the existing test at lines 100–104 OR add adjacent): also assert `.spiral-of-fifths__heard` count is 13 — the tempered branch's y=−10 slot is filled by heard, not ratio.

    All assertions follow existing test style: `expect(el.querySelectorAll(".spiral-of-fifths__heard").length).toBe(13)` etc. `textContent` on text nodes returns the exact string; do NOT trim.

    Edge case: with `temperedFifthCents = 696.578`, the reduced cents at k=2 is `(2 × 696.578) % 1200 = 1393.156 % 1200 = 193.156`, which `toFixed(1)` rounds to `"193.2"`. Floating-point drift in the modulo is well-bounded at single-digit cents; the `toFixed(1)` is the contract.

    Edge case for the 12-TET case (`temperedFifthCents: 700`) used by the existing tests at lines 100–104 and 142–155: at k=12 the reduced cents is `8400 % 1200 = 0`, so `toFixed(1) = "0.0"`. Compatible — no change needed to those tests other than the optional heard-count assertion noted above.
  </behavior>
  <action>
    Implement RED → GREEN in three coordinated edits. The three files form one atomic unit — write the failing tests first, run them to confirm RED, then add the component render + CSS to reach GREEN.

    **RED step — extend `src/components/__tests__/spiral-of-fifths.test.ts`:**

    Append the new tests described in `<behavior>` to the existing `describe("spiralOfFifths factory (DOM smoke)", …)` block (after line 109 / before the closing `})` at line 110). Reuse the existing import block at the top — no new imports needed (vitest `expect` / happy-dom DOM / `spiralOfFifths` factory are all already in scope). Use the exact assertion style from the surrounding tests (`expect(el.querySelectorAll(...).length).toBe(...)`, `expect(node?.textContent).toBe("...")`, `expect(node?.getAttribute("y")).toBe("-10")`).

    Run `npm test -- src/components/__tests__/spiral-of-fifths.test.ts` and confirm the new cases FAIL (no `.spiral-of-fifths__heard` element exists yet — count is 0, all `textContent` lookups undefined).

    Commit RED: `test(quick-260514-b2r): add failing tempered-branch heard-label tests`.

    **GREEN step 1 — render in `src/components/spiral-of-fifths.ts`:**

    Inside the DOM factory `spiralOfFifths`, locate the node-group loop body (lines 204–245). The pure-branch ratio render sits inside `if (s.ratio) { … }` at lines 223–232. Add an `else { … }` branch immediately after — render the tempered-branch heard label there, mirroring the ratio render's shape:

    - Compute `reducedCents = ((s.cumulativeCents % 1200) + 1200) % 1200` (NOT a new `SpiralStep` field — local scalar only, per `<interfaces>`).
    - Create a `<text>` element via `document.createElementNS(SVG_NS, "text")`.
    - `setAttribute("class", "spiral-of-fifths__heard")` (new sibling class — see CSS step below).
    - `setAttribute("x", "0")` — same as ratio.
    - `setAttribute("y", "-10")` — same y-slot the pure-branch ratio uses (the locked CONTEXT placement).
    - `setAttribute("text-anchor", "middle")` — same as ratio.
    - `textContent = `${reducedCents.toFixed(1)}¢`` — 1 decimal, U+00A2 cent glyph, unsigned. NEVER `innerHTML`.
    - `g.appendChild(heardText)`.

    Strict-TS hygiene already implicit because all values are string literals — no numeric `setAttribute` here. The existing `if (!s) continue` at line 206 already guards the `s` access. Do NOT touch the signed-cents block at lines 234–242 (y=14 label stays).

    Update the JSDoc header comment at lines 1–23: replace the existing tempered-branch description in line 9 — `"When `temperedFifthCents` is passed, ratio labels are dropped and the given fifth (...) is used."` — with text noting the tempered branch ALSO renders an octave-reduced cents-from-1/1 label at the y=−10 slot (the same slot the pure branch uses for ratios). Keep the rest of the header (R-01 / D-24 / U+2212 / String() / noUncheckedIndexedAccess / textContent bullets) unchanged.

    **GREEN step 2 — CSS in `src/components/spiral-of-fifths.css`:**

    Add a new rule between the existing `.spiral-of-fifths__ratio` rule (lines 28–33) and the `.spiral-of-fifths__cents` rule (line 34). The new rule duplicates the four typographic declarations from `__ratio` verbatim — per `discretion_resolved`, the choice is deliberate (separate class, identical cascade):

    ```
    svg.viz.spiral-of-fifths .spiral-of-fifths__heard {
      font-family: var(--monospace);
      font-size: 12px;
      fill: var(--theme-foreground);
      pointer-events: none;
    }
    ```

    Do NOT touch the existing `__ratio`, `__cents`, `__wolf`, `__gap`, `__dot`, or `is-clickable` rules.

    **Confirm GREEN:**

    Run `npm test -- src/components/__tests__/spiral-of-fifths.test.ts`. All previously-RED tests pass; the existing 28 tests in the file still pass; no regression in `spiralGeometry` or `closingErrorCents` suites.

    Run the full test suite once with `npm test` to confirm no other consumer (pythagorean-comma, monzo-builder, scale-table, etc.) is affected — those tests should be unchanged (this change is additive in a single component and adds new DOM elements only inside the tempered branch).

    Commit GREEN as a single atomic feat commit: `feat(quick-260514-b2r): add tempered-branch heard-interval label to spiral-of-fifths`.

    **Per CONTEXT.md scope guarantees** — DO NOT modify: `src/pages/meantone.md` (the consumer page reads the new label visually; no markdown change), the spiral geometry (`spiralGeometry` function), the click handler `onStepClick` wiring, the signed cents-from-12-TET label at y=14, the pure-branch ratio render, the wolf-chord render, the `SpiralStep` interface (the heard reduction is a local scalar in the DOM factory only). Per CLAUDE.md memory file: no `--no-verify` flag on commits; plain `git commit` is fine.
  </action>
  <verify>
    <automated>npm test -- src/components/__tests__/spiral-of-fifths.test.ts</automated>
  </verify>
  <done>
    - `npm test -- src/components/__tests__/spiral-of-fifths.test.ts` passes including the 5 new tempered-branch heard-label assertions.
    - `npm test` full-suite passes — no regression in any other file (existing 312 baseline + 5 new ≈ 317).
    - `grep -c "spiral-of-fifths__heard" src/components/spiral-of-fifths.ts` returns `1` (exactly one className setAttribute).
    - `grep -c "spiral-of-fifths__heard" src/components/spiral-of-fifths.css` returns `1` (exactly one rule selector).
    - `grep -v '^[[:space:]]*//' src/components/spiral-of-fifths.ts | grep -c 'innerHTML'` returns `0` (T-3-18 discipline preserved — no innerHTML introduced).
    - `npx tsc --noEmit` is clean (or at baseline — only the 5 pre-existing `npm:` specifier errors per STATE.md).
    - `npm run build` is clean — 18 pages render, 0 link errors.
    - Visually (by inspection of rendered HTML for meantone page or by examining the test-asserted strings): k=0 shows `0.0¢`, k=1 shows `696.6¢`, k=2 shows `193.2¢` above each tempered-branch node.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `cumulativeCents` (number) → DOM textContent | Internally-computed scalar (loop index × fifthCents); no untrusted user input crosses this boundary. |
| Component → host page | Component appends SVG-text DOM; host page (meantone.md) consumes the rendered HTMLDivElement read-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-b2r-01 | T (Tampering / XSS via injected text) | `<text>` content in `spiral-of-fifths.ts` | mitigate | Use `textContent` only (never `innerHTML`); value is a `toFixed(1)` numeric string concatenated with the literal `¢` glyph — no DOM injection surface. T-3-18 discipline already in the file's header docstring; preserved by template. |
| T-b2r-02 | I (Information disclosure) | viz component | accept | Component renders public theory-page data (cents values derived from a public ratio). No secrets. |
| T-b2r-03 | D (Denial of service via huge `n`) | `spiralGeometry` loop | accept | `n` is bounded by the call-site (always 12 in the meantone use case; geometry has its own `R_MAX` shrink). New render adds O(n) work — same complexity class as the existing ratio/cents renders. No new DOS surface. |
| T-b2r-04 | T (Tampering / numeric precision) | reduced-cents formula | mitigate | Use the EXACT formula `((cumulativeCents % 1200) + 1200) % 1200` that matches the audition call-site (meantone.md:305) and the geometry's `centsFrom12tet` reduction (spiral-of-fifths.ts:117). Tested at three concrete `k` values (0/1/2 with `temperedFifthCents=696.578`) plus 12-TET (`700`) for the integer-closure case. |

</threat_model>

<verification>
1. Test file passes: `npm test -- src/components/__tests__/spiral-of-fifths.test.ts` shows green for all heard-label assertions plus existing 28 cases.
2. Full vitest run is green: `npm test` shows green.
3. Type check clean / baseline: `npx tsc --noEmit` produces no new errors.
4. Build clean: `npm run build` produces 18 pages, 0 link errors.
5. Grep hygiene gates (from `<done>` block) all pass.
6. Manual visual check (optional, post-task): start `npm run dev`, open `/pages/meantone`, scroll to "The wolf at k=12 (1/4-comma meantone)" section, confirm each node carries a heard-cents label above the dot and the signed cents-from-12-TET label below.
</verification>

<success_criteria>
- Single atomic feat commit lands on main containing all three file edits.
- Optional preceding RED commit (`test(quick-260514-b2r): …`) lands first if executor uses two-commit TDD shape.
- `.spiral-of-fifths__heard` class exists in both the component and the stylesheet, with identical typographic style to `.spiral-of-fifths__ratio` (per discretion_resolved).
- Tempered branch renders k=0 → `0.0¢`, k=1 → `696.6¢`, k=2 → `193.2¢` (asserted by test).
- Pure branch is byte-equivalent in behavior (no `.spiral-of-fifths__heard` rendered).
- Component, geometry, wolf-chord, signed-cents-label, and click handler all unchanged.
- `src/pages/meantone.md` is NOT modified.
</success_criteria>

<output>
After completion, create `.planning/quick/260514-b2r-wolf-at-k-12-1-4-comma-meantone-section-/260514-b2r-SUMMARY.md` capturing:
- Files changed + line counts.
- Commit SHA(s).
- Confirmation of test counts before/after.
- Confirmation that meantone.md was not touched.
- Note the discretion resolution (new `.spiral-of-fifths__heard` class chosen over `__ratio` reuse).
</output>
