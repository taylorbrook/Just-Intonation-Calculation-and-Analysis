---
quick_id: 260615-f49
slug: repo-hygiene-exact-pin-xenharmonic-0-x-d
date: 2026-06-15
status: complete
---

# Quick Task 260615-f49 — Summary

Four independent repo-hygiene fixes from the v1.1 close review. Executed inline
on `main` (no worktree isolation: task #7 mutates git worktree metadata, which is
incompatible with worktree-isolated execution).

## What changed

### Task 1 — Exact-pin the four 0.x xenharmonic deps (#8) — `68408a6`
Dropped the `^` caret in `package.json` for the four 0.x deps so they cannot
float past the version `patches/sonic-weave+0.14.1.patch` is pinned to:
- `xen-dev-utils` `^0.13.1` → `0.13.1`
- `sonic-weave` `^0.14.1` → `0.14.1`
- `sw-synth` `^0.4.0` → `0.4.0`
- `ji-lattice` `^0.3.2` → `0.3.2`

Synced `package-lock.json` root specifiers via `npm install --package-lock-only`.
Resolved versions were already exactly the pin targets, so **no node_modules
change**. npm also normalized two accurate metadata fields in the lockfile
(`hasInstallScript: true` for the postinstall; removed a stale `extraneous` flag).

### Task 2 — Consolidate CI gates + patch-package guard (#20) — `582f401`
In `.github/workflows/deploy.yml`, replaced the five duplicated gate steps
(type-check, test, lint, format-check, build) with a single `npm run ci` call
(one source of truth; same D-02 order). Added
`npx patch-package --error-on-fail` after `npm ci` so CI hard-fails when a patch
no longer applies cleanly (postinstall's `patch-package` only warns, exit 0).
Deny-list (D-21) and Pages steps retained. YAML validated.

### Task 3 — Delete dead `.eslintignore` (low) — `99e2133`
ESLint 9 flat config does not read `.eslintignore`, and it had drifted from the
authoritative `ignores` block in `eslint.config.js` (which covers strictly more:
adds `src/.observablehq/**`). Removed. Confirmed ESLint 9.39.4 resolves the flat
config for source files without it.

### Task 4 — Remove stale locked agent worktrees (#7) — no file commit
Removed the 5 stale locked worktrees under `.claude/worktrees/agent-*` via
`git worktree remove --force` (after `git worktree unlock`) and deleted their
branches, plus 1 orphan `worktree-agent-*` branch that had no worktree dir.
**All 6 branches were confirmed 0 commits ahead of `main` — no work lost.**
Final state: `git worktree list` shows only the main checkout; no
`worktree-agent-*` branches remain; `.claude/worktrees/` and `.git/worktrees/`
are empty. (Touches only git metadata, so no tracked-file commit.)

## Verification
- `package.json` + `package-lock.json` in sync; no caret on the four deps; locked
  versions unchanged.
- patch-package present in lockfile (8.0.1) → CI's `npm ci` installs it; the
  sonic-weave patch is currently applied to the installed package.
- `deploy.yml` parses as valid YAML; deny-list + Pages steps retained.
- ESLint flat config resolves without `.eslintignore`.
- No `worktree-agent-*` worktrees or branches remain.

## Notes
- Local `node_modules` is missing `patch-package` (pre-existing dev-env drift,
  unrelated to this task — only `--package-lock-only` was run, which doesn't
  install). The lockfile carries it, so CI is unaffected. A fresh `npm ci`
  locally would restore it.
