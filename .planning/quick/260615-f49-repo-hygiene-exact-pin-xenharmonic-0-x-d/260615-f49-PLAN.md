---
quick_id: 260615-f49
slug: repo-hygiene-exact-pin-xenharmonic-0-x-d
date: 2026-06-15
mode: quick
---

# Quick Task 260615-f49: Repo hygiene

Four independent hygiene fixes from the v1.1 close review (findings #8, #20, low, #7).
Executed inline on `main` (no worktree isolation — task #7 mutates git worktree
metadata, which is incompatible with worktree-isolated execution).

## Task 1 — Exact-pin the four 0.x xenharmonic deps (#8)

**Files:** `package.json`, `package-lock.json`

Drop the `^` caret from the four 0.x xenharmonic deps so they cannot float past
the version the `patches/sonic-weave+0.14.1.patch` is pinned to:

- `xen-dev-utils`: `^0.13.1` → `0.13.1`
- `sonic-weave`: `^0.14.1` → `0.14.1`
- `sw-synth`: `^0.4.0` → `0.4.0`
- `ji-lattice`: `^0.3.2` → `0.3.2`

Sync the lockfile root specifiers with `npm install --package-lock-only`
(resolved versions already match the pins — no node_modules change).

- **verify:** `npm ci` succeeds (package.json ↔ lockfile in sync); `grep '\^'`
  shows no caret on the four deps.
- **done:** the four deps are exact-pinned in both package.json and lockfile.

## Task 2 — Consolidate CI gates + patch-package guard (#20)

**Files:** `.github/workflows/deploy.yml`

Replace the five duplicated gate steps (Type-check, Unit tests, Lint, Format
check, Build static site) with a single `npm run ci` call (the script is the
local source of truth: `lint:types && test && lint && format:check && build`).
Keep the deny-list (D-21) and Pages steps. Add a step after `npm ci` that runs
`npx patch-package --error-on-fail` so CI hard-fails if a patch no longer applies
cleanly (postinstall's patch-package only warns, exit 0).

- **verify:** workflow YAML parses; deny-list + Pages steps retained; gate order
  preserved inside `npm run ci`.
- **done:** one `npm run ci` gate step + an explicit patch-package failure guard.

## Task 3 — Delete dead .eslintignore (low)

**Files:** `.eslintignore` (delete)

ESLint 9 flat config ignores `.eslintignore`; it has drifted from the `ignores`
block in `eslint.config.js`. Delete it.

- **verify:** `eslint .` runs clean using only the flat-config ignores.
- **done:** `.eslintignore` removed.

## Task 4 — Remove stale locked agent worktrees (#7)

**Files:** none tracked (git worktree/branch metadata only)

Remove the 5 stale locked worktrees under `.claude/worktrees/agent-*` via
`git worktree remove --force`, delete their dirs, and delete the orphaned
`worktree-agent-*` branches (all confirmed 0 commits ahead of main → no work
lost). Includes one orphan branch with no worktree dir.

- **verify:** `git worktree list` shows only the main checkout; no
  `worktree-agent-*` branches remain; `.claude/worktrees/` is empty.
- **done:** all stale worktrees and their branches removed.
