---
quick_id: 260512-fxk
description: Embed spiral-of-fifths component + Plot drift chart + Further reading on pythagorean-comma page
date: 2026-05-12
status: in_progress
---

# Quick Task 260512-fxk

## Goal

Wire the `spiralOfFifths` component (built in quick-260512-f0z, currently
unused) into `src/pages/pythagorean-comma.md` and add a complementary Plot
line chart showing the cumulative-cents-from-12-TET drift as 1..12 pure
fifths are stacked. Add a "Further reading" section.

## Constraints

- **BigInt is the source of truth.** Stacking happens via `Interval.mul`; the
  `.cents` projection is taken ONLY at the display boundary (Pitfall #1).
- **Don't refactor the existing math cell.** It already builds
  `twelveFifths = (3/2)^12` and the `cycleOctave`. Add new code; don't touch
  the existing chain.
- **Component CSS opt-in.** `spiral-of-fifths.css` is currently NOT in
  `src/styles.css`. The component renders nothing visible without it. Add
  the `@import` line.
- **Match existing chart style.** Follow the `Plot.plot` pattern used in
  `src/pages/comma-pump.md` (the two drift charts there): blue line, dot
  marks, ruleY at zero, monospace cents tick formatter with `+` sign.
- **Section placement.** Spiral + chart go after the audition bullet list
  ("Audition the overshoot") and before "## In monzos". "Further reading"
  goes at the very end after "## See also" (matching `comma-pump.md`).

## Files

- `src/styles.css` — add `@import "./components/spiral-of-fifths.css";`
- `src/pages/pythagorean-comma.md` — add imports, spiral widget, Plot drift
  chart, "Further reading" section

## Plan

### Task 1 — wire spiral CSS + page integration

1. Append `@import "./components/spiral-of-fifths.css";` to `src/styles.css`
   in the component-CSS @import block (after `scale-compare.css`).
2. In `src/pages/pythagorean-comma.md`:
   - Add imports: `spiralOfFifths` from `../components/spiral-of-fifths.js`
     and `* as Plot from "npm:@observablehq/plot"`.
   - After the audition bullets, add a new `## The closure gap, visualized`
     section containing:
     - a brief paragraph framing what's being shown,
     - the spiral widget: `${spiralOfFifths(12, { highlightWolf: true })}`,
     - a brief paragraph framing the chart,
     - a Plot line chart in a ```ts cell. Data is built by stacking 12
       pure 3/2's via `Interval.mul` in a loop; for each k=0..12 the y
       value is `acc.cents - k * 700` (cumulative deviation from k 12-TET
       fifths). `.cents` is read once per step at the display boundary.
       k=0 anchors at 0; k=12 lands at +23.46¢ (the Pythagorean comma).
   - After the existing `## See also` section, add `## Further reading`
     with a single bullet linking to `https://en.xen.wiki/w/Pythagorean_comma`
     with a short blurb (match the tone of comma-pump's wiki entry).

### Task 2 — verify

- `npx tsc --noEmit` passes (no new TS errors).
- `npx vitest run src/components/__tests__/spiral-of-fifths.test.ts`
  still 18/18 (component is untouched; this confirms the CSS-import wiring
  didn't break anything via stylesheet side-effects).
- Manual inspection: page renders an SVG spiral with wolf-chord dashed
  line + a Plot line chart that rises monotonically to ~23.46¢.

## must_haves

**truths**
- The Plot data MUST be generated by stacking `Interval.mul(new Interval("3/2"))`
  in a loop; the BigInt fraction is the source of truth and `.cents` is
  read only at data-row construction time.
- Step k=12 of the chart MUST land at ~23.46¢ (the Pythagorean comma value,
  matching the prose's `${tex}` claim).

**artifacts**
- `src/pages/pythagorean-comma.md` contains an `import` for `spiralOfFifths`
  AND `Plot`, calls `spiralOfFifths(12, { highlightWolf: true })`, and has
  a `## Further reading` section linking to `en.xen.wiki/w/Pythagorean_comma`.
- `src/styles.css` imports `./components/spiral-of-fifths.css`.

**key_links**
- `src/components/spiral-of-fifths.ts` — component (built in 260512-f0z)
- `src/pages/comma-pump.md` — reference for Plot chart + Further reading pattern
- `src/lib/interval.ts` — `.mul`, `.cents` API
