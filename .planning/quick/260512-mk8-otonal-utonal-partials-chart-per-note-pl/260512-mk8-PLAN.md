---
phase: 260512-mk8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/otonality-utonality.md
autonomous: true
requirements:
  - QUICK-OTONAL-UTONAL-PARTIALS-CHART-PER-NOTE-PLAY-FURTHER-READING-01
must_haves:
  truths:
    - "src/pages/otonality-utonality.md gains a single `import * as Plot from \"npm:@observablehq/plot\"` line added to the existing imports cell at the top of the page (lines 5-9). The existing four imports (Interval, createSynth, ratioPill, and the page's local synth/baseHz/otonal/utonal bindings) are preserved byte-equivalent."
    - "src/pages/otonality-utonality.md also imports `playInterval` from `../components/play-interval.js` in that same imports cell — currently absent."
    - "A new paired Plot chart appears between the otonal H2 section's `Audition all four notes together: ${otonalBtn}` line and the `## The utonal chord (under-N)` H2. Two `Plot.plot` calls in a horizontal flex container (one `<div style=\"display:flex;gap:24px;flex-wrap:wrap;justify-content:center;\">` parent with two child `<div>`s, one per chart), so the mirror reads at a glance on wide screens and stacks gracefully on narrow ones."
    - "The LEFT subchart plots otonal partials 4, 5, 6, 7 as horizontal lines (Plot.ruleY with x1=0, x2=cents) on a cents-from-fundamental axis 0..1050¢. Y axis is categorical and lists the ratios 4/4, 5/4, 6/4, 7/4 in ascending pitch order (top→bottom reversed so 7/4 sits at the top — the higher pitch is the higher row, matching musical staff intuition). Cents values are derived from `otonal[i].cents` (the `Interval` BigInt-Fraction kernel objects already bound on lines 25-30) and read ONCE at chart-build time — never as kernel input (Pitfall #1)."
    - "The RIGHT subchart plots the utonal chord using its re-rooted-ascending form (1/1, 8/7, 4/3, 8/5) drawn the same way as the otonal subchart — horizontal lines from 0 to `utonal[i].cents`, categorical Y axis labeled `1/1`, `8/7`, `4/3`, `8/5` top→bottom reversed, x domain shared with the left chart [0, 1050]¢. The point is visual A/B: when you look at the two charts side by side, the otonal members sit higher in their interior spacing (5/4 ≈ 386¢ vs 8/5 ≈ 814¢; 3/2 ≈ 702¢ vs 4/3 ≈ 498¢; 7/4 ≈ 969¢ vs 8/7 ≈ 231¢) — the mirror is in the interior-interval distribution, not in a literal up/down inversion."
    - "Both subcharts use the same x-axis domain [0, 1050] cents and the same width (~360px) and height (~220px) so the mirror is visible. Each chart has a one-line title via `Plot.plot({ title: \"Otonal — 4:5:6:7\" })` (LEFT) and `Plot.plot({ title: \"Utonal — 1/1:8/7:4/3:8/5\" })` (RIGHT), or equivalent caption markup."
    - "Chart cells use existing `display(...)` Observable pattern or inline `${...}` interpolation; the choice does not matter as long as the rendered output sits between line ~104 (otonal `Audition…` paragraph) and line ~106 (`## The utonal chord (under-N)` H2)."
    - "Per-note play buttons appear next to EVERY member bullet in both chord sections. For the otonal section (lines 78-81), each of the four `${ratioPill(otonal[i])} — <description>` bullets gets a trailing ` ${playInterval(otonal[i], synth, { label: true })}` so the bullet reads `<ratio pill> — <description> ▶ Play <ratio>`. Same pattern for the utonal section (lines 112-115), using `utonal[i]`."
    - "Per-note play buttons reuse the page's existing `synth` binding from the synth cell (line 16) — they MUST NOT call `createSynth()` again (Pitfall #2 — one AudioContext per page; the lazy synth allocates on first user click)."
    - "Per-note play buttons use `playInterval(iv, synth, { label: true })` — the `label: true` opt renders `▶ Play 5/4` etc. (vs the default unlabeled `▶`). `baseHz` is left at the factory default of 440 (D-08); duration default 1.5s (D-18). Do NOT override either."
    - "The existing chord buttons (otonalBtn on lines 87-101 and utonalBtn on lines 140-154) and the truth-check `display(utonalDerived)` cell (lines 117-131) are NOT modified. The new per-note buttons are ADDITIVE — chord buttons still play 4-note simultaneous chords."
    - "A new `## Further reading` H2 is appended at the very bottom of the file (AFTER the existing `## See also` H2, which currently ends at line 207). The section contains two bulleted links: `https://en.xen.wiki/w/Otonality_and_utonality` (Partch-rooted reference covering the duality in full); and La Monte Young's *Well-Tuned Piano* — link Kyle Gann's notes `https://www.kylegann.com/wtp.html` as the primary reference (canonical composer-notes survey with structural-tonality analysis). Plain `## Further reading` heading, plain bulleted links — matches the precedent established by the 260512-mvw, 260512-fxk, 260512-hdv, 260512-ilb, 260512-aph, 260512-kzw quick tasks."
    - "Pitfall #1 honored throughout: every cents value in the chart comes from `Interval.cents` on existing `otonal[i]` / `utonal[i]` bindings (BigInt-Fraction kernel objects), never as a typed numeric literal (`969`, `498`, etc.). Grep gate: `grep -nE '\\b(969|498|814|231|386|702)\\b' src/pages/otonality-utonality.md` should return ZERO numeric-literal matches in the chart cell body."
    - "XSS discipline: any new DOM construction (chart wrapper `<div>`, chart titles) uses Observable Plot's built-in marks/titles or Markdown prose — does NOT call `.innerHTML =` with interpolated user-controlled strings. None of the data is user-controlled, but the pattern stays consistent with the rest of the codebase (T-02-22/T-02-23 defense-in-depth)."
    - "No new component files are created. No new CSS files are created. No `@import` lines are added to src/styles.css. `playInterval`'s `play-btn` styles are already loaded via `src/styles.css` line 20 (`@import \"./components/play-buttons.css\"`)."
    - "tsc clean (`npm run lint:types` exits 0). No new TypeScript errors introduced beyond the project's baseline."
    - "`npm run build` clean — Observable Framework builds the page with the new Plot import; bundle size increase comparable to other Plot-using pages (otonality-utonality currently has no Plot import; expect ~11-13kB → ~25-30kB per-page bundle and ~390kB → ~900kB total imports footprint, matching harmonic-series.md / edo-approximation.md / prime-limits.md / monzos.md after their Plot additions)."
    - "No regressions on existing page sections: prose (lines 1-72), monzo table (lines 167-184), See also block (lines 186-207) all remain byte-identical to baseline."
  artifacts:
    - path: "src/pages/otonality-utonality.md"
      provides: "Paired otonal/utonal partials chart between H2 sections; per-note play buttons on every chord-member bullet (8 new buttons total — 4 otonal + 4 utonal); Further reading section linking en.xen.wiki/w/Otonality_and_utonality + Kyle Gann's Well-Tuned Piano notes."
      contains: "import * as Plot from \"npm:@observablehq/plot\""
    - path: "src/pages/otonality-utonality.md"
      provides: "Plot chart for otonal partials and utonal partials side by side."
      contains: "Plot.plot"
    - path: "src/pages/otonality-utonality.md"
      provides: "Per-note play buttons reusing page-level synth + playInterval factory."
      contains: "playInterval(otonal["
    - path: "src/pages/otonality-utonality.md"
      provides: "Further reading H2 section linking xen-wiki + kylegann.com."
      contains: "## Further reading"
  key_links:
    - from: "paired Plot chart cell"
      to: "otonal[i].cents / utonal[i].cents (kernel)"
      via: "Interval.cents (display-boundary projection — Pitfall #1)"
      pattern: "otonal\\[.*\\]\\.cents|utonal\\[.*\\]\\.cents|\\.cents"
    - from: "per-note play button (otonal/utonal bullets)"
      to: "playInterval(iv, synth, { label: true })"
      via: "import from ../components/play-interval.js; reuses page-level synth cell binding"
      pattern: "playInterval\\((otonal|utonal)\\["
    - from: "imports cell"
      to: "@observablehq/plot + play-interval factory"
      via: "single Plot import + single playInterval import added"
      pattern: "import \\* as Plot from \"npm:@observablehq/plot\""
    - from: "Further reading section"
      to: "external references (xen-wiki Otonality_and_utonality + kylegann.com/wtp.html)"
      via: "plain Markdown bulleted links after See also"
      pattern: "## Further reading"
---

<objective>
Three additive edits to `src/pages/otonality-utonality.md`:

1. **Paired partials chart between the otonal and utonal H2 sections** — two
   horizontal-rule Plot subcharts in a flex row, both plotting cents from
   fundamental on the x-axis with the four chord members as categorical y
   rows. LEFT shows otonal (4/4, 5/4, 6/4, 7/4 — ascending pitch). RIGHT shows
   utonal in its re-rooted-ascending form (1/1, 8/7, 4/3, 8/5). Same x domain
   [0, 1050]¢, same dimensions — the mirror reads in the interior-interval
   distribution. Cents derived from `Interval.cents` (kernel as source of
   truth, Pitfall #1).

2. **Per-note play buttons on every chord-member bullet** in both sections.
   The existing chord buttons (`otonalBtn`, `utonalBtn`) stay — they continue
   to play the 4-note simultaneous chord. New buttons ADD per-member auditioning:
   each bullet now reads `<ratio pill> — <description> ▶ Play <ratio>` via
   `playInterval(iv, synth, { label: true })` reusing the page-level synth
   binding (Pattern 4 / Pitfall #2 — no new AudioContext).

3. **`## Further reading` H2 appended at the very bottom of the file** (after
   `## See also`) with two bulleted links: the Xenharmonic Wiki entry on
   Otonality and utonality, and Kyle Gann's Well-Tuned Piano notes.

Purpose: drive home the otonal/utonal mirror claim visually and aurally.
The current page makes the duality argument in prose + a kernel-side truth
check; the chart makes the structural mirror legible at a glance, and the
per-note buttons let you ABCD the four members of each chord against the
fundamental directly without re-clicking the simultaneous chord button.
Output: src/pages/otonality-utonality.md +~75 lines additive, no refactor
of existing prose / synth cell / chord-binding cells / monzo table /
truth-check / See also block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/pages/otonality-utonality.md
@src/components/play-interval.ts
@src/audio/synth.ts
@src/lib/interval.ts

# Reference patterns for chart structure (read these so you don't reinvent):
@src/pages/harmonic-series.md
@src/pages/edo-approximation.md

# Precedent quick task for plan discipline and "Further reading" style:
@.planning/quick/260512-mvw-odd-limits-static-diamonds-and-table/260512-mvw-PLAN.md

<interfaces>
<!-- Key APIs and bindings the executor needs. -->
<!-- All extracted from files above — no codebase exploration required. -->

From src/components/play-interval.ts:
```typescript
export interface PlayIntervalOpts {
  baseHz?: number;     // default 440 (D-08)
  duration?: number;   // default 1.5  (D-18)
  label?: boolean;     // when true, "▶ Play 5/4"; when false, "▶". Default false.
}
export function playInterval(
  interval: Interval,
  synth: SynthHandle,
  opts?: PlayIntervalOpts,
): HTMLButtonElement;
```

From src/audio/synth.ts:
```typescript
export interface SynthHandle {
  playNote(hz: number, dur?: number): () => void;
  playNotes(freqs: number[], dur?: number): void;
  playArpeggio(freqs: number[], stepSec?: number): void;
  startDrone(hz: number): () => void;
  panic(): void;
  readonly activeVoices: number;
  dispose(): void;
}
export function createSynth(opts?: CreateSynthOpts): SynthHandle;
```

From src/lib/interval.ts (read-only access on existing bindings):
```typescript
export class Interval {
  readonly fraction: Fraction;        // BigInt-backed (fraction.js v5)
  get monzo(): number[];              // lazy cached
  get cents(): number;                // lazy cached — DISPLAY ONLY (Pitfall #1)
  get centsFrom12tet(): number;
  // mul, div, inv, octaveReduce, equals, toString — all immutable
}
```

Page-level bindings already present in src/pages/otonality-utonality.md
(do NOT redeclare; do NOT shadow):

```typescript
// Line 11-17 — synth cell (Pattern 4, owns AudioContext lazily)
const synth = createSynth();
invalidation.then(() => synth.dispose());

// Line 20-50 — chord bindings + baseHz
const otonal = [
  new Interval("1/1"),
  new Interval("5/4"),
  new Interval("3/2"),  // = 6/4 — page prose displays as "6/4 = 3/2"
  new Interval("7/4"),
];
const utonal = [
  new Interval("1/1"),
  new Interval("8/7"),
  new Interval("4/3"),
  new Interval("8/5"),
];
const baseHz = 440;
```

Cents reference values (computed at build time by `Interval.cents`):

| Otonal | cents  | Utonal | cents  |
|--------|--------|--------|--------|
| 1/1    | 0.000  | 1/1    | 0.000  |
| 5/4    | 386.31 | 8/7    | 231.17 |
| 3/2    | 701.96 | 4/3    | 498.04 |
| 7/4    | 968.83 | 8/5    | 813.69 |

These values are for sanity-check only — your chart code MUST read them from
`otonal[i].cents` / `utonal[i].cents`, NEVER hardcode them as numeric literals.

For chart display labels (the categorical y-axis tick label), use the
`Interval.toString()` projection (`"5/4"`, `"3/2"`, etc.) — see how
harmonic-series.md does it on line 132: `tickFormat: (n) => String(n)`.
Note: `new Interval("6/4").toString()` returns `"3/2"` (fraction.js canonicalises),
which matches the current prose treatment ("6/4 = 3/2"). That's fine — the
y-axis label can read `3/2`, and the chart caption / x-axis label still
references the partial number context.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add paired partials chart + per-note play buttons + Further reading to otonality-utonality.md</name>
  <files>src/pages/otonality-utonality.md</files>
  <action>
Single-file additive edit to `src/pages/otonality-utonality.md`. The work has three independent additions; do them in this order to keep the diff readable.

**Step 1 — imports cell (lines 5-9 currently).**

Extend the imports cell to include `playInterval` and `@observablehq/plot`. The post-state of the cell:

```ts
import { Interval } from "../lib/interval.js";
import { createSynth } from "../audio/synth.js";
import { ratioPill } from "../components/ratio-pill.js";
import { playInterval } from "../components/play-interval.js";
import * as Plot from "npm:@observablehq/plot";
```

Framework module-scope rule applies — all imports go in this ONE cell. Do not introduce a second imports cell anywhere else on the page; doing so would re-bind `Plot` / `playInterval` and break reactivity. (Lesson from the 260512-kzw prime-limits quick task.)

**Step 2 — per-note play buttons in the otonal section (lines 78-81).**

For each of the four otonal bullets, append the per-note play button via `${playInterval(otonal[i], synth, { label: true })}`. The bullets become:

```md
- ${ratioPill(otonal[0])} — the fundamental (4/4 = 1/1). ${playInterval(otonal[0], synth, { label: true })}
- ${ratioPill(otonal[1])} — 5-limit major third (5th harmonic, octave-reduced). ${playInterval(otonal[1], synth, { label: true })}
- ${ratioPill(otonal[2])} — perfect fifth (6/4 = 3/2; the 6th harmonic, octave-reduced). ${playInterval(otonal[2], synth, { label: true })}
- ${ratioPill(otonal[3])} — harmonic seventh (7th harmonic; 7-limit, ~969¢, a touch flatter than the 12-TET minor seventh). ${playInterval(otonal[3], synth, { label: true })}
```

Note: `playInterval(iv, synth, { label: true })` renders `▶ Play 5/4` etc. — the button text comes from `interval.fraction.toFraction()` so `otonal[2]` (`new Interval("3/2")`) will read `▶ Play 3/2` (NOT `▶ Play 6/4`). That matches the post-octave-reduced reading already used in the prose and ratio pills; do NOT add any kind of override.

**Step 3 — paired chart between the otonal and utonal H2 sections.**

Insert the chart block AFTER the `Audition all four notes together: ${otonalBtn}` line (currently line 104) and BEFORE the `## The utonal chord (under-N)` H2 (currently line 106). Add a one-line intro sub-heading `### The mirror, visualized` and then the chart code:

```ts
// Paired partials chart — visual A/B of the otonal/utonal mirror.
// Both charts share x ∈ [0, 1050]¢ and use horizontal rules from 0 to each
// member's cents. Cents come from Interval.cents on the existing otonal/utonal
// bindings (Pitfall #1 — kernel BigInt-Fraction is source of truth; cents is
// a display projection read ONCE here at chart-build time).
const partialsCharts = (() => {
  const X_DOMAIN = [0, 1050];
  const CHART_WIDTH = 360;
  const CHART_HEIGHT = 220;

  const otonalRows = otonal.map((iv) => ({
    label: iv.toString(),  // "1/1", "5/4", "3/2", "7/4"
    cents: iv.cents,       // display-boundary projection (Pitfall #1)
  }));
  const utonalRows = utonal.map((iv) => ({
    label: iv.toString(),  // "1/1", "8/7", "4/3", "8/5"
    cents: iv.cents,
  }));

  // Y-axis domain: array order ascending (so 1/1 is the lowest pitch),
  // reverse:true puts the higher pitch (7/4 / 8/5) at the TOP. That
  // matches musical-staff intuition: higher pitch, higher on the chart.
  const otonalChart = Plot.plot({
    title: "Otonal — 4:5:6:7",
    width: CHART_WIDTH,
    height: CHART_HEIGHT,
    marginLeft: 50,
    marginRight: 20,
    x: { label: "Cents from fundamental", domain: X_DOMAIN, grid: true },
    y: { label: null, domain: otonalRows.map((r) => r.label), reverse: true },
    marks: [
      Plot.ruleY(otonalRows, { y: "label", x1: 0, x2: "cents", stroke: "#4269d0", strokeWidth: 3 }),
      Plot.dot(otonalRows, { x: "cents", y: "label", fill: "#4269d0", r: 4, stroke: "white", strokeWidth: 1 }),
    ],
  });

  const utonalChart = Plot.plot({
    title: "Utonal — 1/1:8/7:4/3:8/5",
    width: CHART_WIDTH,
    height: CHART_HEIGHT,
    marginLeft: 50,
    marginRight: 20,
    x: { label: "Cents from fundamental", domain: X_DOMAIN, grid: true },
    y: { label: null, domain: utonalRows.map((r) => r.label), reverse: true },
    marks: [
      Plot.ruleY(utonalRows, { y: "label", x1: 0, x2: "cents", stroke: "#ef8e3a", strokeWidth: 3 }),
      Plot.dot(utonalRows, { x: "cents", y: "label", fill: "#ef8e3a", r: 4, stroke: "white", strokeWidth: 1 }),
    ],
  });

  // Flex container so the two charts read as a paired A/B on wide viewports
  // and stack on narrow ones. Use createElement (no innerHTML for user values
  // — defense-in-depth T-02-22/T-02-23; nothing here is user-controlled but
  // the pattern stays consistent).
  const wrapper = document.createElement("div");
  wrapper.style.display = "flex";
  wrapper.style.gap = "24px";
  wrapper.style.flexWrap = "wrap";
  wrapper.style.justifyContent = "center";
  const leftBox = document.createElement("div");
  leftBox.appendChild(otonalChart);
  const rightBox = document.createElement("div");
  rightBox.appendChild(utonalChart);
  wrapper.appendChild(leftBox);
  wrapper.appendChild(rightBox);
  return wrapper;
})();
display(partialsCharts);
```

Use D-33 anchor palette colors (`#4269d0` blue for otonal, `#ef8e3a` orange for utonal — these are the same two anchor colors used in scale-compare.ts and reused by 260512-fg3 harmonic-series and 260512-fxk pythagorean-comma; they're color-blind-safe and already established as the project's primary chart palette).

Add a short one-paragraph prose tag immediately after the chart, before the `## The utonal chord (under-N)` H2:

```md
The two charts share the same cents axis. Read the spacing: the otonal chord
stacks its 386¢ third near the bottom (close to the fundamental), then 702¢,
then 969¢ — the gaps between members get smaller as you go up, the signature
shape of the harmonic series. The utonal chord (re-rooted upward for direct
A/B against the otonal root) stacks its 231¢ supermajor second near the
bottom, then 498¢, then 814¢ — the gaps between members get *larger* as you
go up. That divergent shape IS the otonal/utonal mirror.
```

**Step 4 — per-note play buttons in the utonal section (lines 112-115).**

Identical treatment to step 2, but for the utonal bullets. The bullets become:

```md
- ${ratioPill(utonal[0])} — the guide tone (root, unchanged). ${playInterval(utonal[0], synth, { label: true })}
- ${ratioPill(utonal[1])} — 7-limit "supermajor second" (inv of 7/4 → 4/7, octave-reduced to 8/7). ${playInterval(utonal[1], synth, { label: true })}
- ${ratioPill(utonal[2])} — perfect fourth (inv of 3/2 → 2/3, octave-reduced to 4/3). ${playInterval(utonal[2], synth, { label: true })}
- ${ratioPill(utonal[3])} — 5-limit minor sixth (inv of 5/4 → 4/5, octave-reduced to 8/5). ${playInterval(utonal[3], synth, { label: true })}
```

**Step 5 — `## Further reading` H2 appended at the very bottom.**

After the existing `## See also` block ends (currently line 207), append a blank line and then:

```md
## Further reading

- [Otonality and utonality on the Xenharmonic Wiki](https://en.xen.wiki/w/Otonality_and_utonality) — community-curated reference covering Partch's duality in full, with worked tonality-diamond examples and the canonical numerator/denominator framing.
- [Kyle Gann, "La Monte Young's *The Well-Tuned Piano*"](https://www.kylegann.com/wtp.html) — Gann's composer's-eye survey of Young's six-hour solo-piano cycle, the most-cited example of long-form otonal-chord-cluster composition; the dream-house tunings draw their pitch lattice from selected otonal chords on 7- and 11-limit harmonics.
```

(Match the prose-y "— short editorial framing —" style established by 260512-fg3 / 260512-mvw / 260512-ilb. Plain `## Further reading` heading, plain bulleted links, brief contextualizing prose per link.)

**What NOT to change:**

- Do NOT modify the synth cell (lines 11-18).
- Do NOT modify the otonal/utonal/baseHz binding cell (lines 20-50).
- Do NOT modify the otonalBtn cell (lines 84-101) — chord button stays.
- Do NOT modify the utonalBtn cell (lines 140-154) — chord button stays.
- Do NOT modify the `display(utonalDerived)` truth-check cell (lines 117-131).
- Do NOT modify the prose paragraphs on lines 52-72, 159-164.
- Do NOT modify the `## Monzo signs flip` section (lines 167-184) or the `## See also` block (lines 186-207).
- Do NOT add CSS files; do NOT add @import lines to src/styles.css.
- Do NOT introduce any second `const synth = createSynth()` (Pitfall #2).
- Do NOT hardcode cents values as numeric literals in the chart cell — read from `otonal[i].cents` / `utonal[i].cents`. (Sanity-check values in the prose paragraph after the chart are acceptable as PROSE — they describe what the reader sees; the chart itself must derive from kernel.)

D-XX traceability for this plan:
- D-07: playInterval is for inline prose use (this is exactly that case).
- D-08: 440 Hz default base — left untouched (factory default).
- D-18: 1.5s duration default — left untouched (factory default).
- D-24: Immutability — Interval read-only access only; no `.mul/.div/.inv/.octaveReduce` calls in new code (the existing cell on lines 122-130 already exercises the round-trip).
- T-02-22 / T-02-23: defense-in-depth XSS — all new DOM via createElement + textContent (chart wrapper) or Markdown prose (further reading). No `.innerHTML = <interpolated user values>` anywhere.
- Pitfall #1: cents read from `Interval.cents` ONCE at chart-build, never fed back into kernel.
- Pitfall #2: existing page-level synth reused; no new AudioContext.
- Pitfall #5: playInterval factory reused (no inline reimplementation of "▶ button" widget — that's what 260512-eru play-dyad SHOULD have been but is a separate component for chord-pair audition).
  </action>
  <verify>
    <automated>npm run lint:types 2>&1 | tail -5</automated>
    <automated>npm run build 2>&1 | tail -10</automated>
    <automated>grep -c 'playInterval(otonal\[' "src/pages/otonality-utonality.md"</automated>
    <automated>grep -c 'playInterval(utonal\[' "src/pages/otonality-utonality.md"</automated>
    <automated>grep -c 'import \* as Plot from "npm:@observablehq/plot"' "src/pages/otonality-utonality.md"</automated>
    <automated>grep -c 'from "../components/play-interval.js"' "src/pages/otonality-utonality.md"</automated>
    <automated>grep -c '^## Further reading' "src/pages/otonality-utonality.md"</automated>
    <automated>grep -n 'en.xen.wiki/w/Otonality_and_utonality\|kylegann.com/wtp' "src/pages/otonality-utonality.md"</automated>
    <automated>grep -nE '\b(386\.31|701\.96|968\.83|231\.17|498\.04|813\.69)\b' "src/pages/otonality-utonality.md" | grep -v '^[0-9]*:.*The two charts share\|spacing\|stacks its' || echo "OK: no hardcoded float cents literals in chart cell"</automated>
    <automated>grep -c 'Plot\.plot' "src/pages/otonality-utonality.md"</automated>
    <automated>grep -n 'createSynth()' "src/pages/otonality-utonality.md"</automated>
    <!-- Last gate: exactly ONE createSynth() call (the existing synth cell). -->
  </verify>
  <done>
1. tsc baseline unchanged (no new TypeScript errors).
2. `npm run build` exits clean. Site builds the page with both Plot subcharts rendered.
3. `grep -c 'playInterval(otonal\['` returns 4 (one per otonal bullet).
4. `grep -c 'playInterval(utonal\['` returns 4 (one per utonal bullet).
5. `grep -c 'import \* as Plot from "npm:@observablehq/plot"'` returns 1.
6. `grep -c 'from "../components/play-interval.js"'` returns 1.
7. `grep -c '^## Further reading'` returns 1 (heading appended after See also).
8. Both required URLs present (xen.wiki Otonality_and_utonality + kylegann.com/wtp.html).
9. No hardcoded cents float literals (386.31, 701.96, 968.83, 231.17, 498.04, 813.69) appear inside the chart cell — only inside the post-chart prose paragraph (where they're descriptive text, not data).
10. `grep -c 'Plot\.plot'` returns 2 (one otonal subchart, one utonal subchart).
11. `grep -c 'createSynth()'` returns exactly 1 (the existing synth cell, not duplicated).
12. Visual smoke (executor runs `npm run dev` and opens /pages/otonality-utonality): chart appears between the two H2 sections; per-note buttons appear next to all 8 chord-member bullets; the four otonal buttons read `▶ Play 1/1`, `▶ Play 5/4`, `▶ Play 3/2`, `▶ Play 7/4`; the four utonal buttons read `▶ Play 1/1`, `▶ Play 8/7`, `▶ Play 4/3`, `▶ Play 8/5`; Further reading section renders at the bottom; existing chord buttons (otonalBtn / utonalBtn) and truth-check display still work.
  </done>
</task>

</tasks>

<verification>

Final state check (all run from project root):

```bash
# Baseline still passes
npm run lint:types     # exits 0 (or with the project's pre-existing baseline errors only — see STATE.md)
npm run build          # exits 0; 79+ links validated; new page bundle in line with other Plot-using pages

# Required additions exist
test "$(grep -c 'Plot\.plot' src/pages/otonality-utonality.md)" -eq 2
test "$(grep -c 'playInterval(otonal\[' src/pages/otonality-utonality.md)" -eq 4
test "$(grep -c 'playInterval(utonal\[' src/pages/otonality-utonality.md)" -eq 4
test "$(grep -c '^## Further reading' src/pages/otonality-utonality.md)" -eq 1

# Imports cell has BOTH new imports
grep -q 'import \* as Plot from "npm:@observablehq/plot"' src/pages/otonality-utonality.md
grep -q 'import { playInterval } from "../components/play-interval.js"' src/pages/otonality-utonality.md

# Single synth (Pitfall #2)
test "$(grep -c 'createSynth()' src/pages/otonality-utonality.md)" -eq 1

# Both Further reading URLs
grep -q 'en.xen.wiki/w/Otonality_and_utonality' src/pages/otonality-utonality.md
grep -q 'kylegann.com/wtp' src/pages/otonality-utonality.md

# Pitfall #1 — no hardcoded cents float literals in the chart cell. The two
# canonical cents values that COULD have been hardcoded (386.31 for 5/4, 968.83
# for 7/4) MUST NOT appear inside the chart code block — only in the prose
# paragraph describing the chart.
! grep -nE 'cents:\s*(386\.31|701\.96|968\.83|231\.17|498\.04|813\.69)' src/pages/otonality-utonality.md
```

</verification>

<success_criteria>

The plan succeeds when:

1. Visual: paired Plot chart appears between the otonal "Audition all four notes together…" line and the `## The utonal chord (under-N)` H2. Left subchart shows otonal partials as blue horizontal rules ending in dots; right subchart shows utonal partials as orange horizontal rules ending in dots. Both subcharts share x ∈ [0, 1050]¢. The visual asymmetry of the two chord shapes is immediately apparent.
2. Auditory: 8 per-note play buttons (4 otonal + 4 utonal) render inline at the end of each chord-member bullet. Each button plays the labeled ratio (as a dyad against 1/1 at 440 Hz for 1.5 s — the playInterval defaults). The two chord buttons (otonalBtn, utonalBtn) still work as before.
3. Pedagogical: `## Further reading` section appended at the bottom links the Xenharmonic Wiki entry on Otonality and utonality + Kyle Gann's Well-Tuned Piano notes, matching the precedent style of the other quick-task-added Further reading sections (Harmonic_series, Comma_pump, Pythagorean_comma, EDO, Schisma, Prime_limit, Odd_limit, Monzo).
4. Discipline: kernel-as-source-of-truth honored (Pitfall #1) — chart cents read from `Interval.cents` on existing bindings, never as hardcoded floats. One AudioContext per page (Pitfall #2) — no second `createSynth()`. Reuse over reinvention (Pitfall #5) — playInterval factory used, not reimplemented inline.
5. Build: tsc clean, observable build clean, no new lint errors.

</success_criteria>

<output>
After completion, create `.planning/quick/260512-mk8-otonal-utonal-partials-chart-per-note-pl/260512-mk8-SUMMARY.md` describing:
- Files touched (just `src/pages/otonality-utonality.md`) with line-count delta
- Imports added (Plot, playInterval)
- Chart structure (paired Plot subcharts in flex container; D-33 anchor colors)
- Per-note buttons placement (8 total: 4 otonal + 4 utonal; reuses page synth)
- Further reading links chosen and brief rationale
- Build + test results (tsc, npm run build, page bundle size delta)
- Any decisions made about chart framing (otonal-vs-utonal coloring, x domain choice, y axis order)
</output>
