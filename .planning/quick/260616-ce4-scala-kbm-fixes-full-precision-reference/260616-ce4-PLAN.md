---
phase: quick-260616-ce4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/kbm.ts
  - src/lib/text.ts
  - src/lib/scala.ts
  - src/lib/scala-archive.ts
  - src/lib/__tests__/kbm.test.ts
  - src/lib/__tests__/text.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "parseKbm(writeKbm(m)).referenceHz === m.referenceHz for arbitrary finite positive floats (no toFixed(6) truncation)"
    - "kbmToFrequencies no longer silently drops out-of-range mapped degrees — skipped entries are observable via a diagnostics-returning sibling function"
    - "The existing kbmToFrequencies(scale, kbm): Map<number, number> signature is unchanged so src/index.md keeps compiling"
    - "All three BOM-strip call sites (kbm.ts, scala.ts, scala-archive.ts) use one shared stripBom helper; behavior is byte-identical to before"
    - "No literal BOM character remains in source — only the U+FEFF escape form (\\uFEFF)"
    - "tsc --noEmit passes, vitest run passes, prettier --check passes"
  artifacts:
    - path: "src/lib/text.ts"
      provides: "Shared stripBom(text: string): string helper using the \\uFEFF escape"
      contains: "stripBom"
    - path: "src/lib/kbm.ts"
      provides: "Full-precision referenceHz serialization + diagnostics-returning frequency mapper"
      contains: "kbmToFrequenciesWithDiagnostics"
    - path: "src/lib/__tests__/kbm.test.ts"
      provides: "Tests for #13 full-precision round-trip and #14 surfaced skips"
      contains: "kbmToFrequenciesWithDiagnostics"
    - path: "src/lib/__tests__/text.test.ts"
      provides: "Tests for stripBom helper"
      contains: "stripBom"
  key_links:
    - from: "src/lib/kbm.ts"
      to: "src/lib/text.ts"
      via: "import { stripBom } from './text.js'"
      pattern: "stripBom"
    - from: "src/lib/scala.ts"
      to: "src/lib/text.ts"
      via: "import { stripBom } from './text.js'"
      pattern: "stripBom"
    - from: "src/lib/scala-archive.ts"
      to: "src/lib/text.ts"
      via: "import { stripBom } from './text.js'"
      pattern: "stripBom"
---

<objective>
Three correctness/hygiene fixes in the .kbm kernel, in priority order:

- **#13** writeKbm serializes referenceHz at full precision so it round-trips through parseKbm (today `toFixed(6)` truncates high-precision references).
- **#14** kbmToFrequencies stops silently dropping out-of-range mapped scale degrees; skips become observable via a backward-compatible diagnostics channel.
- **low** Collapse three copies of the BOM-strip regex into one shared `stripBom` helper, eliminating the two remaining literal-BOM-char source hygiene violations.

Purpose: serializer round-trip fidelity (math-precision project constraint at the documented audio-tier float boundary), silent-data-loss elimination, and DRY/hygiene cleanup.
Output: edited `src/lib/kbm.ts`, new `src/lib/text.ts`, edited `src/lib/scala.ts` + `src/lib/scala-archive.ts`, new/extended tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/lib/kbm.ts
@src/lib/scala.ts
@src/lib/scala-archive.ts
@src/lib/__tests__/kbm.test.ts

Key design facts established during planning (do not re-derive):

- parseFloatStrict (kbm.ts line ~118) ACCEPTS `[eE]` exponents, so `String(referenceHz)` output (JS shortest round-trippable form) is always re-parseable. Confirmed safe.
- The ONLY caller of kbmToFrequencies is src/index.md:204, which uses the `Map<number, number>` return + `.get()`. The signature MUST stay backward compatible.
- No `src/lib/text.ts` / `util.ts` / `strings.ts` exists yet — create `src/lib/text.ts`.
- scala-archive.ts already imports from `"./scala.js"`; adding a `"./text.js"` import fits the existing pattern.
- kbm.ts:94 already uses the correct `\uFEFF` escape; scala.ts:248 and scala-archive.ts:60 use a LITERAL BOM char (the hygiene violation to fix).
- Test command: `vitest run`. Type check: `tsc --noEmit`. Format check: `prettier --check`. Tests live in `src/lib/__tests__/*.test.ts`; fixtures in `src/lib/__tests__/fixtures/kbm/`.

Note: throughout this plan, `U+FEFF` / `\uFEFF` denotes the byte-order-mark code point. In source files always write the `\uFEFF` JS string escape — never paste the invisible character itself.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Full-precision referenceHz (#13) + diagnostics-surfaced skips (#14) in kbm.ts</name>
  <files>src/lib/kbm.ts, src/lib/__tests__/kbm.test.ts</files>
  <behavior>
    #13 referenceHz round-trip:
    - parseKbm(writeKbm(m)).referenceHz === m.referenceHz for a value that loses precision under toFixed(6), e.g. 432.0011223344 and 440.000001 (use === / toBe, NOT toBeCloseTo — exact equality is the point).
    - The existing 4-fixture round-trip test (writeKbm describe block) still passes unchanged.
    #14 surfaced skips:
    - kbmToFrequenciesWithDiagnostics(scale, kbm) returns { frequencies, skipped } where a keyMap entry pointing at degree >= degrees.length appears in `skipped` as { midiNote, mapEntry } (or equivalent shape), and is NOT present in `frequencies`.
    - kbmToFrequencies(scale, kbm) still returns Map<number, number> and equals the `.frequencies` of the diagnostics call (delegation, backward compatible).
  </behavior>
  <action>
    #13 (writeKbm, line ~259): Replace `kbm.referenceHz.toFixed(6)` with `String(kbm.referenceHz)` — the JS shortest round-trippable representation. This is safe to re-parse because parseFloatStrict's regex accepts `[eE]` exponents (confirmed during planning), so any String() output (including rare exponential form for extreme magnitudes) is re-parseable. Update the writeKbm doc comment (line ~242): remove the sentence claiming "The 6-decimal float for referenceHz is the Scala convention." and replace it with language stating referenceHz is serialized at full JS precision via String() so it round-trips exactly through parseKbm.

    #14 (kbmToFrequencies, lines ~300-337): Introduce a sibling function `kbmToFrequenciesWithDiagnostics(scale: Scale, kbm: KbmMapping)` returning `{ frequencies: Map<number, number>, skipped: Array<{ midiNote: number; mapEntry: number }> }`. Move the existing mapping loop body into it. Where today the loop does `const iv = degrees[mapEntry]; if (iv === undefined) continue;` (lines ~328-329), instead push `{ midiNote: n, mapEntry }` onto `skipped` before `continue`, so the out-of-range degree is observable rather than silently dropped. Then make the existing `kbmToFrequencies(scale, kbm): Map<number, number>` delegate: `return kbmToFrequenciesWithDiagnostics(scale, kbm).frequencies;` — preserving the exact existing signature and return contract (only caller is src/index.md:204 via `.get()`; confirmed during planning). Export both functions. Add a short doc comment on the new function explaining the design rationale: callers needing diagnostics opt in, the legacy Map signature is untouched, nothing is dropped silently. Honor existing module discipline — no `!` non-null assertions (D-16), no Fraction import (R-01), referenceHz stays a float (audio-tier boundary, S-3 / Pitfall #1 — do NOT convert to Fraction/BigInt).

    Tests (src/lib/__tests__/kbm.test.ts): match existing Vitest style (describe/it, fixtures via readFixture, Scale + Interval from "../scale.js" / "../interval.js"). Add a writeKbm test asserting exact `toBe` round-trip of referenceHz for 432.0011223344 and 440.000001 (construct a KbmMapping in-memory or clone a parsed fixture and overwrite referenceHz; do NOT use toBeCloseTo). Add a kbmToFrequenciesWithDiagnostics test: build a scale and a kbm whose keyMap contains a degree >= degrees.length, assert that note appears in `skipped` (with its midiNote) and is absent from `frequencies`, and assert legacy-vs-diagnostics equivalence (kbmToFrequencies returns the same Map as `.frequencies` and omits the skipped note).
  </action>
  <verify>
    <automated>cd "/Users/taylorbrook/Dev/Tuning Systems" && npx vitest run src/lib/__tests__/kbm.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>#13: referenceHz round-trips exactly for high-precision values; writeKbm doc comment updated. #14: kbmToFrequenciesWithDiagnostics surfaces skipped out-of-range degrees; kbmToFrequencies keeps its Map<number, number> signature and src/index.md still type-checks. All kbm tests + tsc pass.</done>
</task>

<task type="auto">
  <name>Task 2: Shared stripBom helper (low) + tests, dedupe three call sites</name>
  <files>src/lib/text.ts, src/lib/kbm.ts, src/lib/scala.ts, src/lib/scala-archive.ts, src/lib/__tests__/text.test.ts</files>
  <action>
    Create `src/lib/text.ts` exporting `export function stripBom(text: string): string` that returns `text.replace(/^\uFEFF/, "")`. Use ONLY the `\uFEFF` escape — never a literal BOM character in source (project hygiene rule, stated in kbm.ts's normalizeLines comment). Add a brief doc comment explaining the BOM is U+FEFF and stripping is effective on the first position only.

    Rewire all three call sites to import and use it (`import { stripBom } from "./text.js";` — Framework `.js`-extension convention for TS imports):
    - kbm.ts normalizeLines (line ~94): replace `text.replace(/^\uFEFF/, "")` with `stripBom(text)`.
    - scala.ts normalizeLines (line ~248): replace the LITERAL-BOM `text.replace(/^…/, "")` with `stripBom(text)` (this removes a literal-BOM-char hygiene violation).
    - scala-archive.ts (line ~60): the chained `sclText.replace(/^…/, "").replace(/\r\n?/g, "\n").split("\n")` becomes `stripBom(sclText).replace(/\r\n?/g, "\n").split("\n")` (also removes a literal-BOM-char hygiene violation). Keep the rest of each chain identical — this is a pure refactor, zero behavior change.

    Tests (src/lib/__tests__/text.test.ts): match existing Vitest style. Assert stripBom removes a leading BOM, leaves BOM-free text untouched, and only strips the first position (an internal BOM is preserved). Construct test inputs using the `\uFEFF` escape — no literal BOM character in the test source.
  </action>
  <verify>
    <automated>cd "/Users/taylorbrook/Dev/Tuning Systems" && npx vitest run && npx tsc --noEmit && npx prettier --check "src/lib/text.ts" "src/lib/kbm.ts" "src/lib/scala.ts" "src/lib/scala-archive.ts" "src/lib/__tests__/text.test.ts" "src/lib/__tests__/kbm.test.ts"</automated>
  </verify>
  <done>One shared stripBom helper in src/lib/text.ts; all three call sites import and use it; no literal BOM char remains in source (only the \uFEFF escape). Full vitest suite + tsc + prettier --check pass. Behavior unchanged from before the refactor.</done>
</task>

</tasks>

<verification>
- `npx vitest run` — full suite green (existing 723 tests plus the new #13/#14/stripBom tests).
- `npx tsc --noEmit` — clean (proves src/index.md still compiles against the unchanged kbmToFrequencies signature).
- `npx prettier --check src/lib` — formatting clean on all touched files.
- Manual grep gate: `grep -rn $'\uFEFF' src/lib --include=*.ts | grep -v 'uFEFF'` returns ZERO matches (no literal BOM characters remain — only the \uFEFF escape, which `grep -v 'uFEFF'` filters out).
</verification>

<success_criteria>
- #13: parseKbm(writeKbm(m)).referenceHz === m.referenceHz exactly for 432.0011223344 and 440.000001; writeKbm doc comment no longer claims a 6-decimal Scala convention.
- #14: out-of-range mapped degrees are surfaced via kbmToFrequenciesWithDiagnostics({ frequencies, skipped }) and never silently dropped; kbmToFrequencies keeps its Map<number, number> contract so src/index.md is untouched.
- low: a single stripBom helper backs all three former call sites; the two literal-BOM-char hygiene violations (scala.ts, scala-archive.ts) are gone; pure refactor with zero behavior change.
- tsc + full vitest suite + prettier --check all pass.
</success_criteria>

<output>
Create `.planning/quick/260616-ce4-scala-kbm-fixes-full-precision-reference/260616-ce4-SUMMARY.md` when done.
</output>
