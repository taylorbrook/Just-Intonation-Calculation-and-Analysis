---
phase: quick-260615-w5t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/audio/synth.ts
  - src/components/audio-panel.ts
  - src/index.md
  - src/audio/__tests__/synth.test.ts
autonomous: true
requirements: ["#6", "#15", "#16"]

must_haves:
  truths:
    - "A held drone is stopped when the dashboard cell re-runs after a baseHz or scale change (no orphaned voice)."
    - "ctx.resume() failures log to console at most once, and resume is re-attempted on the next gesture whenever the context is not running."
    - "When Web Audio is unavailable, the synth warns once and the drone button stays in its visible 'off' state."
    - "startDrone returns null when no voice actually started, so the drone button never falsely shows 'Drone on'."
  artifacts:
    - path: "src/audio/synth.ts"
      provides: "startDrone null-on-failure contract, once-only resume/.catch logging, non-running resume retry, once-only no-Web-Audio warning"
      contains: "(() => void) | null"
    - path: "src/components/audio-panel.ts"
      provides: "disposeAudioPanel + WeakMap cleanup registry, conditional button-state flip on non-null startDrone return"
      contains: "disposeAudioPanel"
    - path: "src/index.md"
      provides: "audioPanel cell wiring that disposes the prior panel on invalidation"
      contains: "disposeAudioPanel"
  key_links:
    - from: "src/index.md (audioPanel cell ~line 209)"
      to: "src/components/audio-panel.ts disposeAudioPanel"
      via: "invalidation.then capturing the panel element"
      pattern: "disposeAudioPanel\\("
    - from: "src/components/audio-panel.ts drone button"
      to: "src/audio/synth.ts startDrone"
      via: "store stop callback + flip aria-pressed only when return value is non-null"
      pattern: "startDrone\\("
---

<objective>
Fix three in-browser audio-layer defects: a held-drone voice leak on baseHz/scale change (#6), silent `ctx.resume()` failures with no retry (#16), and silent no-Web-Audio failure where the drone button lies about playing (#15).

Purpose: Stop orphaned drone voices, surface (once) audio init failures, and keep the drone button's visible state honest. All three are user-facing audio-correctness bugs.
Output: Updated `synth.ts`, `audio-panel.ts`, `index.md` wiring, and a corrected drone test. Three-layer discipline preserved (synth.ts imports nothing from src/lib or src/components).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/audio/synth.ts
@src/components/audio-panel.ts
@src/index.md
@src/components/scale-compare.ts
@src/audio/__tests__/synth.test.ts
@src/components/__tests__/test-utils.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: synth.ts — startDrone null contract, once-only resume/.catch + non-running retry, once-only no-Web-Audio warning (#16, #15a)</name>
  <files>src/audio/synth.ts</files>
  <behavior>
    - startDrone(NaN) (or any non-playable Hz) returns null; no noteOn; activeVoices stays 0.
    - startDrone after dispose returns null; no new AudioContext.
    - startDrone(440) on a healthy synth still returns a callable stop function; activeVoices increments to 1; stop() decrements to 0 and is idempotent (existing tests must still pass).
    - ctx.resume() rejection logs to console.warn at most once across many gestures (assert via a rejecting resume mock + console.warn spy: called once after two ensure-triggering calls).
    - When resolveAudioCtxCtor() returns null, console.warn fires at most once and ensure() returns false (drone returns null in that environment).
  </behavior>
  <action>
    Three edits, all inside `createSynth`, honoring the existing once-only `arpeggioTruncationWarned` closure-boolean discipline.

    (1) #15a no-Web-Audio warning: add a closure-scoped `let noWebAudioWarned = false;` alongside the other once-flags (near `arpeggioTruncationWarned`). In `ensure()`, at the `if (!Ctx) return false;` branch, before returning, if `!noWebAudioWarned` set it true and `console.warn` a single message identifying the module (prefix `[audio/synth]`) stating Web Audio is unavailable in this environment so playback is disabled.

    (2) #16 resume robustness: add a closure-scoped `let resumeWarned = false;`. Replace BOTH `void ctx.resume()` sites (the already-initialised branch ~line 214 and the fresh-context branch ~line 225) with a call to a small local helper, e.g. `const tryResume = () => { ctx?.resume().catch(() => { if (!resumeWarned) { resumeWarned = true; console.warn("[audio/synth] AudioContext.resume() failed; audio may be blocked until the next user gesture."); } }); };` Keep it fire-and-forget (do NOT await). In the already-initialised branch, change the guard from `if (ctx.state === "suspended")` to re-attempt whenever the context is not running: `if (ctx.state !== "running") tryResume();` — this covers iOS Safari's "interrupted" state and retries on the next ensure(). In the fresh-context branch keep the `=== "suspended"` semantics by calling `tryResume()` where the old `void ctx.resume()` was (a brand-new context is "suspended"); using `!== "running"` there is also acceptable. Preserve the existing Pitfall #8 "no await between gesture and this call" comment intent.

    (3) #15 startDrone null contract: change the `SynthHandle.startDrone` signature to `startDrone(hz: number): (() => void) | null;` and update its JSDoc to state it returns the stop callback, or `null` when no voice started (disposed, unplayable Hz, or no Web Audio). In the `startDrone` implementation, change all three early-return branches — `if (disposed)`, `if (!isPlayableHz(hz))`, and `if (!ensure() || !synth)` — to `return null;` instead of `return () => {};`. The success path still returns the stop closure.

    Do NOT import anything from src/lib or src/components (three-layer discipline). Do NOT touch playNote / playNotes / playArpeggio return contracts.
  </action>
  <verify>
    <automated>cd "/Users/taylorbrook/Dev/Tuning Systems" && npm run lint:types</automated>
  </verify>
  <done>tsc --noEmit passes. SynthHandle.startDrone is typed `(() => void) | null`; all three early-returns return null; both resume sites route through a once-warning `.catch`; the already-initialised guard uses `!== "running"`; no-Web-Audio path warns once before returning false. No src/lib or src/components imports added.</done>
</task>

<task type="auto">
  <name>Task 2: audio-panel.ts — disposeAudioPanel registry + honest drone button (#6, #15b)</name>
  <files>src/components/audio-panel.ts</files>
  <action>
    Mirror the EXISTING `disposeScaleCompare` precedent in scale-compare.ts (lines 200-213 + 520-526) exactly.

    (1) #6 disposer: add a module-level `const AUDIO_PANEL_CLEANUPS = new WeakMap<HTMLElement, () => void>();` and an exported `export function disposeAudioPanel(el: HTMLElement): void { const fn = AUDIO_PANEL_CLEANUPS.get(el); if (fn) { fn(); AUDIO_PANEL_CLEANUPS.delete(el); } }` (same shape as disposeScaleCompare). Place the WeakMap + function above `audioPanel` with a short doc comment explaining page cells call it from `invalidation.then(...)`. Inside `audioPanel`, BEFORE `return root;`, register cleanup: `AUDIO_PANEL_CLEANUPS.set(root, () => { stopDrone?.(); stopDrone = null; });` so a sounding drone is released when the panel is discarded. Keep `audioPanel`'s return type as `HTMLElement` — no call-site signature break.

    (2) #15b honest button: the drone-on branch in the click handler currently does `stopDrone = synth.startDrone(baseHz);` then flips text + aria-pressed unconditionally. Change it to capture the result first and only flip when non-null: assign `const started = synth.startDrone(baseHz);` then `if (started) { stopDrone = started; droneBtn.textContent = ...; droneBtn.setAttribute("aria-pressed", "true"); }` — when `started` is null, leave `stopDrone` null and the button in its "🔇 Drone off" / aria-pressed="false" state (do not change text/aria). Update the `let stopDrone: (() => void) | null = null;` declaration comment (Pitfall #9) to note startDrone may now return null when no voice started, in which case the button stays off. Keep the stop branch (`if (stopDrone)`) unchanged.

    Three-layer discipline: continue importing only the `SynthHandle` / `Scale` TYPES (no runtime imports from src/audio or src/lib added).
  </action>
  <verify>
    <automated>cd "/Users/taylorbrook/Dev/Tuning Systems" && npm run lint:types</automated>
  </verify>
  <done>tsc --noEmit passes. `disposeAudioPanel` is exported and backed by a module-level WeakMap mirroring disposeScaleCompare. Cleanup registered on `root` calls `stopDrone?.()`. Drone-on branch only flips text/aria-pressed/stores stop when startDrone returns non-null. audioPanel still returns HTMLElement.</done>
</task>

<task type="auto">
  <name>Task 3: index.md wiring + drone test update (#6 wiring, contract test)</name>
  <files>src/index.md, src/audio/__tests__/synth.test.ts</files>
  <action>
    (1) #6 wiring in index.md: in the import cell (line 16) change `import { audioPanel } from "./components/audio-panel.js";` to also import the disposer: `import { audioPanel, disposeAudioPanel } from "./components/audio-panel.js";`. In the audioPanel cell at line 209 (`if (scale) display(audioPanel(scale, synth, effectiveBaseHz));`), capture the panel element and dispose it on invalidation so the OLD panel's held drone is released when the cell re-runs (new baseHz/scale). Rewrite the cell to: build the element into a const, `display(...)` it, then register `invalidation.then(() => disposeAudioPanel(el));`. Follow the existing synth cell's `invalidation.then(...)` style already in this file (index.md:42-46). Keep it guarded by `if (scale)`.

    (2) Contract test fix in synth.test.ts: the "rejects bad Hz in startDrone too" test (~line 446) currently does `const stop = synth.startDrone(NaN); ... expect(typeof stop).toBe("function"); stop();`. Under the new null-on-failure contract this returns null. Update it to `const stop = synth.startDrone(NaN);` then `expect(mockSynthInstance.noteOn).not.toHaveBeenCalled();`, `expect(synth.activeVoices).toBe(0);`, and `expect(stop).toBeNull();` (remove the `stop()` call and the `typeof === "function"` assertion). Leave the happy-path startDrone tests (~lines 361-381) unchanged — they still expect a callable. Optionally add a brief comment that null is the no-voice contract.

    Note (no change required): src/components/__tests__/test-utils.ts:30 mocks `startDrone: vi.fn(() => () => {})` — still assignable to `(() => void) | null`, so it stays type-compatible. A future test may add a "returns null" stub case, but it is not needed for this fix.
  </action>
  <verify>
    <automated>cd "/Users/taylorbrook/Dev/Tuning Systems" && npm run lint:types && npm run test</automated>
  </verify>
  <done>tsc --noEmit passes; vitest suite passes. index.md imports disposeAudioPanel and the audioPanel cell calls disposeAudioPanel(el) in invalidation.then(...). The bad-Hz startDrone test asserts the return is null (no noteOn, activeVoices 0) instead of calling it. Happy-path drone tests unchanged and still green.</done>
</task>

</tasks>

<verification>
- `npm run lint:types` (tsc --noEmit) passes across all three source edits.
- `npm run test` (vitest run) passes, including updated bad-Hz startDrone contract test and unchanged happy-path drone + dispose tests.
- Three-layer discipline intact: `src/audio/synth.ts` adds no imports from src/lib or src/components.
- No call-site signature break: `audioPanel(...)` still returns `HTMLElement`.
</verification>

<success_criteria>
- #6: Discarding an audioPanel (cell re-run / invalidation) releases any held drone voice via `disposeAudioPanel(el)` → `stopDrone?.()`; no orphaned voice survives a baseHz/scale change.
- #16: `ctx.resume()` failures log once (closure-scoped boolean) and resume retries on the next ensure() whenever `ctx.state !== "running"` (covers iOS "interrupted").
- #15: No-Web-Audio environment warns once; `startDrone` returns `null` when no voice started and the drone button stays visibly "off" (no false "Drone on").
- tsc + vitest both green.
</success_criteria>

<output>
Create `.planning/quick/260615-w5t-fix-held-drone-leak-and-audio-silent-fai/260615-w5t-SUMMARY.md` when done.
</output>
