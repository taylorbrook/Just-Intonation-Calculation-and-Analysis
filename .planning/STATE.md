---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: MVP
status: shipped
stopped_at: ""
last_updated: "2026-05-13T14:55:00.000Z"
last_activity: 2026-05-13 -- quick-260513-a7c: add Tenney height theory page (`log₂(n·d)` scalar measure of harmonic complexity; companion to the EDO ranker's Tenney-weighted error metric used in `src/lib/edo.ts`/`bestEdosForScale`) — new `src/pages/tenney-height.md` covering Definition (KaTeX `log₂(n·d)` + monzo-form `Σ |e_i| · log₂(p_i)`, with explicit note that the kernel uses natural log and the two forms are ranking-equivalent via constant factor `ln 2`), Worked examples (9-row plain-DOM table with 1/1, 2/1, 3/2, 5/4, 7/4, 9/8, syntonic comma, schisma, Pythagorean comma — `n·d` rendered via `BigInt(n)*BigInt(d)` then `.toLocaleString()` for 12-digit comma's 278,628,139,008 readability; `log₂` derived in-cell from `tenneyHeight(iv.monzo) / Math.LN2` — zero hardcoded float literals in data rows; 3-decimal precision throughout), Cents vs Tenney height (Plot scatter of 32 canonical 7-/11-/13-/17-limit ratios + syntonic comma + schisma, x = cents [0, 1220], y = log₂(n·d) [0, 32] linear axis so the schisma at ~30 stays visible as the chart's pedagogical punchline; color-encoded by prime-limit via `primeLimitOfMonzo(iv.monzo)` with project palette (`#888888`/`#4269d0`/`#efb118`/`#ff725c`/`#6cc5b0`/`#a463f2`/`#9c6b4f`); `Plot.dot` + `Plot.text` ratio labels + `Plot.ruleY([0])` baseline + per-point hover title with cents/TH/limit), Tenney-weighted error in the EDO ranker (3-paragraph explanation of `Σ |centsError| / max(1, tenneyHeight(monzo))` from `src/lib/edo.ts:98-126` as a musically-biased weighting favoring simple-ratio fit over high-complexity-comma fit; explicit comparison to max-error and RMS-error metrics; `Math.max(1, …)` clamp documented as numerical safety guard for the 1/1 case where `tenneyHeight([]) = 0` and the 2/1 case where TH ≈ 0.693 < 1), See also (prose links to Monzos, prime-limits, EDO approximations, Analysis, dashboard), Further reading via `furtherReading([...])` listing James Tenney *John Cage and the Theory of Harmony* (1983, PlainSound Music Edition PDF at plainsound.org/pdfs/JC&ToH.pdf — canonical Sabat/von Schweinitz imprint; archive.org PDF requested by brief does not exist under common slugs, verified 404 on `/details/john-cage-and-the-theory-of-harmony` and variants) + Xenharmonic Wiki Tenney_height (note: page-name redirects to /w/Tenney_norm); kernel-exact anchors throughout (every cents and TH value derived in-cell from `new Interval(...).cents` / `tenneyHeight(iv.monzo) / Math.LN2`; zero hardcoded canonical-cent or canonical-TH float literals — grep-clean); no synth cell (glossary-style reference page, no audio); plain-DOM tables via createElement + textContent (T-02-22/T-02-23 XSS discipline); `tenneyHeight` + `primeLimitOfMonzo` imported from `../lib/monzo.js` (R-01 guard — never from `xen-dev-utils` directly); critical correction caught during research: user's brief gave the Pythagorean-comma value as `log₂(279,936,432,033,792) ≈ 47.987` but the correct `n·d` for 531441/524288 (both already coprime) is 278,628,139,008 → `log₂ ≈ 38.020` — page uses the correct value, verified via node `Math.log2(BigInt(531441)*BigInt(524288))`; one-line append to observablehq.config.ts inserting `{ name: "Tenney height", path: "/pages/tenney-height" }` as the LAST entry under "Theory notes" per user's "the last Theory notes page" instruction (after "The septimal comma" at line 32; brief's "after EDO approximations" interpreted as "logically downstream of EDO pedagogy", not literal-array-position); kernel side (`src/lib/`, `src/audio/`, `src/components/`) untouched — pure presentational page edit + 1-line config wiring; `npm run build` clean — 20 pages rendered (was 19), 107 links validated (was 101), zero errors; `npx tsc --noEmit` exit 0 (kernel math sanity-checked via node probe — 3/2 → log₂ = 2.5850, 5/4 → 4.3219, 81/80 → 12.6618 all match the page's worked-examples table to 4 decimals); flag --research triggered focused researcher first to verify the xen-dev-utils tenneyHeight source (`node_modules/xen-dev-utils/dist/core.js:418` returns `Math.log(n) + Math.log(d)` — natural log), pre-compute all 32 scatter values, sanity-check the brief's worked examples, and verify Further reading URLs (plainsound `200 OK` via curl; xen.wiki Cloudflare-blocks automated fetches but content confirmed via WebSearch) (Werckmeister III 1691 / Kirnberger III 1779 / Vallotti 1779 — historical bridge between meantone and 12-EDO) — new src/pages/well-temperament.md (480 lines) with per-temperament 12-fifth-size profiles (sum=8400.000000000¢), 12-key major-third profiles, Plot bar charts (fifths-deviation + major-thirds-per-key, 3 pairs), six triad-audition buttons (C-major + F♯-major × 3 temperaments) via inline `playTriadAt(label, rootCents, thirdCents, fifthCents)` factory mirroring meantone.md's `playTempered`, kernel-exact anchors throughout (pureFifth / pythComma / syntonic / schisma / pure5Third / pythThird — zero hardcoded canonical-cent float literals in source per grep gate), page-level synth cell owns the only AudioContext (Pitfall #2), prerequisite aside (matching quick-260513-91h cross-page pass) pointing to ./meantone + ./pythagorean-comma, Further reading via furtherReading([...]) listing Xen Wiki /w/Well_temperament + Bradley Lehman larips.com + Mark Lindley *Lutes, Viols and Temperaments* (Cambridge UP 1984 ISBN 0-521-28950-5); numerical anchors verified Werckmeister C-major 3rd = 390.225¢ + F♯-major 3rd = 407.820¢ (= 81/64 exactly — purest Pythagorean third lands at F♯), Kirnberger C-major 3rd = 386.314¢ (= pure 5/4, the headline property), Vallotti A-major 3rd = exactly 400.000¢ (12-TET coincidence at rotation's center); one-line edit to observablehq.config.ts inserting `{ name: "Well-temperaments", path: "/pages/well-temperament" }` between Meantone and EDO approximations at line 29; spiral-of-fifths.ts intentionally NOT embedded (research confirmed `temperedFifthCents` accepts only a single scalar, not the per-fifth array needed for well-temperaments — Plot bar charts chosen); kernel side (src/lib/, src/audio/, src/components/) untouched; 10 grep gates + tsc-baseline + build-clean + rendered-HTML check all pass; `npm run build` clean — 19 pages (was 18), 101 links validated (was 95); tsc baseline unchanged (5 pre-existing `npm:` specifier errors only); flag --research triggered focused researcher first to source exact published fifth-size values from xen.wiki + tonalsoft + larips and verify all three sets close at 8400¢; commit 4202b6a; previously: quick-260513-8jw: add reusable `furtherReading(items)` presentational helper (new src/components/further-reading.ts — Pattern 1 pure-presentational factory accepting `{ title: string; url: string; note?: string | Node }[]` returning `<section class="further-reading"><ul>...</ul></section>` with one `<li>` per item; XSS-safe via `createElement` + `textContent` for string notes, with `Node` notes appended as-is so callers can pass `` html`text with ${tex`81/80`} inline` `` to preserve inline KaTeX / links / bold; external URLs auto-get `rel="noopener noreferrer"`; the page's Markdown `## Further reading` heading stays in Markdown so Framework TOC continues to surface it — the helper renders the list body only) + matching src/components/further-reading.css (disc bullets at 1.5rem indent, 0.5rem item spacing, line-height 1.5, link weight 500, `.further-reading__note` muted via `--theme-foreground-alt` matching project theme-token convention) + one-line `@import "./components/further-reading.css";` in src/styles.css after monzo-builder.css; adoption on seven theory pages + backfill of missing classic-text references — monzos.md adds David B. Doty *The Just Intonation Primer* (3rd ed., Just Intonation Network, 2002), odd-limits.md adds Harry Partch *Genesis of a Music* (2nd ed., Da Capo Press, 1974) on archive.org mirroring prime-limits.md URL form, prime-limits.md adoption-only (already had XW + Partch), otonality-utonality.md adoption-only (already had XW + Kyle Gann WTP essay), comma-pump.md adoption-only with `html`-tagged-template note body preserving inline `${tex`81/80`}` in the Ben Johnston Suite for Microtonal Piano (1977) row (Lubman NWR 80637 recording reference preserved), pythagorean-tuning.md gains a brand-new `## Further reading` H2 (was missing entirely — the only target page without one) with XW Pythagorean_tuning entry + Hermann von Helmholtz *On the Sensations of Tone* (Ellis tr., 1875) on IMSLP using the `Helmholtz,_Hermann_von` URL form (verified per prior task 260512-ilb that the bare-`Hermann` form returns 404), meantone.md adopts the helper with `html`-tagged-template note body preserving inline `${tex`n \in \mathbb{R}`}` in the XW Meantone_family row AND adds Easley Blackwood *The Structure of Recognizable Diatonic Tunings* (Princeton UP, 1985) as a second item — the modern canonical text on meantone temperament families that lays out the 1/n-comma continuum as recognizability bands; each page gets `import { furtherReading } from "../components/further-reading.js"` joined inline to its existing single import cell (Framework module-scope rule — a second import cell would re-bind `Interval`, etc.), Markdown `-` bullets under `## Further reading` replaced with a single JS code fence calling `furtherReading([...])` keeping the H2 in Markdown for TOC integrity; eight atomic commits on main (d9fc91f component + CSS + styles.css, f7bb735 monzos + Doty, 0b536e1 odd-limits + Partch, 8d0e785 prime-limits adoption, 630301f otonality-utonality adoption, fe379f3 comma-pump adoption + html-template Johnston note, 1d0f626 pythagorean-tuning new section + XW + Helmholtz, 8bbb00e meantone + Blackwood); HARD CONSTRAINT honored throughout: src/lib/* and src/audio/* untouched, no kernel changes — pure presentational + per-page Markdown edits; out-of-scope pages with existing Further-reading sections (syntonic-comma, pythagorean-comma, schisma, septimal-comma, harmonic-series, edo-approximation, commas) left untouched per user's explicit list — they can adopt the helper in a follow-up task without disrupting their existing references; tsc baseline unchanged (5 pre-existing `npm:` specifier errors only); vitest 302/302 across 24 files; prettier clean on all 9 touched files; eslint clean; observable build clean — 18 pages rendered, 88 links validated; furtherReading module emitted as dist/_import/components/further-reading.<hash>.js with the expected `export function furtherReading(items)` signature; rendered HTML grep gate `grep -c 'furtherReading\|further-reading' dist/pages/<page>.html` returns 8 on each of the 7 target pages (import + call site + module reference); per-page bundle delta 1–8 kB Imports (the helper itself is tiny — most of each page's bundle is dominated by inherited Plot / d3 / xen-dev-utils stacks already on the page); previously: quick-260512-udc: bring four comma pages (pythagorean / syntonic / schisma / septimal) to consistent 5-section structure — new src/pages/commas.md indexing all 16 named commas from src/lib/commas.ts grouped by canonical prime-limit (3-limit Pythagorean comma + Mercator's comma; 5-limit syntonic/schisma/diaschisma/diesis/greater diesis/kleisma; 7-limit septimal comma + septimal kleisma + harmonic seventh comma + jubilisma + ragisma + breedsma; 11-limit rastma + undecimal comma) — kernel-bound throughout: every Interval comes from `commaByName(c.name)!` (R-01 / Pitfall #6 — source-table is canonical, BigInt-Fraction round-trip exact even for Mercator's 25-digit ratio), prime-limit derived at runtime as "last nonzero index of `iv.monzo`" mapped through `[2,3,5,7,11,13,17,19]` so the Pythagorean comma and Mercator's comma classify into 3-limit despite the source file organizing them under `// 5-limit` comment header; per-section DOM tables built via `buildCommaTable(entries)` helper (single `<table>` per section, createElement + textContent throughout — T-02-22/T-02-23 XSS discipline) with columns Name | Ratio | Monzo | Cents | Limit | ▶: Name cell renders `<a href>` for the four commas with dedicated pages (syntonic comma → /pages/syntonic-comma, Pythagorean comma → /pages/pythagorean-comma, schisma → /pages/schisma, septimal comma → /pages/septimal-comma) via DEDICATED_PAGES Map exact-key lookup and plain text for the other 12; Ratio cell appends `ratioPill(iv, {showCents: false})` (cents has its own column); Monzo cell appends the HTMLElement returned by `tex\`\begin{bmatrix} ${iv.monzo.join(' & ')} \end{bmatrix}\rangle\`` on the canonical (minimal-length) monzo; Cents cell shows `iv.cents.toFixed(3)` (3-decimal precision so the breedsma 0.721¢ and ragisma 0.396¢ remain distinguishable from zero); Limit cell shows the canonical prime-limit; Play cell appends `playInterval(iv, synth)` (▶ only, no label — labeling would crowd the row); page-level synth cell owns the only AudioContext (Pitfall #2 honored, exactly one `createSynth()` call site); no `@observablehq/plot` import — this is a tabular index, not a viz, and the bundle stays at 16 kB Page / 391 kB Imports matching the no-Plot family (syntonic-comma 14kB/392kB, pythagorean-tuning 16kB/400kB); footer mirrors syntonic-comma.md / pythagorean-comma.md / schisma.md / septimal-comma.md convention — `## See also` links the four dedicated pages + /pages/monzos (canonical bra-ket primer) + /pages/comma-pump + dashboard; `## Further reading` with en.xen.wiki/w/Comma as the jump-off into the wider regular-temperament literature; one-line edit to observablehq.config.ts inserting `{ name: "Commas (glossary)", path: "/pages/commas" }` under Theory notes immediately after Comma pump (alphabetical slot — "Comma pump" < "Commas (glossary)" < "EDO approximations"); single atomic feat commit 69b7a88; tsc clean; `npm run build` clean — 18 pages (was 17), 88 links validated (was 79; the new page contributes 9 new internal links to the four dedicated comma pages + /pages/monzos + /pages/comma-pump + / + xen.wiki); grep gate `! grep -nE '[0-9]+\.[0-9]+¢' src/pages/commas.md` clean (no hardcoded float-cents literals in prose — every cents value derives at the .cents display boundary); previously: quick-260512-ngj: meantone.md additions — Plot horizontal cents-axis strip chart placed BEFORE the variants table showing six tempered fifths (1/3, 2/7, 1/4, 1/5, 1/6, 1/8) as dashed-blue `Plot.ruleX` verticals + `Plot.dot` heads + per-row alternating-`dy` 1/n labels against a 0..`Math.ceil(pureFifth.cents)` axis with a pure-3/2 reference rule at the right edge and a red `Plot.link` horizontal bracket spanning the syntonic comma from `pureFifth.cents − syntonic.cents` to `pureFifth.cents` (all `.cents` from existing `pureFifth`/`syntonic` Interval bindings + new `verheijen`/`eighth`/`zarlino` tempered-fifth bindings — Pitfall #1 honored, no float-cents literals in chart data rows) + variants table extended 3→6 rows via `verheijen = tempered(5)` / `eighth = tempered(8)` / `zarlino = tempered(7 / 2)` (2/7-comma encoded as n=7/2 — kernel-true substitution into `fifth = pureFifth.cents − syntonic.cents/n` with no `tempered` factory change) and three new rows appended to the local `rows` array in the variantsTable IIFE preserving createElement + textContent shape; final order 1/4 Aron → 1/3 Salinas → 1/6 Silbermann → 1/5 Verheijen → 2/7 Zarlino → 1/8; signature-property cells one-line each + new `## The wolf at k=12 (1/4-comma meantone)` H2 between the third 1/6-comma bullet and the existing `## Audition` H2 embedding `${spiralOfFifths(12, { temperedFifthCents: quarter.fifth, highlightWolf: true })}` — spiral component's tempered branch drops ratio labels (1/4-comma fifth is irrational), shows signed cents-from-12-TET per node, dashed red wolf chord between k=12 and k=0 visualizes the ≈ -40.7¢ closure undershoot (mirror of pythagorean-comma.md's +23.46¢ Pythagorean-comma overshoot via the same component) + `## Further reading` H2 after `## See also` with en.xen.wiki/w/Meantone_family (regular-temperament family generated by tempering out the syntonic comma; full continuum of comma fractions; historical variants as points on that line); new imports `spiralOfFifths` + `* as Plot` joined inline to existing single import cell (Framework module-scope rule); spiral-of-fifths.css already globally imported via src/styles.css:32 from prior quick-260512-fxk wiring — no styles.css edit; existing synth/kernel-anchors/tempered-factory/playTempered/playPure+Quarter+PythMajor3 cells unchanged byte-for-byte (Pitfall #2 — one createSynth call site); single-file additive edit src/pages/meantone.md across three feat commits + merge: f78369d variants table, 4478eff Plot chart, 4c5b540 spiral wolf + Further reading; chart-cell deviations (executor discretion): Plot.ruleY with x1/x2 args → Plot.link for the syntonic-comma horizontal bracket; per-row alternating `dy` callback for label collision avoidance; tsc baseline unchanged (5 pre-existing `npm:` specifier errors only); vitest 302/302 across 24 files; build clean 79 links validated; meantone page bundle 25→34 kB and 391→920 kB matching Plot-using pages footprint; previously: quick-260512-sxc: add simultaneous-A/B `playDyad(harmonicSeventh, pythagoreanMinorSeventh, …)` button (≈12.222 Hz beat at A=440 — roughly twice the syntonic-comma rate, matching 64/63's roughly-twice-21.5¢ size) + inline `beatHz` IIFE derived at audio boundary `Math.abs(baseHz * Number(pythagoreanMinorSeventh.fraction.valueOf()) − baseHz * Number(harmonicSeventh.fraction.valueOf()))` surfaced via `${beatHz.toFixed(3)}` (Pitfall #1 — no `12.222` literal in prose) + small Plot strip chart on src/pages/septimal-comma.md showing partial-7 (7/4 ~968.83¢ blue) and stack-of-two-4/3s (16/9 ~996.09¢ orange) on a shared cents axis (domain [955, 1010]¢) with dashed `Plot.ruleX` markers, stacked Plot.text labels, and `↔ ${septimal.cents.toFixed(2)}¢ (64/63)` red `#c45656` gap annotation centered between the two pitches — all `.cents` read once at data-row construction from existing Interval bindings, no hardcoded cents literals (Pitfall #1) + `> **Tempered out by.**` blockquote callout listing 22-EDO + Superpyth (a.k.a. "archy" in 2.3.7) + Dominant temperament (the 7-limit meantone supported by 12-EDO where 16/9 = 7/4) with explanatory clause that huygens septimal meantone / 31-EDO is the contrast case (preserves the 64/63 distinction by design) — callout list factually corrected from user's originally-stated `22-EDO, 31-EDO, septimal meantone` after Xen Wiki + Wikipedia research confirmed 31-EDO patent val maps 7/4 → step 25 and 16/9 → step 26 (so 64/63 → step 1, NOT tempered out); user approved corrected list before edit + `## Further reading` H2 with en.xen.wiki/w/64/63 + en.xen.wiki/w/Archytas_clan + Andrew Barker *Greek Musical Writings, Vol. II: Harmonic and Acoustic Theory* (CUP, 1989) print citation for Archytas of Tarentum's three tetrachords (enharmonic 28:27:36:35:5:4, chromatic 28:27:243:224:32:27, diatonic 28:27:8:7:9:8); single-file additive edit src/pages/septimal-comma.md +117/-0 (no refactor of existing imports/synth/three-Interval-bindings/${tex} header math/audition bullets/`## In monzos`/`## See also` — byte-identical in their ranges); new `import { playDyad }` + `import * as Plot` joined inline to existing single import cell (Framework module-scope rule); reuses existing `harmonicSeventh`, `pythagoreanMinorSeventh`, `septimal` Interval bindings + page-level `synth` — Pitfall #2 honored (one createSynth call site even though token appears 2× via comment on line 17); chart color encoding harmonic-blue `#4269d0` / Pythagorean-orange `#ef8e3a` / comma-red `#c45656` matches schisma/comma-pump family convention; tsc baseline unchanged (5 pre-existing `npm:` specifier errors only); build clean 79 links validated; page bundle 11kB→21kB and 391kB→916kB matching Plot-using pages footprint; previously: quick-260512-msv: add simultaneous-A/B `playDyad(fiveLimit, pythagorean, …)` button + static beat-frequency display (`beatHz = 6.875 Hz` derived in IIFE at audio-boundary via `Math.abs(baseHz * Number(pythagorean.fraction.valueOf()) - baseHz * Number(fiveLimit.fraction.valueOf()))` then surfaced through `${beatHz.toFixed(3)}` — Pitfall #1 honored, no hardcoded `6.875` literal in prose) + `> **Tempered out by.**` blockquote callout listing meantone (quarter-comma / third-comma / sixth-comma / seventh-comma), 12-EDO, 19-EDO, 31-EDO, 53-EDO with explanatory clause that these are exactly the temperaments mapping 81/80 to a unison so the 5/4 and 81/64 thirds collapse onto the same scale degree + `## Further reading` H2 with bulleted link to en.xen.wiki/w/81/80 and short descriptive sentence covering the comma's role as defining comma of 5-limit meantone + meantone/12/19/31/53-EDO temperament family + worked comma-pump examples; single-file additive edit src/pages/syntonic-comma.md +33/-0 (no refactor of existing imports cell or synth cell or three-Interval-bindings cell or `${tex}` header math or "Audition the difference:" bulleted list or `## In monzos` block or `## See also` — all byte-identical to baseline in their ranges); new `import { playDyad } from "../components/play-dyad.js"` joined inline to the existing single import cell (Framework module-scope rule — second import cell would have re-bound the existing names); reuses existing `fiveLimit = new Interval("5/4")` and `pythagorean = new Interval("81/64")` bindings — no new Interval allocations, grep gate `! grep -E 'new Interval\("(5/4|81/64)"\)' src/pages/syntonic-comma.md | wc -l` confirms exactly 1 each (unchanged from baseline); reuses page-level `synth = createSynth()` — Pitfall #2 honored, only one AudioContext owner (the literal "createSynth()" token appears 2× because of the explanatory code comment on line 16; one actual call-site only); placement: dyad button + beatHz IIFE + beat-frequency prose between the existing "Audition the difference:" list and `## In monzos`, callout blockquote between `## In monzos` and `## See also`, `## Further reading` appended last (matching schisma.md / pythagorean-comma.md / edo-approximation.md / monzos.md convention); no `.callout` CSS class exists in src/styles.css so plain `> **Tempered out by.**` markdown blockquote per project minimal-but-design-conscious style (no new CSS file, no styles.css edit); previously: quick-260512-jiq: add interactive Monzo builder to src/pages/monzos.md (new src/components/monzo-builder.ts Pattern 2 closure-component — five `<input type="number" min="-12" max="12" step="1">` for primes 2/3/5/7/11 + one ratio-string `<input type="text">` with bidirectional sync; BigInt-Fraction stays source of truth via `Interval.fromMonzo([...exponents])` and `new Interval(raw).monzo` with cents derived only at display boundary; Unicode bra-ket `[ a  b  c  d  e ⟩` on a `<code>` with tabular-nums — NOT KaTeX, which would re-typeset on every keystroke and flicker; `ratioPill(iv, {showCents:false})` + `(~ddd.dd¢)` cents readout + `playInterval(iv, synth, {label:false})` ▶ button re-rendered via `replaceChildren()` per state change so discarded button GCs with its click listener and AudioContext is owned once by the page-level cell; two-way sync correctness from programmatic `.value =` writes which do NOT refire input events on the same target — no manual suppress flag — plus `document.activeElement !== ratioInput` gate prevents canonicalised `4/2`→`2` from clobbering the user's literal keystrokes mid-typing; primes-above-11 detection `iv.monzo.slice(5).some(x=>x!==0)` surfaces inline error without touching exponents; exponent out-of-range [-12,12] on ratio-driven sync also surfaces inline error without clobbering state; empty-input rule (`parseInt(input.value,10)` → NaN, listener returns) preserves prior state instead of coercing to 0; T-02-22/T-02-23 XSS discipline — every dynamic surface uses `createElement` + `textContent`, never `innerHTML` for user values) + new `## Worked examples — projected to the 3-5 plane` Plot.dot scatter section below the builder reading `iv.monzo[1] ?? 0` / `iv.monzo[2] ?? 0` from the existing four Interval bindings (no hardcoded coordinates) with `Plot.text` labels — collision of 3/2 + 7/6 at (-1, 0) explicitly acknowledged in prose as pedagogical not a rendering bug + new `## Further reading` section appended after `## See also` linking en.xen.wiki/w/Monzo; single-file page edit src/pages/monzos.md +71 lines + new `src/components/monzo-builder.ts` (262 lines) + matching `monzo-builder.css` (109 lines, mos-builder token vocabulary) + happy-dom vitest spec `src/components/__tests__/monzo-builder.test.ts` (9 tests covering exponent→ratio sync, ratio→exponent sync with [-2,0,1,0,0] for "5/4", "13/12" primes-above-11 error path, empty-input preserve-state, range error, and DOM smoke) + one-line `@import` in src/styles.css; tsc clean; vitest 302/302 across 24 files (293 baseline + 9 new); observable build clean (79 links); monzos page bundle 11→26 kB and 391→918 kB matching Plot-using pages footprint (comma-pump 912kB, pythagorean-comma 920kB, edo-approximation 913kB)
progress:
  total_phases: 4
  completed_phases: 4
  total_plans: 26
  completed_plans: 26
  percent: 100
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-05-07 after v1.0 milestone close)

**Core value:** I can design any JI scale (arbitrary ratios, no prime-limit ceiling), see it expressed as ratios and cents-from-12tet, hear it, and export it to Scala `.scl`/`.kbm` — all from a self-hosted notebook where the calculator and the research prose live together.
**Current focus:** Planning next milestone — run `/gsd-new-milestone` to scope v1.1 (or v2.0).

## Current Position

Milestone: v1.0 MVP — SHIPPED 2026-05-07
Phases: 4/4 complete (full archive at .planning/milestones/v1.0-ROADMAP.md)
Last activity: 2026-05-13 -- quick-260513-kl9: CMD-4 of 260513-foy handoff — add inline `ratioPill` + bare ▶ `playInterval` row directly below the cents-vs-Tenney-height scatter chart on `src/pages/tenney-height.md` (Plot.dot() not clickable in Observable Plot — audition lives next to chart). One pill per `scatterRatios` entry — **32 ratios, not 15** as the handoff estimated. Three additions: (a) imports `createSynth` / `playInterval` / `ratioPill` folded into existing import block; (b) standalone synth cell after imports / before prereq aside (Pattern 4 / Pitfall #2: `const synth = createSynth(); invalidation.then(() => synth.dispose())` — lazy AudioContext); (c) plain-DOM IIFE pills-row cell directly after `display(scatterChart)` with one-line plain-Markdown caption "Click any ratio to hear it." — `div.ratio-pills-row` flex-wrap container iterating existing `scatterRatios` (cell scope, NOT redeclared), each pair wrapped in inline-flex `<span>` so pill + ▶ stay glued across wrap. `playInterval` defaults preserved per D-08 / D-18: 440 Hz, 1.5s, bare ▶. XSS T-02-22/T-02-23/T-kl9-01 — createElement + appendChild only, dynamic text delegated to ratioPill/playInterval factories. BigInt-Fraction source of truth — `new Interval(r)` per row (Pitfall #1). **No component/CSS/test changes** — pure markdown-page edit. **Scatter chart byte-identical** — no onclick, no extra mark, no domain change. tsc clean; `npm run build` clean (111 links, 18 pages); one atomic feat commit `27cb390` (+50/−0). **CMD-4 done** — remaining 260513-foy items: CMD-5 (well-temperament per-row play buttons via `playTempered()` factory pattern from meantone.md), CMD-6 (Plot scatter ratio-pills on edo-approximation/schisma/septimal-comma/syntonic-comma — could split per-page); independent, runnable in any order. Previously: quick-260513-kb6: CMD-3 of 260513-foy handoff — wire `spiralOfFifths.onStepClick` on `pythagorean-comma.md` (pure 3/2 branch, dyad `[440, 440 × Number(step.ratio.fraction.valueOf())]`) and `meantone.md` (1/4-comma tempered branch, octave-reduce `step.cumulativeCents` mod 1200 then dyad via the page's already-imported `centsToRatio`); two single-call-site additions, no component/CSS/import/test changes; D-08 three-layer discipline preserved (page-owned synth cells); 312/312 tests; tsc clean; `npm run build` clean (111 links). Commit 946248a. CMDs 4/5/6 remain (tenney-height pills · well-temperament per-row buttons · Plot scatter pills on edo-approximation/schisma/septimal-comma/syntonic-comma) — all independent, runnable in any order. Previously: quick-260513-jqi: CMD-2 of 260513-foy handoff — extend `renderDiamondSVG` in `src/components/tonality-diamond.ts` with optional `synth?/baseHz?/duration?`; when synth is supplied each cell flips to `role="button" tabindex="0"` with click + Enter/Space handlers calling `synth.playNotes([baseHz, baseHz × ratio], duration)` (D-07 dyad / D-18 1.5s, same shape `playInterval` uses). Wired through to `/pages/odd-limits.md` for all three diamonds (5-/7-/11-odd-limit, widths 280/320/440 preserved); zero CSS changes (tonality-diamond.css already gates `cursor: pointer` on `.diamond-cell[role="button"]`); D-08 three-layer discipline preserved (page-owned synth cell from monzos.md pattern hands `SynthHandle` reference through opts). 5 new vitest cases (legacy regression guard, role/tabindex/aria-label switch, click→playNotes dyad with default baseHz=440 + duration=1.5, Enter keypress, custom baseHz forwarding); 312/312 tests green; tsc clean; `npm run build` clean (111 links validated, 18 pages); odd-limits bundle 42→45 kB / 705→706 kB. Commit 8b4e356. Previously: quick-260513-jir: CMD-1 of 260513-foy handoff — extend `src/components/spiral-of-fifths.ts` with optional `onStepClick` prop + `.is-clickable` cursor toggle + 4 new vitest cases (23/23 green); commit b469080; CMD-3 (wire spiralOfFifths onStepClick on pythagorean-comma.md + meantone.md) is now unblocked. Previously: quick-260513-foy: audit 18 theory pages for playback/interactivity gaps and hand off a six-command `/gsd-quick` sequence the user will paste into fresh context windows to land the full sweep — no source code edited in this task; deliverable is `.planning/quick/260513-foy-i-would-like-as-many-of-the-graphs-and-f/260513-foy-SUMMARY.md` containing the audit table (18 rows, one per page in `src/pages/`), three structural blockers (1: `spiral-of-fifths.ts` needs an optional `onStepClick` D3-bound callback to make node clicks audition the corresponding ratio — currently viz-only per its existing T-02-22/D-08 three-layer-discipline header docstring; 2: `tonality-diamond.ts` exports both a click-wired `tonalityDiamond(scale, synth, opts)` AND a display-only `renderDiamondSVG(n, opts)` — `src/pages/odd-limits.md` calls the display-only form three times for 5-/7-/11-odd-limit diamonds and needs either an extended `renderDiamondSVG(n, { synth })` overload OR a page-side switch to build odd-limit `Scale`s and call `tonalityDiamond` directly with the page's synth; 3: Observable Plot's `Plot.dot()` mark does not expose click events natively — pages with static scatter charts adopt the proven ratio-pills-beside-chart pattern from `src/pages/prime-limits.md` instead of trying to overlay invisible rects), and the six commands ordered so dependencies land first (1 `/gsd-quick Extend src/components/spiral-of-fifths.ts with an optional onStepClick callback prop` → 2 `/gsd-quick make odd-limits diamonds clickable` → 3 `/gsd-quick wire spiralOfFifths onStepClick on pythagorean-comma.md + meantone.md` [depends on 1; pythagorean-tuning.md deliberately skipped — uses scaleTable not spiralOfFifths per audit] → 4 `/gsd-quick add play buttons to tenney-height scatter` → 5 `/gsd-quick add per-row play buttons to well-temperament variants table` → 6 `/gsd-quick add ratio-pills audition rows to remaining Plot scatter charts on edo-approximation/schisma/septimal-comma/syntonic-comma`); audit identified 9 pages with no work needed (analysis, commas, comma-pump, harmonic-series, monzos, otonality-utonality, prime-limits, pythagorean-tuning, scale-workshop-interop — all already have playback wiring) and 9 pages with work scoped by the six commands (odd-limits, tenney-height, well-temperament, pythagorean-comma, meantone, edo-approximation, schisma, septimal-comma, syntonic-comma); user explicitly requested handoff-as-commands rather than a single sweeping execution because the full sweep is bigger than the 1–3 atomic-task quick-task envelope — each command in the sequence is independently runnable in a fresh `/gsd-quick` context window; no source code modified, no kernel changes, only `.planning/quick/260513-foy-*/PLAN.md` and `SUMMARY.md` written; out-of-scope items documented in SUMMARY.md (comma-pump per-point chord-root clickability deferred — cycle buttons already adequate; 3D lattice work per `CLAUDE.md` v1 constraint; hover-audition affordances — separate spike); previously: quick-260513-d2e: fact-check all 18 theory pages in `src/pages/` and add Chicago author-date citations + per-page `## Sources` sections — research-driven audit (gsd-phase-researcher inventoried ~165 claims across the corpus, produced a deduplicated 48-entry citation pool spanning primary historical (Werckmeister 1691, Kirnberger 1779, Salinas 1577, Aron 1523, Zarlino 1558, Vallotti 1779, Tartini 1754, Helmholtz 1885, Mersenne 1636, Partch 1974, Tenney 1983, Doty 2002), modern academic (Benson 2007, Sethares 2005, Blackwood 1985, Lindley 1984, Duffin 2007/2020, Barker 1989, West 1992, Plomp & Levelt 1965, Lehman 2005), Xenharmonic Wiki (16 entries with Accessed 2026-05-13 dates), and Huygens-Fokker Foundation); 9 verbatim corrections applied — Vallotti "published posthumously 1779" → corrected: Vallotti d. 10 Jan 1780 *after* the 1779 publication; Vallotti recipe → reattributed to **Tartini–Vallotti** (first appeared in print in Tartini's 1754 *Trattato di musica* per Duffin's modern scholarship); bare "Garibaldi" disambiguated to **Sábat-Garibaldi** (Eduardo Sábat-Garibaldi, dinarra developer, late 20th c.); Verheijen → **Verheyen, ca. 1600** as a prose footnote (cell-internal labels left untouched per no-JS/TS-cell-edits constraint); syntonic-comma 21.506¢ / Pythagorean-comma 23.460¢ / schisma 1.954¢ canonical-value re-anchorings; Tenney 1983/1984 year-convention normalization; Gene-Ward-Smith / Joe-Monzo origin note on `monzos.md`; pre-Pythagoras lineage aside added to `pythagorean-tuning.md` citing West 1992 + Barker 1989 (chain-of-fifths method documented in cuneiform Mesopotamian sources >1000 years pre-Pythagoras); 4 `(citation needed)` markers placed for unverifiable claims; Q-5 JND threshold claims (~5–10¢) satisfied inline with canonical Plomp & Levelt 1965 + Sethares 2005 citations; per-page Sources counts (18 pages total) — well-temperament 9, comma-pump/meantone/schisma/syntonic-comma 8 each, harmonic-series/edo-approximation/pythagorean-tuning 6 each, commas/pythagorean-comma/septimal-comma 5 each, prime-limits/scale-workshop-interop 4 each, monzos/odd-limits/otonality-utonality 3 each, tenney-height 2, analysis 1; **citation style: Chicago author-date** inline `(Helmholtz 1885, 33)` with full reference-list entries (alphabetized by author surname) in each page's bottom `## Sources` H2 — duplication of bibliographic entries across pages accepted in exchange for locality (no shared `references.md` page per locked CONTEXT.md decision); Xen Wiki citations use `(Xen Wiki, "Page Title")` inline + `Accessed 2026-05-13` in the reference list; HARD CONSTRAINT honored: reactive `js`/`ts` code cells UNTOUCHED in every page (verified via `git diff` audit — only prose Markdown changes); no new files created; `npm run build` clean — 21 pages, 111 links validated, zero errors; no emojis in any edited file; single atomic feat commit 872f6e8 covering all 18 page edits (`feat(260513-d2e): fact-check theory pages and add Chicago author-date citations`, +362 / −137 lines, 18 files); orchestrator merged worktree-isolated executor branch via 712312f; flags --research (focused gsd-phase-researcher pre-planning) + --discuss (gray-areas surfaced upfront — citation style locked to Chicago author-date, fact-check depth locked to FULL incl. math derivations, all four source tiers accepted, per-page `## Sources` layout with single-commit policy); follow-up items logged in SUMMARY.md for the 4 `(citation needed)` markers awaiting future research; previously: quick-260513-9s8: add well-temperament theory page (Werckmeister III 1691 / Kirnberger III 1779 / Vallotti 1779 — the historical bridge between meantone and 12-EDO) — new src/pages/well-temperament.md (480 lines) with per-temperament 12-fifth-size profiles (each sums exactly to 8400.000000000¢), 12-key major-third profiles derived by stacking 4 fifths down 2 octaves, Plot bar charts (fifths-deviation + major-thirds-per-key, 3 pairs) with red `#c45656` fill for tempered-flat bars catching Kirnberger's schisma-tempered F♯-C♯ at −1.954¢, six triad-audition buttons (C-major + F♯-major × 3 temperaments) via inline `playTriadAt(label, rootCents, thirdCents, fifthCents)` factory mirroring meantone.md's `playTempered` pattern, kernel-exact anchors throughout (pureFifth/pythComma/syntonic/schisma/pure5Third/pythThird via new Interval + commaByName, cents constants PF/PC_4/PC_6/SC_4/SCHIS derived from .cents projections — zero hardcoded canonical-cent float literals 701.955/386.314/23.460/21.506/1.954/407.820 in source per grep gate), page-level synth cell owns the only AudioContext (Pitfall #2, one createSynth() callsite + invalidation.then(synth.dispose)), prerequisite aside (matching the quick-260513-91h cross-page consistency pass) pointing to ./meantone + ./pythagorean-comma, Further reading via furtherReading([...]) listing Xen Wiki /w/Well_temperament + Bradley Lehman's larips.com (Bach-temperament reconstruction) + Mark Lindley *Lutes, Viols and Temperaments* (Cambridge UP 1984 ISBN 0-521-28950-5); numerical anchors verified Werckmeister C-major 3rd = 390.225¢ + F♯-major 3rd = 407.820¢ (= 81/64 exactly — purest Pythagorean third lands at F♯), Kirnberger C-major 3rd = 386.314¢ (= pure 5/4, the headline property), Vallotti A-major 3rd = exactly 400.000¢ (12-TET coincidence at the rotation's center); one-line edit to observablehq.config.ts inserting `{ name: "Well-temperaments", path: "/pages/well-temperament" }` between Meantone and EDO approximations at line 29; spiral-of-fifths.ts intentionally NOT embedded (research confirmed its `temperedFifthCents` accepts only a single scalar, not the per-fifth array needed for well-temperaments — Plot bar charts chosen); kernel side (src/lib/, src/audio/, src/components/) untouched; 10 grep gates + tsc-baseline + build-clean + rendered-HTML check all pass; `npm run build` clean — 19 pages (was 18), 101 links validated (was 95); tsc baseline unchanged (5 pre-existing npm: specifier errors in non-markdown files only); flag --research triggered focused researcher first to source exact published fifth-size values from xen.wiki + tonalsoft + larips + verify all three sets close at 8400¢; commit 4202b6a; previously: quick-260513-8jw: add reusable `furtherReading(items)` presentational helper (new src/components/further-reading.ts — Pattern 1 pure-presentational factory accepting `{ title: string; url: string; note?: string | Node }[]` returning `<section class="further-reading"><ul>...</ul></section>` with one `<li>` per item; XSS-safe via createElement + textContent for string notes, Node notes appended as-is so callers can pass `` html`text with ${tex`81/80`} inline` `` to preserve inline KaTeX/links/bold; external URLs auto-get `rel="noopener noreferrer"`; the page's Markdown `## Further reading` heading stays in Markdown so Framework TOC continues to surface it — the helper renders the list body only) + matching src/components/further-reading.css (disc bullets at 1.5rem indent, 0.5rem item spacing, line-height 1.5, link weight 500, `.further-reading__note` muted via `--theme-foreground-alt`) + one-line `@import` in src/styles.css after monzo-builder.css; adoption on seven theory pages + backfill of missing classic-text references — monzos.md adds David B. Doty *The Just Intonation Primer* (3rd ed., 2002), odd-limits.md adds Harry Partch *Genesis of a Music* (2nd ed., Da Capo Press, 1974) on archive.org mirroring prime-limits.md URL form, prime-limits.md adoption-only (already had XW + Partch), otonality-utonality.md adoption-only (already had XW + Kyle Gann WTP essay), comma-pump.md adoption-only with `html`-tagged-template note body preserving inline `${tex`81/80`}` in the Ben Johnston Suite for Microtonal Piano (1977) row, pythagorean-tuning.md gains a brand-new `## Further reading` H2 (was missing entirely — the only target page without one) with XW Pythagorean_tuning + Hermann von Helmholtz *On the Sensations of Tone* (Ellis tr., 1875) on IMSLP using the `Helmholtz,_Hermann_von` URL form (verified per prior task 260512-ilb that bare `Hermann` returns 404), meantone.md adopts the helper with `html`-tagged-template note body preserving inline `${tex`n \in \mathbb{R}`}` AND adds Easley Blackwood *The Structure of Recognizable Diatonic Tunings* (Princeton UP, 1985) — the modern canonical text laying out the 1/n-comma continuum as recognizability bands; each page gets `import { furtherReading }` joined inline to its existing single import cell (Framework module-scope rule — second import cell would re-bind `Interval`); Markdown `-` bullets under `## Further reading` replaced with a single JS code fence calling `furtherReading([...])` keeping the H2 in Markdown for TOC integrity; eight atomic commits on main (d9fc91f component + CSS + styles.css, f7bb735 monzos + Doty, 0b536e1 odd-limits + Partch, 8d0e785 prime-limits adoption, 630301f otonality-utonality adoption, fe379f3 comma-pump adoption + html-template Johnston note, 1d0f626 pythagorean-tuning new section + XW + Helmholtz, 8bbb00e meantone + Blackwood); HARD CONSTRAINT honored: src/lib/* and src/audio/* untouched, no kernel changes — pure presentational + per-page Markdown edits; out-of-scope pages with existing Further-reading sections (syntonic-comma, pythagorean-comma, schisma, septimal-comma, harmonic-series, edo-approximation, commas) left untouched per user's explicit list — they can adopt the helper in a follow-up without disrupting their existing references; tsc baseline unchanged (5 pre-existing `npm:` specifier errors only); vitest 302/302 across 24 files; prettier clean on all 9 touched files; eslint clean; `npm run build` clean — 18 pages rendered, 88 links validated; furtherReading module emitted as dist/_import/components/further-reading.<hash>.js with the expected `export function furtherReading(items)` signature; rendered HTML grep gate `grep -c 'furtherReading\|further-reading' dist/pages/<page>.html` returns 8 on each of the 7 target pages; per-page bundle delta 1–8 kB Imports (helper is tiny — most of each page's bundle is dominated by inherited Plot / d3 / xen-dev-utils stacks); previously: quick-260512-udc: bring four comma pages (pythagorean / syntonic / schisma / septimal) to consistent 5-section structure — (1) definition + cents, (2) audition (dyads + simultaneous playDyad), (3) "In monzos", (4) "Tempered out by", (5) "See also" + "Further reading"; pythagorean-comma.md gains `playDyad(pureOctave, cycleOctave, synth, …)` simultaneous-dyad audition button between the existing playInterval bullets and "## The closure gap, visualized" + new `> **Tempered out by.**` blockquote between `## In monzos` and `## See also` listing 12-EDO/19-EDO/24-EDO (per user spec — "any 12-stable EDO"; 19-EDO flagged in SUMMARY for SME confirmation since the patent-val 19 maps 12 fifths to 132 steps vs 7 octaves at 133 steps so the comma is NOT vanished there — accept verbatim implies non-patent mapping or tighten the list); schisma.md gains canonical `> **Tempered out by.**` blockquote between `## In monzos` and `## See also` listing schismatic temperament (Helmholtz / Groven / Garibaldi) + 53-EDO (Mercator–Holder cycle, 53 fifths = 31 octaves) + 41-EDO (alternative 5-limit-friendly host); existing "## Schismatic temperament" deep-dive section in schisma.md preserved byte-equivalent (still includes the 8192/6561 + 5/4 playDyad and Helmholtz 0.24¢-narrowing recipe); syntonic-comma.md + septimal-comma.md unchanged — already at target structure per prior tasks msv (#13) and sxc (#14); HARD CONSTRAINT honored: src/lib/* untouched throughout, BigInt-exact arithmetic unchanged everywhere (kernel-untouched check via `! git diff --name-only src/lib/ | grep -q .` ran clean in both task verify gates AND the plan-level cross-check); cross-page consistency verified: all four pages contain exactly one `> **Tempered out by.**` blockquote with the bare-`>` canonical shape (no `.callout` div anywhere) and all four import `playDyad` from `"../components/play-dyad.js"` exactly once; verifier status: human_needed (single SME content question on the 19-EDO list — no execution gaps); two atomic feat commits + merge commit on main: c632a8f pythagorean + 0e922e1 schisma + b324099 merge; tsc baseline unchanged (5 pre-existing `npm:` specifier errors); previously: quick-260512-tlv: add Commas (glossary) page — new src/pages/commas.md indexing all 16 named commas from src/lib/commas.ts grouped by canonical prime-limit (3-limit: Pythagorean comma + Mercator's comma; 5-limit: syntonic/schisma/diaschisma/diesis/greater diesis/kleisma; 7-limit: septimal comma + septimal kleisma + harmonic seventh comma + jubilisma + ragisma + breedsma; 11-limit: rastma + undecimal comma) — kernel-bound throughout: every Interval comes from `commaByName(c.name)!` (R-01 / Pitfall #6, source-table canonical, BigInt-Fraction round-trip exact even for Mercator's 25-digit ratio), prime-limit derived at runtime as "last nonzero index of iv.monzo" mapped through `[2,3,5,7,11,13,17,19]` so the Pythagorean comma and Mercator's comma classify into 3-limit despite the source file organizing them under a `// 5-limit` comment header; per-section DOM tables built via `buildCommaTable(entries)` helper (createElement + textContent throughout — T-02-22/T-02-23 XSS discipline) with columns Name | Ratio | Monzo | Cents | Limit | ▶: Name cell renders `<a href>` for the four commas with dedicated pages (syntonic comma → /pages/syntonic-comma, Pythagorean comma → /pages/pythagorean-comma, schisma → /pages/schisma, septimal comma → /pages/septimal-comma) via DEDICATED_PAGES Map and plain text for the other 12; Ratio cell appends `ratioPill(iv, {showCents:false})`; Monzo cell appends `tex\`\begin{bmatrix} ${iv.monzo.join(' & ')} \end{bmatrix}\rangle\`` on the canonical minimal-length monzo; Cents cell shows `iv.cents.toFixed(3)` so the breedsma 0.721¢ and ragisma 0.396¢ remain distinguishable from zero; Play cell appends `playInterval(iv, synth)` (▶ only, no label); page-level synth cell owns the only AudioContext (Pitfall #2, exactly one `createSynth()` call site); no `@observablehq/plot` import — tabular index not viz, bundle stays at 16 kB Page / 391 kB Imports matching no-Plot family (syntonic-comma 14kB/392kB, pythagorean-tuning 16kB/400kB); footer mirrors syntonic-comma / pythagorean-comma / schisma / septimal-comma convention — `## See also` linking the four dedicated pages + /pages/monzos + /pages/comma-pump + dashboard; `## Further reading` with en.xen.wiki/w/Comma; one-line edit to observablehq.config.ts inserting `{ name: "Commas (glossary)", path: "/pages/commas" }` immediately after Comma pump (alphabetical slot between "Comma pump" and "EDO approximations"); single atomic feat commit 69b7a88; tsc clean; `npm run build` clean — 18 pages (was 17), 88 links validated (was 79; new page contributes 9 new internal links to the four dedicated pages + /pages/monzos + /pages/comma-pump + / + xen.wiki); grep gate `! grep -nE '[0-9]+\.[0-9]+¢' src/pages/commas.md` clean — no hardcoded float-cents literals in prose; previously: quick-260512-ngj: meantone.md additions — (a) Plot horizontal cents-axis strip chart placed BEFORE the variants table showing six tempered fifths (1/3, 2/7, 1/4, 1/5, 1/6, 1/8) as dashed-blue `Plot.ruleX` verticals + `Plot.dot` heads + per-row alternating-`dy` 1/n labels against a 0..`Math.ceil(pureFifth.cents)` axis with a pure-3/2 reference rule at the right edge and a red `Plot.link` horizontal bracket spanning the syntonic comma from `pureFifth.cents − syntonic.cents` to `pureFifth.cents` (all `.cents` from existing `pureFifth`/`syntonic` Interval bindings + new `verheijen`/`eighth`/`zarlino` tempered-fifth bindings — Pitfall #1 honored, no float-cents literals in chart data rows); (b) variants table extended 3→6 rows via `verheijen = tempered(5)` / `eighth = tempered(8)` / `zarlino = tempered(7 / 2)` (2/7-comma encoded as n=7/2 since `fifth = pureFifth.cents − syntonic.cents/n` substitutes to `pureFifth.cents − (2/7)·syntonic.cents` — no `tempered` factory change); rows appended to the local `rows` array in the variantsTable IIFE with the same createElement + textContent shape and `c.toFixed(3)` precision; final order 1/4 Aron → 1/3 Salinas → 1/6 Silbermann → 1/5 Verheijen → 2/7 Zarlino → 1/8; signature-property cells: "fifths only ~4.3¢ flat; major 3rd ~6.9¢ sharp of pure 5/4 — milder than 1/4-comma" / "equalizes the major-3rd and minor-3rd deviations from pure (both ~5.4¢ off)" / "softest meantone — closest variant to 12-TET (fifths only ~2.7¢ flat)"; (c) new `## The wolf at k=12 (1/4-comma meantone)` H2 between the third 1/6-comma bullet and the existing `## Audition — the 1/4-comma equivalence` H2, embedding `${spiralOfFifths(12, { temperedFifthCents: quarter.fifth, highlightWolf: true })}` — spiral component's tempered branch: ratio labels dropped (1/4-comma fifth is irrational), signed cents-from-12-TET per node via octave-reduced cumulative cents, dashed red wolf chord between k=12 and k=0 visualizing the ≈ -40.7¢ closure undershoot (mirror of the +23.46¢ Pythagorean-comma overshoot on the companion pythagorean-comma.md page); (d) `## Further reading` H2 appended after `## See also` with en.xen.wiki/w/Meantone_family (regular-temperament family generated by tempering out the syntonic comma; full continuum of comma fractions; historical variants as points on that line); new imports `spiralOfFifths` + `* as Plot` joined inline to the existing single import cell (Framework module-scope rule); spiral-of-fifths.css already globally imported via src/styles.css:32 from the prior quick-260512-fxk wiring task — no styles.css edit; existing synth/kernel-anchors/tempered-factory/playTempered/playPureMajor3/playQuarterMajor3/playPythMajor3 cells unchanged byte-for-byte (Pitfall #2 honored — one `createSynth()` call site); single-file additive edit src/pages/meantone.md across three feat commits + merge: f78369d variants table, 4478eff Plot chart, 4c5b540 spiral wolf + Further reading; chart-cell deviations (within executor discretion): `Plot.ruleY` with x1/x2 args → `Plot.link` for the syntonic-comma horizontal bracket (Plot.ruleY does not accept x1/x2 endpoints); per-row alternating `dy` callback for label collision avoidance; tsc baseline unchanged (5 pre-existing `npm:` specifier errors in non-markdown files only); vitest 302/302 across 24 files; prettier + eslint clean; `npm run build` clean — 17 pages, 79 links validated; meantone page bundle 25→34 kB (+9 kB Page) and 391→920 kB Imports (+529 kB — Plot is heavy) matching pythagorean-comma 920kB / comma-pump 912kB / edo-approximation 913kB / schisma 916kB / monzos 918kB / septimal-comma 916kB footprint; previously: quick-260512-mk8: add paired partials Plot chart (left otonal 4:5:6:7 horizontal-rule cents-axis ascending / right utonal 1/1:8/7:4/3:8/5 same cents-axis re-rooted upward — the mirror is unmistakable when the two share-axis charts sit side-by-side; Plot.ruleY x1:0→x2:cents stems + Plot.dot heads, y-domain is array order with `reverse:true` so high pitch reads up matching musical-staff intuition, x-domain shared [0,1050]¢ for room past 7/4≈969¢; data rows derive cents from `otonal[i].cents` / `utonal[i].cents` on existing Interval bindings — Pitfall #1 honored, no float-literal cents anywhere in chart cell, grep gate `! grep -nE 'cents:\s*(386\.31|701\.96|968\.83|231\.17|498\.04|813\.69)'` clean) + per-note `playInterval(iv, synth, {label:true})` ▶ buttons appended inline to every chord-tone bullet in both `## The otonal chord` and `## The utonal chord` H2 sections (4+4 = 8 new buttons; existing chord-button cells `otonalBtn` / `utonalBtn` untouched so simultaneous-chord audition remains available alongside per-note audition; reuses page-level `synth` cell via existing closure — grep gate `grep -c 'createSynth()' src/pages/otonality-utonality.md == 1` clean, Pitfall #2 honored — no second AudioContext owner) + new `## Further reading` H2 appended at file bottom after `## See also` with two bulleted links (en.xen.wiki/w/Otonality_and_utonality community-curated reference covering Partch's duality + tonality-diamond worked examples; Kyle Gann's "La Monte Young's *The Well-Tuned Piano*" composer's-eye survey of the six-hour solo-piano cycle, the most-cited example of long-form otonal-chord-cluster composition with dream-house tunings drawing from selected 7-/11-limit otonal harmonics) matching schisma.md / pythagorean-comma.md / edo-approximation.md / harmonic-series.md / odd-limits.md placement convention; new imports added to the page's single existing import cell — `import * as Plot from "npm:@observablehq/plot"` + `import { playInterval } from "../components/play-interval.js"` (Framework module-scope rule — second import cell would re-bind `Plot`); single file edit src/pages/otonality-utonality.md +96/−8 net +88 additive (no refactor of synth cell / otonal+utonal Interval-array cells / baseHz const / four ratioPill bullets in each section / kernel-side `.inv().octaveReduce()` truth-check cell / two existing chord-button IIFEs / `## Monzo signs flip` / `## See also` — all byte-identical to baseline in their ranges); tsc baseline unchanged (5 pre-existing `npm:` specifier errors in non-markdown source files only, present pre-edit); previously: quick-260512-kzw: add Limit explorer (slider 3→5→7→11→13 + reactive table of newly-entering ratios with Ratio/Monzo/Cents/Play columns) + prime-identity Plot strip chart (each prime's basic identity `p/2^floor(log₂ p)` plotted against the 12-TET grid: 3→3/2 ~702¢, 5→5/4 ~386¢, 7→7/4 ~969¢, 11→11/8 ~551¢, 13→13/8 ~841¢ — 11 and 13 visibly between 12-TET grid lines) + `## Further reading` (Xenharmonic wiki Prime_limit + Partch *Genesis of a Music* on archive.org) above the four `## N-limit` sections on src/pages/prime-limits.md; default slider value=1 opens at 5-limit; reactive `Prime limit: N` caption rendered below the slider; Plot import added to the existing single import cell (Framework module-scope rule — second import cell would have re-bound `Plot`); Unicode bra-ket `[ a  b  c  d  e  f ⟩` on `<code>` with tabular-nums NOT KaTeX (which would re-typeset and flicker on every slider tick — lesson from monzo-builder); reuses page's existing `synth` cell + `ratioPill`/`playInterval` factories; `limitSet` lookup table precomputes 12 `new Interval(ratio)` once at top of section, `.fraction`/`.monzo`/`.cents` read only at the display step in `limitTable` IIFE (Pitfall #1); strip chart uses `y: { axis: null, domain: [-1, 1] }` + `Plot.dot(..., { y: 0 })` single horizontal strip with `Plot.text(dy:-14)` prime-labels stacking neatly above; src/pages/prime-limits.md +180 lines additive (no refactor of existing imports/synth cell/example-Interval bindings/four `## N-limit` sections/`## Limits in the kernel`/`## See also` — all byte-identical to baseline restricted to `## 3-limit … ## See also` range); H2 ordering verified `## Limit explorer` @ line 48 (before `## 3-limit (Pythagorean)` @ 214) + `## Further reading` @ line 333 (after `## See also` @ 314); tsc baseline unchanged (5 pre-existing `npm:` specifier errors in non-markdown files, present on merge-base 1272e43); previously: quick-260512-ksu: add Live EDO explorer (reactive N=5..72 slider + one-row deviation table mirroring static table; inline round(c/s) math since bestEdosForScale returns aggregate metrics not per-anchor cells; Pitfall #1 honored — jiIntervals stay as Interval, iv.cents read only at display); see prior quick-260512-jiq: add interactive Monzo builder to src/pages/monzos.md (new src/components/monzo-builder.ts Pattern 2 closure-component — five `<input type="number" min="-12" max="12" step="1">` for primes 2/3/5/7/11 + one ratio-string field with bidirectional sync; BigInt-Fraction stays source of truth via `Interval.fromMonzo([...exponents])` and `new Interval(raw).monzo` with cents derived only at display boundary; Unicode bra-ket `[ a  b  c  d  e ⟩` on a `<code>` with tabular-nums — not KaTeX, which would flicker on every keystroke; `ratioPill` + `(~ddd.dd¢)` cents readout + `playInterval` ▶ button re-rendered via `replaceChildren()` per state change; two-way sync from programmatic `.value =` writes which do NOT refire input events on the same target + `document.activeElement !== ratioInput` gate prevents `4/2`→`2` canonicalisation from clobbering the user's keystrokes mid-typing; primes-above-11 (`iv.monzo.slice(5).some(x=>x!==0)`) and exponent out-of-range surface inline errors without touching state; empty-input (`parseInt` NaN) preserves prior state instead of coercing to 0; T-02-22/T-02-23 XSS — every dynamic surface uses createElement + textContent, never innerHTML) + new `## Worked examples — projected to the 3-5 plane` Plot.dot + Plot.text scatter section below the builder reading `iv.monzo[1] ?? 0` / `iv.monzo[2] ?? 0` from the existing four Interval bindings (no hardcoded coordinates) with the 3/2 + 7/6 collision at (-1, 0) acknowledged in prose as pedagogical not a rendering bug + new `## Further reading` section linking en.xen.wiki/w/Monzo; src/pages/monzos.md +71 lines + new monzo-builder.ts (262 lines) + matching CSS (109 lines, mos-builder token vocabulary) + happy-dom spec __tests__/monzo-builder.test.ts (9 tests: exponent→ratio sync, "5/4"→[-2,0,1,0,0], "13/12" primes-above-11 error, empty-input preserve, range error, DOM smoke) + one-line `@import` in src/styles.css; tsc clean; vitest 302/302 across 24 files (293 baseline + 9 new); observable build clean (79 links); monzos page bundle 11→26 kB and 391→918 kB matching Plot-using pages footprint

Progress: [██████████] 100%

## Performance Metrics

**Velocity:**

- Total plans completed: 18
- Average duration: —
- Total execution time: —

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 01 | 4 | - | - |
| 02 | 7 | - | - |
| 03 | 7 | - | - |

**Recent Trend:**

- Last 5 plans: —
- Trend: —

*Updated after each plan completion*
| Phase 01-bootstrap-build P01 | 16min | 2 tasks | 11 files |
| Phase 01-bootstrap-build P02 | 3min | 2 tasks | 11 files |
| Phase 01-bootstrap-build P03 | 3min | 2 tasks | 4 files |
| Phase 01-bootstrap-build P01-04 | 6min | 2 tasks | 1 files |
| Phase 02-math-kernel-composition-anchor-mvp P01 | 4min | 2 tasks | 27 files |
| Phase 02-math-kernel-composition-anchor-mvp PP02 | 6min | 2 tasks tasks | 9 files files |
| Phase 02-math-kernel-composition-anchor-mvp P03 | 4min | 1 tasks | 2 files |
| Phase 02-math-kernel-composition-anchor-mvp P05 | 3min | 1 tasks | 2 files |
| Phase 02-math-kernel-composition-anchor-mvp P04 | 5min | 1 tasks | 2 files |
| Phase 02-math-kernel-composition-anchor-mvp P06 | 5min | 2 tasks | 12 files |
| Phase 02-math-kernel-composition-anchor-mvp P07 | 50min | 3 tasks | 6 files |
| Phase 03-visualization-mobile-audio-audit P06 | ~10h | 6 tasks | 11 files |
| Phase 04-analysis-sharing P07 | 57min | 4 tasks | 5 files |

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- Roadmap: 4-phase structure follows research-recommended dependency order (bootstrap → kernel-MVP with composition anchor → viz + mobile → analysis); temperament browser deferred to v2
- [Phase ?]: Honored D-17: @observablehq/framework exact-pinned to 1.13.4 (no caret); transitive deps locked via package-lock.json
- [Phase ?]: Honored D-13: package.json contains the 9-script set verbatim (dev, build, lint:types, test, test:watch, lint, format, format:check, ci); no observable deploy script per D-05
- [Phase ?]: Honored D-18: Node 20 LTS pin via .nvmrc (=20) and engines.node (>=20)
- [Phase 01]: Honored D-16: tsconfig.json has all four strict flags (strict, noUncheckedIndexedAccess, noImplicitOverride, exactOptionalPropertyTypes)
- [Phase 01]: Honored D-12: ESLint 9 flat config uses recommendedTypeChecked baseline scoped to src/**/*.{ts,js}; root-level configs linted without type-aware rules
- [Phase 01]: Honored D-07: tests live at src/lib/__tests__/ adjacent to source; vitest.config.ts excludes Framework cache and Markdown
- [Phase 01]: Added src/.observablehq/** to ESLint ignores - Framework cache lives at src/.observablehq/, not repo root
- [Phase ?]: [Phase 01]: Honored D-14: src/index.md imports {Fraction} from npm:fraction.js, computes new Fraction("81/79"), and renders ratio + cents (~43.28¢) inline — proves Framework + npm: + BigInt + strict TS end-to-end
- [Phase ?]: Honored D-17: fraction.js exact-pinned to 5.3.4 (no caret); xen-dev-utils, sonic-weave, ji-lattice, sw-synth caret-ranged
- [Phase ?]: Honored D-21: temperaments and mathjs absent from package.json (deny-list grep); accepted ji-lattice peer-dep warning per CLAUDE.md compatibility table
- [Phase ?]: Honored D-08: src/lib/INVENTORY.md seeded with first kernel-discipline entry (Fraction → fraction.js@5.3.4); establishes wrap-don't-reimplement pattern (Pitfall #5)
- [Phase ?]: Reworded src/index.md prose from 'composition dashboard' to 'piece dashboard' to satisfy plan's deny-grep on 'composition|theory'; D-15 intent (no skeleton-page files) preserved
- [Phase ?]: [Phase 01]: Honored D-02/D-03/D-05: single GitHub Actions workflow chains npm ci → 4 gates → deny-list → build → configure-pages → upload-artifact → deploy-pages on push to main; PRs run gates + build only; no observable deploy invocation
- [Phase ?]: [Phase 01]: Honored security_constraints: only first-party actions/* (checkout@v4, setup-node@v4, configure-pages@v5, upload-pages-artifact@v3, deploy-pages@v4); minimum-needed permissions (contents:read, pages:write, id-token:write); concurrency:{group:pages, cancel-in-progress:false}
- [Phase ?]: [Phase 01]: Honored D-18 + D-21: setup-node reads node-version-file: .nvmrc (Node 20 single-source-of-truth); in-CI deny-list step rejects temperaments / mathjs in package.json (defense-in-depth alongside Plan 03 local check)
- [Phase ?]: [Phase 02 Plan 01]: Honored D-23 — KaTeX 0.16.45 CSS head-injected via SRI-pinned CDN; no auto-render JS (KaTeX JS lazy-loads on first tex cell)
- [Phase ?]: [Phase 02 Plan 01]: Honored R-01 — eslint.config.js no-restricted-imports rule blocks Fraction from xen-dev-utils repo-wide (kept active in test files; no kernel test legitimately needs that import)
- [Phase ?]: [Phase 02 Plan 01]: Honored Phase 1 D-07 extension — vitest.config.ts include glob now covers src/lib/__tests__/, src/audio/__tests__/, and src/__tests__/
- [Phase ?]: [Phase 02 Plan 01]: Sourced 3 golden archive .scl files from live Huygens-Fokker scales.zip; renamed slendro_av.scl→slendro.scl and young-lm_piano.scl→young_lm.scl; per-file source + license documented in golden/LICENSE.md
- [Phase ?]: [Phase 02 Plan 02]: Honored R-01 at source — src/lib/interval.ts imports Fraction from fraction.js directly
- [Phase ?]: [Phase 02 Plan 02]: Honored D-24 immutability — Interval.mul/div/inv/octaveReduce return new instances, verified with referential-inequality test assertions
- [Phase ?]: [Phase 02 Plan 02]: Honored Pitfall #13 — Interval.octaveReduce(period?) accepts arbitrary periods (default 2/1; tested with 9/1 reduced by 3/1 → 1/1)
- [Phase ?]: [Phase 02 Plan 02]: Honored Pitfall #6 + D-21 — 16 hand-verified commas keyed on canonical monzo; Mercator's comma 25-digit ratio round-trips exactly through BigInt path (live R-01 proof)
- [Phase ?]: [Phase 02 Plan 02]: Honored Pitfall #5 — 8 INVENTORY rows added; every new export has Source + Notes
- [Phase ?]: [Phase 02 Plan 02]: Curated commas table — dropped 4 unverified candidates from planner draft, substituted verified replacements (harmonic seventh comma 49/48, jubilisma 50/49, breedsma 2401/2400); fixed rastma sign and Mercator direction
- [Phase 02]: Plan 03: Honored D-24 + Pitfall #13 — Scale.reduce treats period-equal inputs specially (preserves period; doesn't reduce to 1/1) so [9/8, 5/4, 9/8, 2/1].reduce() yields length-3 [9/8, 5/4, 2/1] while Bohlen-Pierce [9/1, 3/1] still reduces to [1/1, 3/1]
- [Phase 02]: Plan 03: Honored R-01 — jiSubsetOfEdo round-trips xen-dev-utils' Number-backed Fraction through ${n}/${d} string into Interval's BigInt-backed Fraction; maxExponent=5 (not 8) keeps the search inside Number.MAX_SAFE_INTEGER for 31-EDO 7-limit
- [Phase 02]: Plan 03: Wave-2 file-ownership — INVENTORY.md NOT modified in this plan; Plan 06 will consolidate Scale + jiSubsetOfEdo rows alongside Plans 04/05 to avoid merge conflicts
- [Phase ?]: [Phase 02]: Plan 05: Honored D-16/D-17/D-18 + Pitfall #2/#9 + ARCHITECTURE Pattern 4 — createSynth factory with lazy AudioContext, voice tracking via noteOff callbacks, terminal idempotent dispose; defense-in-depth Hz clamp [20, 20000] (T-02-17), arpeggio cap 256 (T-02-18), polyphony clamp [1, 64] (T-02-19); three-layer discipline preserved
- [Phase ?]: [Phase 02]: Plan 05: Wave-2 file-ownership — INVENTORY.md NOT modified; Plan 06 (Wave 3) will add createSynth + SynthHandle rows alongside Plans 03/04's queued rows to avoid merge conflicts
- [Phase ?]: [Phase 02]: Plan 04: Honored D-12/D-13/D-14/D-15/D-19 + Pitfall #6 — single shared parseScala body parser feeds .scl import + dashboard textarea; auto-prepends 1/1; rejects negative-ratio + multi-slash with clear errors; cents detection by '.' membership; monzo bra-ket as project extension
- [Phase ?]: [Phase 02]: Plan 04: Added formatRatio helper because fraction.js' toFraction() drops '/1' for whole-number ratios — writeScl/scalaToCsv read iv.fraction.n/.d (BigInt) directly to emit explicit n/d form
- [Phase ?]: [Phase 02]: Plan 04: Honored R-01 + T-02-10/T-02-11 — only centsToValue imported from xen-dev-utils (NOT Fraction); 1MB input cap; monzo length cap 32; per-component magnitude cap ±1024
- [Phase ?]: [Phase 02]: Plan 04: Wave-2 file-ownership — INVENTORY.md NOT modified; Plan 06 (Wave 3) will consolidate parseScala/parseScl/writeScl/scalaToCsv rows alongside Plans 03/05 to avoid merge conflicts
- [Phase ?]: [Phase 02]: Plan 06: Honored D-09 + ARCHITECTURE Pattern 2 — six widgets as plain (data, ...rest, opts?) => HTMLElement factories; SynthHandle passed in by caller (no global synth registry); three-layer discipline preserved (no sw-synth direct imports in components)
- [Phase ?]: [Phase 02]: Plan 06: Honored D-06/D-07/D-08/D-10/D-22 — 4-column scaleTable (Degree/Ratio/Cents/¢ from 12-TET) with cents at 0.1¢ default per Pitfall #16; audioPanel layout interval-selector + arpeggiate + drone toggle (aria-pressed, Pitfall #9 stop-callback); baseHz default 440; sclIo filename default scale-{N}-tone-{YYYY-MM-DD}
- [Phase ?]: [Phase 02]: Plan 06: Wave-3 INVENTORY consolidation — appended four sections (Scale, Scala I/O, Audio, Components) with 12 new symbol rows; Plans 03/04/05 deferred their rows to avoid Wave-2 merge conflicts
- [Phase ?]: [Phase 02]: Plan 06: Defense-in-depth XSS — all dynamic cell values, status messages, option labels rendered via createElement+textContent; only static <th> row in scaleTable uses innerHTML (no interpolated values); T-02-22, T-02-23, T-02-14 mitigated
- [Phase ?]: [Phase 02]: Plan 06: Deferred CSS-import-from-.ts question to Plan 07 — Framework's esbuild.transform passes 'import "./foo.css"' through unchanged, which fails at runtime; kept per plan instruction since plan acknowledged this as a Plan 07 smoke-test concern; recommended migration to per-page style: frontmatter or <link> head injection
- [Phase 02]: Plan 07: Honored D-01 end-to-end — src/lib/pieces/ does NOT exist; seed scale (D-02) baked as a string constant in src/index.md; COMP-01/02/03 reframed: COMP-03 is now a CI gate on the seed text + .scl round-trip rather than a piece-module test
- [Phase 02]: Plan 07: Honored Pattern 4 (cell-owned synth) on both pages — src/index.md and src/pages/syntonic-comma.md each isolate createSynth() + invalidation.then(synth.dispose) in a cell with no dependencies on scale/baseHz; cross-page navigation does not leak AudioContexts (verified at Checkpoint 2)
- [Phase 02]: Plan 07: COMP-03 reframed gate — src/__tests__/dashboard-seed.test.ts asserts the seed parses to 8 intervals via parseScala (1/1 auto-prepended), period is 2/1 (D-14), round-trips writeScl→parseScl with all intervals equal, and writeScl never emits a 1/1 unison line (D-13); 5/5 tests passing, full Phase 2 suite at 136 passing
- [Phase 02]: Plan 07: Rule 3 deviation — applied prettier --write to scale.ts and scale.test.ts (pre-existing line-length drift from Plan 03 noted in 02-04 SUMMARY's deferred items); pure whitespace, zero logic change; necessary to satisfy plan acceptance criterion 'npm run ci exits 0'
- [Phase 03]: Plan 06: Honored D-01/D-02/D-11/D-13/D-16/D-17/D-18 — viz widgets ship on src/index.md (no theory pages); full-bleed vertical stack lattice→diamond→keyboard; combined sclIo with .scl/.kbm auto-detection by extension; imported .kbm applies to playback by default with override toggle surfacing only after import; Stop button + Esc bound globally with 100ms activeVoices polling; single-column responsive at all widths with 16px input font-size; Safari macOS RDM as verification target
- [Phase 03]: Plan 06: Honored Pitfall #11 (Esc bound in synth cell, no scale-dependency rebinding), Pitfall #2 (no new AudioContext in viz cells), Pitfall #5 (12 INVENTORY rows added with requirement + decision back-references)
- [Phase 03]: Plan 06: UAT discovery (Rule 1 deviation in commit 7d943ae) — per-page `style:` frontmatter REPLACES Framework's default stylesheet rather than augmenting it. Pattern recorded: theme tokens MUST live in styles.css when any page uses style: frontmatter. Also fixed lattice fit-to-viewBox, isometric (col-row, col+row) tonality-diamond grid for true rhombus, translucent color-mix fills that read on both themes, viz max-width caps
- [Phase 03]: Plan 06: Mobile-audit checkpoint PASSED on Safari macOS RDM (iPhone preset) after UAT fix commit; gaps filed: none. mobile-audit.md documents RDM-only limitations (hardware silent switch, autoplay-policy nuances) as accepted residual risk per D-18; physical-iPhone verification + iOS<16.4 fallback deferred to v2
- [Phase ?]: [Phase 04]: Plan 07: Honored D-17/D-19/D-20/D-26 — page-cell hash IO uses replaceState (NEVER pushState; grep gate enforces) + 300ms debounce + decodeHashToScale silent override on success + textContent status surface on null with hash retained for debug-copy
- [Phase ?]: [Phase 04]: Plan 07: Honored D-34 + Pitfall #11 — analysis page synth cell is verbatim mirror of dashboard's; Esc + activeVoices polling bound IN the synth cell (not in feature cells); two pages own SEPARATE AudioContexts (Pattern 4 inheritance verified at Checkpoint 1)
- [Phase ?]: [Phase 04]: Plan 07: INVENTORY consolidation — appended 15 Phase 4 rows (4 sub-tables: edo/mos/url kernels + components incl. disposeScaleCompare); intermediate plans deferred rows to avoid wave-2 merge conflicts
- [Phase ?]: [Phase 04]: Plan 07: Both UAT checkpoints (10/10 each) approved — analysis page UAT (synth lifecycle clean, all 4 widgets functional, mobile-responsive) + URL share-path UAT (BigInt round-trip across fresh-tab paste, replaceState back-button cleanliness, malformed-hash D-20 retention)

### Pending Todos

None yet.

### Blockers/Concerns

None yet.

### Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 260504-i3v | fix CR-01: Interval.octaveReduce infinite loop on period <= 1/1 | 2026-05-04 | e288037 | [260504-i3v-fix-cr-01-interval-octavereduce-infinite](./quick/260504-i3v-fix-cr-01-interval-octavereduce-infinite/) |
| 260504-lb6 | fix CR-02 + CR-03: writeScl description sanitation + parseScl UTF-8 byte cap | 2026-05-04 | 2c44907 | [260504-lb6-fix-cr-02-cr-03-writescl-description-san](./quick/260504-lb6-fix-cr-02-cr-03-writescl-description-san/) |
| 260507-ios | fix tonality-diamond zoom snap: first zoom event jumps diamond to top-left | 2026-05-07 | 98602f5 | [260507-ios-fix-tonality-diamond-ui-shift-bug-on-zoo](./quick/260507-ios-fix-tonality-diamond-ui-shift-bug-on-zoo/) |
| 260507-mgl | app UX/visual upgrade: Framework theme + global stylesheet, sidebar/TOC, orientation card, H2 anchors, header/footer | 2026-05-07 | 2fdd558 | [260507-mgl-look-into-ways-to-make-this-app-better-l](./quick/260507-mgl-look-into-ways-to-make-this-app-better-l/) |
| 260511-j8l | add Pythagorean-comma theory page mirroring syntonic-comma.md; sidebar entry registered | 2026-05-11 | 4d06e66 | [260511-j8l-add-pythagorean-comma-theory-page-at-src](./quick/260511-j8l-add-pythagorean-comma-theory-page-at-src/) |
| 260511-jbq | add schisma theory page (32805/32768) mirroring syntonic-comma.md; sidebar entry registered | 2026-05-11 | ddb15a4 | [260511-jbq-add-schisma-theory-page-at-src-pages-sch](./quick/260511-jbq-add-schisma-theory-page-at-src-pages-sch/) |
| 260511-jyh | add septimal-comma theory page (64/63, Archytas) mirroring syntonic-comma.md; sidebar entry registered | 2026-05-11 | 924edb5 | [260511-jyh-add-septimal-comma-theory-page-at-src-pa](./quick/260511-jyh-add-septimal-comma-theory-page-at-src-pa/) |
| 260511-rb4 | add monzos theory page (prime-factor vectors; 3/2, 5/4, 81/80, 7/6 worked examples) mirroring syntonic-comma.md; sidebar entry registered | 2026-05-12 | 9470db5 | [260511-rb4-add-monzos-theory-page-at-src-pages-monz](./quick/260511-rb4-add-monzos-theory-page-at-src-pages-monz/) |
| 260511-uuh | add otonality-utonality theory page (Partch's over-N/under-N duality; 4:5:6:7 vs 1/4:1/5:1/6:1/7 with kernel-side .inv().octaveReduce() verification); sidebar entry registered | 2026-05-12 | 64ab9a2 | [260511-uuh-otonality-utonality](./quick/260511-uuh-otonality-utonality/) |
| 260511-vlq | add harmonic-series theory page (partials 1-16 over baseHz=110 with per-row playInterval; positioned as "ground floor" first in Theory notes sidebar); See-also back-link from syntonic-comma.md; forward-links to /pages/prime-limits + /pages/odd-limits | 2026-05-12 | 816af02 | [260511-vlq-add-harmonic-series-theory-page](./quick/260511-vlq-add-harmonic-series-theory-page/) |
| 260512-aph | add prime-limits theory page (3-limit 3/2,9/8 -> 5-limit 4:5:6 triad -> 7-limit 7/4 -> 11-limit 11/8 with playInterval at each rung; references commas.ts 5/7/11-limit grouping); sidebar entry after Monzos; harmonic-series.md forward-link parenthetical dropped | 2026-05-12 | fb5a736 | [260512-aph-add-prime-limits-theory-page](./quick/260512-aph-add-prime-limits-theory-page/) |
| 260512-cv2 | add odd-limits theory page (Partch's classification; 7/4 odd-limit-7 vs 9/8 odd-limit-9 vs 16/15 odd-limit-15 worked examples with playInterval + kernel round-trip via oddLimit(); 5-/7-/11-odd-limit diamond walks with ratioPill rows; 11-identity adds 10 new pitches headline; links to interactive diamond on /); sidebar entry after Prime-limits; harmonic-series.md forward-link parenthetical dropped | 2026-05-12 | 0ff3958 | [260512-cv2-add-odd-limits-theory-page](./quick/260512-cv2-add-odd-limits-theory-page/) |
| 260512-ce6 | add comma-pump theory page (5-limit I-vi-ii-V-I cadence; root motion 1/1 -> 5/3 -> 10/9 -> 40/27 -> 80/81 accumulates one syntonic comma flat per cycle; two setTimeout-chained playNotes buttons: drifting cycle (pure 5-limit) + re-anchored cycle (final I forced back to 1/1)); sidebar entry after The syntonic comma | 2026-05-12 | 975cf38 | [260512-ce6-add-comma-pump-theory-page](./quick/260512-ce6-add-comma-pump-theory-page/) |
| 260512-cst | add pythagorean-tuning theory page (12-note chain of pure 3/2 fifths; inline chainOfFifths(n) building a Scale via Interval.mul + octaveReduce, sort by cents, append 2/1 period; scaleTable render at baseHz 261.625 (C4) shows the 13 rows; wolf 5th derived as 262144/177147 ≈ 678.49¢ = (2/1)^7 / (3/2)^11 and auditioned against pure 3/2 via two playInterval buttons); sidebar entry after The Pythagorean comma | 2026-05-12 | ee2cbdf | [260512-cst-add-pythagorean-tuning-theory-page](./quick/260512-cst-add-pythagorean-tuning-theory-page/) |
| 260512-d38 | add meantone theory page (1/4-comma Aron / 1/3-comma Salinas / 1/6-comma Silbermann; tempered 5ths computed at the display layer via 1200*log2(3/2) − (1/n)*1200*log2(81/80) from pureFifth.cents and syntonic.cents projections; new centsToRatio in src/lib/cents.ts wraps xen-dev-utils centsToValue at the project boundary; kernel stays exact via Interval for 3/2, 5/4, 6/5, 81/64, 81/80; variants table at 0.001¢ precision via plain-DOM createElement/textContent; audition: pure 5/4 vs cents-built 1/4-comma meantone third sound identical, with Pythagorean 81/64 contrast); sidebar entry after Pythagorean tuning | 2026-05-12 | 6d22640 | [260512-d38-add-meantone-theory-page-at-src-pages-me](./quick/260512-d38-add-meantone-theory-page-at-src-pages-me/) |
| 260512-dcp | add edo-approximation theory page (12-/19-/31-/53-EDO approximation of 3/2, 5/4, 7/4, 9/8, 11/8; per-cell nearest-step math `Math.round(ji.cents / stepCents)` mirroring bestEdosForScale's discipline — src/lib/edo.ts unchanged; 4×5 DOM table at 2-decimal precision via createElement/textContent shows step + signed cents-deviation; highlights: 31-EDO step 25 = 967.74¢ for 7/4 (1.08¢ off pure, headline 7-limit anchor), 53-EDO step 31 = 701.89¢ for 3/2 (0.07¢ off, Mercator's comma closure); audition: pure 7/4 + 31-EDO 7/4 (centsToRatio audio-boundary pattern) + 12-EDO 7/4 contrast; sidebar entry after Meantone | 2026-05-12 | dbf5b5c | [260512-dcp-add-edo-approximation-theory-page-at-src](./quick/260512-dcp-add-edo-approximation-theory-page-at-src/) |
| 260512-eru | add play-dyad component for simultaneous dyad audition (new src/components/play-dyad.ts mirrors play-interval.ts factory shape, .play-btn class, ARIA, owner-allocates SynthHandle pattern; signature `playDyad(a, b, synth, { baseHz?, duration?, label? })` returns HTMLButtonElement; click handler calls `synth.playNotes([baseHz*Number(a.fraction.valueOf()), baseHz*Number(b.fraction.valueOf())], dur)` — one chord call, BigInt-Fraction stays source of truth, no cents round-trip on the kernel side; PlayDyadOpts.label is `string \| undefined` (vs play-interval's `boolean`) so callers can render arbitrary chord labels like "▶ 4:5:6"; happy-dom vitest spec covers class+ARIA, default/labeled render, playNotes call shape, and custom baseHz/duration forwarding — 6/6 pass; component NOT wired into any page yet — deferred to follow-up tasks) | 2026-05-12 | 3c5976d | [260512-eru-play-dyad-component](./quick/260512-eru-play-dyad-component/) |
| 260512-f0z | add spiral-of-fifths SVG visualization component (new src/components/spiral-of-fifths.ts uses raw `document.createElementNS` keyboard.ts pattern, NO D3; exports `spiralOfFifths(n, opts?: {temperedFifthCents?, highlightWolf?, width?}) => HTMLDivElement` plus pure-kernel `spiralGeometry(n, fifthCents, tempered?)` and `closingErrorCents(n, fifthCents)` for testability; pure-3/2 branch uses exact Interval chain via `.mul(3/2).octaveReduce()` so every step carries an exact BigInt-Fraction ratio, tempered branch returns `ratio: null` and derives cents from octave-reduced cumulative cents; geometry θ_k = k\*(fifthCents/1200)\*2π with auto-shrink dr keeps r_n inside half-viewBox for arbitrary n; for n=12 pure 3/2 the chain fails to close — step 12 lands at 531441/524288 with +23.46¢ Pythagorean-comma gap visible as ≈7° angular offset; optional dashed wolf chord between k=n and k=0 via highlightWolf; matching theme-token spiral-of-fifths.css mirrors keyboard.css; happy-dom vitest spec covers 3 closing-error cases, 5 pure-3/2 geometry cases (incl. full 7-step Pythagorean chain), 3 tempered-branch cases, and 7 DOM smoke cases — 18/18 pass; component NOT wired into any page yet — deferred to follow-up tasks) | 2026-05-12 | 26517a6 | [260512-f0z-build-src-components-spiral-of-fifths-ts](./quick/260512-f0z-build-src-components-spiral-of-fifths-ts/) |
| 260512-fg3 | add vertical Plot chart + sweep arpeggiator + Further reading to harmonic-series page (additive edit to src/pages/harmonic-series.md: new `import * as Plot from "npm:@observablehq/plot"`; chart cell with `y: { domain: [1..16], reverse: true }` + `Plot.ruleY` stems + `Plot.dot` heads colored by largest-prime-in-monzo (`PRIME_AXIS = [2,3,5,7,11,13]`, empty monzo → "other"); D-33-anchored color-blind-aware palette `#4269d0/#ef8e3a/#3ca951/#9c6ade/#d4378a/#1ca8a8/#888888`; "▶ Sweep partials 1→16" button calls `synth.playArpeggio(freqs, 0.35)` where freqs = `partials.map(({iv}) => baseHz * Number(iv.fraction.valueOf()))` (audio-boundary Number coercion, Pitfall #1); `iv.cents` accessed exactly once per partial in the chart IIFE (display boundary); reuses page's existing cell-owned synth (Pattern 4); Further reading section with en.xen.wiki/w/Harmonic_series + Kyle Gann's "Just Intonation Explained" appended after "## See also"; +107 lines additive, no refactor of existing partials array/table/prose) | 2026-05-12 | 17c0939 | [260512-fg3-harmonic-series-partials-chart-sweep-but](./quick/260512-fg3-harmonic-series-partials-chart-sweep-but/) |
| 260512-fo8 | add drift charts + 3-cycle button + Further reading to comma-pump page (additive edit to src/pages/comma-pump.md: new `import * as Plot from "npm:@observablehq/plot"`; "## Visualizing the drift" section between chord-root-math and audition with two Plot.line(curve: "step-after") charts sharing `DRIFT_Y_DOMAIN = [-28, 6]` — drift chart steps down to `Iflat.cents` at chord 5 with `Plot.text` "−21.5¢ (one syntonic comma)" annotation; re-anchored chart holds flat at 0 with red bracket (three `Plot.link` marks terminating at `y: Iflat.cents` negative — bracket points DOWN; grep gate `! grep -q 'y2: -Iflat.cents'` enforces direction) plus `Plot.text` "comma absorbed (21.5¢)"; third "▶ Play 3 drift cycles" button reuses existing `cycleButton`/`playCycle` with `drift3Cycle` — 15 chords built by an IIFE iterating k=0..2 with `shift = shift.mul(COMMA_FLAT = new Interval("80/81"))` so cycle k's chord roots = base × (80/81)^k via `Interval.mul` BigInt-Fraction accumulation; cycle 2's final I' lands at (80/81)^3 ≈ -64.5¢ for the ~65¢ cumulative drift; total run 15 × 1.1s = ~16.5s; Further reading section with en.xen.wiki/w/Comma_pump + Ben Johnston *Suite for Microtonal Piano* (1977) paragraph noting arrow-comma notation and Lubman recording NWR 80637; +189 lines additive, no refactor of existing chord-root constants/cycles/playCycle/cycleButton/synth cell) | 2026-05-12 | 0959878 | [260512-fo8-comma-pump-drift-charts](./quick/260512-fo8-comma-pump-drift-charts/) |
| 260512-fxk | embed spiral-of-fifths component + Plot drift chart + Further reading on pythagorean-comma page (wire previously-unconsumed `src/components/spiral-of-fifths.ts` into `src/pages/pythagorean-comma.md`; +1 line `@import "./components/spiral-of-fifths.css";` in src/styles.css after scale-compare so the SVG picks up theme-token fill/stroke/dashed-wolf styling; new imports `spiralOfFifths` + `* as Plot from "npm:@observablehq/plot"`; new "## The closure gap, visualized" section between audition bullets and "## In monzos" with `${spiralOfFifths(12, { highlightWolf: true })}` widget (13 nodes k=0..12, dashed red wolf chord between k=12 and k=0) + Plot drift chart built by independent BigInt-Fraction accumulator `acc = acc.mul(new Interval("3/2"))` in 12-iter loop; per-step y = `acc.cents - k*700` reads the .cents projection ONCE at the data-row construction boundary (Pitfall #1); ramp rises linearly from 0¢ to +23.46¢ at k=12, `Plot.text` annotation "+23.46¢ (Pythagorean comma)"; chart style mirrors comma-pump.md (blue `#4269d0` line+dots, dashed `#888` ruleY, red `#c45656` annotation, monospace `+`-prefixed tick formatter); new "## Further reading" section appended after "## See also" with en.xen.wiki/w/Pythagorean_comma; existing twelveFifths/cycleOctave math cell untouched; component NOT modified — 18/18 spiral-of-fifths.test.ts still pass; tsc clean; observable build clean; page bundle ~400kB → 920kB matching comma-pump's Plot footprint) | 2026-05-12 | cfebb96 | [260512-fxk-embed-spiral-of-fifths-component-and-add](./quick/260512-fxk-embed-spiral-of-fifths-component-and-add/) |
| 260512-hdv | add Plot scatter chart + three new EDO rows (22/41/72) + Further reading to edo-approximation page (additive edit to src/pages/edo-approximation.md: new `import * as Plot from "npm:@observablehq/plot"`; scatter chart cell `Plot.dot(data, { x: "jiCents", y: "error", fill: "edo", stroke: "edo", r: 5 })` reads from `approxMatrix.flatMap(({N, cells}) => cells.map((c, i) => ({ edo, jiLabel, jiCents: jiIntervals[i].iv.cents, error })))` — one colored series per EDO across the five JI anchors, constant point radius, dashed `ruleY([0])`; new "## Visualizing the deviations" section places `display(scatterChart)` ABOVE the unchanged `display(deviationTable)`; `edos` array expanded from [12, 19, 31, 53] to [12, 19, 22, 31, 41, 53, 72] — three new EDOs in sorted order, nearest-step math `round(ideal/stepCents)` and signed `step*stepCents - ideal` byte-equivalent; section heading "## The four canonical EDOs" renamed "## The seven canonical EDOs" with three new bullets inserted in sorted order: 22-EDO bullet covers ~709.09¢ 5th (+7.14¢ sharp, largest 3-limit deviation among these seven) + ~13¢ 7/4 + "superpyth" temperament context, 41-EDO bullet covers ~0.48¢ 5th (second only to 53-EDO) + ~3¢ 7/4 + note that direct nearest-step 5/4 is ~5.8¢ flat (schismatic mapping aside), 72-EDO bullet covers 72 = 6×12 exact 12-EDO superset + ~3¢ on every anchor + theoretical-common-ground framing; new "## Further reading" section appended as final section linking en.xen.wiki/w/EDO + per-EDO pages for all seven EDOs (12, 19, 22, 31, 41, 53, 72); 22 grep gates pass; tsc + observable build clean; page bundle ~14kB→34kB and 391kB→913kB total imports matching comma-pump's Plot footprint) | 2026-05-12 | d80bf96 | [260512-hdv-edo-approximation-add-plot-and-more-edos](./quick/260512-hdv-edo-approximation-add-plot-and-more-edos/) |
| 260512-ilb | add `Plot.barX` comma-magnitude bar chart (Pythagorean ≈23.46¢ / syntonic ≈21.51¢ / schisma ≈1.95¢, schisma bar highlighted `#c45656` against `#4269d0` parents, x-domain [0,26]¢, `Plot.text` labels to 2 d.p. with `dx:6, textAnchor:"start"`, data rows read `pythagorean.cents`/`syntonic.cents`/`schisma.cents` from existing Interval bindings — no hardcoded numeric literals) + `## Schismatic temperament` section replacing prior single tempering paragraph (new Interval bindings `schismaticThird = new Interval("8192/6561")` ≈ 384.36¢ Pythagorean diminished fourth and `fiveLimitThird = new Interval("5/4")` ≈ 386.31¢, exactly one schisma apart; prose covers the canonical eight-fifths-down-five-octaves-up identity + equivalent `(4/3)^8/2^3` framing + Helmholtz's 0.24¢-per-fifth narrowing recipe yielding 701.71¢ fifth instead of 701.96¢; inline `playDyad(schismaticThird, fiveLimitThird, synth, {label:"8192/6561 + 5/4 (schisma beat)"})` button — the dyad beats at the schisma rate ~2 Hz against the major-third chord, audibly demonstrating the JND-edge framing of the page's opening claim) + `## Further reading` section appended after `## See also` matching pythagorean-comma.md / edo-approximation.md placement (en.xen.wiki/w/Schisma covering Helmholtz/Groven/Garibaldi schismic temperaments + related commas + JND-edge framing; IMSLP Helmholtz *Die Lehre von den Tonempfindungen* / *On the Sensations of Tone* — canonical URL uses `Helmholtz,_Hermann_von` with the `_von` suffix verified against IMSLP, the bare-`Hermann` form returns HTTP 404; hosts both 1863 German original and Ellis 1875 English translation public-domain); math correction during planning — user's original prompt `(3/2)^8 / 2^4 = 6561/4096 ≈ 815.64¢` is one schisma sharp of 8/5 (minor sixth) not 5/4, replaced with `8192/6561 ≈ 384.36¢` after three-source confirmation (Tonalsoft Encyclopedia, Wikipedia *Schismatic temperament*, Xenharmonic Wiki *Schismatic family*); single file edit src/pages/schisma.md +107/−4, no refactor of intro/audition-bullets/`## In monzos`/`## See also`/synth-cell which remain byte-equivalent; BigInt-Fraction stays source of truth throughout (Pitfall #1 — `.cents` read only at display-boundary data-row construction); tsc clean exit 0; vitest 293/293 across 23 test files including play-dyad 6/6; prettier clean; `npm run build` clean with 79 links validated; schisma page bundle 11kB→23kB and 391kB→916kB matching Plot-using pages (comma-pump 912kB / pythagorean-comma 920kB / edo-approximation 913kB) | 2026-05-12 | 59a122b | [260512-ilb-on-src-pages-schisma-md-add-comma-magnit](./quick/260512-ilb-on-src-pages-schisma-md-add-comma-magnit/) |
| 260512-ksu | add Live EDO explorer to src/pages/edo-approximation.md (new `## Live EDO explorer` section inserted between `## What the table says` and `## Audition`; `Inputs.range([5, 72], {step:1, value:31, label:"EDO size N"})` wrapped in `view()` exposes reactive scalar `liveN`; one-row table mirrors the static deviation table cell-for-cell — header `EDO \| 3/2 \| 5/4 \| 7/4 \| 9/8 \| 11/8`, `<th scope="row">${liveN}-EDO</th>` row label, cells `${step} @ ${fmtErr(error)}` with the page's existing `fmtErr` sign rules (`+`/`−`/`""`, 2 d.p., `¢` glyph); decision: NOT reusing `bestEdosForScale` from src/lib/edo.ts — that helper returns aggregate metrics (`maxCentsError`, `rmsCentsError`, `tenneyWeightedError`) not per-anchor `{step, error}` cells; inline same `round(c/s)` math the static table uses (`s = 1200/N`, `step = round(iv.cents/s)`, `error = step*s - iv.cents`); Pitfall #1 honored — reuses existing `jiIntervals` (5 anchors as `Interval` BigInt-Fraction); `iv.cents` read only at display-layer data construction, no `new Interval(<cents>)` anywhere (grep gate `! grep -q "new Interval(.*cents"` clean); src/pages/edo-approximation.md +64 lines, no refactor of static table / four-canonical-EDO bullets / audition / monzo blocks; tsc clean) | 2026-05-12 | c1117f8 | [260512-ksu-add-live-edo-explorer-to-src-pages-edo-a](./quick/260512-ksu-add-live-edo-explorer-to-src-pages-edo-a/) |
| 260512-kzw | add Limit explorer slider (3→5→7→11→13) + reactive table (newly-entering ratios with Ratio/Monzo/Cents/Play columns) + prime-identity Plot strip chart (each prime's basic identity `p/2^floor(log₂ p)` against 12-TET grid: 3→3/2 ~702¢, 5→5/4 ~386¢, 7→7/4 ~969¢, 11→11/8 ~551¢, 13→13/8 ~841¢ — 11 and 13 visibly between 12-TET pitches) + `## Further reading` (Xenharmonic wiki Prime_limit + Partch *Genesis of a Music* archive.org) above the four `## N-limit` sections on src/pages/prime-limits.md; default slider value=1 (5-limit); reactive `Prime limit: N` caption below slider; Plot import added to existing single import cell (Framework module-scope rule); Unicode bra-ket on `<code>` tabular-nums NOT KaTeX (avoids re-typeset flicker per monzo-builder lesson); reuses page's existing synth/ratioPill/playInterval; `limitSet` precomputes 12 `new Interval(ratio)` once, `.cents`/`.monzo` read only at display step (Pitfall #1); strip chart `y: { axis: null }` single horizontal strip with `Plot.text(dy:-14)` labels above dots; src/pages/prime-limits.md +180 lines additive (no refactor of existing imports/synth/example-Intervals/four `## N-limit` sections/`## Limits in the kernel`/`## See also` — byte-identical to baseline in `## 3-limit … ## See also` range); H2 ordering verified `## Limit explorer` @ line 48 before `## 3-limit (Pythagorean)` @ 214 + `## Further reading` @ 333 after `## See also` @ 314; tsc baseline unchanged | 2026-05-12 | c29b60e | [260512-kzw-prime-limit-explorer-slider-identity-plo](./quick/260512-kzw-prime-limit-explorer-slider-identity-plo/) |
| 260512-mvw | embed static `renderDiamondSVG` previews (5-/7-/11-odd-limit) inline under their respective H3 sections on src/pages/odd-limits.md + new `## Prime-limit vs odd-limit, side by side` H2 with 12-row plain-DOM comparison table (9/8, 5/4, 7/6, 81/80, 25/24, 15/8, 9/7, 11/8, 13/8, 81/64, 7/4, 16/15 — sorted by ascending odd-limit, tiebreak on prime-limit; columns Ratio/Odd-limit/Prime-limit/Agree? with ✓ on the five axis-1 rows) + new `## Further reading` section (en.xen.wiki/w/Odd_limit) after `## See also`; new pure-presentational `renderDiamondSVG(oddLimit, opts?)` export on src/components/tonality-diamond.ts — no synth/scale/zoom/click dependencies, reuses existing `diamond-cell--in-scale diamond-cell--axis-N` CSS classes so theme tokens cascade, role=img + aria-label + per-cell `<title>` tooltips, createElementNS + setAttribute + textContent throughout (XSS T-02-22/T-02-23 mitigated); `primeLimitOfMonzo` promoted from tonality-diamond.ts into src/lib/monzo.ts (single source of truth, Pitfall #5 wrap-don't-reimplement); existing `tonalityDiamond` interactive factory + tests untouched (5/5 still pass — refactor is import-site-only and semantics-preserving); two small additive CSS blocks in src/styles.css (`.limit-comparison-table td.num` tabular-nums+right-align, `.limit-comparison-table td.agree` centered ✓ in --theme-green, `.tonality-diamond-static` flex-centered wrapper); Pitfall #1 honored throughout — iv.monzo is read at data-row step, no .cents in comparison table (table is about the two limits not pitch height); src/lib/monzo.ts +20 lines, src/components/tonality-diamond.ts net +98 (renderDiamondSVG added, primeLimitOfMonzo removed locally), src/pages/odd-limits.md +96 additive (no refactor of imports cell / synth cell / 3 example Interval bindings / oddLimitChecks display / two `### prime-vs-odd disagree` paragraphs / 5-/7-/11-odd-limit pill rows / `## In the kernel` / `## See also` — all byte-identical to baseline in those ranges), src/styles.css +24; tsc clean; vitest 302/302 across 24 files; prettier+eslint clean; observable build clean with 79 links validated; odd-limits page bundle 12→42 kB and 391→705 kB matching the d3 footprint that travels with tonality-diamond.ts | 2026-05-12 | 29a8956 | [260512-mvw-odd-limits-static-diamonds-and-table](./quick/260512-mvw-odd-limits-static-diamonds-and-table/) |
| 260512-mk8 | add paired partials Plot chart (otonal 4:5:6:7 ascending / utonal 1/1:8/7:4/3:8/5 re-rooted upward, shared cents-axis [0,1050]¢, Plot.ruleY stems + Plot.dot heads, `iv.cents` read at data-row construction — Pitfall #1, no float-literal cents) + per-note `playInterval(iv, synth, {label:true})` ▶ buttons on every chord-tone bullet in both H2 sections (4+4=8 new buttons, existing chord-buttons preserved, reuses page synth cell — Pitfall #2 honored, one createSynth) + `## Further reading` H2 after `## See also` (en.xen.wiki/w/Otonality_and_utonality + Kyle Gann's "La Monte Young's *The Well-Tuned Piano*"); new Plot + playInterval imports added to existing single import cell (Framework module-scope rule); src/pages/otonality-utonality.md +96/−8 additive (no refactor of synth cell/otonal+utonal Interval arrays/ratioPill bullets/`.inv().octaveReduce()` truth-check/chord-button IIFEs/`## Monzo signs flip`/`## See also` — byte-identical in their ranges); tsc baseline unchanged | 2026-05-12 | ce78eab | [260512-mk8-otonal-utonal-partials-chart-per-note-pl](./quick/260512-mk8-otonal-utonal-partials-chart-per-note-pl/) |
| 260512-msv | add simultaneous-A/B `playDyad(fiveLimit, pythagorean, …)` button + static beat-frequency display + `> **Tempered out by.**` blockquote callout (meantone quarter-/third-/sixth-/seventh-comma variants, 12-EDO, 19-EDO, 31-EDO, 53-EDO — the temperaments that map 81/80 to a unison) + `## Further reading` H2 (en.xen.wiki/w/81/80) on src/pages/syntonic-comma.md (single-file additive edit +33/-0; new `import { playDyad } from "../components/play-dyad.js"` joined to existing import cell; reuses existing `fiveLimit = new Interval("5/4")` + `pythagorean = new Interval("81/64")` Interval bindings — no new Interval allocations; reuses page-level `synth = createSynth()` (Pitfall #2 honored, one AudioContext owner); `beatHz` derived in dedicated IIFE cell at audio-boundary `Math.abs(baseHz * Number(pythagorean.fraction.valueOf()) - baseHz * Number(fiveLimit.fraction.valueOf()))` then surfaced via `${beatHz.toFixed(3)}` so the 6.875 Hz value is computed not literal — Pitfall #1; placement: A/B dyad + beatHz IIFE + beat-frequency prose inserted between the existing "Audition the difference:" list and `## In monzos`, callout blockquote between `## In monzos` and `## See also`, `## Further reading` appended last matching schisma.md / pythagorean-comma.md / edo-approximation.md / monzos.md convention; no callout CSS class exists in src/styles.css so plain `> **Tempered out by.**` markdown blockquote used per project minimal-but-design-conscious style; tsc baseline unchanged (5 pre-existing `npm:` specifier errors only); `npm run build` clean, 79 links validated; page bundle 11kB→14kB and 391kB→392kB total imports (playDyad deduplicated with schisma.md) | 2026-05-12 | 8b94311 | [260512-msv-on-src-pages-syntonic-comma-md-add-a-sim](./quick/260512-msv-on-src-pages-syntonic-comma-md-add-a-sim/) |
| 260512-jiq | add interactive Monzo builder above the round-trip cell on src/pages/monzos.md (new src/components/monzo-builder.ts — Pattern 2 closure-component `monzoBuilder(synth, opts?) => HTMLElement`, no module-level state, mirrors mos-builder.ts shape; five `<input type="number" min="-12" max="12" step="1">` for primes 2/3/5/7/11 with `aria-label="Exponent of prime N"` + one `<input type="text">` ratio-string field with bidirectional sync; BigInt-Fraction is canonical — `applyExponents()` builds `Interval.fromMonzo([...exponents])` and writes `ratioInput.value = iv.toString()` only if `document.activeElement !== ratioInput`; `applyRatio()` does `try { new Interval(raw) }` on transient invalid input — preserves prior exponent state and surfaces `err.message` via `errHost.textContent`; on success detects primes above 11 via `iv.monzo.slice(5).some(x=>x!==0)` and range-checks each padded slot against [-12,12] before writing back; cents is read ONLY at the display boundary (Pitfall #1 — `.cents.toFixed(2)` into a `<span>` textContent); display surfaces are Unicode bra-ket `[ a  b  c  d  e ⟩` on a `<code>` with `font-variant-numeric: tabular-nums` — explicitly NOT KaTeX, which would re-typeset on every keystroke and flicker; `ratioPill(iv, {showCents:false})` + `(~ddd.dd¢)` cents readout + `playInterval(iv, synth, {label:false})` ▶ button all re-rendered via `replaceChildren()` per state change so the discarded button GCs with its click listener — AudioContext stays owned once by the page-level synth cell (Pattern 4); two-way sync correctness: programmatic `.value =` writes do NOT refire input events on the same target so the 5+1 listeners cannot enter a feedback loop, and the `document.activeElement` gate prevents canonicalised `4/2`→`2` from clobbering the user's literal keystrokes; empty-input rule mirrors mos-builder.ts:53-59 — `parseInt(input.value,10)` returning NaN preserves prior canonical value rather than coercing to 0; T-02-22/T-02-23 XSS discipline — every dynamic surface uses `createElement` + `.textContent =`, never `.innerHTML =` for user values; error region is `role="status" aria-live="polite"` for screen readers) + new `## Worked examples — projected to the 3-5 plane` Plot.dot + Plot.text scatter section below the builder deriving x = `iv.monzo[1] ?? 0` (prime-3) and y = `iv.monzo[2] ?? 0` (prime-5) from the existing four `fifth` / `majorThird` / `syntonic` / `septimalSubminorThird` bindings — no new Interval allocations, no hardcoded coordinates, no new kernel calls; 3/2 + 7/6 collision at (-1, 0) explicitly acknowledged in prose as pedagogical (they differ only in the prime-7 axis which this projection collapses to zero) not a rendering bug + new `## Further reading` section appended after `## See also` linking en.xen.wiki/w/Monzo (community-curated reference covering bra-ket form, monzo arithmetic, prime limits, and vals as the dual lattice for regular-temperament theory) matching schisma.md / pythagorean-comma.md / edo-approximation.md placement; src/pages/monzos.md +71 lines (additive only — no refactor of imports/synth-cell/four worked-example Interval bindings/Round-trip check cell/Monzo-addition section/`## See also`) + new monzo-builder.ts (262 lines) + matching monzo-builder.css (109 lines, exact mos-builder.css token vocabulary `--theme-foreground[-alt/-focus]`, `--theme-background`, `--monospace`, `--sans-serif`) + happy-dom vitest spec __tests__/monzo-builder.test.ts (9 tests covering exponent→ratio sync, ratio→exponent sync "5/4"→[-2,0,1,0,0], "13/12" primes-above-11 error path, empty-input preserve-state, exponent out-of-range error, ratioPill/playInterval re-render, DOM smoke) + one-line `@import "./components/monzo-builder.css";` in src/styles.css after the existing component imports matching scale-compare / spiral-of-fifths pattern; `grep -n 'innerHTML' src/components/monzo-builder.ts src/pages/monzos.md` → 0 matches involving user values; tsc clean exit 0; vitest 302/302 across 24 files (293 baseline + 9 new); prettier + eslint clean; observable build clean with 79 links validated; monzos page bundle 11→26 kB and 391→918 kB matching Plot-using pages footprint (comma-pump 912kB / pythagorean-comma 920kB / edo-approximation 913kB / schisma 916kB) | 2026-05-12 | 0e9504b | [260512-jiq-on-src-pages-monzos-md-add-an-interactiv](./quick/260512-jiq-on-src-pages-monzos-md-add-an-interactiv/) |
| 260512-sxc | add simultaneous-A/B `playDyad(harmonicSeventh, pythagoreanMinorSeventh, …)` button (≈12.222 Hz beat at A=440 — roughly twice the syntonic-comma beat-rate as expected from 64/63's roughly-twice-21.5¢ size) + inline `beatHz` IIFE derived at audio boundary `Math.abs(baseHz * Number(pythagoreanMinorSeventh.fraction.valueOf()) − baseHz * Number(harmonicSeventh.fraction.valueOf()))` surfaced via `${beatHz.toFixed(3)}` (Pitfall #1 — no `12.222` literal in prose, only `// 12.222… Hz` end-of-line comment) + small Plot strip chart on src/pages/septimal-comma.md showing partial-7 (7/4 ~968.83¢ in `#4269d0`) and stack-of-two-4/3s (16/9 ~996.09¢ in `#ef8e3a`) on a shared cents axis (domain [955, 1010]¢ — ~13¢ of room each side for surrounding 5¢ tick marks), dashed `Plot.ruleX` markers, stacked Plot.text labels (name dy:-28 + cents readout dy:-14), `↔ ${septimal.cents.toFixed(2)}¢ (64/63)` red `#c45656` gap annotation centered between the two pitches at y:0.55 — all `.cents` read once at data-row construction from existing Interval bindings, no hardcoded 968.83/996.09/27.26 literals (Pitfall #1) + `> **Tempered out by.**` blockquote callout listing **22-EDO + Superpyth temperament (a.k.a. "archy" in 2.3.7) + Dominant temperament** (the 7-limit meantone supported by 12-EDO where 16/9 = 7/4) with explanatory clause distinguishing these from huygens septimal meantone / 31-EDO which preserves the 64/63 distinction by design — callout list factually corrected from the user's originally-stated `22-EDO, 31-EDO, septimal meantone` after Xen Wiki + Wikipedia research confirmed 31-EDO patent val maps 7/4 → step 25 and 16/9 → step 26 (so 64/63 → step 1, NOT tempered out) and that standard huygens septimal meantone is the discipline that distinguishes 7/4 from the four-fifths-up augmented sixth; user approved the corrected list before edit + `## Further reading` H2 with three references: en.xen.wiki/w/64/63 (canonical Xen Wiki page for Archytas's comma + Superpyth-in-22-EDO + Dominant-in-12-EDO temperament family + chain of 7-limit relations 7/4↔16/9, 9/7↔four-fifths-up, 9/8↔8/7), en.xen.wiki/w/Archytas_clan (temperament family — Archytas/Superpyth/Pajara/Dominant and the equivalence of two octave-reduced fifths with a tone that is both 8/7 and 9/8), Andrew Barker, *Greek Musical Writings, Vol. II: Harmonic and Acoustic Theory* (Cambridge University Press, 1989) print citation for Archytas of Tarentum's three tetrachords (enharmonic 28:27:36:35:5:4, chromatic 28:27:243:224:32:27, diatonic 28:27:8:7:9:8) — historical entry point of prime 7 / 28/27 / 64/63 into Greek music theory in the 4th century BCE; single-file additive edit src/pages/septimal-comma.md +117/-0 (no refactor of existing imports cell or synth cell or three-Interval-bindings cell or `${tex}` header math or "Audition the difference:" bulleted list or `## In monzos` block or `## See also` — all byte-identical to baseline in their ranges); new `import { playDyad } from "../components/play-dyad.js"` and `import * as Plot from "npm:@observablehq/plot"` joined inline to existing single import cell (Framework module-scope rule — second import cell would have re-bound the existing names); reuses existing `harmonicSeventh = new Interval("7/4")`, `pythagoreanMinorSeventh = new Interval("16/9")`, `septimal = commaByName("septimal comma")!` Interval bindings — no new Interval allocations (grep gate `! grep -E 'new Interval\("(7/4\|16/9)"\)' src/pages/septimal-comma.md` matches only baseline lines 29-30); reuses page-level `synth = createSynth()` — Pitfall #2 honored, only one AudioContext owner (literal token appears 2× because of explanatory comment on line 17 "lazy createSynth()", one actual call-site on line 20); chart color encoding: harmonic-blue `#4269d0` for the natural 7th (the "JI" side) + Pythagorean-orange `#ef8e3a` for 16/9 (the "chain-of-fifths" side) + red `#c45656` for the comma annotation (matches schisma commaBarChart, comma-pump bracket-annotation, comma-magnitude family color convention); chart domain [955, 1010]¢ chosen so the 27.26¢ gap reads as the dominant visual feature with surrounding 5¢ grid ticks visible; placement: dyad button + beatHz IIFE + beat-frequency prose + strip-chart intro + strip-chart cell inserted between the existing "Audition the difference:" list and `## In monzos`, callout blockquote between `## In monzos` and `## See also`, `## Further reading` appended last matching syntonic-comma.md / schisma.md / pythagorean-comma.md / edo-approximation.md / monzos.md convention; tsc baseline unchanged (5 pre-existing `npm:` specifier errors only); `npm run build` clean, 79 links validated; page bundle 11kB→21kB and 391kB→916kB matching Plot-using pages footprint (schisma 916kB / harmonic-series 912kB / monzos 918kB / comma-pump 912kB) | 2026-05-12 | 176fc73 | [260512-sxc-on-src-pages-septimal-comma-md-add-a-sim](./quick/260512-sxc-on-src-pages-septimal-comma-md-add-a-sim/) |
| 260512-tlv | add Commas (glossary) page — new src/pages/commas.md indexing all 16 named commas from src/lib/commas.ts grouped by canonical prime-limit (3-/5-/7-/11-limit); kernel-bound throughout (every Interval via `commaByName(c.name)!` so the source table is canonical, prime-limit derived at runtime as last-nonzero-index of `iv.monzo` so Pythagorean comma + Mercator's comma classify as 3-limit despite the source file's `// 5-limit` comment header); per-section DOM tables built via `buildCommaTable(entries)` helper with columns Name (linked `<a href>` for syntonic/Pythagorean/schisma/septimal — the four with dedicated pages — plain text for the other 12) / Ratio (ratioPill, showCents:false) / Monzo (tex bra-ket on canonical minimal-length monzo) / Cents (iv.cents.toFixed(3) so 0.721¢ breedsma and 0.396¢ ragisma stay distinguishable from zero) / Limit / ▶ (playInterval, no label); page-level synth cell owns the only AudioContext (Pitfall #2); no @observablehq/plot import — tabular index not viz, bundle stays 16kB/391kB matching syntonic-comma 14kB/392kB; footer `## See also` + `## Further reading` (en.xen.wiki/w/Comma) mirrors syntonic-comma/pythagorean-comma/schisma/septimal-comma convention; one-line edit to observablehq.config.ts inserting `{ name: "Commas (glossary)", path: "/pages/commas" }` immediately after Comma pump (alphabetical slot between "Comma pump" and "EDO approximations"); tsc clean; npm run build clean — 18 pages (was 17), 88 links validated (was 79); grep gate `! grep -nE '[0-9]+\.[0-9]+¢' src/pages/commas.md` clean — no hardcoded float-cents literals | 2026-05-12 | 69b7a88 | [260512-tlv-create-commas-glossary-page](./quick/260512-tlv-create-commas-glossary-page/) |
| 260512-ngj | meantone.md additions (additive edit): (a) Plot horizontal cents-axis strip chart placed BEFORE the variants table — six tempered fifths (1/3, 2/7, 1/4, 1/5, 1/6, 1/8) plotted as dashed-blue `Plot.ruleX` verticals + `Plot.dot` heads + per-row alternating-`dy` 1/n labels against a 0..`Math.ceil(pureFifth.cents)` axis, with a pure-3/2 reference rule at the right edge and a red `Plot.link` horizontal bracket spanning the syntonic comma from `pureFifth.cents − syntonic.cents` to `pureFifth.cents` at the lower half of the strip lane — all `.cents` derived from existing `pureFifth`/`syntonic` Interval bindings + the new `verheijen`/`eighth`/`zarlino` tempered-fifth bindings, no float-cents literals in chart data rows (Pitfall #1); (b) variants table extended from 3 rows to 6 rows by adding `verheijen = tempered(5)` / `eighth = tempered(8)` / `zarlino = tempered(7 / 2)` callsites to the existing factory cell (2/7-comma uses n = 7/2 since the formula is `pureFifth.cents − syntonic.cents/n`, so n=7/2 ⇒ fifth = pureFifth.cents − (2/7)·syntonic.cents — kernel-true substitution, no new branch in `tempered`) and appending three rows to the local `rows` array in the variantsTable IIFE — same `createElement` + `textContent` shape, same `c.toFixed(3)` precision, signature-property strings: "fifths only ~4.3¢ flat; major 3rd ~6.9¢ sharp of pure 5/4 — milder than 1/4-comma" (1/5 Verheijen), "equalizes the major-3rd and minor-3rd deviations from pure (both ~5.4¢ off)" (2/7 Zarlino), "softest meantone — closest variant to 12-TET (fifths only ~2.7¢ flat)" (1/8); table now reads 1/4 Aron → 1/3 Salinas → 1/6 Silbermann → 1/5 Verheijen → 2/7 Zarlino → 1/8; (c) new `## The wolf at k=12 (1/4-comma meantone)` H2 inserted between the third 1/6-comma Silbermann bullet and the existing `## Audition — the 1/4-comma equivalence` H2, embedding `${spiralOfFifths(12, { temperedFifthCents: quarter.fifth, highlightWolf: true })}` which exercises the spiral component's tempered branch — ratio labels dropped (1/4-comma fifth is irrational), signed cents-from-12-TET shown per node via octave-reduced cumulative cents per spiral-of-fifths.ts:110-128, dashed red wolf chord between k=12 and k=0 visualizing the ≈ -40.7¢ closure undershoot (meantone wolf = twelve 1/4-comma fifths fall *short* of seven octaves, mirror-image of the +23.46¢ Pythagorean-comma overshoot covered on the companion pythagorean-comma.md page); (d) `## Further reading` H2 appended after `## See also` (now the final section) with en.xen.wiki/w/Meantone_family — covers the regular-temperament family generated by tempering out the syntonic comma, the full continuum of comma fractions (1/n-comma meantone for arbitrary n ∈ ℝ), the historical named variants as points on that line, the MOS pattern induced by each variant, and how meantone fits inside the broader temperament zoo (schismatic, dominant, mavila); new imports `import { spiralOfFifths } from "../components/spiral-of-fifths.js"` and `import * as Plot from "npm:@observablehq/plot"` joined inline to the existing single import cell (Framework module-scope rule — second import cell would have re-bound `Interval`, etc.); spiral-of-fifths.css already globally imported via src/styles.css:32 from the prior quick-260512-fxk wiring task — no styles.css edit needed; variantsTable IIFE preserves T-02-22 XSS discipline (createElement + textContent, no innerHTML); existing synth/kernel-anchors/tempered-factory/playTempered/playPureMajor3/playQuarterMajor3/playPythMajor3 bindings unchanged byte-for-byte (Pitfall #2 honored — exactly one `createSynth()` call site remains, plus an unrelated comment mention preserved from baseline); single-file additive edit src/pages/meantone.md +121/-4 net +117 additive across three feat commits + one merge (f78369d variants table, 4478eff Plot chart, 4c5b540 spiral wolf + Further reading); chart-cell deviations from initial plan (within executor discretion): switched `Plot.ruleY` with x1/x2 args → `Plot.link` for the syntonic-comma horizontal bracket (Plot.ruleY does not accept x1/x2 as segment endpoints); per-row alternating `dy` callback to avoid label collisions in the strip chart; tsc baseline unchanged (5 pre-existing `npm:` specifier errors in non-markdown source files only); vitest 302/302 across 24 files unchanged (no test files added); prettier + eslint clean; `npm run build` clean with 17 pages rendered + 79 links validated; meantone page bundle 25→34 kB (+9 kB Page) and 391→920 kB Imports (+529 kB — Plot is heavy) matching the project's Plot-using pages footprint (pythagorean-comma 920kB / comma-pump 912kB / edo-approximation 913kB / schisma 916kB / monzos 918kB / septimal-comma 916kB) | 2026-05-12 | 252b209 | [260512-ngj-meantone-md-plot-fifths-chart-1-5-1-8-2-](./quick/260512-ngj-meantone-md-plot-fifths-chart-1-5-1-8-2-/) |
| 260513-8jw | add reusable `furtherReading(items)` presentational helper (new src/components/further-reading.ts — Pattern 1 pure-presentational factory accepting `{ title: string; url: string; note?: string | Node }[]` returning `<section class="further-reading"><ul>...</ul></section>` with one `<li>` per item; XSS-safe via createElement + textContent for string notes, Node notes appended as-is so callers can pass `` html`text with ${tex`81/80`} inline` `` to preserve inline KaTeX / links / bold inside note bodies; external URLs auto-get `rel="noopener noreferrer"`; the page's Markdown `## Further reading` heading stays in Markdown so Framework TOC keeps picking it up — the helper renders only the list body) + matching src/components/further-reading.css (disc bullets at 1.5rem indent, 0.5rem item spacing, line-height 1.5, link weight 500, `.further-reading__note` muted via `--theme-foreground-alt`) + one-line `@import` in src/styles.css after monzo-builder.css; adoption on seven target theory pages + backfill of one classic-text reference per page where the user's spec called for one — monzos.md replaces existing bullet with `furtherReading([...])` and adds David B. Doty *The Just Intonation Primer* (3rd ed., Just Intonation Network, 2002); odd-limits.md replaces existing bullet and adds Harry Partch *Genesis of a Music* (2nd ed., Da Capo Press, 1974) on archive.org mirroring prime-limits.md URL form; prime-limits.md adoption-only (already had XW Prime_limit + Partch on archive.org); otonality-utonality.md adoption-only (already had XW Otonality_and_utonality + Kyle Gann "La Monte Young's *The Well-Tuned Piano*" essay); comma-pump.md adoption-only with `html`-tagged-template note body preserving inline `${tex`81/80`}` in the Ben Johnston Suite for Microtonal Piano (1977) row (Lubman NWR 80637 recording reference preserved); pythagorean-tuning.md gains a brand-new `## Further reading` H2 after `## See also` (was missing entirely — the only target page without one) with XW Pythagorean_tuning entry + Hermann von Helmholtz *On the Sensations of Tone* (Ellis tr., 1875) on IMSLP using `Helmholtz,_Hermann_von` URL form (verified per prior task 260512-ilb that the bare-`Hermann` form returns HTTP 404); meantone.md replaces existing bullet using `html`-tagged-template note body preserving inline `${tex`n \in \mathbb{R}`}` in the XW Meantone_family row AND adds Easley Blackwood *The Structure of Recognizable Diatonic Tunings* (Princeton UP, 1985) — the modern canonical text on meantone temperament families that lays out the 1/n-comma continuum as recognizability bands, with the companion Cedille recording CDR 90000 018/019 auditioning all 24 etudes in 13- through 24-EDO; each of the seven pages gets `import { furtherReading } from "../components/further-reading.js"` joined inline to its existing single import cell (Framework module-scope rule — a second import cell would re-bind `Interval`, etc.); Markdown `-` bullets under `## Further reading` replaced with a single JS code fence calling `furtherReading([...])` keeping the H2 itself in Markdown for TOC integrity; eight atomic commits on main (d9fc91f component + CSS + styles.css wiring, f7bb735 monzos + Doty, 0b536e1 odd-limits + Partch, 8d0e785 prime-limits adoption, 630301f otonality-utonality adoption, fe379f3 comma-pump adoption + html-template Johnston note, 1d0f626 pythagorean-tuning brand-new section + XW + Helmholtz, 8bbb00e meantone + Blackwood); HARD CONSTRAINT honored throughout: src/lib/* and src/audio/* untouched, no kernel changes — pure presentational + per-page Markdown edits; out-of-scope pages with existing Further-reading sections (syntonic-comma, pythagorean-comma, schisma, septimal-comma, harmonic-series, edo-approximation, commas) left untouched per user's explicit list — they can adopt the helper in a follow-up task without disrupting their existing references; tsc baseline unchanged (5 pre-existing `npm:` specifier errors in non-markdown source files only, present pre-edit); vitest 302/302 across 24 test files (no test files added — pure-presentational component with no kernel deps to mock); prettier clean on all 9 touched files (per-file `prettier --check` confirmed); eslint clean; `npm run build` clean — 18 pages rendered, 88 links validated; furtherReading module emitted as dist/_import/components/further-reading.<hash>.js with the expected `export function furtherReading(items)` signature; rendered HTML grep gate `grep -c 'furtherReading\|further-reading' dist/pages/<page>.html` returns 8 on each of the 7 target pages (import + call site + module reference); per-page bundle delta 1–8 kB Imports (the helper itself is tiny — most of each page's bundle is dominated by inherited Plot / d3 / xen-dev-utils stacks already on the page) | 2026-05-13 | 8bbb00e | [260513-8jw-further-reading-component](./quick/260513-8jw-further-reading-component/) |
| 260513-91h | cross-page consistency pass on all 13 theory pages: (a) cents-formatting normalised — `.toFixed(1)` → `.toFixed(2)` in two table cells (harmonic-series partials column, prime-limits reactive limit-table), 11 inline-prose 2dp values on edo-approximation downgraded to 1dp (~1.08¢→~1.1¢ etc.), canonical-anchor prose mentions upgraded to 3dp (21.506¢ syntonic / 23.460¢ Pythagorean / 1.954¢ schisma / 27.264¢ septimal / 701.955¢ pure-5th / 678.495¢ pure-Pythagorean wolf), sub-cent meantone-style precision preserved at 3dp where author chose (schismatic 0.244¢ narrowing, meantone variants .toFixed(3) table); tex math, kernel-cell code comments, and Plot chart annotation strings deliberately untouched per PLAN; (b) new section ordering with `<aside class="prereq">` callout added between synth cell and first kernel cell on every theory page except foundation harmonic-series.md (12 asides total, structural-prereq mapping derived semantically not from config order — monzos→harmonic-series, prime-limits→monzos+harmonic-series, odd-limits→prime-limits, otonality-utonality→harmonic-series+prime-limits, syntonic-comma→harmonic-series+prime-limits, comma-pump→syntonic-comma, pythagorean-comma→prime-limits, pythagorean-tuning→pythagorean-comma+harmonic-series, meantone→syntonic-comma+pythagorean-tuning, edo-approximation→prime-limits+pythagorean-tuning+meantone, schisma→pythagorean-comma+syntonic-comma, septimal-comma→prime-limits+syntonic-comma); (c) Interval-constructor audit (grep -nE 'new Interval\\([^"]' src/pages/*.md) returned only `(s)` variable-passes inside `.map((s) => new Interval(s))` callbacks where `s` is a string from an array literal — all `new Interval(...)` calls confirmed to use string args end-to-end, no float-as-source-of-truth violations; no code changes required, audit recorded as a verification step in SUMMARY.md; (d) new `aside.prereq` CSS rule appended to src/styles.css after the existing helpers — quiet left-rule callout (border-left 3px var(--theme-foreground-faint) on a 4%-tinted background, var(--sans-serif) 14px var(--theme-foreground-muted) body, strong+a darker via inherit/text-decoration-color) intended to read as scaffolding rather than as a warning; analysis.md and commas.md explicitly excluded per scope decision (interactive analysis app + glossary index, structurally different from theory expositions); kernel-side (src/lib/, src/audio/, src/components/) untouched; page semantics (rewording, link removal, example changes) explicitly preserved; AskUserQuestion clarified ambiguities upfront — page-set (13 vs 15), callout placement (aside-after-synth vs trailing blockquote, user picked aside-after-synth), and canonical-anchor preservation policy (user picked anchors-at-3dp); 14 atomic commits each titled `chore(260513-91h): src/pages/<file>.md — <delta>` or `chore(260513-91h): src/styles.css — add aside.prereq theory-page callout style`; npm run build clean, 18 pages rendered, 95 links validated (the new relative `./xxx` aside links pass Framework's link checker); rendered HTML inspection on dist/pages/meantone.html confirms aside markup survives Framework's transpilation; flags --validate triggered plan-check (self-audit of PLAN.md against goal before execution) plus post-execution verification (grep counts, build pass, render spot-check) | 2026-05-13 | cd359ba | [260513-91h-consistency-pass-cents-formatting-sectio](./quick/260513-91h-consistency-pass-cents-formatting-sectio/) |
| 260512-udc | bring four comma pages (pythagorean / syntonic / schisma / septimal) to consistent 5-section structure ((1) definition + cents, (2) audition dyads + simultaneous playDyad, (3) "In monzos", (4) "Tempered out by", (5) "See also" + "Further reading"); pythagorean-comma.md gains simultaneous `playDyad(pureOctave, cycleOctave, synth, …)` audition button between the existing playInterval bullets and "## The closure gap, visualized" + new `> **Tempered out by.**` blockquote between `## In monzos` and `## See also` listing 12-EDO / 19-EDO / 24-EDO (per user spec — "any 12-stable EDO"; 19-EDO flagged in SUMMARY for SME confirmation since the patent-val 19 maps 12 fifths → 132 steps vs 7 octaves → 133 steps so the comma is NOT vanished under the patent val); schisma.md gains canonical `> **Tempered out by.**` blockquote between `## In monzos` and `## See also` listing schismatic temperament (Helmholtz / Groven / Garibaldi) + 53-EDO (Mercator–Holder cycle, 53 fifths = 31 octaves) + 41-EDO (alternative 5-limit-friendly host); existing `## Schismatic temperament` deep-dive section in schisma.md preserved byte-equivalent (still includes 8192/6561 + 5/4 playDyad and Helmholtz 0.24¢-narrowing recipe); syntonic-comma.md + septimal-comma.md unchanged — already at target structure per prior tasks msv (#13) and sxc (#14); HARD CONSTRAINT honored: src/lib/* untouched throughout, BigInt-exact arithmetic unchanged (kernel-untouched check `! git diff --name-only src/lib/ \| grep -q .` clean in both task verify gates AND plan-level cross-check); cross-page consistency verified — all four pages contain `> **Tempered out by.**` exactly once with the bare-`>` canonical shape (no `.callout` div anywhere) and all four import `playDyad` from `"../components/play-dyad.js"` exactly once; verifier status: human_needed (single SME content question on the 19-EDO list — no execution gaps); two atomic feat commits c632a8f (pythagorean +13/-0) + 0e922e1 (schisma +7/-0) + merge b324099; tsc baseline unchanged (5 pre-existing `npm:` specifier errors only) | 2026-05-13 | b324099 | [260512-udc-bring-four-comma-pages-to-consistent-sec](./quick/260512-udc-bring-four-comma-pages-to-consistent-sec/) |
| 260513-a7c | add Tenney height theory page (`log₂(n·d)` scalar measure of harmonic complexity; companion to the EDO ranker's Tenney-weighted error in `src/lib/edo.ts:bestEdosForScale`) — new `src/pages/tenney-height.md` covering Definition (KaTeX `log₂(n·d)` + monzo-form `Σ |e_i| · log₂(p_i)`, with explicit note that the kernel `tenneyHeight` from xen-dev-utils uses natural log and the two forms are ranking-equivalent via constant factor `ln 2` — verified at `node_modules/xen-dev-utils/dist/core.js:418` returns `Math.log(n) + Math.log(d)`), Worked examples (9-row plain-DOM table: 1/1 → 0.000, 2/1 → 1.000, 3/2 → 2.585, 5/4 → 4.322, 7/4 → 4.807, 9/8 → 6.170, syntonic comma 81/80 → 12.662, schisma 32805/32768 → 30.002, Pythagorean comma 531441/524288 → 38.020 — `n·d` rendered via `BigInt(n)*BigInt(d)` then `.toLocaleString()` for 12-digit comma's 278,628,139,008 readability; `log₂` derived in-cell from `tenneyHeight(iv.monzo) / Math.LN2`; zero hardcoded float literals in data rows; 3-decimal precision throughout), Cents vs Tenney height (Plot scatter of 32 canonical 7-/11-/13-/17-limit ratios + syntonic comma + schisma, x = cents [0, 1220], y = log₂(n·d) [0, 32] LINEAR axis so the schisma at ~30 stays visible as the chart's pedagogical punchline — "complexity does not track pitch distance"; color-encoded by prime-limit via `primeLimitOfMonzo(iv.monzo)` with project palette; `Plot.dot` + `Plot.text` ratio labels + `Plot.ruleY([0])` baseline + per-point hover title with cents/TH/limit), Tenney-weighted error in the EDO ranker (3-paragraph explanation of `Σ |centsError| / max(1, tenneyHeight(monzo))` from `src/lib/edo.ts:98-126` as a musically-biased weighting favoring simple-ratio fit over high-complexity-comma fit; explicit comparison to max-error and RMS-error metrics; `Math.max(1, …)` clamp documented as numerical safety guard for the 1/1 case where `tenneyHeight([]) = 0` and the 2/1 case where TH ≈ 0.693 < 1), See also (prose links to Monzos, prime-limits, EDO approximations, Analysis, dashboard), Further reading via `furtherReading([...])` with two entries — James Tenney *John Cage and the Theory of Harmony* (1983, hosted by PlainSound Music Edition at `plainsound.org/pdfs/JC&ToH.pdf` — the canonical Sabat/von Schweinitz imprint that publishes Tenney's theoretical writing; archive.org PDF requested by brief does NOT exist under common slugs, verified 404 on `/details/john-cage-and-the-theory-of-harmony` and several variants) + Xenharmonic Wiki Tenney_height (note: page-name redirects to /w/Tenney_norm); kernel-exact anchors throughout (every cents and TH value derived in-cell from `new Interval(...).cents` / `tenneyHeight(iv.monzo) / Math.LN2`; zero hardcoded canonical-cent or canonical-TH float literals); no synth cell (glossary-style reference page, no audio dependency); plain-DOM tables via createElement + textContent (T-02-22/T-02-23 XSS discipline); `tenneyHeight` + `primeLimitOfMonzo` imported from `../lib/monzo.js` (R-01 guard — never from `xen-dev-utils` directly); critical correction caught during research: user's brief gave the Pythagorean-comma value as `log₂(279,936,432,033,792) ≈ 47.987` but the correct `n·d` for 531441/524288 (both already coprime) is 278,628,139,008 → `log₂ ≈ 38.020` (verified via node `Math.log2(BigInt(531441)*BigInt(524288))`) — page uses the corrected value; one-line append to observablehq.config.ts inserting `{ name: "Tenney height", path: "/pages/tenney-height" }` as the LAST entry under "Theory notes" per user's "the last Theory notes page" instruction (after "The septimal comma" at line 32; brief's "after EDO approximations" interpreted as "logically downstream of EDO pedagogy", not literal-array-position); kernel side (`src/lib/`, `src/audio/`, `src/components/`) untouched — pure presentational page edit + 1-line config wiring; `npm run build` clean — 20 pages rendered (was 19), 107 links validated (was 101), zero errors; `npx tsc --noEmit` exit 0; kernel math sanity-checked via node probe — 3/2 → log₂ = 2.5850, 5/4 → 4.3219, 81/80 → 12.6618 all match the page's worked-examples table to 4 decimals; flag --research triggered focused researcher first to verify the xen-dev-utils tenneyHeight source (natural log, not log₂), pre-compute all 32 scatter values, sanity-check the brief's worked examples (caught the Pythagorean-comma value error), and verify Further reading URLs (plainsound `200 OK` via curl; xen.wiki Cloudflare-blocks automated fetches but content confirmed via WebSearch) | 2026-05-13 | c4da7a3 | [260513-a7c-create-src-pages-tenney-height-md](./quick/260513-a7c-create-src-pages-tenney-height-md/) |
| 260513-9s8 | add well-temperament theory page (Werckmeister III 1691 / Kirnberger III 1779 / Vallotti 1779 — the historical bridge between meantone and 12-EDO) — new `src/pages/well-temperament.md` (480 lines) covering per-temperament fifth-size profiles (12 fifths each, summing exactly to 8400.000000000¢) and major-third-per-key profiles (12 keys each, derived by stacking 4 fifths down 2 octaves), with key character made visible (Plot bar charts) and audible (six triad-audition buttons — C major and F♯ major for each of the three temperaments — clean vs rough contrast at the heart of the well-temperament idea); kernel-exact anchors throughout (`pureFifth = new Interval("3/2")`, `pythComma = commaByName("Pythagorean comma")!`, `syntonic = commaByName("syntonic comma")!`, `schisma = commaByName("schisma")!`, `pure5Third = new Interval("5/4")`, `pythThird = new Interval("81/64")` — cents constants `PF` / `PC_4` / `PC_6` / `SC_4` / `SCHIS` derived from `.cents` projections); zero hardcoded canonical-cent float literals in source (grep gate confirms 701.955 / 386.314 / 23.460 / 21.506 / 1.954 / 407.820 do not appear); three 12-entry per-temperament fifth-size arrays (`werckFifths`, `kirnFifths`, `vallottiFifths`) closure-verified at 8400¢ exactly; cumulative-cents + `majorThirdAt` helpers (pure cents arithmetic mod-1200) derive three 12-row `*Thirds` arrays at render; `fifthsTable(fifths)` plain-DOM 3-column builder (createElement + textContent only, 3-decimal precision, T-02-22/T-02-23 XSS discipline); `fifthsDeviationChart(fifths)` Plot bar chart of `cents − PF` with `Plot.ruleY([0])` baseline + red `#c45656` fill for tempered-flat bars (catches Kirnberger's schisma-tempered F♯-C♯ at −1.954¢ visibly red); `majorThirdsChart(thirds)` Plot bar chart of major-third cents per key with three dashed reference rules (pure 5/4 blue at `pure5Third.cents`, 12-TET gray at 400, Pythagorean red at `pythThird.cents`); six chart instances total (werck/kirn/vallotti × FifthsChart/ThirdsChart); inline `playTriadAt(label, rootCents, thirdCents, fifthCents)` factory mirrors meantone.md's `playTempered` pattern — six A/B audition buttons rendered (C-major + F♯-major × 3 temperaments) calling `synth.playNotes([f1,f2,f3], 1.6)` where frequencies are derived at the audio boundary via `centsToRatio()`; page-level synth cell owns the only AudioContext (Pitfall #2, exactly one `createSynth()` callsite + `invalidation.then(synth.dispose)`); prerequisite aside (matching the cross-page consistency pass quick-260513-91h) pointing to `./meantone` + `./pythagorean-comma`; `## Further reading` via `furtherReading([...])` listing Xen Wiki /w/Well_temperament + Bradley Lehman's larips.com (Bach-temperament reconstruction) + Mark Lindley *Lutes, Viols and Temperaments* (Cambridge UP 1984, ISBN 0-521-28950-5); numerical anchors verified: Werckmeister C-major 3rd = 390.225¢, F♯-major 3rd = 407.820¢ (= 81/64 exactly — purest Pythagorean third lands at F♯ in this temperament); Kirnberger C-major 3rd = 386.314¢ (= pure 5/4 — the headline property of Kirnberger III); Vallotti A-major 3rd lands exactly on 400.000¢ (12-TET coincidence at the Vallotti rotation's center); one-line edit to observablehq.config.ts inserting `{ name: "Well-temperaments", path: "/pages/well-temperament" }` between "Meantone" and "EDO approximations" in the Theory notes sidebar (line 29); spiral-of-fifths.ts intentionally NOT embedded — confirmed in research that its `temperedFifthCents` option accepts only a single scalar, not the per-fifth array needed for well-temperaments (Plot bar charts chosen instead); kernel side (src/lib/, src/audio/, src/components/) untouched (no new component, no kernel change — inline `playTriadAt` factory mirrors meantone.md's `playTempered`, deferred to a shared `playTriad` component until a second page needs it); 10 grep gates + tsc-baseline + build-clean + rendered-HTML check all pass; `npm run build` clean — 19 pages rendered (was 18), 101 links validated (was 95), zero errors; tsc baseline unchanged (5 pre-existing `npm:` specifier errors in non-markdown source files only) | 2026-05-13 | 4202b6a | [260513-9s8-create-src-pages-well-temperament-md-cov](./quick/260513-9s8-create-src-pages-well-temperament-md-cov/) |
| 260513-d2e | fact-check all 18 theory pages in `src/pages/` and add Chicago author-date citations + per-page `## Sources` sections — research-driven audit (gsd-phase-researcher inventoried ~165 claims across the corpus, produced a deduplicated 48-entry citation pool spanning primary historical (Werckmeister 1691, Kirnberger 1779, Salinas 1577, Aron 1523, Zarlino 1558, Vallotti 1779, Tartini 1754, Helmholtz 1885, Mersenne 1636, Partch 1974, Tenney 1983, Doty 2002), modern academic (Benson 2007, Sethares 2005, Blackwood 1985, Lindley 1984, Duffin 2007/2020, Barker 1989, West 1992, Plomp & Levelt 1965, Lehman 2005), Xenharmonic Wiki (16 entries with Accessed 2026-05-13 dates), and Huygens-Fokker Foundation); 9 verbatim corrections applied — (C-1) `well-temperament.md` Vallotti "published posthumously 1779" → corrected: Vallotti d. 10 Jan 1780, *after* the 1779 publication of *Della scienza teorica e pratica della moderna musica* (Padua); (C-2) Vallotti recipe → reattributed to **Tartini–Vallotti**, first appearing in print in Giuseppe Tartini's 1754 *Trattato di musica* per Duffin's modern scholarship; (C-3) `schisma.md` bare "Garibaldi" disambiguated to **Sábat-Garibaldi** (Eduardo Sábat-Garibaldi, dinarra developer, late 20th c.) with Xen Wiki citation; (C-4) `meantone.md` Verheijen → corrected to **Verheyen, ca. 1600** as a prose footnote (cell-internal `label` strings left untouched per the no-JS/TS-cell-edits constraint); (C-5..C-9) including the syntonic-comma 21.506¢ / Pythagorean-comma 23.460¢ / schisma 1.954¢ canonical-value re-anchorings, Tenney 1983/1984 year-convention normalization, and the Gene-Ward-Smith / Joe-Monzo origin note on `monzos.md`; pre-Pythagoras lineage aside added to `pythagorean-tuning.md` citing West 1992 + Barker 1989 (chain-of-fifths method documented in cuneiform Mesopotamian sources >1000 years pre-Pythagoras); 4 `(citation needed)` markers placed for unverifiable claims (Q-1 harmonic-13 reference-note convention on `harmonic-series.md`, Q-2 Silbermann 1/6-comma primary on `meantone.md`, Q-3 Wagner-specific tonal-color musicology study, Q-4 Doty 2002 blue-note 7th-harmonic claim on `odd-limits.md`); Q-5 JND threshold claims (~5–10¢) satisfied inline with canonical Plomp & Levelt 1965 + Sethares 2005 citations; per-page Sources counts (18 pages total) — well-temperament 9, comma-pump 8, meantone 8, schisma 8, syntonic-comma 8, harmonic-series 6, edo-approximation 6, pythagorean-tuning 6, commas 5, pythagorean-comma 5, septimal-comma 5, prime-limits 4, scale-workshop-interop 4, monzos 3, odd-limits 3, otonality-utonality 3, tenney-height 2, analysis 1; **citation style: Chicago author-date** inline `(Helmholtz 1885, 33)` with full reference-list entries (alphabetized by author surname) in each page's bottom `## Sources` H2 — duplication of bibliographic entries across pages accepted in exchange for locality (no shared `references.md` page per locked CONTEXT.md decision); Xen Wiki citations use `(Xen Wiki, "Page Title")` inline + `Accessed 2026-05-13` in the reference list; HARD CONSTRAINT honored: reactive `js`/`ts` code cells UNTOUCHED in every page (verified via `git diff` audit — only prose Markdown changes); the cell-internal `Verheijen` label was deliberately left intact and the correction applied as a prose footnote below the table; same approach for Aron/Salinas/Silbermann variants (citations in prose paragraphs immediately following the tables, not in cell-internal `label:` strings); no new files created; `npm run build` clean — 21 pages, 111 links validated, zero errors; no emojis in any edited file; single atomic feat commit `872f6e8` covering all 18 page edits (`feat(260513-d2e): fact-check theory pages and add Chicago author-date citations`, +362 / −137 lines, 18 files); orchestrator merged worktree-isolated executor branch via `712312f chore: merge quick task worktree`; flags --research (focused gsd-phase-researcher pre-planning) + --discuss (gray-areas surfaced upfront — citation style locked to Chicago author-date, fact-check depth locked to FULL incl. math derivations, all four source tiers accepted, per-page `## Sources` layout with single-commit policy); follow-up items logged in SUMMARY.md for the 4 `(citation needed)` markers awaiting future research | 2026-05-13 | 872f6e8 | [260513-d2e-fact-check-theory-pages-and-add-academic](./quick/260513-d2e-fact-check-theory-pages-and-add-academic/) |
| 260513-akn | add Scale Workshop interop tutorial — new `src/pages/scale-workshop-interop.md` (157 lines) walking a 4-phase round-trip of a 5-limit just diatonic (`9/8 5/4 4/3 3/2 5/3 15/8 2/1` — Ptolemy's intense diatonic) between this notebook and [Scale Workshop](https://scaleworkshop.plainsound.org/) via `.scl`: (1) build-the-scale instructions for the dashboard `/` textarea + inline `ratioPill` row rendering the seven non-unison degrees; (2) export — mounts the existing `sclIo` component (`src/components/scl-io.ts`) in-page scoped to the worked-example Scale with `defaultFilename "diatonic-5-limit-just"` and renders a dynamic `<pre><code>` block showing the exact bytes `writeScl(diatonic)` produces (kernel-derived, not hardcoded — Pitfall #1) plus format anchors per the Huygens-Fokker spec + project decisions (D-13 `1/1` auto-prepend, D-14 last-pitch-is-period, D-19 `.` triggers cents); (3) import instructions for `scaleworkshop.plainsound.org` (File → Open from disk / paste-import) with cents anchors `5/4 → 386.314¢`, `3/2 → 701.955¢`, `15/8 → 1088.269¢`, `2/1 → 1200.000¢` derived inline at runtime via `${diatonic.intervals[i].cents.toFixed(3)}¢` (no hardcoded float-cent literals in source — grep gate `! grep -nE '[0-9]+\.[0-9]+¢' src/pages/scale-workshop-interop.md` clean); (4) round-trip back via embedded sclIo import button + an in-page reactive cell that calls `parseScl(writeScl(diatonic))` and compares Fraction-equality element-by-element with a plain-DOM table (#, Original, Re-parsed, Cents, Fraction-equal?) + summary caption — all 8 intervals round-trip Fraction-exact through the BigInt-Fraction kernel; cents-source-is-lossy caveat documented in a `> **Caveat**` blockquote per the `src/lib/scala.ts` header docstring (writeScl always re-emits ratios; round-trip equivalence holds by Fraction-equality, not file-byte identity); `## Further reading` rendered via the shared `furtherReading([...])` helper with three entries — Scale Workshop app at scaleworkshop.plainsound.org (canonical microtonal scale editor, same xenharmonic-devs ecosystem as the math kernel — xen-dev-utils / sonic-weave / sw-synth), Scale Workshop GitHub repo (xenharmonic-devs/scale-workshop — source + docs/ folder + issue tracker), and the Huygens-Fokker `.scl` format spec at huygens-fokker.org/scala/scl_format.html (canonical from the foundation publishing Scala since 1992); one-line edit to observablehq.config.ts adding a NEW top-level **Tools** section (cleaner than appending to Theory notes per brief's choice — this is a how-to, not a theory primer; "Tools" is the natural home for future interop / export / workflow tutorials); kernel side (`src/lib/`, `src/audio/`, `src/components/`) untouched — no new components, no new tests, no new CSS; `## Further reading` H2 stays in Markdown so Framework's TOC picks it up (helper renders the list body only); single import cell honored (Framework module-scope rule); no `createSynth()` — no AudioContext owner (Pitfall #2 trivially honored, glossary-style how-to with no audition cells); T-02-22/T-02-23 XSS discipline — every dynamic surface (pillRow, sclPreview, roundTripTable) built via `createElement` + `textContent`, no `innerHTML` for any derived value; `npx tsc --noEmit` exit 0; vitest 302/302 across 24 files; eslint clean; prettier clean (one `observablehq.config.ts` reformatting auto-applied via `prettier --write`); `npm run build` clean — **21 pages rendered (was 20)**, **111 links validated (was 107)**, zero errors; new page renders at 20 kB Page + 129 kB Imports (light bundle — no Plot dependency, only sclIo + ratioPill + furtherReading); rendered-HTML verification confirms embedded sclIo mount (`grep -c 'class="scl-io"' dist/pages/scale-workshop-interop.html` ≥ 1), runtime placeholders for the four inline-cents substitutions present (`<observablehq-loading></observablehq-loading><!--:hash:-->¢` × 4), Further-reading section present | 2026-05-13 | e1b5a17 | [260513-akn-scale-workshop-interop](./quick/260513-akn-scale-workshop-interop/) |
| 260513-foy | audit theory pages for playback/interactivity gaps and hand off a six-command `/gsd-quick` sequence the user will paste into fresh context windows — **no source code modified in this task**; deliverable is `.planning/quick/260513-foy-*/260513-foy-SUMMARY.md` containing (a) audit table covering all 18 `src/pages/*.md` pages with columns Page / Visual elements / Already interactive? / Interactivity gap / Reuse opportunity (Explore-agent–driven survey reading every page file plus `src/components/spiral-of-fifths.ts`, `tonality-diamond.ts`, `play-interval.ts`, `play-dyad.ts`, `play-scale.ts`, `src/audio/synth.ts`); (b) 9-page no-op set: analysis, commas, comma-pump, harmonic-series, monzos, otonality-utonality, prime-limits, pythagorean-tuning, scale-workshop-interop — all already wire playback (per-row buttons / chord buttons / cycle buttons / interactive builders); (c) 9-page gap set with concrete reasons: odd-limits (3× `renderDiamondSVG` display-only — interactive `tonalityDiamond` exists but isn't used), tenney-height (zero play buttons on the 15-ratio scatter), well-temperament (variants table has no per-row buttons), pythagorean-comma + meantone (use `spiralOfFifths` which has no click affordance), edo-approximation + schisma + septimal-comma + syntonic-comma (static Plot scatter dots — Plot.dot does not expose click events natively); (d) three structural blockers — B1 extend `spiral-of-fifths.ts` with optional `onStepClick?: (step: SpiralStep) => void` callback, D3 `.on('click', ...)` binding per node, `.is-clickable` CSS class for cursor:pointer affordance, backwards-compatible (omitting callback = current behavior); B2 either extend `renderDiamondSVG(n, opts)` to accept optional `synth` parameter forwarding clicks to playInterval OR have odd-limits.md construct an odd-limit `Scale` and call `tonalityDiamond(scale, synth, opts)` directly; B3 adopt the prime-limits.md "ratio-pills-beside-chart" pattern for Plot scatter charts (do not attempt overlay-rect hacks); (e) six self-contained `/gsd-quick` commands ordered for dependency correctness — **CMD-1** extend `spiral-of-fifths.ts` with `onStepClick` callback (foundational, unblocks CMD-3), **CMD-2** swap renderDiamondSVG → tonalityDiamond on odd-limits.md for all three diamonds (the user's original example), **CMD-3** wire spiralOfFifths `onStepClick` on pythagorean-comma.md + meantone.md [depends on CMD-1; pythagorean-tuning.md explicitly excluded — uses `scaleTable` not `spiralOfFifths`], **CMD-4** add inline ratio-pills + playInterval row beside the tenney-height scatter (15 ratios), **CMD-5** add per-row play buttons to well-temperament variants table (6 schemes × 3 interval columns = 18 buttons) using the `playTempered()` factory pattern from meantone.md, **CMD-6** add ratio-pills audition rows to edo-approximation/schisma/septimal-comma/syntonic-comma Plot scatters (4 pages — could be split per-page if it exceeds quick-task scope); (f) cadence guidance: CMD-1/CMD-2/CMD-4/CMD-5/CMD-6 independent (run in any order), CMD-3 depends on CMD-1 (wait for CMD-1's commit before pasting); (g) intentionally out-of-scope items called out with reasons — comma-pump per-data-point clickability (cycle buttons already adequate, would muddy drift narrative), 3D lattice work (CLAUDE.md v1 constraint: "Three.js for the lattice in v1 — overkill"), hover-audition affordances (UX decision worth a separate spike); each command in the sequence is independently runnable in a fresh `/gsd-quick` context window — commands reference specific file paths and existing reuse patterns (prime-limits.md ratio-pills pattern, monzos.md synth-init pattern, meantone.md playTempered factory) so the planner in each fresh window does not need to rediscover the audit findings; user explicitly requested handoff-as-commands rather than single sweeping execution because the full sweep exceeds the 1–3 atomic-task quick-task envelope; no source code modified, no kernel changes (`src/lib/`, `src/audio/`, `src/components/` untouched), only `.planning/quick/260513-foy-*/PLAN.md` (87 lines) and `SUMMARY.md` (handoff doc with audit table + blockers + six commands + cadence + out-of-scope) written | 2026-05-13 | (handoff) | [260513-foy-i-would-like-as-many-of-the-graphs-and-f](./quick/260513-foy-i-would-like-as-many-of-the-graphs-and-f/) |
| 260513-jir | CMD-1 from 260513-foy handoff: extend `src/components/spiral-of-fifths.ts` with optional `onStepClick?: (step: SpiralStep) => void` prop on `SpiralOfFifthsOpts` — when supplied, the widget root carries `is-clickable` alongside `spiral-of-fifths-widget` and each `<g class="spiral-of-fifths__node">` gets a native `addEventListener("click", () => opts.onStepClick!(s))` binding (vanilla DOM — the existing file does not use D3; D3 phrasing in brief treated as shorthand for click binding); paired new CSS rule scoped under `.is-clickable` on the root applying `cursor: pointer` to `.spiral-of-fifths__node` (real BEM class, not the `.spiral-node` shorthand from the brief); component remains viz-only — no synth/AudioContext import (Pitfall #2 / D-08 three-layer discipline preserved); four new vitest cases under a `describe("spiralOfFifths factory — onStepClick prop")` block — (a) omitting prop: root className is exactly `"spiral-of-fifths-widget"` with no `is-clickable`; (b) supplying prop: root carries `is-clickable`; (c) click on k=1 node in pure-3/2 branch fires callback once with a `SpiralStep` whose `k===1`, `ratio` equals `new Interval("3/2")`, `cumulativeCents > 0`, `centsFrom12tet` in (1.9, 2.0); (d) tempered-700¢ branch click on k=1 fires callback with `ratio: null`, `cumulativeCents === 700`, `centsFrom12tet === 0`; (e) regression: clicks on nodes without a handler don't throw; vitest 23/23 green (19 existing + 4 new); tsc clean for spiral-of-fifths files (pre-existing `npm:` specifier errors in unrelated `synth.ts` / `lattice.ts` / `scale-compare.ts` left alone); deferred items logged in SUMMARY: (1) keyboard accessibility — nodes look clickable but aren't keyboard-focusable; call site should add focus/keypress wiring or wrap nodes in `<button>`; (2) ARIA role/label — no `role="button"` or `aria-label` yet; future a11y pass can add `aria-label="Play fifth 3/2, +1.96¢ from 12-TET"` once a real call site is wired; (3) pre-existing tsc errors in unrelated files flagged for visibility only; single atomic feat commit (executor) `b469080` covering all three source files (ts + css + test); worktree-isolated execution merged back via `4d7fbb9` (merge commit); CMD-1 of the six-command 260513-foy handoff is now done — CMD-3 (wire spiralOfFifths onStepClick on pythagorean-comma.md + meantone.md) is now unblocked | 2026-05-13 | b469080 | [260513-jir-add-onstepclick-prop-to-spiral-of-fifths](./quick/260513-jir-add-onstepclick-prop-to-spiral-of-fifths/) |
| 260513-kb6 | CMD-3 from 260513-foy handoff: wire `spiralOfFifths.onStepClick` (added in 260513-jir / b469080) on the two `spiralOfFifths` consumers in the codebase. **`src/pages/pythagorean-comma.md` (pure 3/2 branch):** added `onStepClick: (step) => { if (!step.ratio) return; const baseHz = 440; synth.playNotes([baseHz, baseHz * Number(step.ratio.fraction.valueOf())], 1.5); }` to the existing `spiralOfFifths(12, { highlightWolf: true })` call at line 72 — `step.ratio` is a non-null octave-reduced `Interval` in this branch (the component runs `acc = acc.mul(3/2).octaveReduce()`), so the handler is the same one-liner `playInterval` writes to the synth, with a defensive `if (!step.ratio) return` guard keeping the closure type-safe under `SpiralStep.ratio: Interval | null`. **`src/pages/meantone.md` (1/4-comma tempered branch):** added `onStepClick: (step) => { const baseHz = 440; const reducedCents = ((step.cumulativeCents % 1200) + 1200) % 1200; const ratio = centsToRatio(reducedCents); synth.playNotes([baseHz, baseHz * ratio], 1.5); }` to the existing `spiralOfFifths(12, { temperedFifthCents: quarter.fifth, highlightWolf: true })` call at line 286 — `step.ratio` is `null` here (irrational meantone fifth) and `step.cumulativeCents` is intentionally NOT octave-reduced in `SpiralStep` (the closure-error visualization needs the un-reduced cumulative to draw the wolf chord at k=12 ~8359¢), so the handler reduces mod 1200 at the audio boundary before sending — both for audible-range reasons (a naïve dyad at 440 × 2^(8359/1200) ≈ 122 kHz trips synth's defense-in-depth Hz clamping per T-02-17) AND to match the spiral's own visual reading (which renders `cents-from-12-TET` of the octave-reduced cumulative per spiral-of-fifths.ts line 117). `centsToRatio` was already imported at line 8 of `meantone.md` — the existing `playTempered` factory at lines 117–129 uses the same helper for the irrational 1/4-comma major-third audition, same audio-boundary discipline. **No component/CSS/import/test changes** — `spiral-of-fifths.ts` is untouched, the `.is-clickable .spiral-of-fifths__node { cursor: pointer }` rule from CMD-1 lights up automatically because both call sites now supply `onStepClick`, both pages already had `synth` cells (Pitfall #2 — exactly one `createSynth()` per page) and `centsToRatio` import. **`pythagorean-tuning.md` deliberately excluded** — the 260513-foy audit confirmed it uses `scaleTable` (per-row play buttons baked in), not `spiralOfFifths`, so wiring would have no effect. **D-08 three-layer discipline preserved** — viz stays viz-only, audition wiring lives at the call site capturing `synth` from page scope. Audition shape uniform across the site: D-07 dyad / D-08 baseHz=440 / D-18 1.5s — same one-liner shape `playInterval` / `renderDiamondSVG`-with-synth (260513-jqi) / `playTempered` write to the synth. tsc clean; vitest 312/312 across 24 files (no test changes — spiral-of-fifths 23/23 stays green); `npm run build` clean with 111 links validated across 18 pages; both pages render identically to baseline (the inline arrow is ~150 bytes inside an existing JS expression — not a new import). Two-file diff: src/pages/pythagorean-comma.md +9/−1 (single call site), src/pages/meantone.md +12/−1 (single call site, three-line handler with explanatory comment). Single atomic feat commit `946248a`. **CMD-3 done** — remaining handoff items: CMD-4 (tenney-height ratio-pills audition row beside the 15-ratio scatter), CMD-5 (well-temperament per-row play buttons via `playTempered()` factory pattern from meantone.md, 6 schemes × 3 interval columns = 18 buttons), CMD-6 (Plot scatter ratio-pills audition rows on edo-approximation/schisma/septimal-comma/syntonic-comma — could split per-page if it exceeds quick-task envelope); CMD-4/5/6 independent (run in any order in fresh `/gsd-quick` windows) | 2026-05-13 | 946248a | [260513-kb6-spiralOfFifths-onStepClick-pythcomma-meantone](./quick/260513-kb6-spiralOfFifths-onStepClick-pythcomma-meantone/) |
| 260513-kl9 | CMD-4 from 260513-foy handoff: add inline `ratioPill` + bare ▶ `playInterval` row directly below the cents-vs-Tenney-height scatter chart on `src/pages/tenney-height.md` (Plot.dot() is not clickable in Observable Plot — audition surface lives next to the chart, not on it). One pill per entry in the existing `scatterRatios` array — **32 ratios, not the 15 the handoff estimated**. Three additions to the file: (a) three new imports — `createSynth` from `../audio/synth.js`, `playInterval` from `../components/play-interval.js`, `ratioPill` from `../components/ratio-pill.js` — folded into the existing top-of-page `ts` import block; (b) standalone synth cell inserted between imports and the `<aside class="prereq">` block (canonical ARCHITECTURE Pattern 4 / Pitfall #2 shape: `const synth = createSynth(); invalidation.then(() => synth.dispose())` — lazy AudioContext, no allocation until first click); (c) plain-DOM IIFE pills-row cell inserted directly after `display(scatterChart)` with one-line plain-Markdown caption "Click any ratio to hear it." above — `div.ratio-pills-row` flex-wrap container (`display: flex; flex-wrap: wrap; gap: 0.5rem; align-items: center; margin: 0.5rem 0 1rem 0`) iterating the existing `scatterRatios` (cell scope — NOT redeclared) with `for (const r of scatterRatios) { const iv = new Interval(r); ... }`; each pair wrapped in an inline-flex `<span>` (`display: inline-flex; align-items: center; gap: 0.2rem`) so the pill + ▶ stay glued across wrap boundaries. `ratioPill(iv)` defaults preserved (showCents:true) so the pill carries the same cents annotation pattern used elsewhere on the page; `playInterval(iv, synth)` defaults preserved per D-08 / D-18 — 440 Hz root, 1.5s duration, bare ▶ glyph (no `label: true` — the pill already shows the ratio). XSS discipline T-02-22/T-02-23/T-kl9-01 honored — `createElement` + `appendChild` only for wrappers, dynamic strings delegated to the `ratioPill`/`playInterval` factories which already use `textContent` internally; no `innerHTML` for derived values. BigInt-Fraction stays source of truth — `new Interval(r)` from the literal ratio string for each row (Pitfall #1, no float-as-source-of-truth). **No component/CSS/test changes** — `ratio-pill.ts`, `play-interval.ts`, `synth.ts` untouched; no new test file; pure markdown-page edit. **Scatter chart untouched** — no `onclick` on `Plot.dot()`, no extra mark, no domain change; `scatterRatios` (lines 145–152) / `scatterData` (line 153) / `scatterChart` (lines 164–214) byte-identical to baseline. **Unrelated content untouched** — worked-examples table (lines 50–118) / Tenney-weighted-error EDO prose / `## See also` / `## Further reading` / `## Sources` all byte-identical. tsc clean; `npm run build` clean with 111 links validated across 18 pages, all 21 pages render; one atomic feat commit `27cb390` covering src/pages/tenney-height.md +50/−0. **CMD-4 done** — remaining 260513-foy handoff items: CMD-5 (well-temperament per-row play buttons via `playTempered()` factory pattern from meantone.md, 6 schemes × 3 interval columns = 18 buttons), CMD-6 (Plot scatter ratio-pills audition rows on edo-approximation/schisma/septimal-comma/syntonic-comma — could split per-page if it exceeds quick-task envelope); CMD-5/6 independent (run in any order in fresh `/gsd-quick` windows) | 2026-05-13 | 27cb390 | [260513-kl9-add-inline-ratio-pills-row-with-play-but](./quick/260513-kl9-add-inline-ratio-pills-row-with-play-but/) |
| 260513-jqi | CMD-2 from 260513-foy handoff: make the three inline `renderDiamondSVG()` previews on `src/pages/odd-limits.md` (5-/7-/11-odd-limit, widths 280/320/440) click-to-audition. Picked option (a) — extended `renderDiamondSVG` in `src/components/tonality-diamond.ts` rather than swapping over to the dashboard's heavier `tonalityDiamond(scale, synth, opts)` widget — because the user explicitly required the existing visual layout (3 diamonds, widths 280/320/440) and `tonalityDiamond` prepends an `<h2>Tonality diamond</h2>` heading + "Click a cell to audition" helper line designed for the dashboard's standalone widget context that wouldn't fit underneath the page's own `### N-odd-limit` H3s; `RenderDiamondSVGOpts` gains optional `synth?: SynthHandle / baseHz?: number / duration?: number` fields with JSDoc; when synth is supplied each cell flips from `role="presentation"` to `role="button" tabindex="0" aria-label="Play <ratio>"` with click + Enter/Space handlers calling `synth.playNotes([baseHz, baseHz × Number(ratio.fraction.valueOf())], duration)` — same dyad audition shape `playInterval` writes to the synth (D-07 dyad / D-18 1.5s default; BigInt-Fraction → Number coercion only at the audio boundary, Pitfall #1); when synth is omitted the legacy presentation-only path is byte-equivalent (regression guard test enforces); `SynthHandle` type-only import was already present from the `tonalityDiamond` factory — no new imports; D-08 three-layer discipline preserved (viz never instantiates a synth — page's existing `createSynth()` cell at the top of `odd-limits.md` mirrored from `monzos.md` hands the `SynthHandle` reference through `opts.synth`); zero CSS changes — `tonality-diamond.css` already gates `cursor: pointer` and `:focus-visible { outline: 2px solid var(--theme-foreground-focus) }` on `.diamond-cell[role="button"]`; 5 new vitest cases under a `describe("renderDiamondSVG", ...)` block — (a) without synth, every cell stays `role="presentation"` with `tabindex` null (regression guard); (b) with synth, every cell becomes `role="button"` with `tabindex="0"` and aria-label matching `/^Play \d+(\/\d+)?$/` (the regex accepts both shapes because `Fraction.toFraction()` from `fraction.js` returns `"1"` for the unison cell, not `"1/1"` — discovery during first run); (c) clicking the first interactive cell calls `synth.playNotes` once with a 2-element dyad, default `baseHz=440`, default `duration=1.5`; (d) Enter keypress triggers the same audition; (e) custom `baseHz: 261.625` (C4) forwards through the dyad; vitest 312/312 green across 24 files (was 302/302 — net +10 incl. 5 new + tonalityDiamond's existing 5); tsc clean; `npm run build` clean with 111 links validated across 18 pages; odd-limits page bundle 42 → 45 kB and 705 → 706 kB (+3 kB for per-cell click + keydown handler closures, no new imports); single atomic feat commit `8b4e356` covering tonality-diamond.ts (+62/−10) + tonality-diamond.test.ts (+62/−1) + odd-limits.md (3 call sites, `synth` added to opts on each); CMD-2 of the six-command 260513-foy handoff is now done — odd-limits.md is the only remaining `renderDiamondSVG` consumer in the repo, so the new optional path covers all current call sites without disturbing legacy use elsewhere; remaining handoff items: CMD-3 (spiralOfFifths onStepClick wiring on pythagorean-comma.md + meantone.md, unblocked by 260513-jir), CMD-4 (tenney-height ratio-pills audition row), CMD-5 (well-temperament per-row play buttons via `playTempered()`), CMD-6 (Plot scatter ratio-pills on edo-approximation/schisma/septimal-comma/syntonic-comma) | 2026-05-13 | 8b4e356 | [260513-jqi-odd-limits-clickable-diamonds](./quick/260513-jqi-odd-limits-clickable-diamonds/) |

## Deferred Items

Items acknowledged and deferred at milestone close on 2026-05-07:

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| uat_gap | 03-HUMAN-UAT.md — RDM smoke test "Esc/Stop cancels arpeggio mid-flight" | partial (1 pending) | 2026-05-07 (v1.0 close) |
| verification_gap | 03-VERIFICATION.md — mobile-audit.md `{pending}` Post-CR-02 footer signature | human_needed | 2026-05-07 (v1.0 close) |
| quick_task | 260504-i3v-fix-cr-01-interval-octavereduce-infinite (SUMMARY.md present, commit e288037) | missing-marker | 2026-05-07 (v1.0 close) |
| quick_task | 260504-lb6-fix-cr-02-cr-03-writescl-description-san (SUMMARY.md present, commit 2c44907) | missing-marker | 2026-05-07 (v1.0 close) |

Notes:

- The two Phase 3 items share a single root cause: a `{pending}` signature block in `mobile-audit.md` awaiting a Safari RDM re-walk of the new "Esc/Stop cancels arpeggio mid-flight" bullet. The unit-level regression test (`src/audio/__tests__/synth.test.ts:290`) already verifies the code path; per `03-VERIFICATION.md` the gap is "a documentation-trail nicety, not a code defect." 5/5 must-haves on Phase 3 are VERIFIED.
- The two quick-task entries have completed `SUMMARY.md` files and are merged (commits e288037, 2c44907); the audit flags them as `missing` because the audit tool looks for a marker the SUMMARY files don't carry. No actual work outstanding.

## Session Continuity

Last session: 2026-05-07T20:07:29.922Z
Stopped at: v1.0 MVP milestone shipped — ready for next milestone scoping
Resume file:
None
